# Upstream sync — last-reviewed app commits

The last-reviewed commit of each app and of `lib/pkp`, advanced by the
MAINTENANCE upstream-sync loop (`docs/process/MAINTENANCE.md`).
The `stable-3_5_0` branch, read for regressions only, has its own file:
`upstream-sync-stable-3_5_0.md`.

## Baselines

| Repo | Last-reviewed commit | Date | Reviewed by |
|------|----------------------|------|-------------|
| ojs | `802202cb3e` | 2026-09-23 | claude (daily maintenance session) |
| omp | `7f9455d5a` | 2026-09-21 | claude (daily maintenance session); unchanged 2026-09-22, 2026-09-23 |
| ops | `15f0b6e0bd` | 2026-09-22 | claude (daily maintenance session); unchanged 2026-09-23 |
| pkp-lib | `5af3b39336` (ojs) / `f8bacd7658` (ops) / `63945bbd82` (omp) | 2026-09-23 | claude (daily maintenance session); ui-library `2034439a` (ojs) / `5d138aa9` (ops) / `977e460c` (omp); ojs `plugins/generic/crossref` `96bfcaad44`, ops `plugins/generic/crossref` `b6b94dd5de`, ojs `plugins/generic/jatsTemplate` `1065bb02ae` |

## Sync log

_Newest first; one entry per sync: the date and the range per repo, then
one line per change reviewed (commit → no impact / spec and tests touched /
finding filed, with a link)._

- **2026-09-23 (companion `optimize-table-reloads`, prepared before merge) — pkp/ui-library#853 at `cab09538` (32 commits on `2034439a`), pkp/ojs#5444 at `a0372a9d1f` (submodule-only, on `639cbc7a50`); no omp or ops PRs; issue pkp/pkp-lib#13359. Baselines not advanced: the PRs are open; the merge session rebases the companion and deletes its row.**
  - Refs: the ojs PR sits one commit behind the ojs tip (`802202cb3e`, #5849, a pkp-lib bump already reviewed), so OJS was driven at its tip with lib/ui-library at `cab09538`, the merge result (the PR changes only the pointer). ui-library's base `2034439a` is the reviewed OJS pointer. OMP (`977e460c`) and OPS (`5d138aa9`) lag it by 4 and 2 reviewed commits; their suites were also run with lib/ui-library at `cab09538` (the change reaches them with their next update), then put back.
  - ui-library #853 (modal store `dataChanged` flag set by `useFetch` on POST/PUT/DELETE, `Form.vue` success and `AjaxModalWrapper` legacy events; `closeSideModalById()` hands `onClose` `{dataChanged}`, `triggerDataChange(closeData)` returns early on false; nested flags pass to the parent; managers pass `triggerDataChange` as `onClose`; the dashboard keeps unconditional reloads; full-screen spinner while a changed close reloads) → accommodated: U14 Rule 12, Rule 15, scenario 3 and footnote j (a view-only close of the comment panel no longer reloads; `closeCommentPanel({changed})` in the three apps, the report-deleting close keeps its refetch wait); U23 S9 (OJS, OMP) waits for the mark-as-read `consider` POST before "Cancel". The OJS PR check's reds (run 35770880623: U14 S2/S3/S13 and U23 S9) were these.
  - Regression verdict (rr16 on OJS, `.reports/sync/rr16/suspicions.md`: 2 suspicions, 1 reproduced, 6 unverified hunches; plus the session's drives and the OMP/OPS suites), three findings, each held on a fresh reset with a before-control at `2034439a`: (1) publish confirmation closes unflagged, so OMP/OPS "Publish"/"Post"/schedule leave the workflow on the unpublished state until a reload (OJS only on the directly opened confirmation) → U49 🐞 A8 (U40 cross-link), report `docs/reports/2026-09-23-ui-library-853-publish.md`, kept check `checks/sync/ui-library-853/publish-stale.js`; OMP full run at the head red on 7 publish scenarios, OPS on 8, all green at `2034439a` (OMP 38/38, OPS 23/23 on the same databases); (2) the author's "Upload revisions" closed after step 1 leaves "Revisions Uploaded" empty (`workflowStore.fileUpload()` now conditional; the issue keeps upload windows reloading) → U26 🐞 A12, report `…-853-upload.md`, `upload-stale.js`, OMP U26 S4 red at the head; (3) Review Details closed before its mark-as-read answers leaves "Review Submitted"/"View unread recommendation" (`useReviewDetails.js` dropped the post-mark reload; the flag lands on a closed modal; the dashboard reload races the POST, 18 ms in the CI-shaped trace) → U27 🐞 A32 (U23 cross-link), report `…-853.md`, `stale-read.js`. ci-triage "Open regressions" rows for all three.
  - Runs: OJS at the PR ref on a reset database, full suite at 8 workers: 293 passed in 11.6 min, first run (`.reports/pr13359/ojs-final-pr.log`). OMP with lib/ui-library at the head (reset): 262 passed, 8 failed (the findings above), 13 not run behind them; OPS (reset): 162 passed, 8 failed (publish), 12 not run. At the tips: OMP U14+U23 24/24, OPS U14 9/9 with the companion's tests. U23 S9 x8 at the PR ref: 1 red before the test change, 0 after.
  - CI from the companion (`778497e`): dispatch 35871041025, ojs at `blesildaramirez/ojs@a0372a9d1f` (the PR head), omp and ops at `main` → all nine shards green.
- **2026-09-23 (sync) — ojs `38781720df..802202cb3e` (2), omp unchanged at `7f9455d5a`, ops unchanged at `15f0b6e0bd`, pkp-lib `f8bacd7658..5af3b39336` (2, ojs only: the #13354 merge `b878a76ed4`, tree identical to the reviewed `f8bacd7658`, and #13369), ui-library `5d138aa9..2034439a` (2, ojs only: the #988 merge `c441b978`, and #9353), plugins unchanged. The app-side commits are pointer bumps: ojs `639cbc7a50` ("Update submodules": lib/pkp and ui-library) and `802202cb3e` (pkp/ojs#5849, lib/pkp to `5af3b39336`).**
  - CI at the tips: ojs `e2e` run 35783113759 (`802202cb3e`), omp 35379521188 (`7f9455d5a8`, unchanged), ops 35712040684 (`15f0b6e0bd`, unchanged) → all green; the red "schedule" runs on pkp/ojs and pkp/ops are the team's own Cypress matrix (red only on `stable-3_3_0` jobs, and on OPS `stable-3_4_0`; every `main` job green), not this suite. pkp-e2e `main`: the nightly 35814861700 green on all three; of today's push runs, 35850569954 at `532711b` red on OPS shard 1/3 alone, the login smoke on both attempts → the roster-rehash cause `cdf9458` fixed the same day (ci-triage entry deleted there); the next push run 35854852786 at `cdf9458` green. No known-red rows, no companions.
  - pkp-lib #13291 (`5af3b39336`, PR #13369 squashed, tests-only twin pkp/ojs#5849; ojs now, omp once its pointer catches up; OPS has no review): `PUT submissions/{id}/reviewAssignments/{id}/review` accepts `competingInterests` (the policy closure in `EditReview`, `Context::isReviewCompetingInterestRequired()`), sets `competingInterestsDeclared` and `lastModifiedById`, logs the new `SUBMISSION_LOG_REVIEW_REVIEWER_COMPETING_INTERESTS_MODIFIED` with a "View changes" diff; the review edit now runs whenever the assignment data changes, not only on `$isReviewUpdated` → not covered yet (no screen sends the key; the Modify Review field is pkp/pkp-lib#13282; U27 Rule 14b's three editable things unchanged). Regression read rr15 (`.reports/sync/rr15/suspicions.md`; OJS): 3 suspicions written, 1 reproduced, 2 not reproduced, 4 unverified hunches, nothing routed. S1 REPRODUCED, an intention gap: on a journal with no competing-interests policy `"competingInterests": ""` (and `null`) answers 200 and records a declaration ("No competing interests were disclosed." in Review Details, a log row) where the issue asks for 422; text gets the 422. Held twice on freshly reset OJS (`.reports/sync/s23-13291-fresh/`, `s23-13291-fresh2/`), kept `checks/sync/pkp-lib-13291/ci.js`, report `docs/reports/2026-09-23-pkp-lib-13369.md`, ci-triage "Open regressions" row. S2 not reproduced: a competing-interests change leaves `dateCompleted`, `step`, the reviewer's task and the round status alone (the issue's `dateCompleted` question answered as hoped). S3 not reproduced: the Activity Log and its "View changes" render.
  - ui-library #9353 (`2034439a`; ojs now, omp and ops when their pointers move): `@text-light` `#777` → `#505050` in `src/styles/variables.less` (the secondary text of the older Vue components: Page, Dropdown, ListItem, Filter, Orderer, StatsPage, Steps, Pagination, FormLocales, FieldText, the upload fields, MultilingualProgress, Tabs, helpers), and a Chromatic `externals` entry → no impact. Regression verdict in the log line: every use colours text on a light background, so the change only raises contrast; no suite asserts a colour (grepped `toHaveCSS`, `#777`, `rgb(119`); lib/pkp's own `styles/variables.less` keeps `@text-light: #777` for the legacy grids and forms (the issue's scope, not a break).
  - Open regressions re-run on OJS at the new tip, databases reset this morning: pkp-lib#12352 still reproduces (`.reports/sync/s23-12352/`: `afterCancel` fileId 2 `article-rev.pdf`), #13288 still open at `35bb1839df`; pkp-lib#13181 still reproduces (`.reports/sync/s23-13181/`: both landings `index/en/user/profile#contact`, the old-shape decline chain ending in GET 500, `stillPending` 1); pkp/ojs#5813 still reproduces (`.reports/sync/s23-5813/`, the literal abstract split into four `<p>` with `<italic>species names</italic>`). Rows dated. Report `docs/reports/2026-09-04-omp-ops-discussion-save-missing-notification-class.md` still open: omp and ops tips unchanged.
  - Friction folded (10 rows, 2026-09-22 to 09-23): one clause earned, scenarios.md "Field shapes not built yet" gains `licenseUrl` among the context passthroughs (copied into a publication at publish, so set before a `published` seed; rr14). The rest earn nothing: already in the docs (automatic assignment on `publicknowledge` only, `users[].notifications`, ISSNs, Escape closing the workflow dialog, no published item on `publicknowledge`), one screen's fact (the fetch-template-body 500, "Editor Assigned (Auto)" opening its template directly, the OPS Production notice box), general Playwright (`check()` on a checked radio), or a one-off of a single brief's session note (the security probe's read-only older file).
  - Stable line: no range (its log).
  - Final at the new tip on a reset database, eight workers (`.reports/sync/final-run-ojs.log`): OJS 293 passed (9.1 min, first run, serial and solo included, no `login smoke:` line). OMP and OPS not run as finals: their tips and pointers are unchanged since the 2026-09-23 post-fix finals (`cdf9458`: OMP 283, OPS 182 green), and the push run 35854852786 is green on both. PROGRESS banner: the OJS figure.
  - Baselines advanced (ojs and its lib/pkp and ui-library; omp and ops unchanged).
- **2026-09-22 (sync) — ojs `cc81df882a..38781720df` (3), omp unchanged at `7f9455d5a`, ops `53b40233f0..15f0b6e0bd` (2), pkp-lib `63945bbd82..f8bacd7658` (6, ojs and ops; the omp pointer stays at `63945bbd82`), ui-library `977e460c..5d138aa9` (2, ojs and ops), ojs `plugins/generic/crossref` `2ccd35f99b..96bfcaad44` (1), ops `plugins/generic/crossref` `8cea017e15..b6b94dd5de` (1), ojs `plugins/generic/jatsTemplate` `c1c0e5379f..1065bb02ae` (11). The app-side commits are pkp/ojs#5813 (`325931a71c`, the jatsTemplate bump) and the #13353 submodule bumps pkp/ojs#5839 (`0424086f25`, merge `38781720df`) and pkp/ops#1408 (`97e8a3e427`, merge `15f0b6e0bd`).**
  - CI at the tips: ojs run 35711998906 (`38781720df`), omp 35379521188 (`7f9455d5a8`, unchanged), ops 35712040684 (`15f0b6e0bd`) → all green. pkp-e2e `main` at `66021a2`: the push run 35598209289 and the nightly 35683638739 green on all three; the nightly's OJS job had one flaky test, U35 S2's "User added as a stage participant." toast not found in 30 s after the Assign window's `okAndClose()` on the first attempt, green on the retry → a toast lives 5 s and the read began after the action's own waits; HARNESS FIX: `expectToastDuring()` in the shared `NotificationsPages.js` arms the wait before the action, the twelve read-after-action sites in the OJS and OMP U35 suites use it, both suites green alone (`.reports/sync/s22-u35-{ojs,omp}-alone.log`); ci-triage flake-watch entry. The nightly's OJS job took 27.8 min of tests (29.5 min wall; 26.6 the day before): the shard matrix is overdue (PROGRESS banner). No known-red rows, no companions.
  - pkp-lib #13351 (`b22f984b9a`, merge `ed7759d625`, PR #13352; all three apps once omp catches up): `PKPDoiController::getMinorVersionsViaPubObjectHandler()` answers `[$pubObject]` instead of `[]` for a handler without `getMinorVersionsWithSameDoi()`, so the versioned-DOI edit and delete paths (`edit()` line 316, `delete()` line 388, reached only when the client sends `pubObjectType`/`pubObjectId`) update the peer-review DOI and the author-response DOI themselves instead of nothing → not covered yet (U45 DOIs pending). Regression verdict in the log line: the two callers are in the file, Publication and Galley (and OMP's Chapter, PublicationFormat and SubmissionFile) keep their own method and are untouched; the default path without the two parameters is unchanged.
  - pkp-lib #13213 (`cba0f20c11`, merge `1d71584331`, PR #13357; all three apps once omp catches up): the 3.6 upgrade migration `I13213_TaskTemplateForeignConstraint` nulls `edit_tasks.edit_task_template_id` values with no `edit_task_templates` row before adding the foreign key → not covered yet (no shipped spec describes an upgrade; a fresh install takes the install migration, unchanged). Regression verdict in the log line: upgrade only, a guarded two-statement addition ahead of the constraint the 2026-09-11 sync read; not drivable here (no 3.5 database to upgrade).
  - pkp-lib #13358 (`bf020ba77e`; all three apps once omp catches up; the same patch is on `stable-3_5_0` as `fc38d0622c`, in no pointer yet): `SubmissionFilesCategoryGridDataProvider::loadData()` no longer `break`s the file loop on a query-stage file whose `assocType` is not a note (`continue`) and reads `$query` only inside the note branch (the PHP warning of the issue) → no impact on any spec. Regression verdict in the log line: the provider serves the four "Select files" category grids (Edit Review's "Files To Be Reviewed" via `ReviewCategoryGridDataProvider`, the Draft, Copyedited and Production Ready "Upload/Select Files" windows via `Manage{FinalDraft,Copyedit,Proof}FilesGridHandler`, plus OMP's `PublicationFormatCategoryGridDataProvider`); a discussion attachment is always a note's file, so the old `break` was dead in practice and the new `continue` only widens the listing; U27 S6, U32 and U33 ride on the finals.
  - pkp-lib #13353 (`f8bacd7658`) + ui-library `2bccfbe9`, `5d138aa9` + crossref-ojs `96bfcaa` / crossref-ops `b6b94dd` (issue pkp/pkp-lib#13353, PRs pkp-lib#13354, ui-library#988, crossref-ojs#110, crossref-ops#71; ojs and ops; pkp-lib `main` at `b878a76ed4` is the #13354 merge, tree identical): a Crossref deposit answered with warnings counts as registered (`isWarningResult()` keeps it out of `$resultErrors`, so `depositSubmissions()` no longer reports `hasErrors` and the peer-review deposits are queued), the stored success message gains "Crossref reported warnings for this deposit…" on a new line, the DOI list's "registered" dialog renders `registeredMessage` through `v-strip-unsafe-html` with `whitespace-pre-line` and drops the `registeredMessagePreamble` paragraph and its locale key → not covered yet (U45 DOIs pending). Regression verdict in the log line: no reader of `manager.dois.registrationAgency.registeredMessagePreamble` or of the dropped prop is left in lib/pkp, the three apps or their plugins (grepped: the ui-library read a key no PHP ever supplied); the only writer of a DOI's success message is the Crossref plugin's `updateDepositStatus()` (DataCite writes failure messages only), whose text is two locale strings with a newline, so the HTML rendering changes nothing for a stored message; the deposit path is unreachable on the fleets (no egress), CLI return value unchanged by the issue's own design.
  - ojs #5813 (`325931a71c`; pkp/pkp-lib#12356 and #13277; ojs only, the plugin ships in OJS alone): `plugins/generic/jatsTemplate` `c1c0e5379f..1065bb02ae` (11 commits, Kaitlin Newson): the abstract's `htmlAbstractToJats.xsl` is deleted for a PHP pipeline in `JatsHelper` (`htmlToJatsContent()`, `normalizeParagraphs()`, lists, breaks and blank content; `htmlToJatsElement()` now answers null for blank block content), `ArticleFront` gains the history element, NISO ALI license elements, related object ids, the data availability statement, ror casing and the degree-contribution attribute only when set, `ArticleBack` and `PeerReview` adjusted, tests added → not covered yet (U48 JATS & Body Text pending; the plugin builds the Publication › JATS XML tab's default document, the OAI `jats` format and the JATS usage-statistics counter). Regression read rr14 (`.reports/sync/rr14/suspicions.md`; OJS): 6 suspicions written, 2 reproduced, 4 not reproduced, 2 unverified hunches (the peer-review sub-article, `mixed-citation`), nothing routed. REGRESSION (S2), re-held on a freshly reset OJS the same day (`.reports/sync/s22-5813-fresh/`, `rr14/after-literal2.xml`; kept `checks/sync/ojs-5813/default-jats.js`; report `docs/reports/2026-09-22-ojs-5813.md`; ci-triage row): an abstract naming tags as text with a matching close tag comes out of the default JATS with the names turned into `<italic>`/`<sub>` and the sentence split into four `<p>`, where the deleted stylesheet kept the sentence verbatim (`rr14/before-literal2.xml`, the XSL run standalone on the same stored string: no live fleet carries the old plugin); cause the decode-then-map order of `htmlToJatsContent()`, pre-existing in the old `JatsHelper` for `bio`/`fn`/`notes`, new for the abstract, plain-language summary and data-availability section; minor. S6 reproduced as an intended difference: `license/@xlink:href` moves from the journal's stored `licenseUrl` to the CC badge link (`…/by/4.0/`), `ali:license_ref` the same, `ali:free_to_read` added on an open-access journal; noted in the report's evidence, not filed. Not reproduced, all as intended and DTD-valid: S1 the rich abstract (bold, italic, sup, sub, `&`, a link, lists, `<br>`: the intended JATS shape; the old stylesheet's `html:`-prefixed templates never matched, so before this bump every rich abstract lost its inline markup, links and lists with each text run in its own `<p>`, a repair worth knowing), S3 a bare submission (`<history>` received date, no `<back>`), S4 the data availability `<sec>` before `<ref-list>` with and without references, S5 the OAI `jats` GetRecord and ListRecords (`xmlns:ali` declared, `<history>` inside). Pre-existing, not this bump: a journal without an ISSN yields a `journal-meta` the DTD refuses. On the maintainer's question in the thread the same afternoon: the literal-tag abstract driven through TinyMCE itself (`checks/sync/ojs-5813/typed-abstract.js`, `.reports/sync/s22-5813-typed/`: stored entity-encoded, the JATS as reported; evidence bullet added to the report), and one observation routed to the private file, verified the same day (sec22, `.reports/sync/sec22/`), DM sent to @jarda.kotesovec.
  - Open regression pkp-lib#12352 re-run on OJS at the new tip (`.reports/sync/s22-12352/`): still reproduces (`afterCancel` fileId 2 `article-rev.pdf` current, `status:false`); #13288 still open at `35bb1839df`; row dated. Open regression pkp-lib#13181 re-run on OJS (`.reports/sync/s22-13181/`): still reproduces (both landings `index/en/user/profile#contact`, the old-shape decline link ending in GET 500, request pending); row dated. Report `docs/reports/2026-09-04-omp-ops-discussion-save-missing-notification-class.md` still open: omp `7f9455d5a` and ops `15f0b6e0bd` track no `classes/notification/Notification.php`.
  - Friction folded (33 rows, 2026-09-18 to 09-21, 13 clusters, the proposal in `.reports/sync/fold-0922/proposal.md`): five earned a clause on an existing entry, none a kit change: patterns.md "Fixture selection" (never combine a describe-level `test.use({user})` with `asUser`: U32, U33, U35) and pitfall 4 (a legacy window closed by its own Cancel leaves the same hidden shell; the workflow dialog stays aria-hidden through the 450 ms slot after an inner window closes), scenarios.md "Field shapes not built yet" (`reviewRounds[].reviewers[].status: 'complete'`, `files[]`, the OJS `payments` passthrough) and the Mailpit line (a `subject` carrying quotes matches nothing), harness.md "Server output" (the probe servers' log path); 28 rows earned nothing (19 already folded by their own session, the rest one screen's fact, one session's brief or general knowledge); rows deleted; rr14's one row appended after the fold stays for the next.
  - Stable line: no range (its log).
  - Finals at the new tips on reset databases, four workers, one app at a time (`.reports/sync/final-run-{ojs,ops}.log`): OPS 179 passed (8.0 min, first run); OJS 291 passed (17.5 min, first run, serial and solo included, no `login smoke:` line); OMP not run as a final (its tip and lib/pkp pointer unchanged since yesterday's 266 of 267 + 14 alone; the nightly 35683638739 green on OMP at the same tips, 281 in 18.9 min); the touched OMP U35 suite green alone (5 passed, 32.1 s, behind a `--project=setup` pass on the reset database: `--no-deps` skips the install). PROGRESS banner replaced.
  - Baselines advanced (ojs, ops and their lib/pkp; omp unchanged).
- **2026-09-21 (sync) — ojs `7c8d69af3e..cc81df882a` (2), omp `c6a132892..7f9455d5a` (2), ops `4bb66b1469..53b40233f0` (3), pkp-lib `14473fe784..63945bbd82` (1, ojs) / `1bcd4dd55f..63945bbd82` (2, omp and ops: #12163 reviewed for ojs 2026-09-18 plus today's one), ui-library unchanged, ojs `plugins/generic/crossref` `843650012d..2ccd35f99b` (1). The app-side commits are the 2026-09-18 `translations/stable-3_5_0` merges (ojs `f2e617dc67`, omp `8a6ff0375`, ops `69a9cc564e`: locale files and the plugin submodule pointers) and "Submodule updates" bumps (ojs `cc81df882a`, omp `7f9455d5a`, ops `63de6de7dd`).**
  - CI at the tips: ojs run 35379093921 (`cc81df882a`), omp 35379521188 (`7f9455d5a8`), ops 35390711119 (`53b40233f0`) → all green. pkp-e2e `main` at `735bb76`: the U35 push run 35534810553 green on all three; the nightly 35558115088 (2026-09-21 03:36 UTC) RED on OJS and OMP, green on OPS. Triaged: both jobs red on U34 S7 on both attempts (OJS 274 passed, 16 did not run, 26.6 min; OMP 265 passed, 1 flaky, 14 did not run, 23.2 min), the same tree and the same app tips as the green push run seven hours earlier (both jobs checked out lib/pkp `63945bbd82`), so not upstream: the "Add Email Template" window closes itself a second after "Save" (`ManageEmailsPage.vue` `templateSaved()`, `setTimeout(closeSideModal, 1000)`) and the page object's 30 s read of its "Saved" status found the window gone (the CI artifacts, `.reports/sync/s21-ci-artifacts/`). S7 green alone on the VM at the same tips before any change. HARNESS FIX: `ManageEmailsPage.save()` in the OJS and OMP `DecisionSettingsPages.js` waits for the window's own close and presses "Close" only when it stays, `addTemplate()` asserts the new template in the mailable's list; S7 green alone on both apps (`.reports/sync/s21-u34s7-*.log`); ci-triage flake-watch entry. The OMP job's flaky U21 S3 (the wizard's timed autosave not firing within 100 s, green on retry) is a first sighting, ci-triage entry with a watch condition. No known-red rows, no companions. OJS on CI's fresh box now takes 26.6 min with U34 and U35 on board, over the 25-minute line the PROGRESS banner names (the shard matrix in `run-app.yml` is the next task).
  - pkp-lib `63945bbd82` (the `translations/stable-3_5_0` merge into `main`; 51 files, all three apps): 50 non-English locale files and `locale/en/emails.po`, whose only English change is the header and two `#, fuzzy` marks on `orcid.orcidRequestAuthorAuthorization.name` and `orcid.orcidCollectAuthorId.name` → no impact; regression verdict in the log line: the loader (`LocaleFile.php`, gettext `PoLoader` + `ArrayGenerator(includeEmpty: false)`) ignores flags, both keys resolve to their names (checked with the vendored loader on the file); the ORCID mailables' list rows ride on today's finals.
  - ojs `f2e617dc67`, omp `8a6ff0375`, ops `69a9cc564e` (translations merges; plugin pointer bumps for browse, makeSubmission, citationStyleLanguage, crossref, customBlockManager, googleAnalytics, lensGalley, staticPages, webFeed, oaiJats, reviewReport, each to its own `translations/stable-3_5_0` merge): locale files only (`locale/rue`, `cs`, `th`, `mk`, plugin locales) → no impact; regression verdict in the log line: no PHP, JS or template in any of the diffs (grepped per submodule range). `main`'s `locale/en/manager.po` on ojs already had `mailable.subscriptionExpired.description` empty (since `0f31c4c3ed`), the stable line's one English change matches it.
  - ops `53b40233f0` (pkp/pkp-lib#11759, Erik Hanson; ops only): a new 3.4.0 upgrade migration `I11759_RemoveDuplicateFilters` deletes the old-style `plugins.generic.crossref.filter.PreprintCrossrefXmlFilter` row from `filters`, registered at the end of the 3.6.0 upgrade block, and `I7014_DoiMigration` moves before `I6091_AddFilterNamespaces` in the 3.4.0 block → not covered yet (no shipped spec describes an upgrade; a fresh install never runs it); regression verdict in the log line: the delete is by exact class name, the row it removes is the pre-3.4 duplicate the issue shows, the reordering keeps every migration; no caller. Not drivable here (no 3.3 database).
  - omp/ops pointer catch-up on pkp-lib `14473fe784` (#12163, reviewed for ojs 2026-09-18): the kept `checks/sync/pkp-lib-12163/term-suggestions.js` re-run on OMP and OPS on databases reset this morning (`.reports/sync/s21-12163/`, `ONLY=omp,ops`): every read matches the OJS record (published sources offered, a queued, declined or unpublished-again one not, the later-published one without a reload, the other press's term only there, the Author's wizard list the same) → accommodated in U40: A10 RETIRED (writing agent, `.reports/sync/s21/fold-log-U40.md`: Rule 7b a plain claim for all three apps, the Coverage duplicate deleted, the register header, the summary row and the entry moved to Retired, footnote e reworded, f-a10 extended); persona on the rewritten spans (`persona-0921-U40.md`): 2 blockers, the wording one fixed (no shas or pointer talk in the retired line), the fact one a new ❓ entry OJS2 with a lean (whether a scheduled article's terms are offered; lean not offered, the lookup keeps the published status only, footnote f-ojs2, code read); lint zero, `--tests` clean; the three U40 suite headers updated; no test change; PROGRESS row updated.
  - Open regression pkp-lib#12352 re-run on OJS at the new tip (`.reports/sync/s21-12352/`): still reproduces (`afterCancel` fileId 4 `article-rev.pdf` current, `status:false`); #13288 still open, head now `35bb1839df`; row dated. Open regression pkp-lib#13181 re-run on OJS (`.reports/sync/s21-13181/`): still reproduces; row dated. Both re-run on the stable line too (its log). Report `docs/reports/2026-09-04-omp-ops-discussion-save-missing-notification-class.md` still open: omp `7f9455d5a` and ops `53b40233f0` track no `classes/notification/Notification.php`.
  - Harness: `shared/php/classes/testing/ContextFactory.php` guards the `main`-only `Repo::reviewerRecommendation()` call with `method_exists` (a no-op on `main`; the stable line's context seed answered 500 on it). Friction: not folded (the sync accommodated: U40); today's reader appended one row.
  - Finals at the new tips on reset databases, four workers, one app at a time, with the hardened `save()` (`.reports/sync/final-run-{ojs,omp,ops}.log`): OPS 179 passed (8.0 min, first run); OJS 274 of 275 (15.7 min), the one red the login smoke's `reader.rosa` probe (the ci-triage class's seventh sighting, the first carrying the instrumented jar line: the session cookie was in the jar and both probes landed on login, entry updated), green alone in 20.5 s and the 16 serial and solo tests green alone (1.7 min); OMP 266 of 267 (13.2 min), the one red U49 S4's "Create New Version" stage select empty (the ci-triage class, dated), green alone in 10.2 s and the 14 serial and solo tests green alone. U34 S7 green in all three finals. PROGRESS banner replaced (OJS 275 · OMP 267 · OPS 179; OJS 26.6 min on CI's nightly, past the 25-minute line).
  - Baselines advanced.
- **2026-09-18 (sync) — ojs `c0ca4b3caf..7c8d69af3e` (2), omp `b35b32427..c6a132892` (1), ops `e7ab4b6878..4bb66b1469` (1), pkp-lib `efbba94ae7..14473fe784` (6, ojs) / `360badeef5..1bcd4dd55f` (12, omp and ops: the seven reviewed for ojs 2026-09-17 plus the first five of today's six), ui-library and crossref unchanged. Every app-side commit is a "Submodule update" pointer bump (ojs `80baa32662`, `7c8d69af3e`; omp `c6a132892`; ops `4bb66b1469`).**
  - CI at the tips: ojs run 35266899117 (`7c8d69af3e`), omp 35260448287 (`c6a132892f`), ops 35260495031 (`4bb66b1469`) → all green. pkp-e2e `main` at `78dc64d`: push run 35233067098 and the nightly 35303597668 green on all three. Nothing red to triage; no known-red rows, no companions.
  - pkp-lib #13313 (`2ecbd331ee`; issue still open; all three apps): `GET users/{id}` answers for a disabled user and the role-invitation payload carries `disabled` → accommodated in U06. Claim check S18a on OJS, OMP and OPS (`.reports/sync/s18/cc-S18a.md`: 1 holds, 2 wrong, 1 imprecise, 3 new; kept script `checks/sync/pkp-lib-13313/s18a.js`), fold (`fold-log-U06.md`: A6 retired, Rule 14's second half rewritten, the disabled-user bullet moved from No seed to Budget, the state being one on-screen step), persona (`persona-0918-U06.md`: 1 wording blocker, fixed), lint zero, `--tests` clean; the three U06 suite headers updated; no test change. FINDING against the issue's stated intention ("all fields are disabled meaning they can't be edited"): on the disabled user's details step each current role keeps an active "Remove Role" and masthead select, "Remove Role" ends the role and the disabled user is emailed "You have been removed from a role" with "Your account with {journal} is still active…" → U06 A9 ❓ lean 🐞; held on freshly reset databases on the three apps (`.reports/sync/s18-13313-fresh/`); a report with the exact steps was posted in the thread and sent by DM, and deleted once ruled (git keeps it at `3357954`; U06 f-a9 and the kept script hold the evidence). Reviewed by @beaug the same day on the thread: the two controls stay active for a disabled user by design, the email's wording is the open half; A9 narrowed to it with the Reviewed blockquote, Rule 14 now states the controls as a plain claim. A guard scenario for Rule 14 is owed at U06's next scenario work (the fold's note).
  - pkp-lib #12702 / PR #13054 (`c967e34ba4`, `2b13599365`, `462d628de3`; all three apps): the JATS, Body Text and Media controllers split their routes into a read group with the Author and a write group of Manager, Site admin, Sub editor and Assistant, and drop `SubmissionFileStageAccessPolicy` from the writes → not covered yet (U47 Media files, U48 JATS & Body Text pending). Regression read rr11 (`.reports/sync/rr11/suspicions.md`; OJS, media on all three apps): 5 suspicions written, 0 reproduced, 2 unverified hunches; the issue's own case holds (an assigned Section editor and `admin` holding an Author role upload a JATS file, POST 200), the manager's JATS upload, public box and delete, the Body Text save, media add/edit/link/delete and the Author's read-only Publication screens all as before. Kept: `checks/sync/pkp-lib-12702/jats-upload.js` (the three uploads; re-run `.reports/sync/s18-12702/`). One observation routed to the private file, verified the same day by the verification probe (sec18), DMs sent to @jarda.kotesovec and @beaug.
  - pkp-lib #13348 (`1bcd4dd55f`; all three apps): `PKPContainer` aliases `SessionManager` to `session` and the store classes to `session.store` → no impact; regression verdict in the log line: aliases only, the issue itself records that no caller reads the second store, sign-in, CSRF and sign-in-as ride on today's finals.
  - pkp-lib #12163 (`14473fe784`; ojs only): `ControlledVocab::scopeWithContextId()` replaced by `scopeWithContextIdAndPublishedPublications()`, so term suggestions come only from published publications of the asking journal → accommodated in U40. Claim check S18b on OJS with OMP and OPS as the "before" side (`.reports/sync/s18/cc-S18b.md`: 19 lines driven, 2 wrong, 3 imprecise, 1 new; kept script `checks/sync/pkp-lib-12163/term-suggestions.js`), fold (`fold-log-U40.md`: Rule 7 split into 7a and 7b, the Fields row, footnotes e and f-a10; A10 settled from ❓ to 🐞 scoped {OMP OPS}: its empty lists of 2026-08-28 came from the old lookup, which the press and the preprint server keep until their pointers pass the commit; two Budget rows), persona (`persona-0918-U40.md`: 1 fact blocker, settled from the checker's record: the list's first entry is always the typed text), lint zero, `--tests` clean; U40 suite headers updated; no test change. Regression read rr12 (`.reports/sync/rr12/suspicions.md`): 9 suspicions written, 0 reproduced, 3 unverified hunches; no other caller of the removed scope in the three apps or their plugins (a fatal there was the first suspicion), the `vocabs` API, unpublish/republish, versions, a second journal, locales and the wizard's Details step all follow the issue's intention. One observation (the before side) routed to the private file, verified the same day (sec18), in the same DMs. When the OMP and OPS pointers pass `14473fe784`: re-run the kept script there and retire A10.
  - omp/ops pointer catch-up on the seven pkp-lib commits reviewed for ojs 2026-09-17 (#13299/#13340, #13181, #13335, #13343, #13344): the verdicts carry over (shared code). #13181: the kept `emailchange-links.js` on the three apps (`.reports/sync/s18-13181-{ojs,omp,ops}/`) → the open regression now shows on OMP and OPS exactly as on OJS; ci-triage row and the report updated. The re-drive yesterday's log owed (claim check S18c, `.reports/sync/s18/cc-S18c.md`, kept `checks/sync/pkp-lib-13181/a1-a3-redrive.js`, each app twice): U03 A1 (the tab lost on the one-journal forward) fixed on all three apps by `aeac6f75cf`, A3 (the site-level "reject" blank page) fixed on OMP and OPS as on OJS → both retired; A18 now the three apps' (`fold-log-U03.md`; one sentence added to U02's footnote on the `?0=roles` read); the three U03 suite headers updated; no test change. Persona (`persona-0918-U03.md`): 2 blockers, neither on today's sentences, both left for U03's next revision because settling them needs a drive: Rule 3's unsettled one-journal Site Administrator (the row's standing low-confidence item), and Rules 6c/6d naming the landing only for the multi-journal and the one-journal account, not for an account with no journal role or the Site Administrator of a several-journal site.
  - Open regression pkp-lib#12352 re-run on OJS at the new tip (`.reports/sync/s18-12352/`): still reproduces (`afterCancel` fileId 2 `article-rev.pdf` current), #13288 still open at `5f995d86af`; row dated. Report `docs/reports/2026-09-04-omp-ops-discussion-save-missing-notification-class.md` still open: omp `c6a132892` and ops `4bb66b1469` track no `classes/notification/Notification.php`.
  - seed-facts +1 line (the `submitted: false` draft reopens on "Upload Files"; no term keys on the submission scenario), scenarios.md "Field shapes not built yet" +`metadata.keywords`. Friction: not folded (the sync accommodated: U06, U40, U03); today's agents appended rows. Open questions: not posted (due about 2026-09-29).
  - Finals at the new tips on reset databases, four workers, one app at a time (`.reports/sync/final-run-{ojs,omp,ops}.log`), all green on the first run: OJS 262 passed (14.6 min), OMP 251 passed (12.1 min), OPS 172 passed (7.1 min). No flake-watch class tripped, no `login smoke:` line in any log.
  - Baselines advanced.
- **2026-09-18 (maintainer, the Mac) — friction fold and process edits, no upstream range.** Friction folded (98 rows, 2026-09-12 to 09-18, 19 clusters; the proposal and grading in the session's scratchpad): three kit changes, `screen()` reads a closing dialog with a 1 s timeout instead of throwing after 30 s (U26, U14), `launch(app, {record: false})` for the security probe (U03, U12 sec; a line in patterns.md "Probe kit" and the security-verify brief), and `definePkpConfig()` pins `TZ=UTC` so the runner's dates sit in the servers' zone (the ci-triage "Local midnight" row, U27/U28/U29 on OMP twice: deleted as resolved); two brief clauses (test-author: `screen-locators.md` and the kept checks exist only for kit-driven features; the failure snapshot is the fixture page's and the output folder is emptied by the next run; harness: one output folder per suite); four doc clauses (patterns.md lesson 9 the locale segment sticking to the session, pitfall 4 the shared jQuery calendar under a second legacy window, pitfall 2 `#setup-button` duplicated on Site Settings; scenarios.md Mailpit the discussion as the cheapest positive control) and two corrections (patterns.md every pkp Search box commits on Enter, `Search.vue` shared; scenarios.md the settings form's request is a POST with `X-Http-Method-Override`, lines 198 and 484). 76 rows earned nothing: one screen's facts, fixes the writing session already made, facts the docs carry, Playwright knowledge, one-offs; the weakest cluster (a legacy grid's hidden "no items" row, one feature) dropped. Verified on OJS pinned at today's baseline (`.reports/sync/fold-0918/`): the probe check returns after a cancelled side modal with no dialog text and the unrecorded launch leaves `responses: []`; U27 19 passed, U29 13 passed, U28 14 passed with S2, S6 and S7 red in the full file (the "Reviewer dashboard list under load" class, no date assertion) and green alone. Process: MAINTENANCE fold rule gains the bar "most rows earn nothing" (a retry is the ordinary cost; a fact one closer read would have given, or a fix the session already made, earns nothing; when in doubt, delete); Session hygiene pulls this repo first (`git pull --ff-only`) and fetches before the push, the maintainer also commits from the Mac; the monthly open-questions post dropped from the daily session and the standing duties, `npm run questions` stays on demand.
- **2026-09-17 (sync) — ojs `ae597ff9d9..c0ca4b3caf` (5), omp `0ec98a508..b35b32427` (1), ops `9ce633ee1d..e7ab4b6878` (1), pkp-lib `b262d27b81..efbba94ae7` (9, ojs) / `b262d27b81..360badeef5` (2, omp and ops: the first two of the nine), ui-library and crossref unchanged. Every app-side commit is a "Submodule update" pointer bump (ojs `88cf3ff327`, `6f11258771`, `bf855a3f15`, `371ca46eb6`, `c0ca4b3caf`; omp `b35b32427`; ops `e7ab4b6878`).**
  - CI at the tips: omp run 35134952914 (`b35b324276`) and ops 35135253327 (`e7ab4b6878`) green. ojs red on four pushes in a row, 35154618775 (`6f11258771`), 35158566726 (`bf855a3f15`), 35166798684 (`371ca46eb6`) and 35171539288 (`c0ca4b3caf`): U14 S5 on both attempts in all four and U14 S3 in one, both ours and already fixed (the app hooks run pkp-e2e `main`, which carried the U14 checkpoint suite from 2026-09-16 21:17 UTC; S5's heading read fixed in `2f9e8b0`, S3's close-window wait in `9337581`); pkp-e2e push run 35200505897 at `9337581` is green on all three at these tips. No upstream cause, no known-red row. OJS took 24.8 min on that run (255 tests), at the 25-minute line: PROGRESS banner, the shard matrix is the next task. No companions.
  - pkp-lib #13338 (`2dbdd585a6`; issue #13337, a follow-up of #13156 and the open #13258; all three apps): an editor's "Modify Review" save that completes a review also stamps the accepted date when unset and moves the reviewer to step 4 → accommodated in U27. Regression read rr8 (`.reports/sync/rr8/suspicions.md`; OMP and OJS): 7 suspicions written, 0 reproduced, 3 unverified hunches, nothing routed; the reviewer lands on "4. Completion" with no decline and a read-only step 3, an earlier acceptance keeps its date, a save on a submitted review leaves dates and step alone, no "Review accepted" email and no accepted log line, all as the issue states. Its Reach block contradicted U27 A24 ("no screen reaches the save on an unsubmitted review"), so claim check S17 ran on the slice (`.reports/sync/s17/cc-S17.md`, OJS and OMP, 99 lines driven: 23 holds, 2 wrong, 6 imprecise, 10 new; kept script `checks/sync/pkp-lib-13338/s17.js`), then the fold (`fold-log-U27.md`: Rules 14c and 14d new, A24 retired on the issue's stated intention, A29 ❓, A30 🐞, A31 🐞, OMP4 🐞 a press's "Mark as Complete" enabled on a request with no review, OMP5 ❓, OMP6 🐞), the persona read (`persona-0917-U27.md`: 2 wording blockers, fixed, body 17963 → 17962 words), lint zero, `--tests` clean. No test change: Rule 14d sits under Budget and is the first candidate for a coverage request; the three U27 suite headers name the new IDs. incidentals row (the press's "this journal" sentence) absorbed by OMP6 and deleted; seed-facts +1 line.
  - pkp-lib #13334 (`360badeef5`; all three apps): the tasks and discussions API loads participants with `filterByStatus(STATUS_ALL)`, so a disabled participant keeps their name in the list, the detail, the participant edit and the event log → not covered yet (U37 Tasks & discussions pending). Regression verdict in the log line: four call sites, all in the diff, each widening a read that feeds display only.
  - pkp-lib #13340 (`0dce988b35`, `c767c313b9`; issue #13299, follow-up #13339; ojs only): the invitations API's route group lists site admin and manager only, and the role-invitation payload refuses another journal's user group ("The provided user group does not belong to the invitation's context") → accommodated in U06 (footnotes b and f-a1; register A1 unchanged). Regression read rr9 (`.reports/sync/rr9/suspicions.md`): 2 suspicions written, 0 reproduced, 2 unverified hunches; the only clients of the narrowed routes are the Users & Roles invitations table and the send wizard (managers and the Site Administrator), the Add Reviewer window uses the legacy grid forms, the accept side sits outside the group; a cross-journal invitation and an edit-mode "Add Another Role" both sent and accepted with every call 200. One observation routed to the private file, verified the same day by the verification probe (sec17), DMs sent to @jarda.kotesovec and @beaug.
  - pkp-lib #13181 (`4d9ec3cbd0`, `aeac6f75cf`; no PR page; ojs only): invitation links, the decline form and the redirects are built from the invitation's own journal → accommodated in U03 (Rule 6d, A3 scoped {OMP OPS}: the site-level "reject" crash is fixed in OJS; A18 {OJS} new; footnotes d, f-a3, f-a18, the code map row; persona `persona-0917-U03.md` 1 wording blocker, fixed, body 13698 words unchanged; lint zero; no test change, the OJS suite header updated). Regression read rr10 (`.reports/sync/rr10/suspicions.md`): 5 suspicions written, 2 reproduced, 1 in part, 2 not, 2 unverified hunches, nothing routed. REGRESSION, both re-held twice on a freshly reset OJS and the "before" side driven on OPS, which still has the old code (`.reports/sync/s17-13181/`, `s17-13181b/`, `s17-13181-ops-before/`; kept script `checks/sync/pkp-lib-13181/emailchange-links.js`; report `docs/reports/2026-09-17-pkp-lib-13181.md`; ci-triage row): (1) the email-change invitation is created without a journal, so its links are now `index/invitation/…` and an account with roles in two journals lands on the site-level profile after "confirm" or "reject" (minor); (2) a pending request's "reject" link in the shape mailed before the change, one-language journal on a two-language site, ends in a blank GET 500 with the request still pending (low, transitional). Not reproduced: role-invitation links on a two-language journal, the one-journal account's email change, the reviewer's one-click link. Seen on the way: the reviewer-access `decline` address, which no email carries, now declines the access invitation (was a 404); the French accept wizard shows raw locale keys. U03 A1 (the one-journal forward losing its tab) looks fixed by `aeac6f75cf` on the `contact` leg only; re-drive it on `roles` together with A3 when the OMP and OPS pointers pass `4d9ec3cbd0`.
  - pkp-lib #13335 (`b50be82a3d`; ojs only): `FileManager::getImageExtension()` no longer maps `text/javascript` to `.js` → no impact on any spec; regression verdict in the log line: every caller (public profile image, category, highlight, context and site images, announcement, issue cover, the public-file upload, the publication cover) uses it to accept an image, none relied on a script passing.
  - pkp-lib #13343 (`f80f5b9483`; ojs only): `reviewRead` leaves the Author's role assignment and policy list in `AuthorReviewerGridHandler` → accommodated in U26 (footnote j: the operation no screen ever sent is no longer granted to the Author; "Read Review" unchanged); callers in the diff, no client in ui-library `src`, lib/pkp `js` or templates (grepped).
  - pkp-lib #13344 (`efbba94ae7`; ojs only): a publication cover image whose temporary file is missing or is not an image type is dropped on save → no impact (U49 names the Cover Image as a display extra, no claim on refused files); regression verdict in the log line: one guard before `moveTemporaryFile()`, a real image passes it unchanged.
  - Open regression pkp-lib#12352 re-run on OJS at the new tip (`.reports/sync/s17-12352/`): still reproduces (`afterCancel` fileId 2 `article-rev.pdf` current), #13288 still open at `5f995d86af`; row dated. Report `docs/reports/2026-09-04-omp-ops-discussion-save-missing-notification-class.md` still open: omp `b35b32427` and ops `e7ab4b6878` track no `classes/notification/Notification.php`.
  - Stale artifact fixed: GLOSSARY "Reading a spec" now says what a register entry's "Since" date is (the persona's remark; ten specs use it). Friction: not folded (the sync accommodated: U27, U03, U06, U26); today's agents appended four rows. Open questions: not posted (due about 2026-09-29).
  - Finals at the new tips on reset databases, four workers, one app at a time (`.reports/sync/final-run-{ojs,omp,ops}.log`), all green on the first run: OJS 255 passed (14.0 min), OMP 244 passed (12.3 min), OPS 165 passed (7.0 min). No flake-watch class tripped.
  - Baselines advanced.
- **2026-09-16 (sync) — ojs `c40cf7644c..ae597ff9d9` (4), omp `6c2f43041..0ec98a508` (4), ops `a9988de891..9ce633ee1d` (3), pkp-lib `7ea748823e..b262d27b81` (1, ojs) / `77b76d7664..b262d27b81` (2, omp and ops: #13321 reviewed for ojs 2026-09-15 plus one), ui-library `16f3fdad..977e460c` (5, ojs) / `db5b2813..977e460c` (6, omp and ops: #13321's `16f3fdad` reviewed for ojs 2026-09-15 plus five), crossref unchanged. The app-side "Update submodules" / "Submodule update" commits (ojs `3d0b1df3f3`, `5e74c13d87`, `ae597ff9d9`; omp `db69c4aeb`, `2b721e4a1`, `0ec98a508`; ops `8e6f2dd403`, `9ce633ee1d`) are pointer bumps.**
  - CI at the tips: ojs run 35017347571 (`ae597ff9d9`), omp 35017590399 (`0ec98a5087`), ops 35017665886 (`9ce633ee1d`) → all green. pkp-e2e `main` at `ae87d19` (the U49 revision): push run 35115933110 green on all three, the U43 push 35096955719 and the U41 follow-up 35083434269 green too. Nothing red to triage; no known-red rows, no companions. (The ojs repo's own Cypress `schedule` workflow, run 35081749362, is red on ojs-main pgsql and the two stable-3_3_0 jobs; not the e2e suite, not ours.)
  - pkp-lib #13333 (`b262d27b81`) + ui-library #986 (`393dd289`, `d5d70170`, `977e460c`) + ojs #5826 (`e0e0275a55`) / omp #2463 (`6eb3b935a`) / ops #1402 (`fb72f079ba`): the `vue-scrollto` plugin is dropped (`load.js`, the three apps' `package.json` and `vite.config.js`) for a `useScrollTo` composable behind `Form.vue`'s page change and jump-to-error and `Steps.vue`'s step change, scrolling with the browser's own API and, since the same-day follow-up `977e460c`, deciding per call to jump instantly under `prefers-reduced-motion` (a global `scroll-behavior: smooth` rule broke Cypress's own scrolls) → no impact on any spec (no shipped claim describes the scroll). Regression verdict in the log line: the maintainer's own change from the campaign's 2026-09-15 patch (ci-triage's wizard-press entry, `docs/reports/2026-09-15-flake-investigation.md`), the three call sites in the diff, no `$scrollTo` or `vue-scrollto` caller left in ui-library `src`, pkp-lib `js`, the three apps' `js` or their plugins (grepped), the finals below as the drive. Harness: the `asUser` contexts already pass `reducedMotion`; `pressUntil()` kept as a content-verified bounded retry, its watch condition now live (ci-triage).
  - ui-library `70b08920` (#986 too): `WorkflowPage.vue` keys each configured item by its config `key`, and the OJS, OMP and OPS editorial configs (plus the OJS author config) give the Title & Abstract, Metadata and Data Availability `WorkflowPublicationForm` items their own keys, so a section switch mounts a fresh form → no impact on any spec; app-changes row 9 (c) closed on the tips (the U40 OMP `blur()` stays), ci-triage's U40 S4 entry dated.
  - ui-library #985 (`b3ee741f`, Blesilda Biazon; `useSubmission().getWorkflowStages()` filters the submission's `stages[]` to the editorial workflow stages, `WORKFLOW_STAGE_ID_DONE` added to `globals.js`, `useFileAttacherWorkflowStage.js` builds the "Select submission stage" options from it) → the open regression pkp-lib#13109 / #13312 picker row is FIXED: the kept `checks/sync/pkp-lib-13109/regressions.js` on a reset OJS at the tips (`.reports/sync/s16-13109/`) reads the select's options as Submission, Review, Copyediting, Production, none disabled, while `GET submissions/{id}` still answers five `stages` entries, the fifth `{id: 6, label: "Done"}` (by design since #13312); the stats table and the Roles grid still four stages. Row deleted; U24 A10 (ruled a bug 2026-09-14) moved to the register's Retired block with footnote f-a10 extended, Rule 18's marker gone (writing agent, lint zero); no suite header names it. Regression verdict in the log line: one caller of the new filter (the attacher), the mock factories untouched, no other reader of `stages` in ui-library `src` (grepped).
  - Open regression pkp-lib#12352 re-run on the reset OJS at the new tip (`.reports/sync/s16-12352/`): still reproduces (`afterCancel` fileId 2 `article-rev.pdf` current), #13288 still open at `5f995d86af`; row dated. Report `docs/reports/2026-09-04-omp-ops-discussion-save-missing-notification-class.md` still open: omp `0ec98a508` and ops `9ce633ee1d` track no `classes/notification/Notification.php`.
  - Flake watch, the two items ci-triage left to this session: the OJS Notify helper (`notifyParticipant()`) gained a content-verified bounded retry on the template body (U41 S1 and S3 green alone, `.reports/sync/s16-alone-ojs.log`); the login smoke's profile probe now records the cookie jar, the page URL and the probe's landing in the run log and probes once more before it reds (the class's next sighting carries its evidence). The U40 S2 OPS `readRichText` entry closed: the fix's first CI run (35115933110) green on OPS.
  - Tests touched: none (helpers only). Persona read of the rewritten U24 spans (`.reports/sync/persona-0916.md`): 3 wording blockers, 1 on today's sentence (the "discussion attacher" named no screen; Rule 18 now names the discussion's "Add" › "Attach Files" › "Attach Workflow Files" step), 1 pre-existing outside the span (Rule 18's "or neither, Rule 6" pointer, for U24's queued revision), 1 on the register's ✅ · retired shape, which GLOSSARY "Findings register" now explains in one clause. Friction: not folded (the sync accommodated: U24). Open questions: not posted (due about 2026-09-29).
  - Finals at the new tips on reset databases, four workers, one app at a time (`.reports/sync/final-run-{ojs,ops}.log`, `final-run-omp-attempt1.log`, `final-run-omp.log`): OJS 238 passed (13.3 min, first run); OPS 153 passed (6.5 min, first run); OMP 220 of 221 on the first run, the one red U27 S9's "5 out of 5 stars" radio not taking the click (the ci-triage class's second sighting, watch condition tripped and the helper hardened the same day: jQuery idle before the press, a bounded re-check; green alone twice), then 232 passed on the second run on a reset database (11.1 min) with the hardening. No `login smoke:` line in any log (the instrumented probe never missed). CI on this push is the fresh-box run.
  - The push's CI run (35126077430 at `00be9dc`): OJS and OMP green, OPS red on U49 S6 on both attempts (the "Create New Version" dialog's stage select empty: the ci-triage class's second sighting, the first on CI and on OPS; watch condition tripped, the three openers hardened with a bounded wait for the stage value, U49 S6 green alone on the three apps, `.reports/sync/s16-rerun-{ops,ojs,omp}-u49s6.log`); the follow-up push's run is the gate.
  - Baselines advanced.
- **2026-09-15 (sync) — ojs `f0cde27fda..c40cf7644c` (8), omp `bbd28d570..6c2f43041` (5), ops `04bbf46097..a9988de891` (4), pkp-lib `1967e76f38..7ea748823e` (6, ojs) / `9f99bb57a6..77b76d7664` (7, omp and ops: the #12715 pair reviewed for ojs 2026-09-14 plus five), ui-library `db5b2813..16f3fdad` (1, ojs only), ojs `plugins/generic/crossref` `a03c4b76c6..843650012d` (1). The app-side "Submodule update(s)" commits (ojs `a52ad1543a`, `6cd66a8010`, `4322083402`, `c40cf7644c`; omp `cdb57e426`, `0972724f3`, `6c2f43041`; ops `6bd744630a`, `6040a3354a`, `a9988de891`) are pointer bumps.**
  - CI at the tips: ojs run 34956195225 (`c40cf7644c`), omp 34943200456 (`6c2f43041`), ops 34943356503 (`a9988de891`) → all green. pkp-e2e `main` at `d2b89da` (the maintainer's perf round 2 pulled this morning, 14 commits: `_test/session` sign-in, bcrypt cost 4 for seeded users, `persistent = On` in `make-test-config.js`, test-side trims, `docs/reports/2026-09-14-suite-performance-round2.md`): push run 34952057769 and the nightly 34925472900 green on all three. Nothing red to triage; no known-red rows, no companions. (The ojs repo's own Cypress `schedule` workflow, run 34833641883, is red on two ojs-main jobs at `f0cde27fda`; not the e2e suite, not ours.)
  - pkp-lib #13321 (`7ea748823e`) + ui-library #984 (`16f3fdad`) + crossref-ojs#109 (`843650012d`; issue pkp/pkp-lib#13321): the Crossmark button leaves ui-library (`PkpCrossmarkButton` deleted, its `load_frontend.js` registration dropped) for the crossref plugin, which registers `CrossrefCrossmarkButton` from a Vite-built `public/build/crossref.js` on `ArticleHandler::view` and renders `crossmarkButton.blade` on `Templates::Article::Details` → not covered yet (no shipped spec names the Crossmark button; the DOI plugins' surface is pending). Regression read rr7 (`briefs/regression-read.md`, `.reports/sync/rr7/suspicions.md`; callers, script order, the blade resolution, the hook, versions, the settings form): 6 suspicions written and driven on a scratch OJS journal with the plugin, DOIs, the Crossref agency and Crossmark on, 0 reproduced, 4 unverified hunches (the ORE theme is not on the fleets, so pkp/oreTheme#58's call site is unread). Observed: crossmark on + DOI → `<section class="item crossmark" data-vue-root>` with `<a data-target="crossmark"><img alt="Crossmark">`, `crossref.js` loaded after the core bundle, no console warning; no DOI → no section and no scripts; a non-current version keeps it; crossmark unticked → no section. Kept as `checks/sync/pkp-lib-13321/crossmark-button.js` (re-run `.reports/sync/s15-13321/`). OMP and OPS: not there yet (their ui-library `db5b2813` still ships the old component; nothing renders it).
  - pkp-lib #13325 (`3964db27b5`, PR #13326; ojs #5822 is the submodule bump): `TitleAbstractForm` gives the Abstract and Plain Language Summary editors `bold italic superscript subscript | link | bullist numlist` with the `link` and `lists` plugins → accommodated in U40 (the fields paragraph, footnote d), driven on all three apps with the kept `checks/sync/pkp-lib-13325/abstract-lists.js` (`.reports/sync/s15-13325/`): both toolbars read Bold, Italic, Superscript, Subscript, "Insert/edit link", "Bullet list", "Numbered list"; a two-item bullet list saved (200) and read back after a reload and in `GET publications/{id}`. Matches the issue's stated intention for the input side; the reader-side rendering and the JATS export the issue also names were not driven (`allowed_html` already lists `ul`, `ol`, `li`). The statement editors are untouched. No test change.
  - pkp-lib #12827 (`648fee2e04`: `AuthorGridHandler`, `AuthorGridRow`, `PKPAuthorGridCellProvider`, `PKPAuthorForm` and their templates deleted) + ojs `4632d9cae0` (its message cites #12927; the diff is the app `AuthorForm` subclass deletion) + omp `fda2070ab` → accommodated in U41 (footnote m and the code map: the legacy grid, already recorded as unreachable, is deleted upstream). Regression verdict in the log line: no mount site and no caller in any of the three checkouts (grepped), the QuickSubmit plugin was its last user; no suite names it.
  - pkp-lib `77b76d7664` (persistent database connection behind `[database] persistent`, off by default; the maintainer's `docs/reports/2026-09-14-pkp-lib-persistent-db-connection.patch`, now upstream) + ojs `8e14a8f2d9` / omp `c7fa209a1` / ops `dcf5f60e6c` (the commented option in the config templates) → no impact on any spec. The harness's `make-test-config.js` already writes `persistent = On` (pkp-e2e `44f8f5b`), so CI's fresh configs carry it; the VM's fleets still ran the 2026-08-27 configs (`fetch-apps` generates the file only when absent), regenerated today from the current generator (only `persistent = On` and the template's new `filter_galley_html = Off` differ; the old copies are under `.reports/sync/s15-config/`), so today's finals are the first VM runs with the persistent connection.
  - pkp-lib `2eb00ced8f` (`userGroup/Repository::installSettings()` installs the locale settings once per locale after the group loop; the maintainer's second patch, now upstream) → no impact; verified on today's fresh installs (psql): every user group of context 1 has `name` and `abbrev` in en and fr_CA and none empty (18 OJS, 19 OMP, 5 OPS groups).
  - ojs #13225 (`2a94a0218c`, merged as `a4af345eec`, PR pkp/ojs#5811; issue pkp/pkp-lib#13225): the issue-published email's table of contents links articles and galleys through the page router with the journal path, and `IssueEmailVariable::getIssueToc()` assigns `journal` → not covered as a claim (U05 S9 reads the subject and the footer; no shipped spec describes the mail's links). Verified as the intended change on a reset OJS with the kept `checks/sync/ojs-13225/issue-mail-links.js` (`.reports/sync/s15-13225/`: a scratch journal, an issue created on screen, a submission seeded scheduled into it, a remote galley row): the Author's "Just published: Vol. 1 No. 1 (2026): Issue {tag} of {journal}" mail links `/index.php/{journal}/issue/view/{id}`, `/index.php/{journal}/article/view/{id}` (200, the article page) and `/index.php/{journal}/article/view/{id}/{galleyId}` (302 to the remote URL); no "You cannot call this operation without a context". A link bullet for the mail is a coverage change for U05's next revision.
  - pkp-lib `f353a450f2` (#12715 merge, tree identical to `1967e76f38`) → no impact; omp and ops pointers catch up on #12715 (rr6 read on OJS 2026-09-14, shared code, U42 pending).
  - Open regressions re-run on the reset OJS at the new tip: pkp-lib#13109/#13312 picker (`.reports/sync/s15-13109/`): the stage select still ends with a disabled "Done", the stats table and the Roles grid four stages; pkp-lib#12352 (`.reports/sync/s15-12352/`): `status:false`, fileId 2 `article-rev.pdf` current, #13288 still open at `5f995d86af`. Both rows dated. Report `docs/reports/2026-09-04-omp-ops-discussion-save-missing-notification-class.md` still open: omp `6c2f43041` and ops `a9988de891` track no `classes/notification/Notification.php`.
  - The maintainer's round-2 note (OMP and OPS red locally on U40 S3, the author's publication save never posting, PHP 8.4 on the Mac): not reproduced on the VM (PHP 8.3) — U40 S3 green alone on OMP (32.5 s) and OPS (29.7 s) at the new tips (`.reports/sync/s15-u40s3-{omp,ops}.log`).
  - Tests touched: none. Suites run once for the touched specs: OJS U40 + U41, 19 passed (1.6 min, `.reports/sync/s15-u40-u41-ojs.log`). Persona read of the rewritten U40 span (`.reports/sync/persona-0915.md`): 3 wording blockers, 1 on the new sentence ("a list typed there" could mean the buttons or auto-converted shorthand → now "a list made with those two buttons is kept as a list after Save"), 2 pre-existing (the participant permission phrase, the "Settings that modify behavior" pointer) for U40's queued revision. Stale artifact fixed: `shared/playwright/reset.js` drops with `dropdb --force`, because with `persistent = On` the fleet's php -S servers keep their PDO connection between requests and a plain drop failed with "database is being accessed by other users" while the probe server was up (the reset after the 13321 re-run, `.reports/sync/s15-chain1.log`); harness.md's config section says so.
  - Friction: not folded (the sync accommodated); rr7 appended one row.
  - Open questions: not posted (due about 2026-09-29).
  - Finals at the new tips on reset databases, four workers, one app at a time, the first VM runs with the perf round-2 harness and `persistent = On` (`.reports/sync/final-run-{omp,ops}.log`, `final-run-ojs-attempt{1,2}.log`, `final-run-ojs-traced.log`): OMP 225 passed (10.4 min); OPS 153 passed (6.1 min); OJS red twice on flake classes and green on the third, traced run — attempt 1: 213 passed, 3 red (the shared login smoke's profile probe for `reader.rosa` landing on the login page, a new class; U28 S10 with the hardened accept exhausted; U49 S6's "Create New Version" stage select empty for 10 s, a new class), 11.0 min; attempt 2: 214 passed, 2 red (U22 S2's "Save for Later" press swallowed on the Details step, the wizard-press class on a footer button without the bounded retry; U28 S10 again, then red alone on that used database, its first alone red), 11.5 min; attempt 3 with `--trace retain-on-failure`: 229 passed (17.1 min, serial and solo included; no trace retained, so the U28 S10 evidence still waits for a red traced run). All five reds green alone except U28 S10 once; CI green at the same tree and tips. Since the maintainer's 2026-09-14 config the `<app>-serial` and `<app>-solo` projects depend on the parallel project, so locally (no retries) one parallel red skips the 13 serial and solo tests; CI's `--retries=1` keeps them running unless a test fails both attempts. The U28 S10 entry, the wizard-press entry and two new flake-watch classes recorded in ci-triage.
  - Landed on `main` after today's finals, before this push: the maintainer's flake investigation `5639ae6` (`docs/reports/2026-09-15-flake-investigation.md`: the wizard step rail's scroll animation loses a press, the publication form keeps a stale form while the next loads, `reducedMotion` for the `asUser` contexts, traces kept for the U28 and U40 files); today's runs predate it, so tomorrow's finals are its first VM run. Lint: U24's register entry A10 (added 2026-09-14) had no body marker; Rule 18 now carries it.
  - Baselines advanced.
- **2026-09-14 (sync) — ojs `89579d5591..f0cde27fda` (10), omp `ab748e2c3..bbd28d570` (6), ops `a72cacc1c5..04bbf46097` (6), pkp-lib `b48c22ca06..1967e76f38` (12, ojs) / `b48c22ca06..9f99bb57a6` (10, omp and ops), ui-library `c565cd77..db5b2813` (1, ojs) / `445b8b90..db5b2813` (2, omp and ops). Of these, pkp-lib #13317 (`5abf264858`, `9f99bb57a6`, merge `5cdfc98872`), ojs #5817 (`a82c7ba326`, `1ef3fad1b9`, merge `0be7c29f54`), omp #2458 (`e488c5c81`, `bbd28d570`) and ops #1397 (`76a3c73f7c`, `04bbf46097`) were reviewed on companion `13274` (2026-09-12); the app-side "Submodule update(s)" commits (ojs `1b0e9b1acb`, `cea48a066b`, `5287c3de57`, `24782cf17f`; omp `790f5ccf4`, `3e2678ced`; ops `53c2e71949`, `7a6d921374`) are pointer bumps.**
  - CI at the tips: ojs run 34762320022 (`f0cde27fda`, 2026-09-13), omp 34704080151 (`bbd28d570`), ops 34704085074 (`04bbf46097`) → all green. pkp-e2e `main` at `c774c62f` (the maintainer's `perf/combined` merge this morning: the serial project on four workers, a `solo` project, the `_test/jobs` drain wait, HTTP sign-in for the auth cache, the seed lock window; `docs/reports/2026-09-14-suite-performance.md`): push run 34818284774, dispatch 34822306966 and the nightly 34803173192 (`56b47c9`) all green. Nothing red to triage; no known-red rows, no companions.
  - pkp-lib #13312 (`65901c4f7a`, `716419c770`, merge `0356122fdc`; issue #13109) + ojs #5816 (`0dbb274a45`) / omp #2459 (`ce63a5cd8`) / ops #1398 (`97d8a0d2e8`): Done leaves every app's `getApplicationStages()` again; `PKPApplication::getValidStages()` (app stages + Done) feeds `WorkflowStageRequiredPolicy` and `submission/maps/Schema::getPropertyStages()`; `userGroup/Repository::installSettings()` keeps registry stages up to Done → the open regression pkp/pkp-lib#13273 is FIXED on two of its three surfaces with the kept script on a reset OJS (`.reports/sync/s14-13109/`: no "Done" row under Statistics › Editorial Activity, the Roles grid headers Submission/Review/Copyediting/Production only); the third is now by design (`stages` keeps five entries from `getValidStages()`, the discussion attacher's "Select submission stage" still lists a disabled "Done"), row deleted — then, on the session thread, @jarda.kotesovec ruled the picker's Done entry a bug (2026-09-14): U24 gains register entry A10 with the ruling and ci-triage a fresh open-regression row on that one surface, re-run with the same kept script. The fresh-install stage-6 gap closed with it: the databases installed today grant stage 6 to 10 OJS roles, 11 OMP rows, 3 OPS rows (psql). U40 K1 re-run (phases seed, tick, publish, version; `.reports/U40/s14-k1/`, `.reports/sync/s14-k1-0914.log`) on all three apps: the permitted Author's save on the new version answers 200 and persists → A16 retired in U40 (was the 401 `accessibleWorkflowStage`). Spec maintenance: U40 (scenario 3, A16 → Retired, fn-a16, the fresh-install paragraph), U24 fn m/p, U25 fn p, U26 fn p, U28 scenario-17 footnote, U29 fn b; the OPS U28 S17 test comment refreshed. Regression verdict in the log line: the auth policy's stage list is unchanged in effect (Done stays valid for the policy), the callers of `getApplicationStages()` regain the pre-2026-09-08 list, and the full suites at the tips are the evidence (below).
  - pkp-lib #13304 / PR #13309 (`40df36903d`) + ui-library #983 (`db5b2813`): the publication API blanks `supportingAgencies` for an anonymizing review assignment and the reviewer's "View All Submission Details" window gains a "Supporting Agencies" row when the list is non-empty → accommodated in U28 (Rule 7 and fn e), driven on OJS with the kept `checks/sync/pkp-lib-13304/details-modal.js` (`.reports/sync/s14-13304/`): the anonymous reviewer's window "Title", "Abstract" and `supportingAgencies: {"en": []}`; the open reviewer's "Title", "Authors", "Abstract", "Supporting Agencies" with the term. Matches the issue's stated intention; no test change (the seeded submissions carry no agencies; a bullet for scenario 13 is a coverage change for U28's next revision).
  - pkp-lib #13294 (`9ca884fc29`; issue #13294, the `main` commit of a four-branch fix): `SubmissionDocumentsFilesGridHandler::deleteFile()` refuses a library file whose `submissionId` is not the authorized submission's, and `NewLibraryFileForm` takes the submission id from its constructor instead of the request → not covered yet (U39 Submission & Publisher Libraries pending); regression verdict in the log line: both forms already receive `$submission->getId()` from the handler (`EditLibraryFileForm`, `NewLibraryFileForm` constructors), so a well-formed add keeps working and only a forged `submissionId` changes outcome.
  - pkp-lib #12715 (`deb0b70cdd`, `1967e76f38`; issue #12714, ojs only today via `24782cf17f`; omp/ops pointers before it): citation lookups rate-limited per service with jittered releases, 429/503 released for Retry-After, transport failures retried with delays and then failed loudly (`CitationProcessingStatus::FAILED = -1`, `citation.json` validation `in:-1,0,…`), `OrcidJob` split into a fan-out plus `OrcidAuthorJob`, the citation and data-citation maps' author model typed per schema property; `tools/jobs.php work` honours bare `--force` / `--stop-when-empty` → not covered yet (U42 Citations & references pending; the harness drains with `jobs.php run`, not `work`). Regression read rr6 (`briefs/regression-read.md`, `.reports/sync/rr6/suspicions.md`; callers, API, jobs, CLI read): 3 suspicions written, 2 reproduced as the issue's intended change, 1 not, 4 unverified hunches. S1 (the chain stalling under the new rate-limit middleware and the ORCID fan-out): not reproduced — three references added through the References panel on a scratch journal with the lookup enabled completed end to end, 18 `OrcidAuthorJob`s prepended inside the chain, `IsProcessedJob` last, 0 failed jobs, DOI, OpenAlex title and the "No structured information found" badge shown. S2 (a transport failure parks the citation): reproduced with the fleet's dead-proxy config — the 504 path prepends a `CrossrefJob` delayed 5 min and the row shows neither data nor badge until it runs, then completes; intended per the issue, the badge-less wait is what upstream defers to #13308. S3 (`jobs.php work --stop-when-empty`): the worker now exits in 3.5 s on an empty queue. Pre-existing, not this PR: `jobs.php run` counts with a builder bound at loop start, so chain jobs created a second later are invisible and one `run` returns early (the harness's `runJobs()` already loops). No regression; nothing routed.
  - pkp-lib `fe93b84465` (#13214 merge, tree identical) and `0b40119a89` (#12752, composer.json/lock only; `composer install` run in every checkout today) → no impact. ui-library `c565cd77` on omp/ops (the #976 merge, tree identical) → no impact.
  - Open regression pkp-lib#12352 re-checked on the reset OJS at the new tip (`.reports/sync/s14-12352/`): still reproduces (`afterCancel` fileId 2, `article-rev.pdf`, `status:false`); fix PR #13288 still open at `5f995d86af`; row dated.
  - Report `docs/reports/2026-09-04-omp-ops-discussion-save-missing-notification-class.md` still open: omp `bbd28d570` and ops `04bbf46097` track no `classes/notification/Notification.php` (the overlay of app-changes row 12 still carries the fleets); dated.
  - Tests touched: U40 S3 in all three suites now presses the new version's Save and reads "The" back after a reload (the retired A16), headers rewritten; green alone on OJS (1.2 min), OMP (49.6 s), OPS (23.1 s). Persona read of the rewritten spans (`.reports/sync/persona-0914.md`): 4 blockers, 1 fact; the U28 Rule 7 wording (whether the disclosure condition gates the agencies) settled in the sentence; the three U40 ones (Rule 9's unconditional "other versions are not", ⚠ A17, scenario 3's "dependable route") are pre-existing text for U40's queued revision. Stale artifact fixed: the kept 13109 script's Author-row filter never matched the grid's "Settings / Author" cell, so the role form was skipped; fixed and re-run (`.reports/sync/s14-13109b/`: four stage boxes, no "Done").
  - Flake watch: three tripped or recurring conditions hardened, all green alone — U23 S9 `openActivityPopoverFor()` (the two awaiting indicators share one accessible name and the assignments come back unordered, so the opener picks the popover naming the reviewer); OMP and OPS `SubmissionWizardPages.pressUntil()` behind `continueTo()`, `openReview()` and `confirmSubmit()`; the OMP U41 S2 Cancel leg's bounded retry after it red in today's OMP final (the OJS/OPS twins had it since 2026-09-11). Two classes added from the maintainer's overnight report (local midnight; U29 reads under load).
  - Friction: not folded (the sync accommodated); the file keeps its rows for the next fold.
  - Open questions: not posted (due about 2026-09-29).
  - Finals at the new tips on reset databases, four workers, one app at a time, with this morning's `perf/combined` harness (`.reports/sync/final-run-{omp,ops}.log`, `.reports/sync/final-run-ojs-0914.log`): OPS 153 passed (7.1 min, first run); OMP 213 passed + U41 S2 red (12.5 min; the reorder class, hardened above, green alone); OJS 229 passed (19.5 min, first run, `--trace retain-on-failure` on so a U30 S4 red would have kept its trace — it did not red, nor did U28 S10, so the class's next step still waits for an incident; the traced run is not a budget number). The OMP and OPS U40 S3 edits ran green alone after their finals; CI on this push is the fresh-box run.
  - Baselines advanced.
- **2026-09-12 (merge of companion `13274`) — pkp/pkp-lib#13317 merged as `5abf264858` + `9f99bb57a6` (rebased, `git range-diff` identical to the reviewed `3f82add062`), pkp/ojs#5817 as merge `0be7c29f54` (`a82c7ba326` + `1ef3fad1b9`, identical to `75df364d49`), pkp/omp#2458 as `bbd28d570`, pkp/ops#1397 as `04bbf46097`; issue #13274 closed.** Companion rebased onto `main` (one friction.md conflict, both sides kept), U41 and U21 run once on OJS and U41 on OMP and OPS at the merged tips (`.reports/sync/merge13274-{ojs,omp,ops}.log`, see the run line), `main` fast-forwarded, companion row deleted. **Baselines not advanced**: the tips also carry commits nobody has reviewed (pkp-lib `b48c22ca06..9f99bb57a6` minus the PR: #13214 merge, #12752 composer, #13294 library cross-correlation, #13304 anonymised supporting agencies, #13312 Done-stage fix `65901c4f7a` + `716419c770`; ojs `1b0e9b1acb`, `cea48a066b`, #5816 `0dbb274a45` + `5287c3de57`; omp `ab748e2c3..bbd28d570` and ops `a72cacc1c5..04bbf46097` likewise), so the next daily sync reviews that range with the PR's commits already covered above, and re-runs the pkp-lib#13109 open-regression reproduction against the merged fix.
  - Run at the merged tips (`--grep` matched wider than U41/U21, so near-full): OJS 196 passed + U04 S2 red on its 60 s test timeout waiting for the ORCID popup, green alone in 5.5 s (`merge13274-u04s2-rerun.log`; three suites ran in parallel on the VM, a load one-off, ci-triage flake watch), 1 dependent did not run; OMP 198 passed (28.6 min at one worker); OPS U41 11 passed. Commit-worthy gate met; `main` fast-forwarded to the companion.
- **2026-09-12 (companion `13274`, prepared before merge) — pkp/pkp-lib#13317 at `3f82add062` (2 commits on `40df36903d`), pkp/ojs#5817 at `75df364d49` (on `cea48a066b`), pkp/omp#2458 at `dd80a5a28f` and pkp/ops#1397 at `1cedcea446` (submodule-only); issue pkp/pkp-lib#13274. Baselines not advanced: the PRs are open; the merge session advances ojs and pkp-lib past the merge commits and deletes the companion row.**
  - Merge bases checked: each PR sits on its repo's current tip (ojs `cea48a066b`, omp `790f5ccf4c`, ops `53c2e71949`, pkp-lib `40df36903d`), so the OJS checkout at the PR ref also carries ojs `1b0e9b1acb..cea48a066b` (submodule bumps: pkp-lib `b48c22ca06..40df36903d`, four commits — #13214 merge, #12752 composer, #13294 library cross-correlation, #13304 anonymised supporting agencies — and ui-library `c565cd77..db5b2813`, #983 supporting agencies in the details modal); those are the next daily sync's range, not reviewed here.
  - pkp-lib #13317 `classes/affiliation/Repository.php` (`migrateUserAffiliation()`: the profile affiliation is copied verbatim per locale as `name`, `ror` never derived from a name match, the submission locale filled from the user's default-locale text when the profile has none there; sole caller `author/Repository::newAuthorFromUser()`) → accommodated in U41 (cross-feature line and footnote d; U03 Rule 7 and its cross-feature bullet already described the copy as plain text). Regression verdict, driven at the PR head on a reset OJS (`checks/sync/pkp-lib-13317/runtime.js`, `.reports/sync/pr13274/runtime-ojs.json`): with a cached record planted under exactly the profile's text, the en and fr_CA submissions' contributor rows carry the text as `name` (fr_CA under both locales) and no `ror`; the same script at `b48c22ca06` (`.reports/sync/pr13274-main/`) stored the `ror` and no name, the issue's complaint. On the fleets the old branch was dead code (the install-time ROR import fails without network: `rors` holds 0 rows, `scheduledTaskLogs/UpdateRorRegistryDataset-*.log` "cURL error 7"), so no shipped test changes; U21's "non-ROR affiliation names in submission locale" submit check is unchanged.
  - pkp-lib #13317 `I7135_CreateNewRorRegistryCacheTables` (`migrateAffiliations()` replaces the name-join `migrateRorAffiliations()` and `migrateNonRorAffiliations()`; `ror` only from a syntactically valid `rorId` setting normalised to `https://ror.org/<id>` and reconciled with the freshly imported cache; the import's boolean decides whether an unknown id is dropped or kept) → not covered (the fleets never upgrade). Regression verdict: driven directly on the fresh install through `checks/sync/pkp-lib-13317/migration.js` + `migrate-driver.php` (eight legacy `author_settings` shapes on eight author rows, the cache marked complete then failed; `.reports/sync/pr13274/migration-ojs-{complete,failed}.json`): all 16 outcomes match the issue's table (name only → one row, no `ror`; valid id in the cache → `ror`, no name, with or without text; valid id not in the cache → dropped with the text kept when the import succeeded, kept beside the text when it failed, no row for id-only when it succeeded; malformed → text only or no row; a bare id with a trailing slash normalised and resolved). Unverified: a real upgrade run (`tools/upgrade.php` from a 3.4 database) and the "repair script for sites already on 3.5.0" the issue asks for, which is not in these PRs.
  - Regression read rr5 (`briefs/regression-read.md`, `.reports/sync/rr5/suspicions.md`; callers, API, exports, CLI, upgrade path, jobs read): 2 suspicions written, 1 reproduced, 1 not, 5 unverified hunches. Reproduced and re-held on a fresh reset (`.reports/sync/rr5-fresh/`), then run again at the OJS tip without a registry record (`.reports/sync/rr5-maintip-typed/`): the wizard's mid-draft language change leaves the copied affiliation and given name in the first language, so the Contributors panel and the Review check ask for them in the new language; the same happened before the PR for typed text, and the PR moves the registry-matching population into it (the issue's own case). Not a regression against the issue's intention; the wizard's language change relies on validation for every language-bound value and the author fills the new language in, the copy-over being the post-submission route's → ruled expected by @jarda.kotesovec the same day, recorded as U21 ✅ A13 (fn-a13, with the Reviewed verdict), the U41 footnote points at it, kept as `checks/sync/pkp-lib-13317/wizard-locale-change.js`; the report was posted in the thread and deleted once ruled. Not reproduced: a profile affiliation only in a locale the journal takes no submission metadata in lands as `name.en` (the intention; the old code would have thrown there). Read-only: one caller of `migrateUserAffiliation()` (author-group submitters only), no fresh-install path to the migration, exports and the article page read unchanged getters.
  - ojs #5817 `cypress/tests/integration/SubmissionWizard.cy.js` (fills the French institution name the wizard now requires after a locale change, since the submitter's affiliation is a name, not a registry record) → no impact (the app's own Cypress suite).
  - CI at the PR refs before this work: omp run 34683633990 and ops 34683649689 green; ojs run 34683529824 red (21 min, "Run Playwright suite"; logs need a token). Locally at the PR ref (reset database, four workers, `.reports/sync/pr13274-final-ojs.log`): 196 passed, U30 S4 red at its 30 s wait for the editor's "Author Response" table after the opener's reload, the 12 serial tests not run behind it; alone on that used database it red again at both the PR ref and the OJS tip `cea48a066b` (the co-author row's "More Actions" button detached and re-rendered for the whole 180 s test timeout), and passed in 15 s on a reset database at both refs and once more on the same database → a used-database class, not the PR (ci-triage flake watch). Second full run at the PR ref (`pr13274-final-ojs-2.log`, traces retained on failure): 209 passed (15.1 min, the serial project included). OMP and OPS at their PR refs: CI green as above, not re-run locally (submodule-only PRs; the shared change is verified on OJS). Companion CI at the three PR refs (pkp-e2e run 34689127094, dispatched with the fork repos, after the wizard-press hardening): ojs, omp and ops all green; the first dispatch (34687878464) red on OJS with the wizard-press class only.
- **2026-09-11 (sync) — ojs `8fc931bcf8..89579d5591` (4), omp `14be789b5..ab748e2c3` (3), ops `9db7bd3d7e..a72cacc1c5` (3), pkp-lib `c59325d675..b48c22ca06` (3, ojs) / `f4db6d22c4..b48c22ca06` (12, omp and ops: the nine reviewed for ojs 2026-09-10 plus the same three), ui-library unchanged.**
  - CI at the tips: ojs run 34493581497 (`89579d5591`), omp 34482176439 (`ab748e2c3`), ops 34482144682 (`a72cacc1c5`) → all green. pkp-e2e `main` at `54b4083`: the push run 34466942823 (2026-09-10) red on OPS U41 S2 with its retry exhausted, the nightly 34558837065 on the same tree (and the newer OPS tip) green → the ci-triage flake-watch class, watch condition tripped, hardened (below). No known-red rows, no companions.
  - pkp-lib #13214 (`bf97c199af`, `b48c22ca06`; issue #13213: foreign key `edit_tasks.edit_task_template_id` → `edit_task_templates` with `nullOnDelete` in the install migration plus `I13213_TaskTemplateForeignConstraint` for upgrades, registered in each app's `upgrade.xml`; `Template::scopeIsNotAlreadyCreated()` and the `template()`/`editorialTasks()` relations replace the per-template exists check in `Repository::autoCreateFromTemplates()`) → not covered yet (U37 Tasks & discussions pending; U21's "Editorial task templates run" side-effect line unchanged). Regression verdict, driven directly on all three apps (`.reports/sync/s11/tasktemplates-13213.{js,log}`): with the stage templates switched on, a seeded submit creates one task per template through the real `Repo::submission()->submit()`, a decision chain that re-enters a stage (OJS/OMP 1→3→4→3, OPS 5→Done→5) adds none twice, deleting a template leaves the task with a NULL template id (`ON DELETE SET NULL` read from `pg_constraint`) and the workflow screen still lists it; nothing to report. HARNESS DEFECT found on the way: no fleet context carried a single task template (`ContextFactory::create()` stopped at `PKPContextService::add`, while the hosted-contexts endpoint has installed the registry's defaults since pkp-lib #12593, 2026-08-03) → the factory now runs the endpoint's tail; parity-ledger row 2026-09-11, seed-facts line.
  - pkp-lib `95be1d0549` (#13306, issue #13305: mPDF receives `TemporaryFileManager::getBasePath()` as `tempDir`, so a read-only `vendor/` no longer breaks the review PDF export) → no impact (U27 Rule 15 unchanged); regression verdict: U27 S10 "download the review" green on OJS and OMP at the tips, both PDFs download non-empty (`.reports/sync/s11/u27s10-{ojs,omp}.log`).
  - ojs `89579d5591` (#12752 follow-up, no PR: `HtmlGalleyHelper` re-assigns the immutable `HtmlSanitizerConfig` after `allowElement()` / `allowMediaHosts()`, so `allowed_galley_html` and `allowed_media_hosts` take effect) → not covered yet (galleys U46 pending); trivial, callers in the diff, the fleets set neither option. pkp-lib `main` sits two commits past the pointers: `fe93b84465` (#13214 merge, tree identical) and `0b40119a89` (#12752, composer.json/lock only) → triaged when the pointers catch up (`composer install` due then).
  - omp/ops pointer catch-up on the nine pkp-lib commits reviewed for ojs 2026-09-10: #12375 locale resolution — the rr4 cookie-less matrix re-run on the OMP and OPS probe servers matches OJS (bare OAI one hop to `/en/`, `/en/` and `/fr_CA/` answered in place, `?setLocale=` ignored with sessions on, `currentLocale` cookie on every response); `c59325d675` re-index — the U24 incidental re-driven on OMP and OPS with `roles-recheck.js` adapted to the press/server manager labels (`.reports/sync/s11/workflow-menu-{omp,ops}.json`): the site administrator without a manager role on the context gets the full workflow menu; #13293 export roles — no OMP/OPS surface beyond the ojs verdict.
  - Open regressions re-checked on the reset OJS at the new tip (`.reports/sync/s11/`): pkp-lib#13109 Done-stage callers still reproduce ("0 Done" under Active Submissions, the "Done" column in the Roles grid, the disabled "Done" picker entry, five `stages`); Vitaliy-1 posted fix PRs pkp/pkp-lib#13312 ("Use DONE stage only for auth checks") and pkp/ojs#5816 on 2026-09-10, unmerged at `0b40119a89`; pkp-lib#12352 still reproduces (`afterCancel` fileId 5, `article-rev.pdf`, `status:false`), fix PR #13288 still open at `5f995d86af`. Both rows kept and dated.
  - Flake watch: OPS U41 S2 — the 2026-09-09 local incident and the 2026-09-10 CI red are the same Cancel-leg press → hardened into a content-verified bounded retry on OPS and its OJS twin, green alone on OPS (5.6 s); line rewritten with the new watch condition.
  - Report `docs/reports/2026-09-04-omp-ops-discussion-save-missing-notification-class.md` still open: omp `ab748e2c3` and ops `a72cacc1c5` track no `classes/notification/Notification.php` (the overlay of app-changes row 12 still carries the fleets); dated.
  - Friction: no rows to fold. Open questions: not posted (due about 2026-09-29).
  - Full `test:final` at four workers on the new tips (`.reports/sync/final-run-{ojs,omp,ops}.log`): OMP 207 passed (10.4 min), OPS 144 passed (6.1 min), OJS 196 passed + 1 red (10.6 min; U28 S10, the flake-watch class's second full-run incident, green alone in 17 s on 2026-09-12; the 12 OJS serial tests did not run behind the red, green in the 2026-09-09 serial run and on CI at the tips). The session timed out before its commit; the tree was committed unchanged on 2026-09-12 with the U28 S10 hardening (ci-triage flake watch) added.
  - Baselines advanced.
- **2026-09-10 (sync) — ojs `05684c7f9d..8fc931bcf8` (5), omp and ops unchanged, pkp-lib `f4db6d22c4..c59325d675` (9, ojs only; omp/ops pointers stay at `f4db6d22c4`), ui-library `445b8b90..c565cd77` (ojs only, merge commit, tree identical).**
  - CI at the tips: ojs run 34425510968 (`8fc931bcf8`), omp 34224699474, ops 34224738339 (unchanged) → all green; pkp-e2e `main` at `48913b9` green on the push (34401345266) and the nightly (34433729027, against the new OJS tip). Nothing red to triage; no known-red rows, no companions.
  - ojs #5799 (`3650bd2127`, `8835b97a5e`; issue pkp/pkp-lib#13280: the oai_marc and marcxml formats printed "Array" per subject since #11557 and read `discipline` instead of `disciplines`; both now pluck the vocabulary entries' `name`) → not covered yet (U19 OAI-PMH pending); regression verdict in the log line: the only callers are the two `record.tpl` templates, which already `{foreach}` the value, and a merged empty array keeps the `{if $subject}` guard false; no reproduction (`publicknowledge` has no published item on any app, seed-facts).
  - ojs `6fb1dc4203` (config template comment `allowed_hosts` → `allowed_media_hosts`) → no impact (the remnant the maintainer withdrew the 2026-09-03 report over; galleys are U46, pending).
  - pkp-lib #13124 (`1012a55717`..`8f11556ff9`, merge `24a982aa6a`; issue #12375: `Locale::getLocale()` consults the URL's locale segment after `setLocale` and before the session and cookie, `?setLocale=` honoured only while sessions are disabled, `PKPPageRouter::_setLocale()` syncs session and cookie to the URL even when no redirect is needed; the cookie-less redirect loop on secondary locales) → not covered yet (U57 Languages & locales pending; U02's locale-segment line and the seeded journals' fr_CA UI locale unchanged). Regression read rr4 (`.reports/sync/rr4/suspicions.md`): 3 suspicions written, 0 reproduced, 3 not reproduced, 4 unverified hunches, nothing routed: OAI answers 200 on the bare, `/en/` and `/fr_CA/` forms without a cookie (the issue's own case, one hop now), the signed-in language toggle round trip and cross-locale links reach the pre-change end states with one self-redirect fewer, the French backend renders; `Set-Cookie: currentLocale` now rides every anonymous well-formed response, an observation only (the session cookie already did). OMP and OPS not probed (change not landed).
  - pkp-lib `2f1eb6d101` (#13293, open issue: the reviews API `export-pdf`/`export-xml` route group drops Reviewer and Author, adds Site admin) → no impact (U27 Rule 15's "Download Review Form" menu is editor-side and its authorFriendly variants stay; no spec claims the endpoint's role list); regression verdict in the log line: every editorial role the menu is shown to stays in the group.
  - pkp-lib `c59325d675` (no PR; `array_values()` on the global-roles fallback in `submission/maps/Schema::getPropertyStages()`) → the U24 incidental of 2026-09-09 (a site administrator whose only journal role is Section editor got `currentUserAssignedRoles` as `{"1":1}` and a menu-less workflow modal) is fixed: re-driven on a reset OJS (`.reports/sync/s10/roles-recheck.js`), every stage answers `[1]` and the modal lists Workflow, the four stages, Publication and its pages; incidentals row deleted. OMP and OPS receive it with their next pointer bump.
  - pkp-lib `0834aa8e34` (#13273 merge commit, tree identical to `f4db6d22c4`) and ui-library `c565cd77` (#976 merge commit, tree identical to `445b8b90`; OJS bundle rebuilt by `fetch-apps --update` anyway) → no impact.
  - Open regressions re-checked on the reset OJS at the new tip (`.reports/sync/s10/`): pkp-lib#13109 Done-stage callers still reproduce ("0 Done" under Active Submissions, the "Done" column with a live Journal-manager toggle, the disabled "Done" picker entry, five `stages`; the fresh-install stage-6 gap is with upstream, jardakotesovec on the issue 2026-09-09); pkp-lib#12352 still reproduces (`afterCancel` fileId 2, `article-rev.pdf`, `status:false`; fix PR #13288 still open at `5f995d86af`). Both rows kept and dated.
  - pkp-lib `main` sits one commit past the OJS pointer at `95be1d0549` (#13306, review PDF export written to the private temporary directory, `PKPReviewController.php`, issues #13305/#13306) → triaged when the pointers catch up; U27 Rule 15 territory.
  - Report `docs/reports/2026-09-04-omp-ops-discussion-save-missing-notification-class.md` still open: omp `14be789b5` and ops `9db7bd3d7e` ship no `classes/notification/Notification.php` (the overlay of app-changes row 12 still carries the fleets); dated.
  - Friction folded (three rows): the U23 revision's two rows (two test authors extending `EditorialDashboardPage.js` at once) → a clause on the test-author brief's `{{page_objects}}` slot (one owner per shared page object, the other returns method names); rr4's row (a baseline range carried a second PR's commit) → a clause on the regression-read brief's `{{pr}}` slot (a range is the merge's own parents); rows deleted. Stale artifact fixed: patterns.md "Probe kit" now says a just-reset database needs `fleet-prep` before the kit's `bootstrapProbe()` answers (cost one wasted run today; the fact lived only in the 2026-09-09 brief).
  - Open questions: not posted (MAINTENANCE mode since 2026-08-29, under a month; due about 2026-09-29).
  - Baselines advanced (ojs and its lib/pkp; omp and ops unchanged).
- **2026-09-09 (sync, restarted from `aa12a61` at the maintainer's request; the 2026-09-08 second-run commit `4751d27` reverted as `9bd62a9`) — ojs `ed3705cdae..05684c7f9d` (3), omp `5365ebd73..14be789b5` (4), ops `b0b24a3127..9db7bd3d7e` (4), pkp-lib `74a8d58571..f4db6d22c4` (4; every app's pointer), ui-library `5c3da336..445b8b90` (all three).**
  - CI at the tips: ojs run 34224652722, omp 34224699474, ops 34224738339 → all green; pkp-e2e `main` at the revert (`9bd62a9`, run 34354844582) green. The 2026-09-08 red on pkp-e2e `main` (run 34215183797, U21 S12 twice, a docs-only push) is a timing class: the same tree and tips are green on 34354844582, the nightly 34183869175 and locally → ci-triage flake watch ("A wizard Continue press swallowed…"), no harness change until it reds again.
  - pkp-lib #13273 / ojs #5793 / omp #2453 / ops #1392 / ui-library `445b8b90` (issue #13109: an Author with the metadata-edit permission edits any version that is not published or scheduled, judged per version; publishing no longer switches the permission off; `canCurrentUserChangeMetadata` moves from the submission to the publication schema; `WORKFLOW_STAGE_ID_DONE` joins each app's `getApplicationStages()` and the registry's group stages; migration `I13109_PermitPublishedMetadataEdit` for upgrades) → accommodated in U40 (claim check K1 on all three apps, 27 entries: Rule 2 and Rule 9 rewritten per version, scenario 3's unpublish leg now "Save works at once", A4 retired, new A16 🐞 and A17 ❓, footnotes b/c/i/j/f-a4 corrected; persona 5 wording blockers, rewrite 5/0; suites: U40 revised per app, 9 tests each, S3 asserts Save at once after an unpublish with the box read as still ticked, the A16 leg never pressed, Rule 10's no-prompt read; OJS S3b/S6b folded into S3/S6 for the `--tests` gate; green alone on all three, then full runs at four workers on the VM: OMP 206 green, OJS 195 + 12 serial green after one U28 S10 load flake, OPS 133 + 9 serial green after one U41 S2 load flake, both green alone and on the flake watch), U49 (Rule 8 sentence and fn-p "Author lock" corrected; persona clean), U24/U25/U26/U29 (stage-list footnotes m, p, a, b dated: Done is in the list, screens unchanged), U41 fn-b unchanged (points at U40's gate). Regression read rr3 (`.reports/sync/rr3/suspicions.md`): 5 suspicions written, 4 reproduced, 1 not reproduced, 6 unverified hunches, nothing routed. REGRESSION (ci-triage row, kept script `checks/sync/pkp-lib-13109/regressions.js`): the Done stage in `getApplicationStages()` reaches callers written for its absence — Statistics › Editorial Activity gains a permanent "0 Done" row, the Roles grid a "Done" column with live toggles on the manager, reviewer and reader rows and a "Done" box on the role form, every submission's `stages` a Done entry and the discussion attacher's stage picker a disabled "Done". FINDING against the issue's intention (U40 A16): on a fresh install no role holds stage 6 (`installSettings()` caps registry stages at Production), a published submission rests in Done, so the issue's own case — the permitted Author editing a new version while another is published — offers Save and answers 401 `user.authorization.accessibleWorkflowStage` with "An unexpected error has occurred…" on all three apps. API: `canCurrentUserChangeMetadata` absent from `GET submissions/{id}`, `null` on `_submissions` items (`getSubmissionsListProps()` still names it), present per publication. Pre-existing, outside the diff, one incidentals row (U24): a site administrator whose only journal role is not a manager one cannot open the workflow modal (`currentUserAssignedRoles` serialised as `{"1":1}`). Harness: the `published: true` seed's mirror of the removed author-permission sweep dropped (`PKPSubmissionScenarioBuilder.php`, parity ledger note).
  - Open regression pkp-lib#12352 re-checked with the kept script at the unchanged tip: still reproduces (`afterCancel` fileId 2, `article-rev.pdf`); fix PR #13288 rebased 2026-09-09 to `5f995d86af` with an identical patch (range-diff), still open; row kept.
  - pkp-lib `main` sits ahead of every pointer (`24a982aa6a`): #12375 (the URL's locale segment honoured, `setLocale` only while sessions are disabled, session/cookie synced to the URL; `Locale.php`, `PKPPageRouter.php`, `PKPRequest::getPathInfo()`) and #13293 (`export-pdf`/`export-xml` on the reviews API drop the Reviewer and Author roles, add site admin; U27 Rule 15's download menu is editor-side) → triaged when the pointers catch up; nothing in the harness sends `setLocale`.
  - Friction folded (two rows): a Users-tab bullet in patterns.md "UI realities" and a `createContext` admin-enrolment option under scenarios.md "Field shapes not built yet" (rr3); a Publication-form `settled()` clause in patterns.md "Probe kit" (ccK1); rows deleted. seed-facts.md gains two install-default lines (no issues on a fresh scratch journal; the Author role's "Permit submission metadata edit." per app).
  - Flake watch: U41 S2 (OPS) reorder under load, red once in the local full run at four workers, green alone; line added.
  - Baselines advanced.
- **2026-09-08 (sync) — ojs, omp, ops and every `lib/pkp` pointer unchanged at the 2026-09-07 baselines; ui-library unchanged. Nothing to triage; baselines stay.**
  - CI at the tips: ojs run 33877061458, omp 33877082296, ops 33877070198 (unchanged, all green); pkp-e2e `main` green after the U21 revision pushes (run 34159007742) and on the nightly (34183869175); no known-red rows, no companions.
  - pkp-lib `main` sits one commit past the pointers at `3ec74f0eb9`, the #12352 merge commit, tree identical to `74a8d58571` → no impact when the pointers catch up.
  - Open regression pkp-lib#12352 re-checked: upstream answered (touhidurabir, 2026-09-08) and re-filed it as pkp/pkp-lib#13286 with fix PR #13288 (`e07727add6`: the replaced file's fileId, name and uploader go into the session at upload time, `cancelFileUpload` restores from there without logging, the cancelled revision's log rows are deleted, `EventLogGridRow` hides Download for a fileId that is no longer a revision, `FileApiHandler` answers 404 instead of a 500 for one). Verified at the PR head on a reset OJS with the kept script: MODE=main (editor A revises after editor B's rename) and the new MODE=other (editor B revises, the issue's Variant 2) both restore fileId 1 / "Renamed by B.pdf" / uploader A with `status:true`; the Activity Log and the file history show no article-rev.pdf rows and their Download links answer 200 (`.reports/sync/verify13288/`). ci-triage row kept until the PR merges; `checks/sync/pkp-lib-12352/cancel-restore.js` gains MODE=other. OJS lib/pkp restored to `74a8d58571` afterwards, mounted, reset.
  - Friction folded (four U21 revision rows): a clause on scenarios.md "Configuring a scratch context" (passthroughs save what the form-encoded PUT would; psql is the parity ground truth), on the scenario-writer brief's Control sentence (check the seeded context's defaults first), on the test-author brief's evidence (error-context.md, no screenshot on failure), on patterns.md "Probe kit" (page objects load inside `fn`), and a seed-facts line on OJS overriding lib/pkp event-log strings; the `contributors[]` ask was already listed under "Field shapes not built yet"; all rows deleted.
  - Open questions: not posted (MAINTENANCE mode since 2026-08-29, under a month).
- **2026-09-07 (sync) — ojs `97663b2850..ed3705cdae` (3), omp `a1aefa3fe..5365ebd73` (3), ops `6bda92fb03..b0b24a3127` (3), pkp-lib `4ddab4b9cf..74a8d58571` (6; every app now at the same pointer), ui-library unchanged.**
  - CI at the tips: ojs run 33877061458, omp 33877082296, ops 33877070198 → all green; nothing red to triage; no open ci-triage rows, no companions.
  - pkp-lib #12352 (issue #12347; `386635ebde`, `1ae5ee47ae`, `6dac76becb`, `c830e1b4f0`, `74a8d58571`: `Repository::edit()` logs the new revision and now persists the submission-level entry it used to drop; migration `I12347_FixRevisionUploadLogData`, registered in each app's `upgrade.xml`) → not covered yet (submission files U36 and activity log U38 pending; U36 row noted). Regression read rr1 (`.reports/sync/rr1/suspicions.md`): 2 suspicions, 1 reproduced, 3 unverified hunches. REGRESSION: the upload wizard's step-1 Cancel after a revision upload no longer restores the previous file when a different user had renamed it (`cancel-file-upload` answers `status:false`; `PKPManageFileApiHandler::findMatchedLogEntry()` finds no entry carrying the original uploader's username with the pre-revision name and fileId). Re-verified on fresh resets: `74a8d58571` keeps fileId 2 / `article-rev.pdf`, `4ddab4b9cf` restores fileId 1 / "Renamed by B.pdf" (`.reports/sync/verifyA`, `verifyB`). Reported on Mattermost and DM'd to @beaug and @jarda.kotesovec the same day. The issue's intended fix holds and the new Activity Log rows render complete with working Download links (S2 not reproduced). The migration never runs on the harness's fresh installs; read only. One observation routed to the private file and dismissed the same day after verification on OJS and OMP.
  - pkp-lib `093be2c7e2` (#12179, ContributorsListPanel registered globally in `js/load.js`) → no impact (QuickSubmit only, not in the checkouts); the UI bundles were rebuilt in all three apps because `js/load.js` is a buildable source.
  - Friction folded (nothing to accommodate): one clause each on patterns.md pitfalls 6, 7 and 15, on the scenarios.md response shape (`stageId`) and on briefs/harness.md step 2; the other asks were already in the docs or one screen's fact; all rows deleted.
  - Stale artifact fixed: `bin/fleet-prep.js` now keeps the other apps' entries in `fleet.json` on a subset run (a single-app re-prep had dropped OMP and OPS from it; harness.md line updated).
  - Baselines advanced.
- **2026-09-04 (sync) — ojs `762415103f..97663b2850` (4), omp and ops unchanged, pkp-lib `6a902ad50a..4ddab4b9cf` (1, ojs only), ui-library `7611b0b8..5c3da336` (ojs only), lensGalley `025f53c..43c8195` (ojs only).**
  - CI at the tips: ojs run 33771247334, omp 33629780688, ops 33629815586 → all green; nothing red to triage; no open ci-triage rows, no companions.
  - ojs `95b03c34ec` (#13271, `getExportable()` groups by `s.submission_id` too; the pubId export grids errored on PostgreSQL) → not covered yet (the method serves only the export plugins: U45/U63 pending).
  - pkp-lib `4ddab4b9cf` (#13184 merge commit; tree identical to `6a902ad50a`) → no impact.
  - ui-library `5c3da336` (#978, Storybook/chromatic files only, no `src/` change) → no impact; no rebuild needed.
  - lensGalley `43c8195` (the i12311 commit re-landed on `stable-3_5_0`; tree identical to `025f53c`) → no impact.
  - Baselines advanced.
- **2026-09-03 (sync) — ojs `979819ae45..762415103f` (22), omp `d34542e83..a1aefa3fe` (6), ops `3f619a3138..6bda92fb03` (6), pkp-lib `13b621e42..6a902ad50a` (16; omp/ops at `7ab247a737`), ui-library `f88b7e6a..7611b0b8` (ojs) / `30beb5e3..ba2d082b` (omp, ops).**
  - CI at the tips: ojs run 33734101572, omp 33629780688, ops 33629815586 → all green; nothing red to triage.
  - ui-library `f88b7e6a` and pkp-lib `ecd12271ed`+`d9e9b3fc7c` now in omp/ops → U43 A13 and U04 A10 retired (verified by the green omp/ops runs above); ci-triage rows U43-A13 and U04-A10 deleted.
  - ui-library `d3e19fc4` (#971, recommendations passed to the dashboard-opened Review Details window) → U27 A25 retired, verified live on OJS (both entry paths identical); the parity scenario/test stays parked per the 2026-09-01 maintainer ruling.
  - pkp-lib `922f895988` (author seq `!== null` + Collector tie-break) → U41 A15 retired: re-probed live on OJS and OMP (new contributor joins at the end, seq 0/1, stable over reloads, previews, landing page and TOC); OPS by the shared mechanism.
  - pkp-lib #13232 / ojs #5769 / omp #2441 / ops #1381 / lensGalley `025f53c` (i12311 JATS usage tracking; `UsageEvent` gains `publication`, `LogUsageEvent` gates on the publication's status, JATS column only when the plugin is enabled) → no impact (usage statistics is U64, pending; every in-tree caller passes the publication; suites green on CI).
  - pkp-lib `44ef66eb90` (#13191, assistants may fetch reviewer suggestions; the API 401 that logged assistants out of a submission with suggestions) → intended fix in U25 Rule 1 territory, OJS only until omp/ops bump their pointer; not probed (no seed key for suggestions); no spec change.
  - pkp-lib `56d3caac55` (vocab suggestions capped at 200 entries) → no impact (U40 fn-e).
  - pkp-lib `6a902ad50a` (#13184, bulk-email user count) → no impact (U55 pending); OJS only.
  - pkp-lib `84e049ebd9` + profile/login/register template hooks, FBV readonly/hidden field flags → no impact (plugin hook points, no visible behavior; U01/U02/U03 territory).
  - pkp-lib NavigationMenuDAO cache invalidation, `384693429b` + ojs `a7a3c6c8da` (#13261 export-grid link to the version), pkp-lib #13003 affiliation/serialization fixes, ojs `fed8315e6b`/`7819a42e98` (pubId export grids) → no impact (U08/U63 pending; U21/U41 suites green).
  - ojs `e470617a65`/`094218bde4` (#12752 HTML galley sanitizer, Off by default) → no impact (galleys are U46, pending; the filter is Off by default). A config-key report filed the same day was withdrawn: the template's `allowed_hosts` line is a copy-paste remnant, not part of the feature (maintainer, 2026-09-03).
  - ui-library `7611b0b8` (FieldControlledVocab watcher rebuilt from the value) and `e414522e` (vocabulary modal button row) → no impact (U40 suites green on CI at `762415103f`; OJS bundle rebuilt locally).
  - crossref plugin bumps (ojs `a03c4b7`, ops `417a43a`) → no impact (U45 pending).
  - App e2e hooks now pass the PR branch name (ojs `79b0a1ca34`, omp `efe0b7800`, ops `2eabaf670c`) → harness fact already in harness.md "CI".
  - Baselines advanced.
- **2026-09-03 — U15 build session, not a sync; baselines unchanged.**
  - Checkouts moved to ojs `c499837187` (lib/pkp `8d5ddf8192`), omp `a1aefa3fe` and ops `6bda92fb03` (lib/pkp `7ab247a737`); ui-library bumped in all three → rebuilt. The range is UNTRIAGED; the next sync owns it.
  - pkp-lib `eb4cef92` (#13265) present in every app's lib/pkp → U21 A11 retired, ci-triage row U21-A11 deleted; full suites green at the tips: OJS 154, OMP 156, OPS 119 (with the new U15 serial suites).
