# Progress — live state

One row per feature; the banner names the mode. Read it with
`docs/process/RUNBOOK.md`. Finding detail lives in each spec's Findings
register, never here.

**Mode: MAINTENANCE** (since 2026-08-29). The resident QA agent runs per
`docs/process/MAINTENANCE.md`, one session a day, and never builds a
pending row; feature sessions are launched by the maintainer under the
RUNBOOK loop. Upstream baselines: `docs/tracking/upstream-sync.md`. CI
failures, flake watch and
companion branches: `docs/tracking/ci-triage.md`, checked first on any
reported failure. Suite size (RUNBOOK "Budget"; last full runs 2026-09-18 on the 4-core VM at
`PLAYWRIGHT_WORKERS=4`, CI's setting, one app at a time, the daily sync's finals, all green on the first run): OJS 262 tests · 14.6 min · OMP 251 · 12.1 min · OPS 172 · 7.1 min. On CI's fresh box OJS took 24.8 min on push run 35200505897 (2026-09-17), at the 25-minute line: the shard matrix in `run-app.yml` is the next task (MAINTENANCE "Keep the budget measured").

## Features

Rows in FEATURE-MAP order. Scenarios is the spec's canonical scenario
count once it has them (RUNBOOK "Budget"); status is pending /
in_progress / done / parked.

| Row | Feature | Apps | Scenarios | Status | Note |
|---|---|---|---|---|---|
| U01 | Login & sessions | OJS OMP OPS | 8 | done | 8 OJS · 8 OMP · 8 OPS (+setup; OPS S6 absence). 6🐞+2❓+1✅ (A9 added 2026-09-13). Headliner A9. Budget cuts: 0 states, 0 variants; No seed: a disabled account, Confirm Access (`password_timeout` is run-global), a second site administrator, a forced change on an account signed in elsewhere, the config-file session and reset lifetimes, the password policy and rate limiting. Revised 2026-09-13 (classed table): 0 scenarios added, scenario 9 retired to No seed, all 8 reshaped to the TEMPLATE shape, 6 grew (S1 ×6, S2, S4 ×5, S5, S7 ×4, S8); T-ojs-1/T-omp-1/T-ops-1 folded as A9 ❓ (the last-login date is recorded but no users-management screen shows it; Side effects corrected, S1 reads the Users & Roles row). Verified at the 2026-09-11 baselines, all three full suites green on the first run. Low-confidence: the impersonation activity-log line (Side effects) is Owned by *Submission activity log & notes* with no scenario there yet. |
| U02 | Registration & account validation | OJS OMP OPS | 8 | done | 8 tests per app (+setup; OPS S3 absence). 8🐞+2❓. Headliner A6. 1 in the private file, verified. Budget cuts: 0 states, 0 variants; No seed: every journal closed, a restricted journal (`restrictSiteAccess` passthrough), the site's own statement, `sitewide_privacy_statement`, the Reviewer role and the site-level boxes closed to self-registration (a per-role self-registration passthrough), the 3-day link expiry, the monthly cleanup, reCAPTCHA, the site password policy. Revised 2026-09-13 (classed table): 0 scenarios added, all 8 reshaped to the TEMPLATE shape and a Coverage section added, 5 grew (S1 ×4: the notification box unticked and the Notifications tab, no welcome email, the Login form's link continuing to the interrupted address, the French page's name copied to the primary language; S5 ×2: a disabled journal seeded with `context.enabled: false`, the manager's completion page at the closed and disabled addresses; S6: nothing ticked on the site level; S7 ×4: ALTCHA without JavaScript, the unvalidated account claiming its username, the button's address reopened, a site-level registration with "an account with , but…" and the site's journal list; S8: "View Submissions" opening "Assigned to me"); Rule 13 and S7 name the Login page each sign-in uses, S8's first user is an Author (persona read); OPS titles renamed to S<n>; no test findings. Verified at the 2026-09-11 baselines: OJS and OPS finals green on the first run; OMP on the second, the first red on U26 S1 and U40 S4 only (ci-triage flake-watch class, every U02 test green in both). |
| U03 | User profile | OJS OMP OPS | 12 | done | 12 tests per app (+setup; OPS S6 absence form). 11🐞+6❓+4✅ (A1, A3 retired 2026-09-18). Headliner A2. Synced 2026-09-18 after pkp/pkp-lib#13181 reached OMP and OPS: A1 (the tab lost on the one-journal forward) and A3 (the site-level "reject" blank page) fixed on all three apps and retired, A18 now the three apps' (an account with roles in two journals lands on the site-level profile after "confirm" or "reject"; report `docs/reports/2026-09-17-pkp-lib-13181.md`); scenarios and suites unchanged. 2 in the private file, verified. Budget cuts: 0 states, 1 variant; No seed: no API secret, a Site Administrator on a single-journal site, the 3-day lapse of a request, a single-language site, the compromised-password check, a non-default minimum password length, applications using a deleted key, `log_audit` lines, the scheduled-role banner, "User Registration" closed and a role's self-registration off (Site Access Options and per-role passthroughs), the form languages. Revised 2026-09-13 (classed table): 2 scenarios added (S11, the profile edited while impersonating is the impersonated user's; S12, the profile copied into a new submission's first contributor, A13/A16 read as today's screen), all 10 reshaped to the TEMPLATE shape and a Coverage section added, 10 grew (S1 ×3, S2 ×3, S3 ×3, S4 ×2, S5, S6 ×4, S7, S8 ×2, S9, S10); 3 persona blockers settled in the Givens (S1 the API secret, S2 French on the scratch journal, S3 two site languages); T-ojs/omp/ops-1 folded into Rule 9a (a non-image extension refused by the browser with "File extension error.", a text file under a `.png` name by the server, S7 two bullets) and T-ojs/omp/ops-2 into S10 (Users & Roles prints "Journal manager" / "Press manager" / "Preprint Server manager"); register unchanged. Verified at the 2026-09-11 baselines: OPS final green on the second run (the first red on U01 S8 only, the ci-triage U01 S8 line dated, every U03 test green in both); OJS green on the ninth attempt (219 passed incl. the serial project), the eight before it lost to the machine, never to a U03 test: the disk full (two harness memory kills, an output folder vanishing mid-run, a browser transport error on U49 S13, U28 S14 and the login smoke at their waits under load 20–40), then the U04 session's fleet reset landing mid-run; every U03 test green in every completed run; OMP green on the second run, the first red on U40 S4 only, the decision-wizard flake-watch class, green alone in 5 s. Low-confidence: Rule 3 (one-journal site, not drivable on the fleets). |
| U04 | ORCID integration | OJS OMP OPS | 10 | done | 10 OJS (S4, S8 serial) · 9 OMP (S4, S8 serial) · 10 OPS (S4 serial; S8, S9 absence) (+setup). 6🐞+5❓+3✅ (A10 retired 2026-09-03, fixed upstream in all three apps). Headliner A5. 1 in the private file, verified. Budget cuts: 0 states, 0 variants; No seed: site-wide ORCID and the locked journal tab (Site Settings → ORCID is one setting for every worker), everything past ORCID's own sign-in (the completion, the emailed link, Deny, the landing variants), every deposit and its emails, the token cancellation, the log, a lapsed permission. Revised 2026-09-13 (classed table): 1 scenario added (S10, where the Author can request verification: the dashboard list read-only on a journal or press, editable on a preprint server per U41's OPS1, the wizard's Contributors step offering the request), all 9 reshaped to the TEMPLATE shape and a Coverage section added, 5 grew (S3 the second user's unauthenticated tab; S4 ×5: the resend link, the sender as the principal contact, the member-API journal's "Requesting ORCID record access", the What-is-ORCID pages differing, the contributor being added waiting for the save; S7 ×2: the site-level Register page, registering without connecting; S8 the verified submitter getting no email; S9 the no-iD reviewer as the control); Rule 14 and S4/S8 name the subject line, S4 reads the requested state on the open form and after reopen (persona read); Rule 13 reads "a verified iD with a live permission" (T-ojs-1 was the tokenless seed: the harness now stamps the live authorization fields on `author` and `users[]` verified seeds, 2 parity rows); T-ops-1 folded as the U41 OPS1 baseline. ORCID legs run against the dead-port proxy with sandbox dummy credentials. Verified at the 2026-09-11 baselines: OPS final green on the first run; OJS on the second, the first red on U23 S9 only (the reviewer-indicator popover under load, ci-triage watch condition tripped, green alone; every U04 test green); OMP green on the first run (every U04 test green in every run). |
| U05 | Notifications center & email preferences | OJS OMP OPS | 11 | in_progress | 10 OJS + S9 serial · 10 OMP · 9 OPS (+setup; OPS S9 and S11 absences asserted inside S7's row list). 6🐞+10❓ (A12, A13 added 2026-09-13). Headliner A10. Blocker: app-changes row 12 (overlay mounted). Budget cuts: 2 states (the Tasks window paged beyond 25 rows; the blue-edged notice toast), 0 variants; No seed: a multi-journal account's journal initials and per-journal choice sets (Rules 2b/2d/5d; pull-back candidates, a second `POST scenarios/context` naming the same username enrols it in two contexts), the announcement email's "Enable…" end, the three scheduled-task emails, the warning toast, the API secret unset, the statistics email off, the rows-per-page config. Revised 2026-09-13 (classed table): 2 scenarios added (S10, a published version raises the Author's task; S11 {OJS OMP}, the review-complete email to the assigned Section Editor alone), all 9 reshaped to the TEMPLATE shape and a Coverage section added, 8 grew (S1 ×4, S2 ×2, S3 ×3, S5 ×5, S6 ×2, S7 ×3, S8, S9 absence sentence); 3 persona blockers settled (S1 the OPS "Relation status" answer, S5 and Rule 6 the From line's name, S7's given holds unread tasks); T-ojs-2/T-omp-1/T-ops-1 folded into Rules 2b/2c/6 and S10 (the "new version" task names the title inside its sentence, no title line under it), T-ojs-1 as A13 ❓ (the Site Administrator's "Dashboard {n}" hidden at desktop width), T-omp-2 as A12 ❓ (same-second tasks in either order; S2 seeds a second apart). Verified at the 2026-09-11 baselines: OMP and OPS finals green on the first run; OJS at the final-run gate: seven OJS full runs at four workers each red on one to three OTHER suites' flake classes, every U05 test green in all seven and S9 green twice alone (`.reports/U05/final-run-ojs-attempt{1..7}.log`): U28 S10 (1), U27 S4 (1), U30 S4 (4, both variants), U01 S7 (3, diagnosed as the page object's Escape-before-focus race and hardened in the OJS and OPS `LoginSessionsPages`, green alone after); the OJS gate waits for one green full run (CI on this push, or an eighth local attempt). Low-confidence: Rules 2d/5d (F26, not drivable on the fleets); A13's narrow-layout half read from code only. |
| U06 | User invitations | OJS OMP OPS | 10 | done | 9 OJS · 9 OMP · 10 OPS (+setup; S9 {OPS}). 7🐞+3❓+1✅ (A6 retired, A9 added 2026-09-18). Headliner A4. Synced 2026-09-18 after pkp/pkp-lib#13313: Edit on a disabled user opens the details step under the disabled-user warning on all three apps (A6 retired, Rule 14 rewritten); A9 ❓ new, reviewed by @beaug the same day ("Remove Role" and the masthead select stay active for a disabled user by design; open: the removal email telling a disabled user their account "is still active"); the disabled-user state moved from No seed to Budget, a guard scenario owed at the next scenario work; scenarios and suites unchanged. 1 in the private file. Budget cuts: 1 state, 3 variants; No seed: an expired invitation (no key backdates a deadline), ORCID's own sign-in window, a disabled user, the daily cleanup, the invitation lifetime in the configuration file. Revised 2026-09-13 (classed table): 1 scenario added (S10, the ORCID step shown only to a recipient without a verified iD, all three apps, on the `orcid` context key and an `orcidIsVerified: true` user), all 9 reshaped to the TEMPLATE shape and a Coverage section added, 6 grew by 13 bullets (S1 ×3: the email's sender, roles, dates, masthead and links, the subject and body as composed, a second send to the same address leaving one row; S2 ×2: the six-character password minimum refusing `Pass5`, the Privacy Statement link; S3 ×2: the roles already held in the email, the link opened alone not accepting; S4: the decline page open declining nothing; S5: the truncated link's not-found error; S8 ×4: the masthead change and its email, Remove Role and its email, the last role, the empty role row); the masthead listing left out as Owned (the body names no masthead screen); Rule 13 and S8 name "Save And Continue" on the details step (persona read); T-ojs/omp/ops folded on every app: the last-role press answers a "Remove Role" dialog "You cannot remove the role. At least one role must be assigned to the user." with one "Close" (the rewrite's ❓ A9 settled by the runs and deleted the same day), the removal confirmation says nothing of an email, a removed role stays listed with End Date today and "User Removed From Role" (Rule 13, Side effects, S8 corrected), A7's masthead sentence made exact from OPS; the 9 OPS titles renamed to S<n>; UserInvitationPages.js per app extended. Verified at the 2026-09-11 baselines: OJS and OPS finals green on the first run; OMP on the third, the first red on U40 S4 (the decision-wizard flake-watch class) and the second on U04 S6 (a page load hanging under desktop load 15–19, new ci-triage watch line), both green alone, every U06 test green in every run. |
| U07 | Journal identity & about pages | OJS OMP OPS |  | pending | |
| U08 | Navigation menus & site chrome | OJS OMP OPS |  | pending | |
| U09 | Custom pages & blocks | OJS OMP OPS |  | pending | |
| U10 | Appearance & theming | OJS OMP OPS |  | pending | |
| U11 | Highlights | OJS OMP OPS | 4 | done | 4 OJS · 4 OMP · 4 OPS (+setup). 5🐞+4❓. Headliner A5 (the site's Highlights tab cannot save, order or list: every request the site panel sends answers 500 on all three apps at the 2026-09-16 tips, app-changes row 13). Budget cuts: 0 states, 4 variants; No seed: the site scope (a site highlight, the site's carousel, a second language at site scope) on A5, the one-journal site, another theme. Blocker: app-changes row 13 (the site scope has no reachable state). Low-confidence: none. Harness: `context.supportedFormLocales` passthrough built (parity PASS, all apps). |
| U12 | Announcements | OJS OMP OPS | 7 | done | 7 OJS · 7 OMP · 7 OPS (+setup; S7 the OJS feed, an absence test on OMP and OPS; S4 and S6 in the serial project, OMP S4 solo). 11🐞+6❓+2✅ (A4 retired: the site's panel saves on every app). Headliner A13 (after "Edit" saves a type's new name the table keeps the old one until a reload, the row refresh answering a server error, all three apps). 1 in the private file (an existing entry extended, verified). Budget cuts: 0 states, 7 variants; No seed: an Editor with "Permit changes to Settings" unticked, a Site Administrator with no role in the journal, the one-journal site. Blocker: none. Low-confidence: A17 (one sighting). Harness: `enableAnnouncements` / `announcementsIntroduction` / `numAnnouncementsHomepage`, `announcementTypes[]`, `announcements[]`, `sidebar` and `plugins{}` on `POST scenarios/context` (5 parity rows PASS, all apps). Verified at the 2026-09-16 baselines on reset databases on the Mac at auto workers: every U12 test green twice on the three apps (the app project in each final, the serial and solo projects alone after it, since a red anywhere in the app project skips them); the finals' reds all outside U12 and green alone: OJS 245/247 twice (U14 S5 both times, U28 S14 once), OMP 236/238 (U01 S6, U14 S5), OPS 158/160 (U40 S1 Mac-only, U14 S5; the first OPS final also red on U12 S3's one-shot grid read under load, fixed as patterns.md lesson 14); U14 S5's Tasks-row red on all three apps is ci-triage's new class; the full greens are the push's CI run. |
| U13 | Article landing page & reading | OJS OPS |  | pending | |
| U14 | Reader comments & moderation | OJS | 14 | done | 13 OJS · 8 OMP · 8 OPS (+setup; S1–S7 common with the press and the preprint server seeded through `userComments[]`, S8–S13 {OJS}, S14 the {OMP OPS} absence of the reader side). 2🐞+10❓. Headliner A10. 2 in the private file, verified. Budget cuts: 2 states (the Editor whose role does not permit settings changes {OJS OMP}; the Site Administrator holding no manager role, with no task row), 0 variants; No seed: none. Low-confidence: A9 (its settling observation sits on a page this spec does not own), OMP1 and OPS1 (driven in the nearest reachable state, a non-manager role kept). Harness: the context passthrough `enablePublicComments` and the submission key `userComments[]` with `reports[]` built (parity PASS). Finals 2026-09-17 on reset databases at four workers, U11 included: OJS 255 green (14.3 min, second run), OMP 244 green (11.6 min, fourth run), OPS 165 green (6.6 min, third run); the earlier runs red on U14 S3 on the three apps until the page objects waited out the modal store's 450 ms close window (patterns.md pitfall 4), on U11 S4 until the PHP overlays were re-mounted after the pull, once on OJS S5 (ci-triage watch) and once on OMP's U49 S4 (the ci-triage class's fourth sighting, green alone). CI: OJS S5 red on the first push until the Comments page heading was read by CSS (run 35190945070 green). |
| U15 | Search | OJS OMP OPS | 12 | done | 10 OJS · 9 OMP · 9 OPS (+setup; serial project; S8, S9 {OJS}, S10 {OPS}, S11 {OMP}, S7 an absence check on OMP). 13🐞+12❓+1✅ (OMP3, OPS4 added 2026-09-13). Headliner A11. 2 in the private file, verified. Budget cuts: 1 state, 2 variants; No seed: a subscription journal's restricted articles, an issue on a scratch journal (no key creates one), the command-line rebuild (the command-line tools are reference only), install or upgrade, the OpenSearch driver and its index name. Revised 2026-09-13 (classed table): 1 scenario added (S12, a published article returned to the workflow and declined stays listed, deleted it is gone at once with nothing queued; all three apps, driven on screen), all 11 reshaped to the TEMPLATE shape and a Coverage section added, 5 grew by 12 bullets (S1 ×3: the "1 - 1 of 1 items" line with no page links, the screen-reader "Found one item." status line, the mail catcher holding no email; S2 ×4: a French title found with the site read in English, whole words only with "publi"/"publication" and "child"/"children", the common word "the" dropped, quotation marks and "AND" ignored and a hyphen not; S6: paging keeping the date filters {OJS OPS}; S7 ×3: the empty box with a filter set, Month and Day without a Year, a journal with nothing published showing blank Year lists; S8: the site-wide address typed after the language code answering page not found); the issue unpublish left out as No seed and the new version as Owned (*Publish, schedule & versions*); the persona read's one wording blocker (the mail catcher's pointer) fixed by the rewrite; T-omp-1/T-ops-1 folded as 🐞 OMP3/OPS4 (a book or preprint declined after publication stays listed but its title opens "404 Not Found" for a visitor, the signed-in manager still gets the page, a journal keeps the landing page open; Rule 2, the Actors "Open a found article" row and S12 corrected, fn-c and fn-s12 dated; the OMP and OPS S12 tests assert the listing and the title's address, never the 404); SearchPages.js per app extended, S12 reusing the shared WorkflowPage; 3 friction lines. Verified at the 2026-09-11 baselines, all three full suites green on the first run (OJS 222 · OMP 219 · OPS 149). Low-confidence: OPS4 (seen once in a suite run; its OMP twin seen twice). |
| U16 | Categories | OJS OMP OPS |  | pending | |
| U17 | Sections | OJS OMP OPS |  | pending | |
| U18 | Web feeds | OJS OMP OPS |  | pending | |
| U19 | OAI-PMH | OJS OMP OPS |  | pending | |
| U20 | Search-engine metadata & analytics | OJS OMP OPS |  | pending | |
| U21 | Submission wizard | OJS OMP OPS | 17 | done | 15 OJS · 16 OMP · 17 OPS (+setup). 11🐞+7❓+3✅. Headliner A8. Budget cuts: 2 states, 3 variants; No seed: task templates, OPS DOIs, the site privacy statement, the start guidance and checklist, the category picker, the reader-site block, a press requiring an abstract, a manager's email opt-out. Revised 2026-09-12 (Budget items as rows): 0 scenarios added, 6 grew by 9 bullets (S2, S4, S5×3, S6, S12, S17×2); S12's Given names the manager's two drafts and both suites now assert the deactivated-section half (T-ojs-1, T-ops-1 settled). Verified at the 2026-09-11 baselines, all three full suites green (the first OPS final, run alongside the OJS final, red on U21 S3 only, a rich-text fill lost under load, green alone; ci-triage flake watch). |
| U22 | My Submissions (author dashboard) | OJS OMP OPS | 5 | done | 5 OJS · 5 OMP · 5 OPS (+setup; OPS S4 absence). 1🐞+7❓. Headliner OPS2. 1 in the private file, verified. Budget cuts: 0 states, 1 variant (the list logging nothing: no screen to read a log on); No seed: none. Revised 2026-09-13 (classed table): 1 scenario added (S5, landing precedence for two-role accounts: Author+Reviewer on the reviewer dashboard, Author+Section Editor and Section Editor+Reviewer on the Dashboard, the author-only control on My Submissions; OPS runs the Section Editor pair only), all 4 reshaped to the TEMPLATE shape and a Coverage section added, all 4 grew (S1 ×6: the retired list address, the empty activity cell awaiting the first move, the panel's address and the exact-address return, the old author-dashboard link, a second author denied on it, a non-authoring Manager denied at the list address; S2 ×3: one submission in several views, the deleted draft gone from every view with no restore and no email, its wizard address dead; S3 ×5: the Copyediting counter {OJS OMP}, the scheduled-into-issue view and cell {OJS}, the declined row's empty cell, Filters without "Assigned To Editor", the mailbox empty, another author's submission absent from every view and search as the control; S4 ×4: the press's Internal Review round {OMP} (the OMP suite's old S5 test folded in), "Review update 1/2" with no "Reviewers assigned:" row on the default review type, the open review's avatar and popover on a second scratch journal set to Open, "Submit revisions" gone after delivery); 2 persona blockers settled (S2 names the "Details" step; the preamble and fn-s2/s3 point to the mail catcher); no test findings; register unchanged; shared MySubmissionsPage +22 methods and EditorialDashboardPage sharing its filter, cell and popover methods; --tests zero. Verified at the 2026-09-11 baselines plus the U15 revision: OPS final green on the first run (151); OMP on the second (221; the first red on U04 S10 only, the submission wizard's Continue not advancing under load, a new ci-triage watch line, every U22 test green in both); OJS never green as a full run in this session: four attempts at four workers (212 tests each), every U22 test green in all four and once alone, each red on other suites' tests only (U05 S6 ×3, its stale unsubscribe page's second goto never leaving the browser, traced in ci-triage for U05's session; U28 S10 once; U30 S4 once, both watched classes); the serial project did not run behind them. Low-confidence: A3 (no feeder for "Scheduled for publication" on a press or preprint server). |
| U23 | Submissions dashboard (editorial) | OJS OMP OPS | 17 | done | 15 OJS · 15 OMP · 12 OPS (+setup). 3🐞+6❓. Headliner A5. Budget cuts: 0 states, 0 variants; No seed: overdue reviews, author revisions, recommendations, the Categories filter, the monthly email. Revised 2026-09-12 (Budget items as rows): 1 scenario added (S17, the confirmed review with and without a minimum), 7 grew (S1, S5, S7, S9, S10, S11, S12); S11 now declines and deletes on screen inside the panel (T-ops-1 folded into fn-s11). Verified at the 2026-09-11 baselines, all three full suites green on the first run. Low-confidence: S17 leaves "Needs reviews" at the default setting unasserted (a footnote-only fact). |
| U24 | Workflow screen & stage access | OJS OMP OPS | 14 | done | 12 OJS · 13 OMP · 8 OPS (+setup; OPS runs S1, S5–S8, S10, S11, S14). 4🐞+10❓+2✅. Headliner A9. 1 in the private file, verified. Budget cuts: 6 states (the language line gone once a second version exists; a journal's second version offering "Publish"; publishing again after an unpublish returning to Done without "Return to Done"; a submission declined in review landing on its round; "Status: Scheduled" with "Unschedule"; "Preview" with "Post" for the Preprint Server Manager), 0 variants; No seed: the "Payments" dropdown, the deletion refused behind the dialog for a role never shown the button, the metadata options' pages, an identifier plugin's "Identifiers" page, a role group's changed stage set. Revised 2026-09-13 (classed table): 4 scenarios added (S11, Reviewer and Reader refused at the dashboard, `workflow/access` and author-dashboard addresses, the preprint server's Editorial Board Member getting the shell with the "Error" dialog; S12 {OJS OMP}, a Manager + Layout Editor account on a scratch journal reaching Production's panels only, no "Activity Log"; S13 {OJS OMP}, an assigned Section Editor's header with "Activity Log", "Change" on "Title & Abstract", no publishing controls, no "Create New Version"; S14, Delete's Cancel and Confirm, the "Declined" count, the mailbox, the stale dashboard and My Submissions addresses answering "Invalid submission."), all 10 reshaped to the TEMPLATE shape and a Coverage section added, S3 and S4 badged {OJS OMP} (the two suite mismatches; OPS1 rides in S11), 9 grew (S1, S2 ×3, S3 ×4, S4 ×3, S5, S6 ×3, S7 ×5, S8 ×9, S10 ×2); 3 persona blockers settled (S7 lands on "Workflow: Copyediting" per Rule 11 and fn-i, the preamble and fn-s point to the mail catcher, S8's Layout Editor control states its two moments); T-ops-1/T-ops-2 folded as the preprint-server markers in S1 and S8 ("Preview", "Activity Log", "Library" on a queued preprint; "Status: Posted"); register unchanged; fn-s extended (S14 on a scratch press or preprint server, S8's OJS scheduled seed through Production); shared WorkflowPage +20 methods and the ACCESS_DENIED export, EditorialDashboardPage.listReload matching the "Assigned to me" fetch, OMP PublicationPages new, OPS PublicationPages +relationsControl; --tests zero; 4 friction lines. Verified at the 2026-09-11 baselines: OPS final green on the first run (153); OMP on the third (225 with the serial project; the first red on U21 S6 and the second on U04 S10 only, both the submission wizard's rail staying on "2 Details" after Continue, the U04 S10 watch condition tripped and dated in ci-triage, every U24 test green in all three); OJS never green as a full run in this session: two attempts at four workers (216 tests each), every U24 test green in both, each red on the same two other suites' tests only (U28 S10, green alone in 30 s afterwards; U30 S4, red alone on the used database too, both ci-triage watch classes, both dated), the serial project 13 green alone with `--no-deps` behind them; CI on this push is the next full OJS run. Low-confidence: the "moved this submission to the Done stage." log line (Rule 18), left out of S8 for want of a verified fact; OPS3's draft bubble text, unasserted (a 🐞 is never the contract). |
| U25 | Submission stage | OJS OMP OPS | 11 | done | 9 OJS · 10 OMP · 1 OPS (S9 absence; +setup). 3❓+2✅. Headliner A2. The A2 area has items in the private file. Budget cuts: 1 state (the author's status summary once the submission has left the stage), 0 variants; No seed: the delete refused behind the button for a role never shown it, the shortcut for a recommend-only editor (no seed sets an assignment's recommend-only flag). Revised 2026-09-15 (classed table): 2 scenarios added (S10 {OJS OMP}, a Funding Coordinator assigned at the stage: the three panels, no decision buttons, the journal's "Schedule For Publication" shortcut pressed to "Title & Abstract"; S11 {OJS OMP}, the "Reviewers Suggested by Author" panel on a scratch journal with the setting on, the suggestion-less submission as control), all 9 reshaped to the TEMPLATE shape and a Coverage section added, S1–S7 badged {OJS OMP} (the preamble states the preprint server's absence once), 6 grew (S1 ×2: the shortcut pressed and the return to "Submission" with the buttons still offered, the no-status-box and "Submission"-label control; S3: the status box "The submission is currently in the Copyediting stage." above "Submission Files" after the move; S4: the "Declined" label, A1 marked; S6: the Author's mailbox unchanged after the delete, the decline's own mail as the control, on a scratch journal; S7: no shortcut in the author view; S8: a second monograph through "Send to Internal Review" and the no-buttons control after either route); Rule 11's OPS test settled as Owned (U24 S6, U22 S1) and removed, the OPS title renamed to S9; 1 persona blocker settled (the preamble points to the mail catcher); no test findings, register unchanged; page objects: shared WorkflowPage +6 methods (status box above a panel, ordered panel headings, action-button labels, the shortcut press), OJS ReviewStagePages +13, OMP ReviewStagePages +11; --tests zero. Verified at the 2026-09-15 evening tips (ojs `5e74c13d87`, omp `db69c4aeb`, ops `8e6f2dd403`, pkp-lib `b262d27b81`, ahead of the sync baselines): OPS final green on the first run (152); OJS on the second (231, 12.2 min; the first red on the login smoke's reader.rosa probe and U30 S4 only, both ci-triage classes dated, the smoke green alone, U30 S4 red alone on the used database as its class records); OMP never green as a full run in this session: three attempts at four workers on reset databases (216 tests each), every U25 test green in all three and twice alone, each red on one other suite's test only (U27 S6's Edit Review file checkbox re-ticked mid-render, new ci-triage line, green alone; the login smoke's reader.rosa probe, its first OMP sighting, green alone; U01 S6's new reviewer missing from the Reviewers list, new ci-triage line, green alone); the push's CI run 35008040155 green on all three apps (OMP 18.3 min), the fresh-box full run that closes the gate. Every U25 test green in every full run. Low-confidence: none. |
| U26 | Review stage & rounds | OJS OMP | 15 | done | 13 OJS · 14 OMP · 1 OPS absence (+setup). 4🐞+10❓+1✅. 1 in the private file. Budget cuts: 5 states, 0 variants (with reasons); No seed: an overdue review (no seed backdates a deadline). Revised 2026-09-12 (coverage rule): 0 scenarios added, 6 grew. Revised 2026-09-13 (Budget items as rows): 1 scenario added (S15, the review minimum at 2: the minimum line replaces the confirmed sentence alone and the submitted sentence stays beneath it, folded from T-ojs-1/T-omp-1 into the Settings bullet and footnote r), 3 grew (S8, S9, S11). Verified at the 2026-09-11 baselines: OJS and OPS finals green first run; the OMP final green on every U26 test in four runs, the accepted run red on U43 S4 only (a click timing out under load, green alone), the first on U40 S4 (ci-triage flake watch), two in between on the CEST-vs-UTC date gap (friction.md). Low-confidence: the cancelled round's files (Rule 12), owned by *Submission files* with no scenario there yet. |
| U27 | Reviewer assignment & management | OJS OMP | 20 | done | 18 OJS · 19 OMP · 1 OPS absence (+setup). 19🐞+8❓+11✅ (10 retired; A26, OMP3, A27 added 2026-09-12; A28 added 2026-09-13; A29–A31 and OMP4–OMP6 added, A24 retired 2026-09-17). Headliner A18. 3 in the private file. Synced 2026-09-17 after pkp/pkp-lib#13338 (claim check S17 on OJS and OMP, 99 lines driven: 23 holds, 2 wrong, 6 imprecise, 10 new; kept script `checks/sync/pkp-lib-13338/s17.js`): the Review Details window and "Modify Review" open on every row that is not cancelled, and "Save Changes" on a request with no review submits the review for the reviewer by design (new Rules 14c and 14d; A24 retired on the issue's stated intention); OMP4 🐞 a press's "Mark as Complete" enabled on a request with no review; persona 2 wording blockers fixed, body 17962 words; scenarios and tests unchanged (Rule 14d sits under Budget, the first candidate for a coverage request). Budget cuts: 10 states, 7 variants (with reasons); No seed: automatic reminders, reviewer files, a section's default review form, the competing-interests badge, a second reviewer group, "Show All {N} Authors", the same-institution badge. Revised 2026-09-12 (coverage rule): 3 scenarios added, 11 grew; S2's own-row read is owned by *Workflow screen & stage access* (A4 there). Revised 2026-09-13 (Budget items as rows): 1 scenario added (S20, the journal's review setup presetting the request), 6 grew (S1, S2, S5, S7, S9, S16); Rule 5's "{N} active" badge corrected to conditional (T-ojs-5, T-omp-2) and A28 ❓ from T-omp-1 (a never-assigned reviewer drops out of the search while "Reviews completed" is enabled; S2 clears the filter first). Verified at the 2026-09-11 baselines, all three full suites green (the first OPS final red on U40 S3 only, a browser transport error mid rich-text fill, green on the re-run; every U27 test green in both). PARKED: the Review Details entry-path parity scenario and its tests (maintainer, 2026-09-01). |
| U28 | Reviewer's review | OJS OMP | 18 | done | 16 OJS · 16 OMP · 1 OPS absence (+setup). 12🐞+3❓+1✅ (A8, OMP4 retired). Headliner A7. Budget cuts: 1 state, 1 variant; No seed: reviewer files in an earlier round's window, the OJS journal's own recommendations. Revised 2026-09-12 (coverage rule): 1 scenario added (S18), 15 grew. Verified at the 2026-09-11 baselines, all three full suites green (the first OJS final red on U30 S3 only, "Submit Response" disabled under load, green alone; every U28 test green in both). fn-p dated on pkp-lib#13109 (the "Done" stage in the OPS Roles screen). Low-confidence: A13. |
| U29 | Review setup & review forms | OJS OMP | 13 | done | 12 OJS · 10 OMP · 1 OPS absence (+setup). 4🐞+6❓+2✅. Headliner A9. 1 in the private file. Budget cuts: 1 state (ordering forms and items), 0 variants; No seed: the automatic reminder email and its log row, a second form language. Revised 2026-09-15 (classed table): 2 scenarios added (S12 the Section Editor, Assistant, Reviewer and Author refused the screen on the seeded journal and press; S13 a submitted form review with an "Included in message to author" item unticked, the editor's "Read Review" in full, the counts 1/0 → 0/1 and the "Minimum Confirmed Reviews Required" lines before and after "Mark as Complete"; the author's window and the decision email Owned), all 11 reshaped to the TEMPLATE shape and a Coverage section added, 6 grew (S1 ×4, S5, S6 ×3, S7 ×3, S9, S10 rewritten on two custom entries with the [A6] read); Settings trimmed to facts; 2 persona blockers fixed (S7 one selected value in the "Edit" window's list, S6 each "Response Options" row fixed with Enter, the suites' own procedure); no test findings, register unchanged; OMP1 test folded into S5, OPS title → S11, --tests zero; shared ReviewSettingsPages +22 methods, OJS ReviewStagePages +7, OMP page objects +25; harness need still open: a `reviewerRecommendations[]` context key (S10 creates its entries through the tab). Finals at the 2026-09-15 evening tips: OPS 152 green and OMP 228 green on the first run; OJS 233 green on the second (12.0 min; the first red on the login smoke's reader.rosa probe alone, the fourth ci-triage sighting, the serial project skipped behind it, green alone). Low-confidence: A6, A7; S11's test runs on a scratch preprint server where the scenario names the seeded one (same screen). |
| U30 | Author response to reviews | OJS | 8 | done | 7 OJS · 1 OMP absence · 1 OPS absence (+setup). 6🐞+3❓. Headliner A7. Budget cuts: 0 states, 0 variants; No seed: "Notify All Authors" at its default and off. Revised 2026-09-13 (Budget items as rows): 1 scenario added (S8, a past round's response beside an empty new round), 4 grew (S1 the declined request, S4 the decision email's button on the journal, S5 the open review's name in the email, S6 the request to two assigned authors); Rule 14 and S6 now name the round id as the address-bar identifier. Verified at the 2026-09-11 baselines, all three full suites green on the first run. Low-confidence: A8 (not seedable). |
| U31 | Reviewer suggestions | OJS OMP | 6 | done | 4 OJS · 5 OMP · 1 OPS absence (+setup). 8🐞+4❓. Headliner A5. 1 in the private file, verified. Budget cuts: 0 states, 0 variants; No seed: the setting switched off after suggestions exist, then on again (Rule 8d; no key changes a journal's setting once it exists). Revised 2026-09-13 (Budget items as rows): 0 scenarios added, 3 grew by 8 bullets (S1 the Edit window closed unsaved, the Journal Manager on the draft, "Submit" with no suggestion, the control reading the panel and the list on the setting-off journal; S2 the Add Reviewer window closed after typing, "Create New Reviewer" with "Email" changed, a submission moved on to Copyediting; S3 the inner window cancelled with a username typed); from the persona read, Rule 8b and Rule 10 name "{name} More Actions" to screen readers and the assigned notice under "Locate a Reviewer", each scenario's address and username its own (fn-s). Verified at the 2026-09-11 baselines, all three full suites green (OJS and OPS first run; OMP on the second, the first red on U26 S10 and U40 S3 only, ci-triage flake-watch class, every U31 test green in both). Low-confidence: A5 (the Enroll path not driven). |
| U32 | Copyediting stage | OJS OMP |  | in_progress | Claim check in flight, paused 2026-09-19 at the weekly Fable limit (RUNBOOK "Model discipline"): draft gate 2026-09-18 (lint zero; 36 Coverage rows, 18 planned; register 🐞1 ❓4 ✅2, headliner A1; 8 to-drive footnotes; no new scenario key). Chunks per `.reports/U32/claimcheck-chunks.md` (four, one at a time): K4 decisions, K3 file lists and author view, K2 notices and the Copyeditor's task DRIVEN (reports `.reports/U32/cc-K{4,3,2}.md` on the maintainer's Mac, scripts `shared/playwright/checks/U32/K{4,3,2}/` committed); K1 (stage screen by role, screenless sections, OPS absence) NOT driven. Next session: fleet-prep, K1, the merge (four chunks), the fold, then `claims.txt` regenerated; K2's report ends with a seed-facts correction for the orchestrator. |
| U33 | Production stage | OJS OMP OPS |  | pending | |
| U34 | Editorial decision recording | OJS OMP OPS |  | pending | |
| U35 | Stage participants | OJS OMP OPS |  | pending | |
| U36 | Submission files | OJS OMP OPS |  | pending | pkp-lib#12352 (2026-09-04) broke the upload wizard's step-1 Cancel restore when another user had renamed the file; reported to the team 2026-09-07 (upstream-sync log); re-filed upstream as pkp-lib#13286, fix PR #13288 verified at its head 2026-09-08. |
| U37 | Tasks & discussions | OJS OMP OPS |  | pending | |
| U38 | Submission activity log & notes | OJS OMP OPS |  | pending | |
| U39 | Submission & Publisher Libraries | OJS OMP OPS |  | pending | |
| U40 | Publication metadata | OJS OMP OPS | 12 | done | 10 OJS · 10 OMP · 10 OPS (+setup; S9 {OJS}, S10 {OMP}, S11 {OPS}). 7🐞+10❓+8✅ (A16 retired 2026-09-14; A10 settled ❓→🐞 {OMP OPS} 2026-09-18). Headliner A1. Synced 2026-09-18 after pkp/pkp-lib#12163 (OJS only so far): term suggestions come only from published versions of the same journal (Rule 7 split into 7a and 7b; two Budget rows); A10 retires when the OMP and OPS pointers pass `14473fe784` and `checks/sync/pkp-lib-12163/term-suggestions.js` is re-run there; scenarios and suites unchanged. 1 in the private file, verified. Budget cuts: 0 states, 0 variants; No seed: none. Revised 2026-09-15 (classed table): 1 scenario added (S12, an assistant, on the preprint server the Section Editor, saves only while the assignment carries the permission: the "Edit Assignment" box, Save disabled and nothing typed kept without it, the readout without "Change", then "Saved" and "Change" once ticked, the Journal Manager as control); all 11 reshaped to the TEMPLATE shape and a Coverage section added; 11 gap bullets and 12 undecided rows ridden: S1 ×3 (the empty required Abstract refused before the save is sent, the press saving [OMP2], the mailbox with a positive control, the log line as control), S2 ×5 (Plain Language Summary at "Ask", Publisher ID and the journal's Article Number, over the section's word limit and a "Do not require abstracts" section {OJS OPS}), S3 ×5 (the author view without Permissions & Disclosure, an unsaved edit dropped, the Author's save on the new version with A16 retired, the other version's own copy, the scheduled article's fields still locked and empty {OJS}), S4 ×1, S5 ×7 ("This is not a valid URL.", no statement blocks, another License URL's link, the override cleared, terms without a license [OMP5], the journal without a default license, the press's copyright line [OMP1]), S6 ×2 (the affiliation copied beside the names, the Author's readout as control), S7 ×2 (nobody else is told: the mailbox and the Author's Tasks), S8 ×2, S9 ×3 (the undated future issue, the reset on the issue basis), S10 and S11 a control each; Settings bullets trimmed to facts; 2 persona blockers fixed (the preamble's mail-catcher pointer; S9's Given names the CC Attribution 4.0 license and the "Copyright (c)" line); 1 test finding folded (T-ojs-1: the Publisher ID box reads "Enable for Publications" / "Enable for Monographs" / "Enable for Preprints", S2 and footnote s2 corrected), register unchanged; OJS PublicationMetadataPages +10 methods, OPS PublicationPages +12, OMP suite helpers extended in place; 3 friction lines. Harness needs returned: an affiliation seed key (OMP S6 asserts the copied names only; OJS and OPS type the affiliation on the contributor form), `issues[]` on the context scenario (OJS S9 creates the undated issue on the Issues screen). Finals at the 2026-09-15 evening tips on reset databases at four workers: OPS 153 green and OMP 229 green on the first run; OJS two full runs each red on one other feature's known class only, with every U40 test green in both (attempt 1 U30 S4's "More Actions" button re-attaching until the timeout, 220 of 221 in 12.0 min; attempt 2 the login smoke's reader.rosa probe, 220 of 221 in 11.5 min, the fifth sighting; both dated in ci-triage), the 13 serial tests skipped behind each red green alone in 33 s and the smoke green alone in 15 s (`.reports/U40/rerun-ojs-{login,serial}.log`); the OJS full-suite green left to CI on this push as the U25 row records for OMP. |
| U41 | Contributors & affiliations | OJS OMP OPS | 11 | done | 10 OJS · 11 OMP · 9 OPS (+setup). 9🐞+10❓+4✅ (A15 retired 2026-09-03; A19 added 2026-09-16 from the push's CI run). Headliner A14. 1 in the private file, verified. Budget cuts: 0 states, 0 variants; No seed: the failed registry search's "ROR API Error" dialog, the stored registry record and its monthly self-update, and the landing page's ROR-mark link (the test installs block server-side egress, A5's shape). Revised 2026-09-16 (classed table): 3 scenarios added (S9 the read-only list for the Author on a journal or press with the badge gone ⚠A6, Preview's three rows, the wizard's Contributors step editable on an unsubmitted draft, the preprint server's full controls [OPS1], the Journal Manager as control; S10 {OMP} an Edited Volume's book page crediting the volume editor "(ed)" with the role name while the catalog list keeps both names [OMP2]; S11 {OJS OMP} the reviewer's own submission-details fetch carrying no contributor on an anonymous assignment and the full list on an open one, read through the page's own traffic), all 8 reshaped to the TEMPLATE shape and a Coverage section added; 9 gap bullets and 6 undecided rows ridden: S1 ×6 (the empty save refused with "This field is required." under Given Name, Email, Country and the roles and "Please correct 4 errors.", a bad Email and Homepage URL refused, the type switch discarding the person's entries, an Anonymous contributor reading "Anonymous" in the row, Preview and the Delete dialog, no email and no log entry after the edits, control), S2 ×2 (the side menu's "Create New Version" copying order, roles, the affiliation and the primary contact; control), S3 ×3 (the move's one "Submission metadata updated" line and no email, the last contributor deleted leaving "No items found." and an empty Preview, control), S4 ×2 (the empty primary-language name refused with "Please provide affiliation name in the submission primary locale."; control), S5 ×2 ("Edit Role" offering only the role's own identifier; control), S6 ×2 (no contact mark on any reader page, "Conceptualization" at "Lead" on the landing page; control), S7 ×2 ("Abbreviated" ignoring the tick; control), S8 ×2 (the setting on again with the statement intact; control); Settings bullets carried no coverage sentence; 3 persona blockers fixed (S2's "Create New Version" settled as the side menu's item offered whatever the version's state per U49, S6's five-contributor book carried into its Given, Coverage's other-document scenario numbers marked as *Publication metadata*'s); 5 test findings folded into 3 corrections (T-ojs-1/T-omp-1/T-ops-1 as one: after a refused save, Save stays disabled while any flagged field still carries its error, Fields and S1 and footnote c; T-ojs-2: the publish window's "Confirm" writes its own "Submission metadata updated" line on a journal, S3 and footnote f; T-omp-2: a press's header "Publish" opens the "Schedule For Publication" window at once with no "Review Publishing Details" step, S3 inline), register unchanged; --tests zero; OJS ContributorPages +24 methods and PublicationMetadataPages +2, OMP ContributorPages extended in place with an optional root, OPS ContributorPages +14 and PublicationPages +1; OMP1 test folded into S6, OJS-A/OMP-A/OPS1 renamed S9, OMP2 renamed S10; two harness needs returned (a `contributors[]` seed family on the submission scenario, already on the "not built yet" list, and a context passthrough for "Require submitting Authors to file a Competing Interest (CI) statement"); coverage-revision queue row and file deleted; 3 friction lines. Finals on 2026-09-16 on reset databases at four workers: OPS 153 green on the second run (the first red on U40 S2's rich-text read alone, 142 of 153 with the 10 serial tests skipped behind it, every U41 test green; a new ci-triage class); OMP 217 of 218 with the login smoke's reader.rosa probe the one red (the sixth sighting, the second on OMP), every U41 test green, the smoke and the 9 serial tests green alone (`.reports/U41/rerun-omp-{login,serial}.log`); OJS two full runs each red on one test only, 221 of 235 both times: attempt 1 U05 S6's page load to the timeout (the known class, every U41 test green, the 13 serial tests green alone in 32 s and the smoke in 16 s), attempt 2 U41 S3's mailbox control (the Notify window's template body never landing in the editor under load, U40's helper), green alone in 10 s, a first sighting dated in ci-triage; the OJS full-suite green left to CI on this push as the U25 and U40 rows record. The push's CI run (35080349964) redded OMP on U41 S1 alone: the contributor form's Email field reads "Email" on CI's fresh install and "Email address" on the VM, because pkp-lib defines the `user.email` key twice (common.po and user.po); folded as A19 ❓ minor with `Basis: test run`, the OMP jump-link read made label-agnostic, S1 green alone and a second full OMP run on a reset database after the fix (see the follow-up commit). |
| U42 | Citations & references | OJS OMP OPS |  | pending | |
| U43 | Funding | OJS OMP OPS | 6 | done | 6 OJS · 6 OMP · 5 OPS (+setup). 3🐞+9❓+2✅ (A13 retired 2026-09-03). Headliner A3. Budget cuts: 0 states, 0 variants; No seed: a hand-named funder's grants never checked, the registry record cache and A11's grant-number rejection (the test installs block server-side egress, A3's shape). Low-confidence: A4 (not re-probed at the fixed tips), A10, A11 (need a server with egress). Revised 2026-09-16 (classed table): 2 scenarios added (S5 the read-only list for the Author on a journal or press with "Add Funder" and "Order" grayed out and no "…" menu, the preprint server's full controls [OPS1], the Journal Manager as control; S6 {OJS OMP} the reviewer's review screen and its "View All Submission Details" window showing no funders list in any review type and the window's publication fetch carrying none, read through the page's own traffic, the manager's own Funding screen's submission fetch as control), all 4 reshaped to the TEMPLATE shape and a Coverage section added; 7 gap bullets and 3 undecided rows ridden: S1 ×5 (the empty save refused with "Search and select a Funder or enter a Funder name" and "Please correct one error.", "Delete" under the chosen funder clearing it for a new search, "not-a-doi" refused with "This is not formatted correctly." and a blank second grant row dropped on save, no email and no log entry after the edits, control), S2 ×1 (control), S3 ×1 (the landing page in the saved order, "Second Foundation" first), S4 ×4 (the published page keeping its "Funders" block with the setting off, the re-tick saved on "Do not request…" as Rule 2's enabled level, "Require" and the Review step's "Funders are required." ⚠A1, control); Settings bullets carried no coverage sentence; 2 persona blockers fixed (S1's registry-funder step now says the search must answer in the browser, names the A9 shape and ties Ordering and Delete to the registry funder; S6's funder and the response to read named); 4 test findings folded into 2 corrections (T-ojs-1/T-omp-1 as one: at the pinned tips funders ride on the submission's data, which no reviewer screen fetches, so the review screen and the details window's publication fetch carry no funders list in any review type and the anonymous-type withholding is code-read only, Rule 13, S6 and footnotes g, h, s6; T-ojs-2/T-ops-1 as one: the Edit Funder panel is dismissed with "Close", not "Cancel", S1), register unchanged; --tests zero; OJS FundingPages +16 methods and a `landingFunders` export, OMP FundingPages +17, OPS FundingPages +23; OJS-A/OMP-A/OPS1 renamed S5; harness needs none; one step left out for want of a fact (the "raises no notification" read, the body naming no screen for it); coverage-revision queue row and file deleted; 3 friction lines. Finals at the 2026-09-15 tips on reset databases at four workers: OPS 153 green and OMP 230 green on the first run; OJS two full runs each red on one U41 test only through the Notify window's template-body helper (the known ci-triage class, its second and third sightings dated, watch condition met), 222 of 236 both times with every U43 test green, the red test and the 11 serial tests green alone right after each run; the OJS full-suite green left to CI on this push. |
| U44 | Identifiers (publisher IDs & URN) | OJS OMP OPS |  | pending | |
| U45 | DOIs | OJS OMP OPS |  | pending | |
| U46 | Galleys | OJS OPS |  | pending | |
| U47 | Media files | OJS OMP OPS |  | pending | |
| U48 | JATS & Body Text | OJS |  | pending | |
| U49 | Publish, schedule & versions | OJS OMP OPS | 18 | done | 15 OJS · 13 OMP · 12 OPS (+setup). 7🐞+9❓ (OJS3 narrowed 2026-09-16 from the test run: the draft's own version address answers "404 Not Found", a number the article has no version under still crashes blank). Headliner OJS2. Blocker: app-changes row 10; S11 OJS is on the ci-triage flake watch. Budget cuts: 2 states, 0 variants; No seed: the plugins-only warning list, the once-daily press check, a saved revision summary for "Insert Content", a dangling issue on a journal (deleting the issue empties the version's choice instead). Revised 2026-09-16 (classed table): 2 scenarios added (S17 {OJS OMP} a submission still in Review already offering the publish button with no Preview among the publishing controls, the header Preview absent too on a journal, the Production-stage submission as control; S18 {OJS OMP} "Send to Text Editor" offered on a Markdown row of Production Ready Files and not on a PDF row, the "Send File to Text Editor" dialog with "Create New Version" first), the planned dangling-issue scenario settled as No seed, all 16 reshaped to the TEMPLATE shape and a Coverage section added, 11–13 badged {OJS}, 14 {OMP}, 15–16 {OPS} (the twelve lint mismatches); 17 gap bullets and 3 undecided rows ridden: S1 ×3 (the preselected "Assign To Current/Back Issue" {OJS}, the press's "Awaiting approval." then "Status / Submission published." {OMP}, the Done-stage log line, control), S3 ×1 (the returned-to-workflow line), S4 ×2 (the draft by address {OJS}, "A new version was created."), S5 ×4 ("Insert Content" and its "No saved summaries found…" panel {OJS OMP}, the Author's profile opt-out with the task notice still filed, the later version unpublished leaving 1.0 live, control), S6 ×2 (a major revision in a stage with versions "2.0", the cleared review round refused {OJS}), S9 ×1 (the unpublish log line after an unschedule), S10 ×1 ("2030/01/01" refused {OJS OPS}), S11 ×3 (nothing stamped, nothing sent, the scheduling log line), S12 ×2 ("Don't Assign To An Issue", no "Future Issue" choice once the issue is published), S14 ×1 (nothing stamped or sent), S15 ×3 (a contributor without an account acknowledged, nothing sent on the future-date leg, "Do not send an email." on a second server seeded `postedAcknowledgement: false`); the filled past date settled as Nothing new to test, "Unassigned version ({date})" as Budget; Settings bullets carried no coverage sentence; 7 persona blockers fixed (the untouched issue fields on Confirm, the version address shape, the profile row quoted as footnote x, the review-round picker's placeholder and the PMUR refusal, S11's no-published-issue rider, S12's screens, S16 read as the wizard ends); 9 test findings folded into 6 corrections (T-ojs-4/T-omp-1/T-ops-1 as one: the opt-out is ticking "Do not send me an email for these types of notifications.", S5 and footnote x; T-ojs-1: the panel's "Associated review round" pre-fills only on the version the round was opened for, Fields, Rule 7, S6; T-ojs-5: the button reads "Publish" on a published submission, S6; T-ojs-6: the panel reopens pre-checked after an unpublish, Rules 3 and 10, S10; T-ojs-3: no header Preview on a journal's in-review submission, Actors, S17 {OJS}, a Budget bullet for the press's; T-ojs-2: OJS3 narrowed; T-ojs-7 a footnote v date, the OJS picker's preselection unsettled); --tests zero; OJS PublishSchedulePages +22 methods, OPS PublicationPages +3, OMP suite helpers extended in place, `notes.md` fixtures on OJS and OMP; harness: the OPS context passthrough `postedAcknowledgement` built (parity PASS, the three U21 suites green); coverage-revision queue row and file deleted; 4 friction lines). Finals at the 2026-09-15 tips on reset databases at four workers: OJS 238 green on the first run (13.1 min); OMP 232 green on the second (11.1 min), the first red on U27 S9's star-rating click alone with every U49 test green, green alone in 33 s, a new ci-triage class with a watch condition; OPS 153 green on the third run (6.1 min) with the `readRichText` helper's bounded retry, the first two red on U40 S2's rich-text read alone with every U49 test green (the ci-triage class's second and third sightings, the watch condition met and the fix applied). |
| U50 | Issues | OJS |  | pending | |
| U51 | Subscriptions & open access control | OJS |  | pending | |
| U52 | Payments & APCs | OJS |  | pending | |
| U53 | Users management | OJS OMP OPS |  | pending | |
| U54 | Roles configuration | OJS OMP OPS |  | pending | |
| U55 | Notify users (bulk email) | OJS OMP OPS |  | pending | |
| U56 | Emails management | OJS OMP OPS |  | pending | |
| U57 | Languages & locales | OJS OMP OPS |  | pending | |
| U58 | Submission intake configuration | OJS OMP OPS |  | pending | |
| U59 | Hosted journals (site admin) | OJS OMP OPS |  | pending | |
| U60 | Site settings | OJS OMP OPS |  | pending | |
| U61 | System administration & jobs | OJS OMP OPS |  | pending | |
| U62 | Plugins management | OJS OMP OPS |  | pending | |
| U63 | Import & export | OJS OMP OPS |  | pending | |
| U64 | Statistics — usage | OJS OMP OPS |  | pending | |
| U65 | Statistics — editorial activity & reports | OJS OMP OPS |  | pending | |
| U66 | Institutions | OJS OMP OPS |  | pending | |
| U67 | Archiving & preservation | OJS |  | pending | |
| U68 | Catalog browse | OMP |  | pending | |
| U69 | Monograph landing page | OMP |  | pending | |
| U70 | Catalog management | OMP |  | pending | |
