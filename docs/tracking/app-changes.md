# App changes & build blockers

> Append-only ledger: rows are dated records — corrected by dated follow-up
> notes, never rewritten.

Per `docs/process/RUNBOOK.md` "What goes where", this file records ONLY:

- **actual app-code changes** made by the campaign (what, why, where, commit);
- **build blockers**: app defects that prevented tests from running green
  (race conditions, nondeterministic UI, harness-hostile behavior) and the
  workaround taken.

Product findings — bugs, divergences, open questions — live in each feature
spec's Findings register, never here.

**Reset 2026-07-26**: the previous contents (8 production fixes and a 272-row
findings archive) were deleted and the fixes reverted (git history keeps both);
the restarted process rediscovers what matters on its own evidence.

**FULL RESET #2, 2026-07-31**: rows 1–15 were removed with the harness and
feature artifacts they described (the Fable-only rebuild, PROGRESS banner).
The one prior app-code change — the `Config.php` `PKP_CONFIG_FILE` line — was
reverted with them. Deliberately kept: the `.gitignore` entries for local test
config (`config.test.inc.php`, `.env.playwright`), so credentials on disk can
never be swept into a commit; they reveal nothing the PRINCIPLES design record
does not already state.

One file for all three apps: name the app per row (`ojs` / `omp` / `ops`;
shared lib/pkp changes say `pkp-lib`).

| # | App | What & why | Files | Commit | Notes |
|---|-----|-----------|-------|--------|-------|
| 1 | pkp-lib | `CONFIG_FILE` honours the `PKP_CONFIG_FILE` env var so the test fleets run against `config.test.inc.php` without touching the dev install (harness rebuild, 2026-07-31) | `classes/config/Config.php` (one line) | harness-rebuild commit (2026-07-31), branch `e2e_ng_2` | Recreates the change scratched at FULL RESET #2; no behavior change when the env var is unset. MERGED UPSTREAM (on pkp-lib `main` by 2026-08-27) — the CI lib/pkp pin and reduced branches are retired |
| 2 | ops | `getPublicationTitleAbstractForm()` passed raw `$section->getData('wordCount')` (null when the section sets no word limit) into `TitleAbstractForm::__construct(int)` → TypeError; the author's workflow view of any such submission showed an Error dialog (hit by U01 scenario 8's author variant). Added the `(int)` cast OJS already has at its equivalent line | `api/v1/submissions/SubmissionController.php` (one line) | U01 commit, branch `e2e_ng_2` | App-code fix (build blocker); fails closed before the fix, no data issue. MERGED UPSTREAM (on pkp/ops `main` by 2026-08-27) |
| 3 | pkp-lib | Build blocker WORKED AROUND, not changed: `SubEditorsDAO::assignEditors()` filters candidate assignments with `$userGroups->keys()` — positional collection indexes, not group ids — so sub-editor auto-assignment on submit silently fails in any context whose user-group ids exceed the group count (every scratch/late-created context; first-context installs pass by accident). Verified in `ops_test`: correct `subeditors` row, matching section, no stage assignment | `classes/submission/SubEditorsDAO.php` (unchanged) | — | U01 OPS scenario-8 test seeds on `publicknowledge` instead of a scratch context to dodge it; product finding belongs to the future editor-assignment spec's register |
| 4 | pkp-lib | Build blocker WORKED AROUND, not changed: the Add Reviewer request-letter prefill races TinyMCE init — selecting a reviewer before the editor's async render finishes has `setContent` wiped by late init, so the letter silently arrives empty and the add then 500s server-side (`EditorAction.php` `logMailable(REVIEW_REQUEST)` renders a null body). Product symptom is the U27 spec's register (silent half-completed add) | `ReviewStagePages.js` `selectReviewer` waits on the letter editor's `initialized` flag (app code unchanged) | — | App fix should guard the prefill and the empty-body mailable render |
| 5 | pkp-lib | Scenario-builder parity fix: `PKPSubmissionScenarioBuilder` seeded review due = today + (response+review) weeks vs the app's today + review weeks (masked on the 4/4-week baseline), fixed by reusing the app's `HasReviewDueDate` trait; second suspected gap (missing "Review pending." reviewer task) DISPROVEN — builder drives `EditorAction::addReviewer`, which creates it; observed absence was the app's own on-submit task deletion. Ledger rows in `lib/pkp/docs/e2e/tracking/parity-ledger.md` (2026-08-02) | `classes/testing/scenario/PKPSubmissionScenarioBuilder.php` | U27 commit, branch `e2e_ng_2` | Campaign-owned harness (self-healing rule); review suites re-run green ×3 fleets post-fix; two OJS test assertions corrected to app-true values |
| 6 | pkp-lib | Build blocker WORKED AROUND, not changed (U40, 2026-08-28, pinned apps): the "Change Submission Language For" panel races its own async loads — interacting before the publication fetch / `changeLanguageMetadata` form fetch / TinyMCE init settle gives three symptoms: stale-language descriptions+prefill kept for that opening (OJS), Confirm accepted with an empty required Title and the locale changed anyway (OMP, ~3/5 under test timing), old-language text stored as the new language's title via nondeterministic prefill (OPS). Settled-state behavior is correct; product symptom is the U40 register's language-panel race entry | OJS suite gates on the panel subtitle rendering; OMP suite bounds every panel open on the `changeLanguageMetadata` response; OPS POM waits on each editor's `initialized` flag and clears before typing (app code unchanged) | — | Mechanism: `workflowChangeSubmissionLanguageModalStore.js` `setCustom` only rewrites once `getData()` fills `publicationProps`; app fix should gate the panel on its loads |
| 7 | pkp-lib | Build blocker WORKED AROUND, not changed (U40/OJS, 2026-08-28): "Review Publishing Details" issue-assignment radios race their async preselection — changing the radio early submits an inconsistent hidden status; server answers 400 and the raw API error surfaces in a dialog ("You can not modify the status directly through the API…"). Belongs to the future Publish/schedule spec's register | OJS suite waits for a preselected radio before touching the group (`PublicationMetadataPages.awaitAssignmentPreselected`; app code unchanged) | — | |
| 8 | ojs | Build blocker WORKED AROUND, not changed (U40, 2026-08-28): after an Issue Data save, the Issue Management modal's Close control re-renders indefinitely ("element is not stable") and the form stays dirty, so the next navigation raises a beforeunload confirm that hangs the run | OJS suite skips Close and accepts beforeunload dialogs (`PublicationMetadataPages.setBackIssueDate`; app code unchanged) | — | Legacy issue UI; product symptom belongs to the future Issues spec's register |
| 9 | pkp-lib | Build blocker WORKED AROUND, not changed (U41, 2026-08-28, pinned apps): after a contributor save, the panel's success handler triggers an async publication refresh that can remount workflow components mid-interaction. Three proven manifestations under load: (a) the contributors panel spontaneously exits Order mode / detaches nodes under an in-flight click, so an arrow or "Save Order" click is silently lost (panel out of Order mode, zero `saveOrder` POSTs in worker logs); (b) Save Order serializes the pre-refresh row state — the response is ok but the persisted order is the reverted one; (c) a rich-text form remount reverts uncommitted editor content before save — payload probe caught U40 OPS S4 POSTing the OLD abstract (~1-in-6) after a confirmed fill, so the save honestly persisted stale text (the reader page was innocent) | U41 suites: all six ordering sites (OJS `ContributorPages.makeFirst` + S2 inline, OMP/OPS `pinOrder` + S2 inline) use content-verified bounded retry loops — success is the persisted first row, never button-mode DOM state or response arrival, redoing enter-order → move → save per attempt. U40: OPS `PublicationPages.fillRichText` fires TinyMCE `change` and S4 content-verifies the save-response JSON; OMP S4 adds a `blur()` commit (OJS already commits via `editor.fire('change')` — unaffected). App code unchanged | — | App fix should stop remounting the panel/forms on the async refresh, or make in-progress UI state survive it. Same session also hardened U26's Record Decision click (both apps) with the outcome-keyed re-click, double-POST-guarded — decisions are not idempotent. Gate condition note: full-suite runs on the loaded dev box (ambient load ~13–15) used `PLAYWRIGHT_WORKERS=5` + a solo `--project=setup` warm after each reset; at 8 workers cold, `_test/scenarios/context` seeding intermittently hits the PHP 30s ceiling (500) |
| 10 | pkp-lib | Build blocker WORKED AROUND, not changed (U49/OJS, 2026-08-29, pinned apps): on a future-issue-only journal the publish panel's first assignment pick keeps the server default's READY_TO_PUBLISH status — `useWorkflowPublicationFormIssue.js`'s `isInitialDataLoad` slot is never consumed when the default `assignmentType` matches no offered option — so "Assign To Future Issue and Schedule Only" publishes immediately on the first pick (deterministic; second pick behaves). Product finding is the U49 register's | U49 OJS suite: S11 reaches scheduling via the Publication-Settings-first route; issue-journal panel opens bounded by the `issueAssignmentStatus` response (`PublishSchedulePages.openPublishPanelExpectingIssueFields`); app code unchanged | — | Refines row 7: that row's early-pick 400 and this settled-state wrong-status save share the same hidden-status plumbing. Follow-up same day: the misfire also covers the Publication Settings page — with no published issues a saved "Schedule Only" reloads as "Assign To Future Issue and Publish Immediately" (radio value 2); S11 therefore seeds a published back issue beside the future issue, and reloads the workflow after the settings save (row-9-family async refresh: an immediate panel open loses the saved issue in the select) |
