# App changes & build blockers

Rows are app defects the tests work around (with the workaround) and any
app code the campaign changed (overlays), for all three apps, the app named
per row (`ojs` / `omp` / `ops`; shared lib/pkp changes say `pkp-lib`). A row
is deleted when the app fix ships upstream (RUNBOOK "What goes where"; git
keeps it).

| # | App | What & why | Files | Commit | Notes |
|---|-----|-----------|-------|--------|-------|
| 3 | pkp-lib | Build blocker WORKED AROUND, not changed: `SubEditorsDAO::assignEditors()` filters candidate assignments with `$userGroups->keys()` — positional collection indexes, not group ids — so sub-editor auto-assignment on submit silently fails in any context whose user-group ids exceed the group count (every scratch/late-created context; first-context installs pass by accident). Verified in `ops_test`: correct `subeditors` row, matching section, no stage assignment | `classes/submission/SubEditorsDAO.php` (unchanged) | — | U01 OPS scenario-8 test seeds on `publicknowledge` instead of a scratch context to dodge it; product finding belongs to the future editor-assignment spec's register |
| 4 | pkp-lib | Build blocker WORKED AROUND, not changed: the Add Reviewer request-letter prefill races TinyMCE init — selecting a reviewer before the editor's async render finishes has `setContent` wiped by late init, so the letter silently arrives empty and the add then 500s server-side (`EditorAction.php` `logMailable(REVIEW_REQUEST)` renders a null body). Product symptom is the U27 spec's register (silent half-completed add) | `ReviewStagePages.js` `selectReviewer` waits on the letter editor's `initialized` flag (app code unchanged) | — | App fix should guard the prefill and the empty-body mailable render |
| 6 | pkp-lib | Build blocker WORKED AROUND, not changed (U40, 2026-08-28, pinned apps): the "Change Submission Language For" panel races its own async loads — interacting before the publication fetch / `changeLanguageMetadata` form fetch / TinyMCE init settle gives three symptoms: stale-language descriptions+prefill kept for that opening (OJS), Confirm accepted with an empty required Title and the locale changed anyway (OMP, ~3/5 under test timing), old-language text stored as the new language's title via nondeterministic prefill (OPS). Settled-state behavior is correct; product symptom is the U40 register's language-panel race entry | OJS suite gates on the panel subtitle rendering; OMP suite bounds every panel open on the `changeLanguageMetadata` response; OPS POM waits on each editor's `initialized` flag and clears before typing (app code unchanged) | — | Mechanism: `workflowChangeSubmissionLanguageModalStore.js` `setCustom` only rewrites once `getData()` fills `publicationProps`; app fix should gate the panel on its loads |
| 7 | pkp-lib | Build blocker WORKED AROUND, not changed (U40/OJS, 2026-08-28): "Review Publishing Details" issue-assignment radios race their async preselection — changing the radio early submits an inconsistent hidden status; server answers 400 and the raw API error surfaces in a dialog ("You can not modify the status directly through the API…"). Belongs to the future Publish/schedule spec's register | OJS suite waits for a preselected radio before touching the group (`PublicationMetadataPages.awaitAssignmentPreselected`; app code unchanged) | — | |
| 8 | ojs | Build blocker WORKED AROUND, not changed (U40, 2026-08-28): after an Issue Data save, the Issue Management modal's Close control re-renders indefinitely ("element is not stable") and the form stays dirty, so the next navigation raises a beforeunload confirm that hangs the run | OJS suite skips Close and accepts beforeunload dialogs (`PublicationMetadataPages.setBackIssueDate`; app code unchanged) | — | Legacy issue UI; product symptom belongs to the future Issues spec's register |
| 9 | pkp-lib | Build blocker WORKED AROUND, not changed (U41, 2026-08-28, pinned apps): after a contributor save, the panel's success handler triggers an async publication refresh that can remount workflow components mid-interaction. Three proven manifestations under load: (a) the contributors panel spontaneously exits Order mode / detaches nodes under an in-flight click, so an arrow or "Save Order" click is silently lost (panel out of Order mode, zero `saveOrder` POSTs in worker logs); (b) Save Order serializes the pre-refresh row state — the response is ok but the persisted order is the reverted one; (c) a rich-text form remount reverts uncommitted editor content before save — payload probe caught U40 OPS S4 POSTing the OLD abstract (~1-in-6) after a confirmed fill, so the save honestly persisted stale text (the reader page was innocent) | U41 suites: all six ordering sites (OJS `ContributorPages.makeFirst` + S2 inline, OMP/OPS `pinOrder` + S2 inline) use content-verified bounded retry loops — success is the persisted first row, never button-mode DOM state or response arrival, redoing enter-order → move → save per attempt. U40: OPS `PublicationPages.fillRichText` fires TinyMCE `change` and S4 content-verifies the save-response JSON; OMP S4 adds a `blur()` commit (OJS already commits via `editor.fire('change')` — unaffected). App code unchanged | — | App fix should stop remounting the panel/forms on the async refresh, or make in-progress UI state survive it. Manifestation (c) traced 2026-09-15 to a stale form kept on screen across Publication sections, fixed upstream by keying the form per section (ui-library `70b0892042`; flake-investigation report, patch B); (a) and (b) still open; (c)'s fix reached the pinned tips 2026-09-16 (the apps' ui-library pointers at `977e460c`), the OMP `blur()` stays as a harmless commit. |
| 10 | pkp-lib | Build blocker WORKED AROUND, not changed (U49/OJS, 2026-08-29, pinned apps): on a future-issue-only journal the publish panel's first assignment pick keeps the server default's READY_TO_PUBLISH status — `useWorkflowPublicationFormIssue.js`'s `isInitialDataLoad` slot is never consumed when the default `assignmentType` matches no offered option — so "Assign To Future Issue and Schedule Only" publishes immediately on the first pick (deterministic; second pick behaves). Product finding is the U49 register's | U49 OJS suite: S11 reaches scheduling via the Publication-Settings-first route; issue-journal panel opens bounded by the `issueAssignmentStatus` response (`PublishSchedulePages.openPublishPanelExpectingIssueFields`); app code unchanged | — | Refines row 7: that row's early-pick 400 and this settled-state wrong-status save share the same hidden-status plumbing. Follow-up same day: the misfire also covers the Publication Settings page — with no published issues a saved "Schedule Only" reloads as "Assign To Future Issue and Publish Immediately" (radio value 2); S11 therefore seeds a published back issue beside the future issue, and reloads the workflow after the settings save (row-9-family async refresh: an immediate panel open loses the saved issue in the select) |
| 11 | pkp-lib | Build blocker WORKED AROUND, not changed (U26/U27, 2026-08-29, i13156 Review Details rework): (a) the rating radios accept clicks before the window's data settles and the on-open mark-viewed round-trip re-renders the control even after the "Modify Review" button enables, silently reverting an early star click (U27 register A21); (b) the workflow modal's reviewer rows unmount beneath the stacked Vue dialogs, so row state cannot be asserted while a dialog is open | OJS `ReviewStagePages.rateReview` / OMP `ReviewerAssignmentPages.rateReview` use outcome-keyed bounded re-clicks (radio must hold + "Reviewer rating saved" toast); all row assertions moved after dialog close; dialogs anchored by role+title (patterns.md locator pitfall 6; app code unchanged) | — | App fix should disable the rating control until the assignment load AND the consider round-trip settle. Patch written and verified 2026-09-15: `docs/reports/2026-09-15-ui-library-reviewer-rating-lock.patch` (flake-investigation report, patch C) |
| 12 | omp, ops | Build blocker WORKED AROUND by overlay (U05, 2026-09-04, at the 2026-09-02 pkp tips, still unfixed on pkp `main` as of 2026-09-11: omp `ab748e2c3` and ops `a72cacc1c5` track no `classes/notification/Notification.php`): lib/pkp's `api/v1/submissions/tasks/EditorialTaskController.php` imports `APP\notification\Notification` (since pkp-lib `139bde1e657`, 2026-02-10), a class only OJS ships. On OMP and OPS, saving a discussion or task from the workflow page (and replying to one) fails with the dialog `Class "APP\notification\Notification" not found`; the row is stored but no task or email follows. Blocks every U05 scenario that needs a discussion email on OMP/OPS | `apps/omp/php/classes/notification/Notification.php`, `apps/ops/php/classes/notification/Notification.php` (empty subclass of `PKP\notification\Notification`, mirroring OJS's own subclass minus its OJS-only constants; mounted by `bin/mount.js`) | U05 commit | Trivially safe mirror; product finding: the Tasks & discussions register A1 (OMP and OPS, as shipped, crash: server). Remove the overlay when OMP and OPS carry the class upstream |
| 13 | pkp-lib | Build blocker NOT WORKED AROUND (U11, 2026-09-16, pinned apps, all three): the site-level Highlights panel (Administration › Site Settings › Site Setup › Highlights, shown on a multi-context site) cannot save anything: "Save" in the site's "Add Highlight" window leaves it open with no message, "Save Order" shows "Error / Call to a member function getId() on null", and every request the panel sends to `index/api/v1/highlights` answers 500 because the `has.roles` middleware reads `$context->getId()` with no context (`lib/pkp/classes/middleware/HasRoles.php:63`). The site-scope scenarios of U11 (a site highlight, the site's carousel) have no reachable state until this ships; the U11 register carries it as A5 | none (site scope left out of the U11 suites, register entry A5) | — | Re-check on each sync; when the site panel saves, pull the three "No seed" site rows of U11's Coverage back into scenarios |
| 14 | pkp-lib (`stable-3_5_0` line only) | App code CHANGED in a working tree, never committed (2026-09-17): `stable-3_5_0` has no `PKP_CONFIG_FILE` support, the harness's only switch between an install's own config and the test config, so `bin/fetch-apps.js --line stable-3_5_0` writes `main`'s version of the one `define('CONFIG_FILE', …)` line into the line's checkouts and re-applies it after every `--update` (harness.md "The stable line"). The `main` checkouts are untouched | `checkouts/stable-3_5_0/<app>/lib/pkp/classes/config/Config.php` | none (working tree) | The row goes when `stable-3_5_0` receives the change upstream (pkp-lib `main` has it) or the line is retired |
| 15 | pkp-lib | Build blocker WORKED AROUND in the seed, not changed (U34, 2026-09-20, OJS and OMP driven; the Contributors form and its API are lib/pkp's): on a context with a second language under "Forms" that is not also a submission language, the Contributors page's "Add Contributor" › "Save" is refused with no message (the window stays under "Saving"): the form posts "Organization Name" in every form language and `POST contributors` answers 400 `{"organizationName":{"fr_CA":["This language is not accepted."]}}`. The U34 S8 seeds `context.supportedSubmissionLocales` alongside `supportedFormLocales`; footnote s1 says so. | `apps/ojs/playwright/tests/U34-editorial-decision-recording.spec.js` (S8 seed), `apps/omp/…/U34-…spec.js` (S8 seed); evidence `.reports/U34/tojs/contrib-fr.log`, `.reports/U34/tomp/s8-formlocale-check.log` | — | Product finding for *Contributors & affiliations* (U41): incidentals row 2026-09-20. Delete when the form reports the refusal or the API accepts the form language. |
| 16 | ui-library | Build blocker WORKED AROUND, not changed (U47, 2026-09-25, OJS driven): the "Media" page's "Edit Metadata" window builds its name field on the row's own name object, so a name typed and then discarded ("Warning" › "Yes") shows in the "Media" list and in the reopened window until the page is reloaded, although nothing was saved (spec register, from T-ojs-1 at the step 8 fold) | U47 suites, scenario 3 | — | The test re-lands the "Media" page by address before reopening "Edit Metadata" and asserts nothing about the stale name; delete the row and the re-land when the fix ships |
| 17 | ui-library / pkp-lib | Build blocker WORKED AROUND, not changed (U47, 2026-09-25, OMP driven): the "Media" page's file-name link is built with `stageId=5` (Production) whatever stage the submission is in, so a press role that sees the list outside Production (the Funding Coordinator in External Review; the Copyeditor and the Marketing and Sales Coordinator in Copyediting) gets the file API's refusal "The current role does not have access to this operation." instead of the file (spec register, from T-omp-1 at the step 8 fold) | OMP U47 suite, scenario 8 | — | Scenario 8 asserts the name link is offered, not the download; delete the row and restore the download assertion when the fix ships |
