# Upstream sync, the stable line — last-read `stable-3_5_0` commits

The last commit of each repo's `stable-3_5_0` branch the daily session has
read for regressions (`docs/process/MAINTENANCE.md` "The stable line:
`stable-3_5_0`"). The regression hunt alone runs here: no spec, no suite
and no CI follows this branch. `main`'s baselines are in
`upstream-sync.md`.

## Baselines

| Repo | Last-read commit | Date | Read by |
|------|------------------|------|---------|
| ojs | `a3dc3b54ff` | 2026-09-25 | claude (daily maintenance session) |
| omp | `f77b23709` | 2026-09-25 | claude (daily maintenance session) |
| ops | `225e25475e` | 2026-09-25 | claude (daily maintenance session) |
| pkp-lib | `479b38a09c` (ojs, omp, ops) | 2026-09-25 | claude (daily maintenance session); ui-library `1a7a4750` (ojs, omp, ops); ojs `plugins/generic/jatsTemplate` `26c6ca0aa2` |

## Read log

_Newest first; one entry per read: the date and the range per repo, then
one line per commit (sha → `=main <sha>` with the date of the `main` read /
`~main <sha>` with what the backport changed / `stable-only`; the
regression verdict; what was filed)._

- **2026-09-26 (daily session, VM) — pulled; no range, baselines stay.** Tips unchanged since 2026-09-25: ojs `a3dc3b54ff`, omp `f77b23709`, ops `225e25475e`, lib/pkp `479b38a09c` on all three, ui-library `1a7a4750`. pkp-lib `stable-3_5_0` holds, past the pointers, the #10813 backport (`7fb5cc6e63` + merge `69d8beabc3`, `=main 5ca591d18d`, read on `main` today: no regression) and the #13370 branch commits behind yesterday's squash; read when a pointer moves. Carried over from `main`: today's regression pkp-lib#12798 is not on the line (`git log --grep 12798` empty). Open stable-line rows not re-run: no new tip.
- **2026-09-25 — ojs `40bf69fd13..a3dc3b54ff` (4), omp `7f38b0441..f77b23709` (2), ops `9db7481e43..225e25475e` (2), pkp-lib `0714131c41..479b38a09c` (2, all three apps), ui-library unchanged, ojs `plugins/generic/jatsTemplate` `14f51fa667..26c6ca0aa2` (1). `main` first: synced and green today (a test fix only, no spec accommodated). Listings in `.reports/sync-3_5/s25/range-*.txt`. pkp-lib `stable-3_5_0` holds two more commits past the pointers (`7fb5cc6e63` + merge `69d8beabc3`, #10813 stats chart labels, the backport of `main`'s `5ca591d18d`, itself in no `main` pointer yet), read when a pointer moves.**
  - ojs `eb6fbae438` + merge `4a76983529` (pkp/ojs#5827, issue pkp/pkp-lib#12414: jatsTemplate `26c6ca0aa2`, `stable-only` as a commit; it ports `main`'s `JatsHelper` so the titles, subtitles and translated ones carry their formatting as JATS). Regression read rr16 (`.reports/sync-3_5/rr16/suspicions.md`): 2 suspicions, 2 reproduced, 1 unverified hunch, nothing routed; both held on a freshly reset line OJS (`.reports/sync-3_5/s25-5827-fresh/`) and driven on `main` (`.reports/sync/s25-5827-main/`). **S2, stable-only regression**: a title stored with a bare "<" ("Effects at p<0.05 in small trials", through the REST API) comes out as `<article-title>Effects at p</article-title>`; `main` keeps it whole (its helper runs `PKPString::stripUnsafeHtml()` first, the backport does not): report `docs/reports/2026-09-25-ojs-5827-stable-3_5_0.md`, ci-triage row, kept `checks/sync/ojs-5827/titles.js`, thread + DMs to @beaug and @jarda.kotesovec. **S1, `main`'s pkp/ojs#5813 mechanism**: tag names written as text in a title become markup; on `main` since jatsTemplate `be30f06` (July), on 3.5 new with this port (3.5's JATS has no abstract): carried in the #5813 row and report as one. Also seen: `trans-subtitle` moves inside `trans-title-group` (a fix), a raw "&" and `&nbsp;` in titles now survive (a fix). The kept `checks/sync/ojs-5813/default-jats.js` runs on the line now ("rich" left unpublished there).
  - pkp-lib `c531825cb4` + `479b38a09c` with ojs `c204b13a72`, omp `6f6f60041a`, ops `e6234d6ac1` (issue pkp/pkp-lib#13370, `stable-only`: Settings › Website › Appearance › Editorial Masthead gains "enable the enrollment-based masthead" and "show last year's reviewers"; off, the masthead page shows the Editorial History text and `editorialHistory` redirects to it). Regression read rr17 on OMP and OPS (`.reports/sync-3_5/rr17/suspicions.md`): 5 suspicions, 1 reproduced, 4 not, 3 unverified hunches, nothing routed → no regression. Reproduced as intended: a context without the setting row reads it off (API `null`, box unticked, the public masthead only its heading), the issue's "unchecked on upgrade"; the note for the team is that a press or journal created on 3.5.0-0…5 that used the enrollment masthead and left Editorial History empty shows an empty masthead page after this update until a manager ticks the box. Hunches: the `AboutContextHandler::editorialMasthead` hook no longer fires when off and `$reviewers` is now a plain collection (no bundled listener).
  - ojs `a3dc3b54ff`, omp `f77b23709b`, ops `225e25475e` (pointer bumps) → nothing of their own.
  - Harness for the line, each a `main` no-op: `CategorySeeder::add()` (today's U16 seeder called `main`'s `Repo::category()->validate()` signature, the line's bootstrap answered 500), `PKPContextScenarioBuilder::applyPlugin()` (`AuditLog` guarded by `class_exists`), `PKPSubmissionScenarioBuilder` (a completed review's `hasCustomizableReviewerRecommendation()` guarded by `method_exists`; a completed-review seed on the line answers 200 after it, `.reports/sync-3_5/s25-guard/`).
  - Carried over from `main`: no new `main` regression today. Open stable-line rows: pkp-lib#13181 not re-run (the new tip carries no change to the email-change path); the new #5827 row held today.
  - Baselines advanced to the tips above.
- **2026-09-24 (daily session, VM) — pulled; no range, baselines stay.** Tips unchanged since the morning's read: ojs `40bf69fd13`, omp `7f38b0441`, ops `9db7481e43`, lib/pkp `0714131c41` on all three (pkp-lib `stable-3_5_0`'s tip), ui-library `1a7a4750`. Nothing carried over from `main` (no `main` range today).
- **2026-09-24 — ojs `769450f2d4..40bf69fd13` (2), omp `318711b05..7f38b0441` (1), ops `cb9eec1add..9db7481e43` (4), pkp-lib `6acb1be2eb..0714131c41` (26, all three apps), ui-library unchanged.**
  - pkp-lib `fc38d0622c` (#13358) → `~main bf020ba77e` (read 2026-09-22): `git range-diff` shows one difference, 3.5's `Query::find()` where `main` has `EditorialTask::find()` in `SubmissionFilesCategoryGridDataProvider::loadData()`, the same `continue` shape; the class 3.5 names is the one its queries live in → no regression.
  - pkp-lib Weblate commits (Serbian Latin, French Canada; 24, stable-only) and the merge `0714131c41` → locale files only, no English, no code (grepped `--stat` outside `locale/`: the one non-locale file is the #13358 provider above).
  - ojs `f1e77a40e7` (`=main 7ae1d297e9`, the translations merge) and `40bf69fd13`, omp `7f38b04419`, ops `9db7481e43` (pointer bumps); ops `1e8205c5de`, `84819acb8f` (`~main 02021e81cc`, Weblate Serbian) and the merge `934933ae0f` → locale only.
  - Carried over from `main`: today's #13369 OMP reach is not on the line (`git log --grep 13291` empty). Open stable-line rows (pkp-lib#13181) not re-run: the new tip carries no change to the email-change path.
- **2026-09-23 — pulled; no range, baselines stay.** Tips unchanged since 2026-09-21: ojs `769450f2d4`, omp `318711b05`, ops `cb9eec1add`, lib/pkp `6acb1be2eb` on all three, ui-library `1a7a4750`; pkp-lib `stable-3_5_0` still one commit past the pointers at `fc38d0622c` (#13358, read on `main` 2026-09-22), in no pointer yet. Carried over from `main`: today's finding pkp-lib#13291 (the review API's competing-interests gap) is not on the line (`git log --grep 13291` empty). Open rows not re-run: no new tip.

- **2026-09-22 — pulled; no range, baselines stay.** Tips unchanged since 2026-09-21: ojs `769450f2d4`, omp `318711b05`, ops `cb9eec1add`, lib/pkp `6acb1be2eb` on all three, ui-library `1a7a4750`; `node bin/line-range.js --line stable-3_5_0 --repo pkp-lib 6acb1be2eb` answers 0 commits. pkp-lib `stable-3_5_0` is one commit past the pointers at `fc38d0622c` (`=main bf020ba77e`, #13358: the files category grid's loop, read on `main` today in `upstream-sync.md`), in no app pointer yet; read when a pointer moves. The open rows (pkp-lib#13181 "3.5 shows it too") were not re-run: no new tip to run them at.
- **2026-09-21 — the whole range read, baselines advanced.** ojs `fa19f69e6f..769450f2d4` (114), omp `77fc278999..318711b05` (30), ops `50ddd4d7b7..cb9eec1add` (9), pkp-lib `6296625771..6acb1be2eb` (140, ojs) / `78d439c8b8..6acb1be2eb` (143, omp) / `5840f7d737..6acb1be2eb` (156, ops; the three pointers have converged), ui-library `1a7a4750` unchanged. `main` first: today's `main` sync accommodated one register move (U40 A10 retired) and a harness fix only, so the line's range was read (MAINTENANCE "The stable line" step 6). Listings kept in `.reports/sync-3_5/s21/range-*.txt`.
  - Translations: 130 pkp-lib, ~100 ojs, 27 omp and 5 ops "Translated/Added translation using Weblate" commits (`stable-only` or `~main` by the tool, all locale files) plus the 2026-09-18 `translations/stable-3_5_0` merges (pkp-lib `6acb1be2eb`, ojs `c14206add7`, omp `15e98f9b89`, ops `20ae604fb5`; the same merges reached `main` the same day, reviewed there today) and the pointer bumps (ojs `63c7e555cc`, `d7f6226b59`, `769450f2d4`; omp `7c20fb4a1a`, `318711b053`; ops `129a31a48d`, `826f23f5af`) → no regression: locale-only. The one English change, ojs `152b230989` (`locale/en/manager.po`: `mailable.subscriptionExpired.description` emptied), matches `main`, where the string has been empty since `0f31c4c3ed`; the incidentals row on raw `##…##` keys (U08) already covers the shape. pkp-lib `locale/en/emails.po` gains two `#, fuzzy` marks on the ORCID mailable names: the loader keeps them (checked on `main`, below).
  - pkp-lib `36a9b59083` `~main 74a8d58571` (#12347, PR pkp/pkp-lib#12350 merged `b0b75d35ce` 2026-09-18): the backport differs from `main` where it counts. `main` logs the file edit under the NEW file's id (`assocId => $newSubmissionFile->getId()`, the cause of the open regression pkp-lib#13286/#12352); 3.5 keeps `$submissionFile->getId()` and only adds the submission-level log entry that was created but never saved. The kept `checks/sync/pkp-lib-12352/cancel-restore.js` on the line's fresh OJS fleet (`.reports/sync-3_5/s21-12352-ojs/`): "Cancel" restores fileId 1 `Renamed by B.pdf`, `cancel-file-upload` answers `status:true` → 3.5 does NOT show the `main` regression (ci-triage row updated; supersedes the 2026-09-18 "does not carry the introducing commit" note).
  - pkp-lib `a607062f75` `~main 14473fe784` (#12163): `git range-diff` (`.reports/sync-3_5/s21/rd/12163.txt`) shows imports, a `PKPSubmission` constant and whitespace, the same query; the one 3.5 caller of the new scope is in the diff (`api/v1/vocabs/PKPVocabController.php:124`), no other `withContextId` on `ControlledVocab` in the three line checkouts or their plugins → the `main` read of 2026-09-18 (rr12, nothing reproduced) carries over. A drive with the kept `term-suggestions.js` on the line stopped at the `published` seed key: 3.5's publish rules refuse a Submission-stage item ("must be in the Copyediting or Production stages", "must be assigned to an issue"; `.reports/sync-3_5/s21-12163.log`), a `main`-only seed shape, not bent further. The `context` seed's `Repo::reviewerRecommendation()` call (`ContextFactory.php`) is now behind `method_exists` (a no-op on `main`), so context seeding works on the line.
  - pkp-lib `2ddc9eb57e` + `35905e27bd` + `45530b23aa` `~main 462d628de3,2b13599365,c967e34ba4` (#12702): the backport touches `api/v1/jats/PKPJatsController.php` alone (3.5 has no Body Text or Media controller); the same read/write route split and policy drop as `main` (rr11, 2026-09-18, nothing reproduced); 3.5 callers: the JATS tab's own client only → carries over. The kept `jats-upload.js` still needs its own Publication › JATS XML navigation on the line (2026-09-18 note), not written today.
  - pkp-lib `f53e83b153` + `ee997759d2` `~main 0dce988b35,c767c313b9` and `d477f69239` `=main c767c313b9` (#13299): the rule class and the route narrowing as `main` (rr9, 2026-09-17); the range-diff's one difference (`===` in the first backport commit) is undone by the second, both lines end on `==` → carries over.
  - pkp-lib `ac6b031624` + `78d439c8b8` `~main 4d9ec3cbd0,aeac6f75cf` (#13181): carried over 2026-09-18 ("3.5 shows it too"); re-run today on the line's OJS fleet (`.reports/sync-3_5/s21-13181-ojs/`): unchanged (both landings `index/en/user/profile#contact`, the old-shape decline link ending in GET 500, request pending); row dated.
  - pkp-lib `527862a149` `~main 5853ad1021,…,2c0ccff9ff` (#12074): a one-line backport of `fa7b4cc3bc` alone: the journal-level ORCID settings form shows a masked description (`*************************`) instead of the site's client secret; the other five `main` commits (Laravel encryption of stored credentials) are not backported → intended (the issue's title), no 3.5 caller beyond the form; verdict in this line.
  - pkp-lib `d3079f201e` `=main 2ecbd331ee` (#13313), `9ce915e07e` `=main 1bcd4dd55f` (#13348), `d7ab1e91a4` `=main b50be82a3d` (#13335), `b50ae1d0d7` `=main f80f5b9483` (#13343), `6296625771` `=main efbba94ae7` (#13344): the `main` verdicts (2026-09-17 and 2026-09-18) carry over; the changed symbols' 3.5 callers are the same files as on `main` (grepped). Merges `88d52314d4` (#13276), `b0b75d35ce` (#12350) and `8a4bd6dfa9` (#13041) carry nothing of their own.
  - pkp-lib `09534070b5`, `1a3da1b587`, `a2df3bc3e7`, `82845b035d`, `a9f9afe41b`, `0ff9e3c568` `stable-only` (#13041, PR pkp/pkp-lib#13043: the web-based task runner tracks each task's last run in a `site_settings` row, `tools/scheduler.php list` renders its own table with the next run, the queue task is skipped when the queue runs elsewhere, the scheduler's time zone follows the config). Regression read rr13 (`.reports/sync-3_5/rr13/suspicions.md`; OJS): 3 suspicions written, 0 reproduced, 6 unverified hunches, nothing routed. S2 (the runner rewriting the `site` row at every request's shutdown) driven through the same `Scheduler::runWebBasedScheduleTaskRunner()` from a CLI boot: the `site` row byte-identical, one new `site_settings` row holding the 13 infrequent tasks' last runs, a second tick writes nothing; S3 (`scheduler.php list/run/test`) driven on the line and `list/run` on `main`: the table renders, `run` skips the queue task by design, `test --name` works, the old `--timezone`/`--next` options are silently ignored. **S1 not reproduced, blocked**: by the code order the web runner's `Schedule` is built in a provider's `booted` callback (`ScheduleServiceProvider.php`) before `PKPApplication::initializeTimeZone()` applies the config's `time_zone`, so it would take php.ini's `date.timezone` where the CLI takes the config's; the fleet has `task_runner = Off` and both zones UTC, so a throwaway server with a differing php.ini zone and the runner on is the reproduction, not run today. An unverified code read, not a finding; for a later session or the team.
  - ops `cb9eec1add` `~main 53b40233f0` (#11759): the same migration as `main` (`I11759_RemoveDuplicateFilters`, reviewed today) registered in 3.5's `upgrade.xml` under the 3.5.0 block; carries over.
  - Baselines advanced to the tips above. Not carried by any pointer yet: pkp-lib `fc38d0622c` (#13358), next read when a pointer moves.
- **2026-09-18 — pulled, listed, `main`'s findings carried over; the range NOT read, baselines stay.** `main` came first: today's `main` sync accommodated three specs (U06, U40, U03), so the line's own range waits for the next session (MAINTENANCE "The stable line" step 6).
  - Tips: ojs `63c7e555cc`, omp `7c20fb4a1`, ops `129a31a48d` (one pointer bump each), lib/pkp `9ce915e07e` on all three (the pointers have converged), ui-library `1a7a4750` unchanged; pkp-lib `stable-3_5_0` at `a607062f75` (#12163, in no app pointer yet).
  - The listing (`bin/line-range.js`), for the next read: pkp-lib from `6296625771` (ojs) 5 commits: `d3079f201e` `=main 2ecbd331ee` (#13313; `main` read 2026-09-18, U06 A9's gap not driven on the line), `2ddc9eb57e` + `35905e27bd` + `45530b23aa` `~main 462d628de3,2b13599365,c967e34ba4` (#12702: the backport touches `api/v1/jats/PKPJatsController.php` alone, 3.5 has no Body Text or Media controller; same route split and policy drop as `main`'s JATS part by a read of the diff, range-diff not run), `9ce915e07e` `=main 1bcd4dd55f` (#13348). From `78d439c8b8` (omp) the same plus `d7ab1e91a4` `=main b50be82a3d`, `b50ae1d0d7` `=main f80f5b9483`, `6296625771` `=main efbba94ae7`. From `5840f7d737` (ops) 21: those plus merge `88d52314d4` (#13276), six `stable-only` commits of #13041 (the web task runner's database last-run tracker, `09534070b5`..`0ff9e3c568`, merge `8a4bd6dfa9`), `f53e83b153` + `ee997759d2` `~main` and `d477f69239` `=main` (#13299), `ac6b031624` + `78d439c8b8` `~main` (#13181).
  - Carried over from `main`: **pkp-lib#13181, 3.5 shows it too** at ojs `63c7e555cc` / lib/pkp `9ce915e07e` (the kept `checks/sync/pkp-lib-13181/emailchange-links.js` on the line's fresh OJS fleet, `.reports/sync-3_5/s18-13181-ojs/`: both landings `index/en/user/profile#contact`, the old-shape decline link ending in GET 500 with the request pending); ci-triage row and `docs/reports/2026-09-17-pkp-lib-13181.md` updated, one report. pkp-lib#12352: 3.5 does not carry the introducing commit (`git log --grep '#12352'` empty). Today's private-file observations: their standing on the line is in the maintainers' DMs, by code read only.
  - For the #12702 read: `checks/sync/pkp-lib-12702/jats-upload.js` does not run on the line as it is (`.reports/sync-3_5/s18-12702-ojs/`: the shared `WorkflowPage.selectPage()` waits for `main`'s version node in the Publication menu, which 3.5's menu does not have); the seed calls worked, so the drive needs its own navigation to Publication › JATS XML.
- **2026-09-17 (line set up) — no range read.** The baselines are the
  tips of the day the line was set up (3.5.0.5 on the three apps); the
  first read starts from them. omp and ops sit behind ojs on pkp-lib
  (`78d439c8b8` and `5840f7d737` against `6296625771`), so the first read
  already has their pointer catch-up to list.
