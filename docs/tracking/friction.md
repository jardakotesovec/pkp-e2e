# Friction — what made an agent's task harder than it needed to be

What cost a screen-driving agent calls, time or retries that a better
brief, doc, kit, seed or fixture would have saved; that agent appends a row
at the end of its task. The maintenance session folds the rows and deletes
them under MAINTENANCE "The daily session".

One line per entry, appended at the end, in this shape:

`YYYY-MM-DD · U<nn> or sync · <role and agent id> · <what cost you calls, time or retries> · <what would have helped>`

Facts only, the same quarantine as everywhere else (nothing
security-shaped, no credentials).

## Entries
2026-09-24 · sync · claim checker fr0924 · About five calls lost: the U11 kept checks (K1–K3) re-read the French strings only inside long phase chains (seed, one, several, then lang), so re-running "the ones that cover your lines" meant a new focused script instead; and a whole-page raw-key grep of a Website settings page listed dozens of keys from hidden tab panels and inline script JSON, needing a second run with visibility and location · kept check scripts carry a small read-only "strings" phase per language that reuses a seeded state, and the probe kit offers one rawKeys(page) helper that reports visible "##key##" codes with their landmark (the one in shared/playwright/checks/U08/K1/k1-fr0924.js)
2026-09-24 · U42 · harness agent hU42 · One smoke rerun: a `citationsRaw` list seeded with an empty line ("" for a blank line) came to the builder as null, because the `_test` POST body goes through the same ConvertEmptyStringsToNull middleware as the screens, inside lists too; scenarios.md says so only under `copySubmissionAckAddress` · one line in scenarios.md "How the endpoints work": every empty string in a request body, nested ones included, reaches the builder as null
2026-09-24 · U42 · claim checker ccK1 · About four runs lost: the q9 set-up ("end the Site Administrator's own manager role… and sign in again") cannot be done on screen, since "Remove Role" on a user's last role answers "You cannot remove the role. At least one role must be assigned to the user." and sends nothing; the working route (enrol `admin` with a second role through the context `users[]`, then end Journal manager) was only in U38 K3's script; and I lost one run to the References search, which filters only on Enter even though patterns.md says so, because the spec line said "as you type" · scenarios.md "POST scenarios/context" could say that `users[]` may name `admin` to give the administrator a second role, and that a user's last role in a context cannot be ended on screen; a spec author's set-up line in a to-drive footnote could be checked against users.md "Users & Roles" first
2026-09-24 · U42 · claim checker ccK3 · About six runs lost: the A9 drive first came back "refused" only because a seeded draft has no main file ("You must upload at least one Article Text file."), then OPS has no `article.pdf` fixture (`preprint.pdf`, as a galley); and table reads straight after a side-panel save or delete showed the old rows (the table refreshes about a second after the panel closes), which read as "not updated in place" until a bounded wait on the row was added · scenarios.md `submitted: false` says a draft has no file and a draft that must submit needs `files[]` (OJS/OMP) or `galleys[]` with `preprint.pdf` (OPS); patterns.md pitfall 4 adds that a Vue list behind a side panel refreshes after the close, so wait on the row
2026-09-24 · U42 · claim checker ccK2 · Two OJS reruns lost: closing a row's "More Actions" menu with Escape closed the whole workflow dialog (every later step then timed out on the dashboard list), and an Author Information row count included the table's text-only empty row, so the first "Add" was read as the second row; patterns.md pitfall 7 names Escape only for inner windows · pitfall 3 or 7 adds that Escape on an open headlessui menu inside the workflow closes the workflow too (close a menu by its own button), and pitfall 4 notes that a PkpTable's empty state is a tbody row (count rows :has(input))
2026-09-24 · U42 · claim checker ccK4 · About four reruns lost to two copied helpers: the wizard `wizardTo` (from K3) presses "Continue" when the target step is not yet reached and the rail otherwise, and "Continue" saves the step at once while a rail move saves nothing until the autosave a minute later, so the same drive meant two different things (it also turned K3's "rail" leave drive into a Continue); and the References row-menu reader (from K1/K2) closed the menu with Escape, which now and then closed the whole workflow dialog and hung the next box fill for 30 s · a SubmissionWizardPage note (or kit helper) that names both moves and says which one saves, and a patterns.md pitfall 3 line that a headlessui menu is closed by pressing its own button again, never Escape inside the workflow dialog
2026-09-24 · U42 · test author tojs · One 7-minute hung run and two extra runs: scenario 4's "Require…" leg said "no warning" on Review and "Submit › Submit refused", both read by the claim check from a snapshot taken while the Review step still showed "Checking your submission"; once the check answers the warning is there and "Submit" is disabled, so the test's Submit press waited out the test timeout · the probe kit's screen() (or claim-check brief) waits for the wizard Review step's `…/submit` check to answer before recording, as SubmissionWizardPage.continueToReview does; and a test author runs a new wizard test with a short `--timeout` first
2026-09-24 · U09 · harness agent hU09 · Two parity reruns lost on the legacy Plugins grid: `check()` on a row's "Enabled" box aborted the drive and the pending waitForResponse then crashed the process (click() inside Promise.all with the response worked), and an enabled plugin's row changes its accessible name to "Settings <name> <description>" (a "Settings" link is prepended to the name cell), so a `/^<name> /` row locator found nothing after the tick · patterns.md could say: tick a Plugins grid box with click() inside Promise.all with the `settings-plugin-grid/enable` response, and match its row by name and description, never by a leading name
2026-09-24 · U09 · claim checker ccK1 · About eight runs lost in the legacy "Custom Page" item and block windows: Escape to close a TinyMCE menu ("Insert Tag", the Paragraph choice) closed the whole side window through the browser's "form has changed" confirm and every later step timed out; with one form language the Content textarea id carries no locale (`content-<uniq>`, `content-en-<uniq>` only with two), so an id-prefix lookup found nothing; the second language's popover (open while a box has focus) covered "Save"; a click into the editor after a tag selected the tag and the next key replaced it; and the sidebar Language block's links land on the site home on the fleets (source without the port) · patterns.md pitfall 4 or "UI realities": close a TinyMCE menu by clicking into the editor, never Escape; find a legacy rich box by `textarea[name="content[en]"]`; blur (click another box) before a window's buttons when two form languages are on; after "Insert Tag" keep typing (End) without clicking; seed-facts names the Language block limit
2026-09-24 · U09 · claim checker ccK3 · About four OJS reruns (4–6 min each) lost in the legacy block window and the public reads: a multilingual box's popover of the other languages stays open over the window's "Save" and swallows the click until the window heading is clicked, and a click on the page behind the window closes it through the "form has changed" confirm; a browser that once opened a `/fr_CA/` address keeps French for every later address without a segment (the visitor's sidebar reads and the manager's Plugins row text), so reads meant for English came back French. Also: "Delete" on a custom block 500s on the PostgreSQL fleets, so the site-level block Rule 27 needs cannot be removed afterwards (left unplaced, plugin unticked) · patterns.md "Locator pitfalls" (a legacy multilingual popover over the window foot; name the locale segment in every address after a French read) and a brief line for site-level mutations that the screens cannot undo
2026-09-24 · U09 · claim checker ccK2 · About two reruns lost: the legacy window's close button is named "Fermer" in the French interface, so a French-interface preview drive died on a `/^Close/` locator, and a toast watcher on a clean save kept catching earlier refusal messages that the app re-shows at the top right (K2-7), which first read as that save's own message; also the K1 helpers I needed (readPublic, mceType, preview, addCustom) live unexported inside k1.js and had to be copied · the U09 lib.js exports the public-page reader, the TinyMCE typing and the preview helpers, and patterns.md pitfall 4 says a legacy window's close button is named by the interface language ("Close" / "Fermer")
2026-09-24 · U09 · claim checker ccK5 · Rule 29 (pictures), 29a, 29b, A5 and two Side-effects lines could not be driven at all: every picture upload on the three fleets answers 500 "The public files directory was not found…" because `make-test-config.js` writes an absolute `public_files_dir` and `PKPUploadPublicFileController` prefixes the app root (the same cause as the seed-facts profile-image 404); found only after the first upload run. Also one OMP rerun lost to a psql `journals` lookup (the table is per app; `createContext` already answers `contextId`) · the harness writes `public_files_dir = public` (relative, the config template's default) or a spec author's to-drive picture footnote is checked against one upload first; seed-facts names the upload failure until then
2026-09-24 · U09 · harness hU09b · One 7-minute run lost and four orphaned `php -S` servers to clean up: the brief's regression command (`npx playwright test -c configs/<app>.config.js <suite> …`) with suite `U12` also matched `tests/serial/U12-announcements.spec.js`, whose `<app>-serial` project depends on the whole app project, so 331 tests started and `timeout` killed the runner but not its servers · RUNBOOK step 4's regression command names `--project=<app>` (and a serial suite separately with `--project=<app>-serial --no-deps`), or harness.md "Running" says a bare suite id also pulls the serial file
2026-09-24 · U09 · claim checker ccK4 · One 10-minute Bash timeout lost waiting on parallel per-app probe runs with a `while pgrep -f k4.js` loop that matched its own shell (the pitfall patterns.md names for detached runs), and a Plugins-grid category read by DOM siblings that returned the grid's empty-row text ("No Items") instead of "Generic Plugins"; also ccK5's upload blocker (500 on every picture) had been lifted in the working tree by the time this chunk ran, which no brief or screen note said · patterns.md "Probe kit": run parallel per-app probes as `node … & … & wait` in one foreground command; a kit helper `pluginRow(page, id)` returning the row with its category from `screen().text.main`; the orchestrator adds a screen-notes line when a harness blocker another checker reported is fixed mid-session
2026-09-24 · U09 · claim checker ccK5b · One 4-minute OJS upload run redone: K5's kept script chained twelve uploads into one box, and TinyMCE's "Insert/edit image" with the last inserted picture still selected replaces that picture instead of adding one, so the saved block showed only the last picture and the public-side read of each accepted type was lost; the fix (caret to the end before each upload) is now in `checks/U09/K5/k5.js` and in screen-notes. Also the brief asked both to "extend K5's kept script" and to write "one entry script" under `K5b/`: resolved as a thin `K5b/k5b.js` that sets the tag prefix and phases and requires `../K5/k5.js`; a brief for a re-drive chunk could name that shape.
2026-09-24 · U09 · security verification sec1 · Two extra OJS runs: the private entry's steps, written from reading before the claim checks, named a drive that the K3 claim check had already shown cannot complete, so the first run placed nothing and the second only printed the 400 body; a verification brief that says "re-read the steps against the claim-check findings (cc-K*.md) of the same screen before driving" would have saved both.
2026-09-24 · U09 · security verification sec2 · One OJS rerun: the Manage Custom Blocks grid's "Block Name" column shows each block's derived lowercased kebab name, not the typed title, so a case-sensitive read of the title came back empty and read as a failure until the second run; sec1's screen note said the derived name is the Sidebar value but not that the grid column shows it · a screen note (now added) that the block grid lists the derived lowercased name, so a block is confirmed case-insensitively against it
2026-09-24 · U09 · test author tojs · Two reruns: a legacy grid's "No … have been created." line is `tbody.empty` (hidden, not removed, once a row exists), which I first read as `tr.empty` (patterns.md pitfall 10 names the rows and controls but not the empty line), and the Custom Block Manager window's `label[for="showName"]` is the section title "Show Name" while the box's own words sit in the label wrapping the input · pitfall 10 gains "the empty line is `tbody.empty`", and the ccK3 screen note on the block window names both labels
2026-09-24 · U09 · test author tops · One probe run to find the Plugins grid's category heading for S8's "under Generic Plugins" control: the ccK4 screen note said the heading is not reachable from the row's DOM and pointed to `screen().text.main`, which a test cannot use; it is the first row of the row's own `tbody.category_grid_body` · the claim checker writes the locator that does reach a fact a scenario asserts, not only the one that failed (now in screen-notes as tops, and `PluginsTab.categoryHeading()`)
2026-09-24 · U44 · claim checker ccK2 · One OMP rerun lost: the legacy URN settings window keeps its stored "pattern" suffix choice between drives, so the OMP1 "Chapters only" save was refused in the browser for the empty chapter pattern instead of reaching the server check; and each run record counted the known plugin-gallery-grid 500 as a new crash on every Plugins landing (23–30 per run), burying the real script error · patterns.md "Probe kit" notes that a legacy plugin settings window starts from its stored state (reset every choice the drive depends on), and the kit tags crashes that match a known incidentals.md row (plugin-gallery-grid) as known in its end-of-run count
2026-09-24 · U44 · claim checker ccK1 · About six calls lost: the Rule 19 context seeded through `plugins.urnpubidplugin.settings` without `urnCheckNo` rendered an empty article "Identifiers" page (the form GET answers `fields: []`), which read as an app bug until a re-save of the settings window and a with/without seed pair (urn-seed-checkno.js) showed the seed was the cause; and OMP "Select Files" offers only production-ready files, so a format file needed the upload wizard ("Change File") · scenarios.md `plugins` could say that `urnpubidplugin` needs every key its settings window writes (at least `urnCheckNo`), or the builder could default the window's keys; a `publicationFormats[]` submission key {OMP} (format name plus a proof file) would spare every press check the wizard
2026-09-24 · U44 · claim checker ccK3 · Four OJS reruns lost: no seed key puts an unpublished article in an issue (the submission `issue` key applies only with `published: true`), so every default-pattern URN drive re-does Publication Settings on screen, and ccK2's note named "Assign To Current/Back Issue", which a journal with only future issues does not offer; the tab's "Clear" confirms with "OK" where the spec said "Delete"; and a version saved at stage PMUR can never be scheduled again (the publish button opens the refused window at once), so the scheduling drive needed a fresh article · the submission scenario's `issue` key also taking effect for an unpublished submission (the Publication Settings save it mirrors), and one patterns.md line that legacy confirmation windows ("Delete" title) answer with OK / Cancel
2026-09-24 · U44 · claim checker ccK4 · About eight calls and one OMP rerun lost: every reader-page, "Nothing else" and Clear line needed a stored URN, and no scenario key stores a pub-id (and a `published: true` seed's Identifiers page is read-only), so the drive seeds unpublished items, assigns the issue, the URN and the Publisher IDs on screen and publishes on screen per app; the OMP book page then showed no format block until the format was also made "Available", which neither the spec nor seed-facts says; and a copied Dublin Core toggle hung on OPS, which ships no such plugin · a `pubIds` map on the submission scenario (publication, galleys, issue: `{urn, publisherId}`) and on `issues[]`, stored as the tabs store them, plus an OMP `publicationFormats[]` key with `approved`/`available`; seed-facts lines for the OPS plugin set and the OMP "Available" gate
2026-09-24 · U44 · test author tojs · Two runs and three by-hand probes lost: U49's `openPublishPanelExpectingIssueFields` arms its wait on the `issueAssignmentStatus` GET after landing, but the workflow already fetches it at landing, so the wait timed out (60 s) on an item that sat on the page a while; the second "Schedule For Publication" press opens the confirmation window straight away (the version stage is saved by the first "Confirm"), which `openPublishPanel` does not expect and then hangs its retry click until the test timeout; and every scenario page is an `asUser` page, so the run's error-context.md is the unused fixture page's and each failure needed a probe · PublishSchedulePages gains one opener that accepts either the panel or the confirmation window (the one in the U44 OJS suite, `openConfirmation`), bounded by what is on screen rather than a request; and the fixtures offer the first `asUser` page as the error-context page (or attach its aria snapshot on failure)
2026-09-24 · U44 · test author tomp · One OMP rerun: the mailbox control copied from U42 OMP (a discussion the Press Manager opens with a spare) also mails the Press Manager, so the suite's "no email to the manager" read found the control's own copy; U42 reads the author only, and nothing says the opener is mailed · scenarios.md "Mailpit" (or the U42/U43 control helper's comment) says a discussion mails every ticked participant and its opener, so a silence read on the opener counts that one copy
2026-09-24 · sync · regression reader rr13263 (PR review pkp-lib#13346 + ui-library#987) · Two French reruns: `user/setLocale/fr_CA?source=…` lands on `/index.php/index/fr_CA` and ignores the source, and every reviewer-row helper keys on English names (`panelRow('Reviewers')`, the "More Actions" button, menu items), so a translated-UI probe rebuilt its own row/menu locators; also the brief's "reset first" and "hold on a fresh reset" had to be overridden by the session because a suite shared the database · a probe-kit `switchLocale(page, contextPath, locale)` plus locale-free row/menu locators in ReviewStagePages (`button[aria-haspopup]`), and an rr brief variant that names a fresh scratch context as the reset-equivalent when the fleet is in use
2026-09-24 · U46 · claim checker ccK1 · About six calls lost: the Search line needed the fleet's queued jobs run (the title control found nothing until then) and the kit has no runJobs for probe scripts, so the checker wrote its own php call; the "no email" line needed a positive control because seed-time submission emails never reach Mailpit; and the galley list's order after an edit was not the seed order (all positions 0), which cost a rerun to explain · the probe kit exports runJobs(app) (the support/jobs.js drain with PKP_CONFIG_FILE set from the bag); scenarios.md "Mailpit" says seeded submission emails are logged but not delivered, and names an on-screen publish as the cheap positive control
2026-09-24 · U46 · claim checker ccK2 · Two reruns and about eight calls lost. First: the HTML galley's Edit form nests the Dependent Files grid, whose search box has its own submit button "Search", so a `button[type=submit]` Save locator hung the path phase on both apps. Second: the spec's and scenarios.md's premises on submission languages sent the drive to the wrong grid (Website Languages has no "Submissions" column; the "Submission Languages" list is the second grid on that tab and a new context lists its primary language alone) and to the wrong preselected language (the window preselects the primary language, not the submission's) · patterns.md pitfall: a legacy form can nest a grid with its own submit button, so press a form's Save by exact role name; seed-facts install-default line for the "Submission Languages" list and its "Add/Remove Languages"; scenarios.md `galleys[]` corrected on the preselected language
2026-09-24 · U46 · claim checker ccK3 · About six calls and three reruns lost re-deriving galley-page helpers the sibling chunks had already debugged: each chunk copied ~300 lines of openGalleys/rowMenu/wizUpload/served from K1 or K2 into its own script, and the copy reintroduced a ^-anchored row regex (the cell text starts with whitespace, a pitfall ccK1 had noted) and an Author sent to the editorial address; a phase that failed mid-way also lost its facts because they were recorded only at the end · a shared `shared/playwright/checks/<feature>/lib.js` written by the first chunk (the brief could name it for later chunks), and record each step's facts as it lands (`record(..., {merge: true})`) rather than at phase end
2026-09-24 · U46 · test author tojs · Two greps lost: the brief pointed to "U49's openers in shared/playwright/pages/WorkflowPage.js", but WorkflowPage.js carries only `expectVersionLoaded()` and the link; the opener and its confirm are the OJS tree's `PublishScreen.openCreateVersionDialog()` / `confirmVersionDialog()` (apps/ojs/playwright/pages/PublishSchedulePages.js) · the test-author brief template names the per-app PublishSchedulePages openers for "Create New Version", with WorkflowPage.expectVersionLoaded() as what they wait on
2026-09-24 · U46 · test author tops · One rerun: S8 starts a submission on screen (the start form, not a seeded draft, since a draft that lists a galley breaks the wizard) and the Review step refused it for an empty abstract, which the start form never asks for while the default section requires one; U21's `completeAndSubmitDraft` assumes a seeded draft with its default abstract, and nothing says so · SubmissionWizardPages' `completeAndSubmitDraft` comment (or scenarios.md's `sections[]` line) says a submission begun on the start form has no abstract, so its section is seeded with `abstractsNotRequired: true` or the Details step's abstract is typed
2026-09-24 · U10 · harness agent hU10 · The key shares its code with the bootstrap, whose seed only runs on a cold database, and a harness agent may not reset a shared fleet; I checked the bootstrap side by posting the fixture's `categories` list to `scenarios/context` and diffing the rows against `publicknowledge` · harness brief step 4 names the check for a key that shares code with the bootstrap (post the fixture's list to a scratch context and diff it against the seeded rows), or a fleet-safe cold-bootstrap dry run under a spare context path
2026-09-24 · U10 · claim checker ccK1 · Two extra runs: the Navigation tab's "Add Menu" area select renders "None" alone and gains the theme's areas a moment after `idle()`, which first read as a missing-areas finding; and the spec's td8 step "press the side menu's Website" assumed a page load, but that entry does nothing while Website is already open, so the leave had to be re-driven with a reload · patterns.md "Locator pitfalls" notes that a legacy window's select options fill after `idle()` (read through `settled()` on the option count), and the spec author's td template uses a reload or another page for "leaving", never the menu entry of the page already open
2026-09-24 · U10 · claim checker ccK2 · Two reruns and one extra phase: the OMP suite's `HighlightsTab` takes its context in `goto(path)` and names the label `buttonLabel` where the OJS/OPS copies take it in the constructor and call it `label` (a highlight step failed on OMP); `createSubmission({published: true, issue})` into an issue not yet published silently yields a scheduled article (status 5), which scenarios.md does not say, so Rule 13's "published into an unpublished issue" end needed an on-screen "Assign To Future Issue and Publish Immediately"; and the home page's "View All Issues" link name carries a trailing space, so an `exact` locator found nothing · scenarios.md `issue` key: state the scheduled outcome (or add a `publishImmediately` flag); one HighlightsTab signature across the three suites; the locator note in patterns.md "Locator pitfalls" 16 extended to public-page links
2026-09-24 · U10 · claim checker ccK5 · One misread and a re-inspection: my access classifier (text match on "does not have access to this operation") read the OJS administrator-without-role Settings page as refused, because U14 A9's "Error" window carries the same sentence over a fully loaded page; U07 K1, U14 K1, U08 K4 and this check each re-implement the same side-menu reader and access classifier · a probe-kit `access(page)` helper returning {open | denied (user/authorizationDenied) | login | open-under-error-dialog} plus a shared `readNav(page)`, with patterns.md "Probe kit" naming them
2026-09-24 · U10 · claim checker ccK4 · About ten extra calls: the search index and a category's page stay empty for seeded or screen-published items until the job queue runs, and support/jobs.js runJobs() from a probe script drains the queue but then burns its 180 s timeout polling the worker-0 server at basePort, which a probe run does not have up; no seed key puts a published item in a category, so three items per app were unpublished, placed and republished through the workflow · a `categories` key on `POST scenarios/submission` (or a note in scenarios.md that category pages and Search read the index), and a runJobs() option that polls the probe server or skips the wait
2026-09-24 · U10 · test author tomp · One rewrite pass: the brief said the OJS author writes shared/playwright/pages/AppearancePages.js beside me, but it did not exist when I started, so the suite was first built and run green on local helpers, then rebuilt on the page object when it landed mid-task (a second full debug pass) · the orchestrator dispatches the page object's owner first and the sibling test authors once its first version is on disk, or the brief says "start from the page object as of <time>; if absent, wait for it" rather than "import it"
2026-09-24 · U10 · test author tojs · Four S1/S3 reruns: two spec claims the claim check had marked verified failed at test time because the kept scripts read them differently from a user: the style sheet's "styleSheet.css" link was read after `openSide()` reloaded the page (right after "Save" the box still shows the chosen file), and "a picture uploaded over a saved one" was driven with `setInputFiles` on the hidden file input, while the screen's "Upload File" is disabled or screen-reader-only while the box holds a picture · briefs/claim-check.md asks a checker to drive an upload through the visible control (file chooser or drop) and to read a save's effect on the same page before any reload, noting when a reading needed the reload
2026-09-24 · U47 · harness agent hU47 · Three calls lost on the reader check: an anonymous view of a freshly seeded HTML galley showed article.html with figure.png unresolved, because the HTML galley plugin caches an anonymous reader's page for 24 hours under the galley id in checkouts/<app>/cache/opcache, which `reset:<app>` does not clear, so a galley id reused after a reset served an earlier install's page; a signed-in view or a new galley id rendered it · `shared/playwright/reset.js` clears `checkouts/<app>/cache/opcache` with the files directory (until then scenarios.md `mediaFiles[]` says so, and reader-side tests view the galley signed in)
2026-09-24 · U47 · claim checker ccK2 · About 15 greps and a long read of U44 k4.js to rebuild an OMP HTML publication format on screen (Add publication format, Change File wizard, Set Terms › Open Access, format approval, Publish, "Not Available" › Available) for one reader-side line, since no seed key makes a publication format with a file; and one wasted table read because the Media Files header reads "DATE UPLOADED" to innerText (CSS upper case) · a `publicationFormats[]` key on the OMP submission scenario ({name, file, openAccess, approved, available}), or at least an "OMP format chain" helper in shared/playwright/probe; patterns.md "Locator pitfalls" noting that innerText of a Vue table header is upper-cased
2026-09-24 · U47 · claim checker ccK1 · Two reruns: seed-facts says the probe servers refuse uploads over 2 MB / 8 MB (U09) and also that 99 MiB uploads (U36), so my first "failed upload" leg used 3 MB and 9 MB files that uploaded fine on the Mac (Homebrew php.ini 100M); and the Components-grid "Edit" idiom I reused from the harness parity script (`#genresGridContainer tr` filtered for an Edit link, `.first()`) opens an earlier row once its controls stay open, so three component windows read as Multimedia · seed-facts states the upload limit as "the host php.ini's, 100M on the Mac" (one line, both readings reconciled); kept scripts take a legacy grid row's link from `row.locator("xpath=following-sibling::tr[1]")` as patterns.md pitfall 10 implies, never a grid-wide `.first()`
2026-09-25 · U47 · claim checker ccK3 · One extra seed-and-drive phase and one 45 s recheck phase: footnote q3 asked for the press bullet's review end only, while the claim names "review or copyediting" (the Copyeditor and Marketing coordinator only reach "Media" in Copyediting); and the assistant roles' "More Information" History "Loading" (U36 A3, already on record) was rediscovered by a bounded wait because nothing on this feature's page pointed to it · spec-author brief: a `to drive:` footnote names every end the claim's own words name (each stage, each role), and a Cross-feature pointer to a window another spec owns carries that spec's open ⚠ markers for the roles the pointer covers
2026-09-25 · U47 · harness agent hU47b · One parity rerun and about ten debugging calls: the second parity drive landed on press id 297, whose URN plugin settings came from an earlier install's 24-hour Laravel file cache (`checkouts/omp/cache/opcache`, key `pluginSettings-<contextId>-<plugin>`), so the screen's format approval assigned a URN no one enabled; `reset:<app>` drops the database but leaves `cache/`, so every per-context-id cache (plugin settings, the HTML galley page) leaks into the next install's scratch contexts; also the brief said to remove `publicationFormats[]` from "Field shapes not built yet", where it was never listed · `reset:<app>` (bin/reset.js) empties `checkouts/<app>/cache/opcache` (the Laravel file cache) with the database; a harness brief names the list entry only when the spec author's return found it there
2026-09-25 · U47 · test author tojs · One failed S3 run and one throwaway probe: the spec's scenario 3 reopens "Edit Metadata" on the row "figure.png" after "Cancel" › "Warning" › "Yes", but on the same page the list keeps showing the discarded typed name until a reload (T-ojs-1), which the claim check did not report · briefs/claim-check.md could ask that a "nothing saved" claim be read twice, on the same page right after the discard and after a reload
2026-09-25 · U47 · test author tomp · One red run and a finding block for S8's Funding Coordinator download: the claim check's record `q3-fu-link-omp.json` held the refusal (200 `application/json`, `{"status":false,…}`, no attachment) yet footnote q3 and scenario 8 read it as "the name downloading", because a 200 status was taken as a download · briefs/claim-check.md: a download claim is settled by the response's `Content-Disposition: attachment` (or the kit's download event), never by the status or a popup opening
2026-09-24 · U13 · harness agent hU13 · About five reruns of the parity drive: the brief's `galleys[].genre` example ("a galley on a non-default component such as … "Image"") and the draft's q7 name a component the galley upload wizard never offers (dependent components are excluded), found only by reading the wizard's code; "Enable article number metadata" sits on the Metadata sub-tab (`#metadata-button`), not on the Workflow page as it opens; the "Create Issue" form has no alt-text box (it appears only on "Issue Data" beside a stored cover); and a DB-compare script guessed `config.inc.php` where the checkouts carry `config.test.inc.php` · the spec author checks a component-dependent state against the wizard's list (components with "Dependent" ticked are never offered) before it reaches a harness brief; harness.md names `config.test.inc.php` (with `public_files_dir = public`) as the checkout's config for scripts that read files
2026-09-25 · U13 · claim checker ccK3 · One OJS rerun of the citation drive: a How-to-Cite download link starts a navigation the browser aborts, so the usual race `Promise.race([waitForEvent("download"), waitForURL(...).catch(() => null)])` resolves null on the aborted navigation before the download event and every second download read as "nothing"; and the chunk plan and q12 named a "Save" the CSL settings window does not have (its submit reads "OK") · patterns.md "Probe kit" gains a `download(page, locator)` idiom (the URL wait never settles on error; k3.js `download()`), and a spec author reads a legacy plugin window's button labels from `fbvFormButtons` (default "OK") before naming them
2026-09-25 · U13 · claim checker ccK1 · Three reseeds of the versions submission (about four runs): the OJS publish page object assumes every "Publish" opens "Review Publishing Details", but a later version goes straight to the confirmation and the POM's 5 s retry press hangs on the overlay; the Publication Settings page refuses "Save" until an issue choice is made on a journal with a published issue; and "Create New Version" is offered before the first version is published, so a failed first publish silently left two drafts · `PublicationScreen` (apps/ojs/playwright/pages/PublicationMetadataPages.js) gains a publish helper that waits for either the panel or the confirmation, and scenarios.md notes that `published: true` cannot backdate a version (no `datePublished` key), so a two-version date check is built on screen
2026-09-25 · U13 · claim checker ccK2 · One components rerun and three extra phases: a Components grid row reads "Settings <name>" (the row arrow's label leads the row text), so a start-anchored row match found nothing; q8 names `notes.md` for both apps but OPS has no such fixture (a .txt stood in, an XML galley had to be uploaded on screen from the OJS fixture); settling A2's last sentence took two more versioned articles because a new version's galley shares the published file (U46 A4) and nothing in the brief or q9 said so · a `notes.md` (and `article.xml`) fixture under apps/ops, a Components row note in patterns.md, and q9 written against U46 A4 (a copy with a file of its own needs "Add galley", not "Change File")
2026-09-25 · U13 · claim checker ccK4 · About ten calls tracing why the Publication Facts panel and both recommendation lists never showed: the app catches a plugin's error inside a hook, logs "Plugin … failed to handle the hook …" to the probe server log and answers 200, so the kit's run record lists no crash; the kit could scan `apps/<app>/playwright/.server-logs/server-<port>-probe.log` for that line during a run and list it under `crashes`. Plus two reruns of the settings window: the PFL "Start Date" datepicker stores a hidden alt field that `fill()` never updates (typed dates silently dropped), and the Masthead save on a scratch journal needs a "Country" no seed key sets.
2026-09-25 · U13 · claim checker ccK5 · Three reruns: the contributor "Edit" window refuses "Save" until "Country" is picked (a seeded author has none), so the first Bio Statement silently did not save and the bio needed a fresh submission after the others were published; the JATS page's "Make available with publication" is an sr-only input a role click cannot press; and the Crossref "Update Policy DOI" box appears only after Crossmark is ticked. A `users[].country` / contributor `biography` seed key and a note in patterns.md on sr-only checkboxes (click the label) would have saved them.
2026-09-25 · U13 · harness hU13b · About eight calls tracing a red OPS U08 S2 in the regression pass ("manager.maya 1" where the suite expects no task): each OPS U42 S1 run opens a control discussion on `publicknowledge` whose NEW_QUERY task lands unread on manager.maya, so on a fleet that has already run OPS U42 (here the first U13 harness pass) OPS U08 S2 is red whatever the change under test; the brief's "a red suite means the change is wrong" could not hold. Either U42's control could run on a scratch server of its own, or U08 S2 could read maya's count relative to a baseline; until then harness.md "Running" could name the pair so a regression pass runs OPS U08 before OPS U42 on a fleet that has not run U42.
2026-09-25 · U13 · security probe sec3 · Two calls: the dispatch asked for scripts outside the repo, but `bin/probe.js` always writes its run record under `.reports/<feature>/<agent>/` (no output-dir override), so the record had to be moved by hand and the empty folder removed; a `PROBE_OUT=<dir>` override in the kit would let a private check keep its run record beside its script.
2026-09-25 · U13 · test author tojs · Two runs and a split of the suite: scenario 9's category page needs the search index, which only a queued job refreshes (the page read "0 Items" in the parallel project), so S9 moved to `tests/serial/` with `runJobs()`; and footnote s named `categories: ['applied-science/eng']`, which the seed refuses (the key takes the leaf path `eng`). A line in scenarios.md's `categories` key ("the visitor's category page reads the search index: a listing test drains the queue in the serial project") and the leaf-path rule in the seed recipe would have saved both.
2026-09-25 · U13 · test author tops · One probe rerun and a serial split for OPS S10, the same two causes as tojs's line above (the leaf category path; the category page listing only after `runJobs()`), plus one S6 run lost to a 5-minute click timeout while the shared ArticleLandingPages.js changed its "More Citation Formats" locator under a running suite · the same two doc lines; a shared page object being built beside a running suite could be edited in one write at the end, or its owner could announce a locator change before saving it
2026-09-25 · U48 · harness agent hU48 · One parity rerun: the "JATS XML" page's "Make available with publication" box is the ui-library `Checkbox`, whose input is `sr-only`, so `getByRole('checkbox').click()` waited out its 30 s ("element is visible, enabled and stable" and then the svg intercepting); clicking the label text works · patterns.md pitfalls: a ui-library `Checkbox` (the Tailwind `peer sr-only` input) is read with `isChecked()` on the role but clicked by its label text
2026-09-25 · U48 · claim checker ccK2 · One extra rerun phase: a same-browser re-download of the public "JATS XML" came back with the earlier response's file name (the download answers with an ETag over the content only, so the browser revalidates and keeps the old headers), which first read as the server ignoring a cleared URL Path; and one failed phase reading the Body Text editor through `sciflow-editor` innerText, which is empty (its text sits in a web component) · patterns.md "Probe kit": a download claim that follows a change is read from a fresh `launch(app)` browser as well as the returning one; web-component editors are read through their `[contenteditable]` child
2026-09-25 · U48 · claim checker ccK1 · Two phase reruns: `PublicationScreen.publish()` (apps/ojs/playwright/pages/PublicationMetadataPages.js) on a scratch journal seeded with an unpublished issue waits 30 s for a preselected "assignment" radio that never comes and fails the publish, so the checker had to hand-roll "Don't Assign To An Issue"; and one Body Text rerun because Escape, pressed to close the editor's "Insert" menu, closed the whole workflow dialog (pitfall 7 names only inner windows) · publish(): check "Don't Assign To An Issue" without awaiting a preselection when no issue label is passed; patterns.md pitfall 7: Escape inside any widget of the workflow dialog (a toolbar menu, a listbox) closes the dialog too
2026-09-25 · U48 · claim checker ccK3 · Two phase reruns: the "Send File to Text Editor" picker names every unpublished version "Unassigned version ({date})", so an import aimed at the first version by position landed on the second (pick by the option value, the publication id); and Playwright's `locator.dragTo()` of a Body Text reference inserted a citation in one of three runs, a slow hand-made `page.mouse` drag (15 steps) works every time · patterns.md: a line on the Send window's option values and on HTML5 drags into the SciFlow editor; the U49 POM's `sendToVersionPicker` could offer a by-publication-id pick
2026-09-25 · U48 · test author tojs · Two red runs (S1, S6) and one diagnostic loop: the JATS page's "Delete JATS File" is a jQuery `$.ajax({type: 'DELETE'})`, a real DELETE, while patterns.md "UI realities" says PUT and DELETE go as POST with `X-Http-Method-Override` (true of `useFetch` only), so a POST-only response wait timed out; and three diagnostic S2 runs to learn that the fullscreen Body Text reads 400 px high in the first frame after its class and the window's size a moment later · patterns.md "UI realities": the POST tunnel is `useFetch`'s; a Vue page's own `$.ajax` call (the JATS panel) sends the real method, so a write wait matches `method !== 'GET'`; and a size read after a class toggle is polled
2026-09-25 · U48 · orchestrator · A probe run lost: the Settings › Distribution › "Access" "Publishing Mode" radio values (0, 1, 2 in on-screen order) are written nowhere, and an earlier kept check (U08 K2) ticks value 2 for the third choice; the choice also decides which tabs the Issues "Edit" window shows · seed-facts names the three values; the unbuilt `publishingMode` passthrough (scenarios.md) gets built
2026-09-25 · U50 · harness agent hU50 · One recon rerun: `screen(page)` right after a legacy grid link (section "Edit", "Create Issue", "Create Issue Galley", "Publish Issue") read the AjaxModal while it still held only its title, because `idle` returns before the modal's form is fetched and inserted; and one follow-up script because the "Full Issue" link of a subscription journal is named "Requires Subscription PDF", not "PDF" · the probe kit's `screen` (or a small `modalReady(page)`) waits for a visible dialog's `form` when the dialog is a legacy AjaxModal; patterns.md notes that a restricted galley link's name carries "Requires Subscription"
2026-09-25 · U50 · claim checker ccK2 · Three phase reruns: a `published: true` seeded article has no Production history, so after its issue is unpublished its workflow header reads "Submission" instead of "Scheduled" (the readout had to be redone on an article published through the workflow); and the workflow's "Schedule For Publication" skips its "Review Publishing Details" panel once the Publication Settings page carries an issue, its confirm button being named "Schedule For Publication" · seed-facts states the seeded article's missing Production history and what the header then shows; scenarios.md names a `publication.datePublished`/stage key so a dated scheduled article needs no screen detour
2026-09-25 · U50 · claim checker ccK1 · About six calls and two reruns lost: the legacy Issues windows show refusals and "Your changes have been saved." only as a passing Vue toast (div.pkpNotification in .app__notifications, about 5 s), which `screen()` never records because it reads main and the dialogs, so a save first read as "no notice" and A1 first read as "no reason"; and a grid read by innerText on a hidden tab panel returned the rows' inline script text · the probe kit's `screen()` also records the visible `.app__notifications` toasts (text and variant), and patterns.md "Probe kit" says where a legacy form's refusal and save notice appear
2026-09-25 · U50 · claim checker ccK3 · About four runs lost: a section heading dragged in the issue "Table of Contents" "Order" mode never moves under page.mouse (three drag shapes; ccK1 hit the same in parallel, its screen note landed mid-drive), so the custom section order on the reader page stayed unreached · a patterns.md "Locator pitfalls" line that TOC section rows do not reorder under a synthetic drag, or a scenario key for an issue's custom section order
2026-09-25 · U50 · test author tojs · Two runs and two probes lost: `fill()` on the Issues forms' "Date Published" changes only the visible box of the jQuery UI date picker, so a typed date was refused as empty until it was typed key by key (select all, Delete, the date, Tab; Escape in the box closes the whole Issue Management window); and every failure on an `asUser` page left an `error-context.md` of the blank fixture page, so each needed a probe to see the screen · patterns.md "Locator pitfalls" gains a line that a legacy date-picker box is typed from the keyboard, never filled; the harness writes an aria snapshot of every open page of the test on failure, not only the fixture `page`'s
