# Friction — what made an agent's task harder than it needed to be

Any agent may append here, at the end of its task, when something cost it
calls, time or retries that a better brief, doc, kit, seed or fixture would
have saved. Nobody reads this file mid-feature. A maintenance session on a
quiet day, or the maintainer at review, reads it and folds the real
patterns into RUNBOOK, TEMPLATE, the harness or the seeds through the
usual maintainer review. A row is deleted when it has been acted on or
judged noise; git keeps it.

One line per entry, appended at the end, in this shape:

`YYYY-MM-DD · U<nn> · <role and agent id> · <what cost you calls, time or retries> · <what would have helped>`

Facts only, the same quarantine as everywhere else (nothing
security-shaped, no credentials). A rule you disagree with is fine to name;
say what it cost.

## Entries
- 2026-09-05 · U29 spec author · Quoting labels from code: the OJS `locale/en/manager.po` overrides several `manager.setup.reviewOptions.*` keys that lib/pkp also defines, so a lib-only grep returns the wrong string and the app override has to be grepped second; a one-line note in patterns.md ("grep the app's locale first, lib/pkp second") would have saved a round-trip.
- 2026-09-05 · U29 · probe pB · Two reruns from premises: the item said press "Upload File" under "Submission Files" (the panel is the Vue file manager's button "Upload"), and the kit's `getByRole('dialog').last()` idiom returns the full-page workflow panel, not the legacy upload modal nested in it; a third rerun came from `Control+A` not selecting all in TinyMCE on macOS · a screen-notes line for the Submission-stage upload button and a kit helper `selectAll(page)` (Meta+A on darwin) or a note in patterns.md "Probe kit"
- 2026-09-05 · U29 · probe pA · Three reruns from kit/DOM premises no doc carried: the Vue form's Save is a POST (the brief's Reference table says PUT), field errors use class `pkpFieldError` (capital E, so the usual `[class*=error]` sweep misses them), and the Add Reviewer entry button is named after the reviewer ("Select Scratch Reviewer"), while U28's notes quote "Select Reviewer" · a line each in patterns.md "Probe kit" (save verb, error class) and in the U27/U28 locators would have saved them.
- 2026-09-05 · U29 · probe pC1 · P15's premise ("record whether a second language tab exists") could not be settled from the scenario API: form languages are a separate context setting (`supportedFormLocales`, Website › Setup › Languages "Forms" column) that `POST scenarios/context` does not accept, so a scratch journal with `supportedLocales: ['en','fr_CA']` has single-language forms and the answer needed three extra runs through the Languages grid · a `supportedFormLocales` key on the context scenario (scenarios.md "Configuring a scratch context") and a seed-facts line saying the seeded journal's forms are English-only.
- 2026-09-05 · U29 · probe pD · Two reruns from the roster premise: the item names `reviewer.julia` as the scratch journal's reviewer, but a seeded reviewer not enrolled on the scratch context is refused the wizard (`roleBasedAccessDenied`), so the reviewer-side steps needed a second context with a throwaway `externalReviewer` in `users[]`; pA had recorded the same gap for the Add Reviewer search only · a users.md/scenarios.md line "a scratch context's reviewer must be created in `users[]`; the seeded reviewers are not enrolled there" and a probe-list premise that says so.
- 2026-09-05 · U29 · probe pC2 · Five reruns from DOM/kit premises no doc carried: the legacy grid's row actions live in the NEXT `tr` (not inside `tr.gridRow`), fixed-position modals have `offsetParent === null` (a visibility filter reads them as hidden), the side modals open empty and fill seconds later (a `readDialog` without a content wait records a blank "Preview"), and the item's seed (`reviewer.julia`, `reviewForm:` on an inactive form) cannot be built as written · a patterns.md "Probe kit" paragraph on legacy grids (row/actions rows, `getClientRects`, wait for `.ui-tabs-nav li`) and a scenarios.md line that `reviewForm:` needs an active form and a reviewer from `users[]`.
- 2026-09-05 · U29 · checker K1 · One rerun (new scratch) because the only throwaway reviewer was already assigned to the seeded round, so the Add Reviewer search offered nobody (`Select …` never appeared); a second `externalReviewer` in `users[]` is needed whenever a script both seeds a request and opens Add Reviewer. A second lost read: `nav a` on the dashboard is empty until the Vue sidebar renders (`idle()` then wait for `nav a`) · a scenarios.md line under `reviewRounds[]` ("an assigned reviewer leaves the Add Reviewer search; seed a spare") and a patterns.md "Probe kit" sentence on waiting for the sidebar.
- 2026-09-05 · U29 · checker K3 · One rerun because a typed `?step=2` on an API-seeded accepted request lands on step 1 until "Save and continue" is pressed once — pB's note said typed steps "work", pC1/pD's said they do not, and the checker trusted the earlier one · a single line in scenarios.md under `reviewRounds[]` ("a seeded `accepted` reviewer starts at step 1; typed `?step=n` renders only after the earlier steps' buttons") would settle it for every reviewer-side agent.
- 2026-09-05 · U29 · claim check K2 · Five reruns (11 script runs for a 40-call chunk) from premises no doc carried: the workflow page is a dialog (`screen().text.main` reads the dashboard list behind it, so the round status line and the suggestions panel looked absent); the submission wizard's rail is not clickable, every uploaded file needs a type before "Submit", and an unassigned section editor gets "Error" on a new submission's workflow; Settings › Workflow › "Emails" is not the templates list (that is "Add and edit templates" → `manageEmails`) · a patterns.md "Probe kit" paragraph on the workflow dialog and the wizard walk, and the scenarios.md "Field shapes not built yet" keys `reviewerSuggestions[]` and a `files[]` with a genre, so the suggestions panel can be seeded instead of walked.
- 2026-09-05 · U29 · checker K4 · Two reruns from premises no note carried: a legacy grid row's arrow, clicked a second time to "close" the controls, hangs the click itself (pC1's note said only that the action link never shows); and a form in use offers no "Edit" (Rule 12), so a checker that seeds an active form carried by a reviewer must open its preview from the row's "Preview" — the script's `openEditWindow` waited 30 s for a link that never exists. A third lost read: the listbuilder's "Add Item" renders the new row after the click returns, so `.last()` on the visible boxes fills the previous row · a patterns.md "Probe kit" paragraph on legacy grids (arrow once; row actions depend on state; listbuilder rows arrive late) would save every agent on the Review Forms, Sections and Languages grids.
- 2026-09-05 · U29 · checker K6 · Two reruns from premises no note carried: the reviewer wizard's hidden step-1 "Save and continue" stays in the DOM, so pD's button filter with `.first()` pressed it three times and never reached step 3 (pD's own note blamed a stale locator); and a Vue form's "Save" is disabled after a refused save until the errors are fixed, so the second save hung for 30 s · one patterns.md "Probe kit" line ("filter wizard buttons with `:visible`; a refused Vue save disables Save until the flagged boxes change") would have saved both.
- 2026-09-05 · U29 · test author OJS · Six debug reruns from premises no note carried: a TinyMCE box on a Vue settings form must be `initialized` before a fill or the save silently posts nothing for it; the "Response Options" listbuilder keeps one row editable (the visible-box count never grows, only the row count); "Reviewer Recommendations" titles are `th` row headers; the Add Reviewer window has no "Cancel"; a `hasText` row filter is case-insensitive so "Inactive form X" matched `row('Form X')` · a patterns.md line on Vue rich-text fields (wait for `tinymce.get(id).initialized`, then `body.fill`) and one on listbuilders (rows, not boxes) would have saved four of the six.
- 2026-09-05 · U29 · test author OMP · One rerun from a shared page-object race no note carried: a Vue settings form's "Saved" footer briefly coexists with the leaving "Saving" span (two `.pkpFormPage__status` elements), so a strict `toHaveText` on the bare locator fails when the save answers between two polls · a patterns.md line on Vue form status ("filter the status span by its text; the transition leaves two for a beat") would have saved it for every settings-form suite.
- 2026-09-05 · U29 · probe pA1 · Two supplement runs to catch the notice of a successful Vue settings save: it is not a `.pkpNotification` (the page bar only shows the refusal "The form was not saved because…"); success is the form's own `role=status` span next to Save, "Saving" (~100 ms) then "Saved" for 5 s, gone before `idle()` returns · one patterns.md "Probe kit" line ("a successful Vue form save shows only the inline `.pkpFormPage__status` 'Saved' for 5 s; read it right after the click, before idle()") would have saved both runs.
- 2026-09-05 · U29 · probe pA2 · Two reruns from premises no note carried: the review stage lives in a dialog (screen().text.main shows the dashboard list, the round status must be read from [role=dialog]), and closing the Add Reviewer window with Escape closes the workflow dialog as well · a patterns.md "Probe kit" line on the dashboard's workflow dialog (read it from the dialog; close inner windows with their own Close, never Escape) would have saved both.
- 2026-09-05 · U29 · probe pC1 · Two reruns from premises no note carried: a legacy side window's content loads by AJAX after the dialog opens and `idle()` returns before it, so `screen()` and the label/toolbar reads caught an empty window; and the item window's id `reviewFormElementForm` sits on both the `<form>` and a `<fieldset>`, so the bare `#id` locator hit strict mode · one patterns.md "Probe kit" line ("a legacy side window: wait for a field of its form, not `idle()`; legacy form ids can repeat on the fieldset — use `form#id`") would have saved both.
- 2026-09-05 · U29 · probe pB · One rerun (two browser calls) because the reviewer wizard's "Accept Review, Continue to Step #2" silently stays on step 1 until the privacy box "Yes, I agree to have my data collected and stored according to the privacy statement." is ticked (the page shows "This field is required." only after the press) · a seed-facts.md line under the reviewer-wizard facts ("an invited reviewer must tick the privacy box before Accept moves to step 2; `?step=2` typed before acceptance shows step 1") would have saved it.
- 2026-09-05 · U29 · probe pC2 (high-effort trial) · One extra run for P21's OMP control: the item sends the scratch manager to Settings › Press › Series, but a scratch press has no series (the context scenario accepts no `series[]`), so the grid is "No Items" and the control had to be read on the seeded press as `manager.maya` · a seed-facts line ("a scratch press has no series") or a `series[]` key on the OMP context scenario would have saved it.
- 2026-09-05 · U29 · probe pD · Three reruns of one script from locator premises no note carried: a legacy grid's row-action links have whitespace-padded text, so `filter({hasText: /^Copy$/})` (the pattern pC1's locators file shows with count 0) never matches and `getByRole('link', {name, exact: true})` inside `tr.row_controls` is the locator; the row arrow is `.show_extras` closed and `.hide_extras` open; and the reviewer wizard's OJS "Recommendation" list is `select[name="reviewerRecommendationId"]`, not `recommendation` (one submit was refused for it) · a patterns.md "Probe kit" line on legacy grid row actions and a U28 screen-locators row for the list would have saved all three.
- 2026-09-06 · U29 · probe pE · Two reruns of one script from locator premises: the Add Recommendation window's "unlabelled list" (probe list P27) is a `select[name="status"]`, not radios, so the radio/label fill hung; and the Vue "More Actions" menu items match `getByRole('menuitem', {name, exact: true})` only (a `hasText` filter, the idiom pD's script used on legacy grids, matches nothing) · a patterns.md "Probe kit" line on Vue DropdownActions menus (menuitem by role) and a probe-list phrasing that names the control's shape when a sibling probe has not seen it would have saved both.
- 2026-09-06 · U29 · probe pF · Two extra runs (one script each, three apps) for the sidebar half of P33: the kit's default `signIn` goes through the site login and lands on the site index, which has no backend sidebar, and once on the dashboard the sidebar's "Settings" is a collapsed group whose "Workflow" link is not in the DOM until clicked · a patterns.md "Probe kit" line ("sign in with `{contextPath}` to land on the dashboard; the sidebar's Settings group must be opened before its entries exist") would have saved both.
- 2026-09-06 · U29 · harness hI · Two reruns of one script from window premises no note carried: the review form item window (`#reviewFormElementForm`) has "Save" and the modal's "Close" only, no "Cancel" (pD's script swallowed that miss with a `.catch`), and a `page.goto` that changes only the `#review/reviewForms` hash is not a fresh load, so the Edit window stays open and the outer grid's row arrow is unreachable · a screen-notes line on the item window's buttons and a patterns.md "Probe kit" line ("leave the page before re-navigating to a hash") would have saved both.
- 2026-09-06 · U29 · claim check ccK1 · One failed run and a reseed from a window premise the notes carried wrong: pA2's note says the "Edit Review" window's buttons are "Cancel" and "OK", but "Cancel" is a link (`getByRole('link', {name: 'Cancel'})`), so a button click hung and the phase's facts (kept only in memory until the end) were lost; a note that names the element's role, and a script that records per step, would have saved the call.
- 2026-09-06 · U29 · claim check ccK2 · One wasted run and two failed sections from window facts no note carried: the Add Reviewer form's `skipEmail` box sits among the file boxes (`selectedFiles[]`), so a "tick all boxes" grant sent no request email; the wizard's step buttons ahead of the current step are not clickable ("Continue" is the only way forward); the Edit Review window's "Cancel" is a link (ccK1 hit the same). A patterns.md line for the Add Reviewer form's inputs (dates, files, skipEmail) and for the wizard's step navigation would have saved all three.
- 2026-09-06 · U29 · claim check ccK3 · One extra run (OJS) from a strict-mode collision no note carried: Settings › Website has two "Setup" tabs (its own and Appearance's), so the role locator that works on Workflow Settings throws there; and a partial rerun of the script replaced the facts file, so the first run's facts had to be re-read from the log and a screenshot · a screen-notes line for the Website page's tabs (now added) and a patterns.md "Probe kit" line ("a facts file a script rewrites must merge on a partial rerun") would have saved both.
- 2026-09-06 · U29 · claim check ccK4 · One rerun (OJS) and two failed sections from window facts no note carried: the reviewer wizard's recommendation list is `select[name="reviewerRecommendationId"]` on OJS (pD's `select[name="recommendation"]` matched only OMP), so the review was not submitted and the "Completed" count had to be re-driven; and the Edit Review window's `responseDueDate` input is a hidden altField, so a wait on it hung twice · both now in screen-notes; a `users.md`/scenarios line naming the per-app recommendation control would have saved the run.
- 2026-09-06 · U29 · claim check ccK6 · Four extra runs (OJS) from two Vue-window facts no note carried: the Add/Edit Recommendation window's "Save" turns `[disabled]` after a refused save (a click on it hangs 30 s), and closing a side modal with "Close" leaves its hidden shell in the DOM so `getByRole` on the table behind it returns nothing until a fresh navigation. Both are now in screen-notes under ccK6; the second belongs in patterns.md "Probe kit" gotchas, since every Vue side modal has it.
- 2026-09-06 · U29 · claim check ccK5 · Four extra runs from two premises no doc carried: a form carried by an accepted assignment has no "Edit" (so a language check must type the French text before the assignment is seeded — the seed order is not in scenarios.md's `reviewForms[]`/`reviewRounds[]` rows), and a scratch context needs `context: {supportedLocales}` before `/fr_CA/` URLs work (seed-facts says fr_CA is "a UI language" of the seeded journals, silent on scratch contexts); plus one run each on the legacy multilingual box (its French twin opens only while the English box is focused; the globe icon is decorative) and on the French wizard's button labels. All four are now screen-notes lines under ccK5; scenarios.md could say that `reviewForms[]` items become uneditable the moment `reviewRounds[].reviewers[]` names the form.
- 2026-09-06 · U29 · claim check ccK7 · Two extra runs (OJS and OMP) from screen facts no note carried: the "Emails" tab of Workflow Settings lists no templates (they live on the Manage Emails page behind "Add and edit templates"), and the Roles grid's edit window submits with "OK" while its rows' text starts with the "Settings" control, so an anchored row filter and a "Save" locator both timed out. Now in screen-notes.md under ccK7.
- 2026-09-06 · U29 · test author tops · One extra run from a Playwright fact no harness doc carries: `page.goto()` to a hash-only change of the page already open (`…/workflow#review/reviewSetup` from `…/workflow`) is a same-document navigation and returns `null`, so a status assertion on it fails; screen-notes ccK5 mentions it under "items, the reviewer's form, languages", where a test author has no reason to look. Belongs in patterns.md's pitfalls (leave the page, or assert on the tab strip, not the response).
- 2026-09-06 · U29 · test author (OJS) · Four extra suite runs from window facts no note carried: a "Response Options" row still in edit mode when "Save" is pressed leaves the item window open (fix it with Enter first), text typed into the item's "Item" box before TinyMCE is initialized is wiped, the "Form Items" grid redraw swallows the next "Create New Item" click, and a seeded `accepted` assignment refuses a typed `?step=3` (ccK6's note held only for an acceptance made on screen); plus one run on the recommendations table's order, which the spec states as a rule though the app asks for no ordering (finding T-ojs-1). All four now in screen-notes under tojs; a scenarios.md line under `reviewRounds[].reviewers[]` ("a seeded acceptance opens the wizard on step 1") would have saved the last one.
- 2026-09-06 · U29 · claim check ccK8 · One extra call: `.reports/U29/claims.txt` was generated before the step 7 fold, so its line numbers sat 12 lines above the spec the brief quoted; a step 9 span checker should get a checklist regenerated on the folded spec (or regenerate it first: `node docs/process/lint/lint-spec.mjs --claims <spec>`).
