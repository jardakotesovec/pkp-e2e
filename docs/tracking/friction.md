# Friction — what made an agent's task harder than it needed to be

What cost a screen-driving agent calls, time or retries that a better
brief, doc, kit, seed or fixture would have saved; that agent appends a row
at the end of its task. The maintenance session folds the rows and deletes
them under MAINTENANCE "The daily session".

One line per entry, appended at the end, in this shape:

`YYYY-MM-DD · U<nn> · <role and agent id> · <what cost you calls, time or retries> · <what would have helped>`

Facts only, the same quarantine as everywhere else (nothing
security-shaped, no credentials).

## Entries
2026-09-06 · U31 · harness hU31 · the spec draft names the Add Reviewer Suggestion window's address box "Email address" while the screen labels it "Email"; the by-hand wizard drive timed out once on that label and cost a second probe run · a draft that quotes labels as screened, or a harness brief line saying to read labels off the recorded window before typing
2026-09-06 · U31 · claim checker ccK1 · the kit's record(name) and shot/screen-snap under the same name overwrite each other, which cost four re-runs (window, base setting, OPS wizard, fcbase) before the -info suffix habit; and the Editor Dashboard has no "Incomplete" view although footnote t1 sends the manager there, costing two t1 re-runs · a kit guard (record() refusing a name shot() already wrote in this process, or a shared -screen/-info convention in patterns.md "Probe kit"), and briefs quoting dashboard views as screened
2026-09-06 · U31 · claim check K2 · the wizard step "Reviewer Suggestions" contains "Review", so the shared substring idiom for "wait until step X is current" (the harness's loop and mine) silently stayed on the suggestions step and cost one full re-run plus a phase split · a kit helper `wizardStep(page, label)` that matches the rail label end-anchored (the OJS page object's `endAnchored` already does this but lives under apps/, out of the probe kit's reach)
2026-09-06 · U31 · claim checker ccK3 · one Internal Review drive was lost because the monograph reused the suggested addresses of the OJS-style R1 drive, and "Create New Reviewer" on R1 had already made them accounts with the Reviewer role (the list's "Select Reviewer" then turned the outer window into the request form instead of opening Create); and `app.mail.messageCount()` sits at Mailpit's 500-message cap on the fleets, so a total-based "no email" reading is void · a scenarios.md line under `reviewerSuggestions[]` saying every no-account address is context-wide once created, and a kit/doc note that Mailpit is capped at 500 so counts go per fresh recipient
2026-09-06 · U31 · claim checker ccK4 · three premises cost one re-run each: the legacy Add Reviewer forms close with a bottom "Cancel" that is an `<a>` link (a `getByRole("button")` Cancel never matches, so every closeAll timed out until the locator read `a:visible`); a typed `workflowMenuKey=workflow_3` without a round id lands on a stage view whose "Add Reviewer" says "Invalid review round." (the OMP external-stage read had to be redone through the tree); and a person created from a suggestion on one submission is a reviewer for every later one, so a seed reusing the same no-account address across submissions loses its "no account" path (I2 had to be seeded) · a patterns.md pitfall line for the link-shaped Cancel and the round-less key, and a scenarios.md sentence saying suggestion addresses meant to stay account-less must be unique per context
2026-09-06 · U31 · test author tops · the scenario response's `stageId` after `published: true` on OPS reads 6, not the Production stage's 5, which cost one re-run of the green run (the sibling U30 suite asserts `stageId` 5 on an unposted preprint, so the habit carried over) · a scenarios.md sentence under the response shape saying what `stageId` echoes once `published: true` (the stored value after posting, app-specific), or the builder echoing the stage the screen shows
2026-09-06 · U31 · security verification probe · one re-run because the wizard step shares `.listPanel__item` with the file list, so a bare `.first()` row picked a file entry (screen-notes carried the Add/Edit window locators but not the panel class that scopes a suggestion row) · a screen-notes or patterns line naming `.listPanel--reviewerSuggestions` as the row scope on the wizard step
2026-09-06 · U31 · test author tojs · two re-runs because `getByRole` reads of the wizard step's entries and of the "Reviewers Suggested by Author" rows return nothing while a side window is open (the page behind is aria-hidden), although every locator row and note handed over for those rows was role-based; one more run to shoot the T-ojs-1 evidence by probe, because the config takes no screenshot on failure · a patterns.md line under pitfall 4/6 saying reads behind an open window need text/CSS locators (and locator rows marked when they were taken with a window open), and `screenshot: only-on-failure` in config-factory or a brief sentence saying the evidence screenshot comes from a kit probe
2026-09-06 · U31 · test author tomp · one re-run (a five-minute S4 timeout) because, as the Funding Coordinator on a press, the "Error" dialog stacks over the Add Reviewer windows AGAIN after the inner "Create New Reviewer" form's "Add Reviewer", and its overlay intercepts the window's header "Close" beneath it; the notes said the dialog comes on a stage visit and on the window's opening, and the OJS suite's plain close-all loop had passed there · a screen-notes line (now added) naming every moment the dialog stacks, and a shared `closeAll` on `ReviewerRequestWindow` that presses "OK" on a stacked "Error" before closing the next window
