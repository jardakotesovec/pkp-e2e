# Friction — what made an agent's task harder than it needed to be

What cost a screen-driving agent calls, time or retries that a better
brief, doc, kit, seed or fixture would have saved; that agent appends a row
at the end of its task. The maintenance session folds the rows and deletes
them under MAINTENANCE "The daily session".

One line per entry, appended at the end, in this shape:

`YYYY-MM-DD · U<nn> or sync · <role and agent id> · <what cost you calls, time or retries> · <what would have helped>`

Facts only, the same quarantine as everywhere else (nothing
security-shaped, no credentials).

## Entries
2026-09-12 · U26 · test author tojs · two runs lost to screen facts the spec and footnote h leave out: the review-files window lists the round's files only until "Show files from all accessible workflow stages." is ticked, and the upload wizard's "Cancel" link deletes the uploaded file where the header "Close" keeps it · footnote h naming the stage box, and Side effects (or footnote l) naming which close keeps the file
2026-09-12 · U26 · test author tomp · one probe rerun lost to the kit: `screen(page)` after closing a legacy window over the workflow waits 30 s for a second visible dialog and throws, so a "read the screen after the close" step must skip `screen()` and read locators directly · a `screen()` that records however many dialogs are open (zero or one) instead of waiting for the count it saw before
2026-09-12 · U27 · test author tops · one probe lost to the spec naming the wrong screen: scenario 15's "Settings › Workflow › Emails" is a settings form, and the templates list is the "Add and edit templates" link off it (Manage Emails, `management/settings/manageEmails`) · the bullet naming the Manage Emails page, or seed-facts recording where the templates list lives
2026-09-12 · U27 · test author tomp · one probe and one 5-minute hung run lost to a stage-access fact no doc names: a manager who is also an assigned reviewer of the submission is refused the review stage, so scenario 2's "Own row" bullet had no screen; and two reruns lost to `[data-cy="active-modal"]` marking the top window only (a "window closed" read on it never settles) · patterns.md "Side modals" naming that the attribute moves to the active window, and U24 (or footnote a here) naming the reviewer-on-the-submission refusal
2026-09-12 · U28 · test author tojs · two red runs lost to facts no doc names: a roster reviewer's step-3 "Your changes have been saved." toast is drained by a parallel test as the same account (the OMP suite had already moved S5 to a scratch journal for it), and the step-1 competing-interests editor re-initializes after its radio reveals it, so typed text loses its tail · patterns.md parallel lesson 2 naming server-fed form toasts as the same race, and the TinyMCE "UI realities" entry naming the revealed-box re-init
2026-09-12 · U28 · test author tomp · two reruns lost to screen facts no doc names: the wizard's step tabs swap their panel by AJAX a beat after the click, so a radio ticked right after the tab click is lost to the fetched panel (revisit by a typed `?step=N`), and the page's one `#ui-datepicker-div` opened after an "Add Reviewer" window sits under the later "Edit" window, so a date pick hangs its full timeout unless the workflow is reloaded first · patterns.md "Locator pitfalls" naming both (the tab swap under the jQuery UI tabs entry, the datepicker under "Side modals")
2026-09-12 · U28 · test author tops · two red runs lost to the S17 Roles bullet's premises drifting from the fleet's tip: the "Create New Role" window opens on "Manager" (stage boxes already disabled, so a "greys out on Reviewer" control needs a stage-taking level first), and ops `a72cacc1c5` lists a second stage "Done" (the ci-triage pkp-lib#13109 row) that stays enabled under "Reviewer", which the brief and the spec written from a 2026-09-05 probe do not mention · a revision brief naming the open ci-triage rows that touch the feature's screens, or fleet.json carrying the checkouts' commits so an author reads the drift as a tracked regression before re-driving
2026-09-12 · U21 · test author tomp (revision) · one judgment call and one re-read lost to the brief's bullet map: scenario 5's press bullets ("the one-section bullets on a press keep the Submission Type ask and the work-type line") were listed under scenario 6, and scenario 5's new "one panel per language" bullet cannot be asserted on the seeded one-language press the shipped S5 ran on, so "the shipped tests stay as they are" had to give and S5 was re-seeded on two scratch presses · a revision brief naming which given a shipped test does not yet meet, so the author re-seeds on purpose
2026-09-12 · U21 · test author tojs (revision) · one call lost to the brief naming kept check scripts under `shared/playwright/checks/U21/`, a directory that does not exist (U21 kept none; its screen facts live in `.reports/U21/probe-*.md`) · the brief template pointing at the checks directory only when the feature has one
2026-09-12 · U23 · test author tojs (revision) · two red runs lost to list facts no doc names: clicking the sidebar entry of the view already open neither refetches nor clears the in-page phrase (a re-list after the reviewer acted in another session needs a fresh `goto()` or a real view switch), and a reviewer popover left open after its "Review Details" window closed ignores Escape (focus is outside it; an outside click closes it) · patterns.md "UI realities" naming the same-view click as a no-op, and the U23 screen note "Escape closes it" qualified to "while focus is inside"
2026-09-12 · U23 · test author tops (revision) · two suites read (U25 and U24 OPS) to settle facts the brief and footnote s11 leave out: the Production stage's decision reads "Decline Submission", not "Decline", and a declined preprint's panel lands on its "Preprint" pages, so the stage's "Delete" needs "Production" selected first · footnote s11 quoting the OPS button verbatim and naming the declined panel's landing (U24 OPS2)
2026-09-12 · U23 · test author tomp (revision) · one red run lost to a reviewer-wizard landing no doc names: after an on-screen accept, a reload of `reviewer/submission/{id}` opens step 2 ("Continue to Step #3"), which U27's OMP `completeReview` (invited or seeded-accepted-on-step-1 only) never reaches, so scenario 9's two-step reviewer leg needed a suite-local submit helper · scenarios.md's "A seeded `accepted` assignment opens the wizard on step 1" sentence naming the on-screen accept's step-2 landing as the third case, and `completeReview` handling it
2026-09-12 · U26 · test author tomp (revision) · two greps lost to brief facts the repo contradicts: the kept check scripts named under `shared/playwright/checks/U26/` (the directory does not exist; U26 kept none) and the press's seed named as `decisions: ['sendExternalReview']` where every OMP scratch-press seed in the suite enters External Review with `skipInternalReview` (OMP's Submission-stage decision; `sendExternalReview` is its Internal-Review decision) · the brief template naming the checks directory only when the feature has one, and footnote s carrying the OMP decision name beside the OJS one
2026-09-13 · U26 · orchestrator (revision 2 finals) · two OMP final runs lost (~13 min) to a clock gap the harness does not close: the PHP servers run in UTC (`date.timezone`) while the tests take "today" from the Mac's local clock, so between 00:00 and 02:00 CEST every due-date expectation is one day ahead of the server (U27 S1/S7/S12, U28 S10, U29 S1 red on OMP twice, green with `TZ=UTC` on the Node side, which is CI's clock); fix wanted: the date helpers (`daysFromNow` and the due-date expectations) should compute in the server's zone, or the runner should pin `TZ=UTC`.
2026-09-13 · U27 · test author tomp · the brief said S5 "check" whether the shipped test uploads a file (it did not; one read) and S1 drives a Press Manager, whose menu holds "Login As" between "History" and "Editorial Notes" — one failed run on the seven-entry order the spec (an Editor) lists; a second legacy Edit window on one page never takes the calendar day click, and the unbounded pickDate click ate the whole 5-min test timeout (one 5-min run) · the brief naming the suite actor per scenario where it differs from the spec, and a patterns.md line that a jQuery UI calendar opened from a second legacy window on the same page needs a fresh landing first
2026-09-13 · U30 · test author tojs (revision) · five calls lost hunting the request page's "To" chips in the old probe records (the pA/pB JSON holds innerText and the dashboard's aria, not the request page's) before the ui-library source settled that a chip is `.pkpAutosuggest__selection` and the disabled autosuggest renders no `.pkpAutosuggest__input` at all · a `loc()` row for the "To" chips and their absent input in `screen-locators.md` when a checker first reads the request page (K1/K2 recorded only the "To" label)
2026-09-13 · U31 · test author tomp (revision) · one run lost to the shared `ReviewerRequestWindow.cancel()`, which waited on a `.last()` dialog handle that re-resolves to the outer window once a stacked inner one is gone (never hidden, 30 s timeout) although `close()` beside it already counted windows; and one more to a "closes without asking" premise counted as workflow + windows, when the workflow dialog behind an open window is not counted by role (pitfall 6 says role reads behind a dialog return nothing, not that the dialog count drops) · page-object waits on stacked dialogs count them, never a `.last()` visibility; pitfall 6 could add the one sentence "the workflow dialog itself leaves `getByRole('dialog')` while a window is open"
2026-09-13 · U31 · test author tojs (revision) · one red run lost to the shared `MySubmissionsPage.findRowByTag` once the brief's second draft for the same author put two tag-bearing rows in My Submissions (a strict-mode violation the method's name does not warn of) · a `findRowByTag` doc line, or the brief, saying the read needs a unique title once a scenario seeds two submissions of one submitter, and a `row(title)` idiom beside it
2026-09-13 · U01 · test author tomp (revision) · one greenless run lost to a guessed heading on the impersonated Author's landing ("My Submissions": the page heads "Active submissions (N)" under a side-nav link "My Submissions as Author"), and four greps across the checkout before settling that no screen or API field of the users list carries the last-login date the spec's Side effects sentence names · a screen-locators row for the My Submissions landing (heading and side-nav link) from any earlier feature that lands there, and a fn-s sentence saying the last-login date has no known screen so the suite reads the row and returns the T-finding without the search
2026-09-13 · U01 · test author tojs (revision) · two probe processes and a red first run spent on the brief's "find where a users-management screen shows the Editor's last login" when no screen shows it (Users & Roles has no such column, the Edit page and its "View more details" none, the ui-library no `dateLastLogin` consumer outside its mocks) · the persona step's "wording" grade on that bullet could carry a one-line code grep (`dateLastLogin` consumers) so the brief says "no screen is known; read the row, return the T-finding" instead of sending each app's author to search
2026-09-13 · U02 · test author tomp (revision) · one red run lost to `ProfileRolesTab.contextSection` (the OMP U02 page object) resolving to two elements on the press-level Roles tab, where the "Register with other presses" fold's outer `.section` wraps each press's own, so a `toHaveCount(1)` positive control failed while the chained `contextBox` reads had always passed · a doc line on `contextSection` (and the shared `ProfilePage.contextSection`, the same selector) saying the press-level tab nests sections and the locator is for chaining, not counting
2026-09-13 · U02 · test author tojs (revision) · one red run mis-read as a product finding: the run's `error-context.md`, which the brief names as the screen record, snapshots the fixture `page` only, so a failure inside a context the test opened itself (`browser.newContext`) shows the default page's Roles ticks under the other account's failure · the brief (or harness.md "Running") naming that the snapshot is the fixture page's, and a failing test-opened context needs its own read (a `page.locator('body').ariaSnapshot()` in the catch, or a trace on retry)
2026-09-13 · U02 · test author tops (revision) · one red run lost to a screen fact no doc names: the site-level page's per-server consent line is parked off-screen by position (`left: -9999px`), not hidden, so Playwright's `toBeHidden()` fails on the spec's "no consent line sits under either journal" while the class read passes · a fn-s or screen-notes sentence saying the line is off-screen by position and is read by class or bounding box, never by visibility
2026-09-13 · U03 · test author tojs (revision) · one red run and a probe spent on scenario 7's "plain-text file", which fn-s pins to a `.txt` while the register's evidence (`.reports/U03/pF`, `notimage.png`) was a text file under an image name; the two are refused by different layers with different sentences (T-ojs-1) · when a scenario's fixture is named in fn-s, the claim-check evidence that backs the bullet should name its fixture the same way, or the bullet should say "a file that is not an image, under an image's name"
2026-09-13 · U03 · test author tomp (revision) · two red S2 runs on the French Users & Roles read: `UsersAccessPage` anchors its `goto` on the English "Invite to a role" button and its rows on the table named /Current Users/, both translated in French ("Utilisateurs-trices actuels-elles (n)"), and the French read flips the session's language so the next English read fails the same way unless it names `/en/` · page objects anchored on language-neutral landmarks (the search box, a row by email) and a screen-notes line saying a Users & Roles read in another language needs the `/en/` address for every later read of that session
2026-09-13 · U03 · test author tops (revision) · one lookup spent on scenario 12's control, which fn-s routes through My Submissions › "Incomplete" while a preprint server has no such view (U22 OPS1: drafts sit under "Active submissions" with "Complete submission") · fn-s naming the OPS placement beside the journal's, or "the draft reopened from My Submissions" without the view's name
2026-09-12 · sync · regression reader rr5 · the brief pointed at `shared/playwright/checks/sync/pkp-lib-13317/` for the kept 13317 checks, but they exist only on the companion branch `13274` (commit 63a965f), not on main or on disk: three calls to find them and read the headers through `git show` · a brief that names the branch (or `git show <sha>:<path>`) when a kept check is not on main yet
2026-09-12 · sync · regression reader rr5 · in the wizard's contributor Edit panel the row's "Edit" button and the "N languages completed" link sit under an `aria-hidden` tooltip button (`.v-popper--has-tooltip`) that intercepts pointer events, and the panel has no Cancel, only a Close: two reruns (`click({force: true})`) · a patterns.md "Locator pitfalls" row for that panel, or a `SubmissionWizardPage` helper that opens a contributor's Edit panel and toggles the languages
2026-09-13 · U05 · test author tomp (revision) · two red S2 runs on "Newest first": the two seeds' tasks are stamped `date_created` to the second and a seed takes about a second, so back-to-back `POST scenarios/submission` calls tie and the grid lists them in either order (T-omp-2) · a fn-s line saying the two submissions of scenario 2 must be raised in different seconds, or a scenarios.md note that a task's creation time is stored to the second
2026-09-13 · U05 · test author tojs (revision) · two claims of the revised scenarios had never been seen on screen, only in DOM text: scenario 7's "Dashboard 1" under the Site Administrator's name (the 2026-09-04 read was the hidden entry's text; the theme hides that count at desktop width, T-ojs-1) and scenario 10's "with the submission's title under it" (footnote s10 says the row's wording "was not recorded"; the row carries the title in its sentence and nothing under it, T-ojs-2): one red run and three probe scripts to settle both · a claim-check rule that an entry read from a closed menu or a hidden element is opened or shown before it is quoted, and a scenario writer's rule that a bullet built on a footnote's "was not recorded" is driven once before it is written
2026-09-13 · U05 · test author tops (revision) · one extra run spent on scenario 10's "with the submission's title under it": the row carries the title inside its sentence and no line under it, which T-omp-1 had already recorded on the press earlier the same session, but the brief's "facts learned since" named only the S2 timing guard · a brief that names every sibling finding block already filed for the feature (the sibling's findings file path is enough), so a same-shape row is asserted as the screen shows it on the first run
2026-09-13 · U04 · test author tomp (revision) · one red parallel run on scenario 10: `MySubmissionsPage.closeWorkflow()` hangs for the full test timeout once the Contributors screen is open inside the author's workflow panel (the Close button is in the tree, the click never lands) · a patterns.md pitfall line, or `closeWorkflow()` pressing the panel's Close by keyboard / falling back to a dashboard reload
2026-09-13 · U04 · harness hU04 · two probe reruns anchoring the workflow's Contributors list row on the seeded iD: the list row shows name, role and Primary Contact only, the iD and its verified state live in the contributor form's "ORCID iD" field (`orcidField(modal)` in OrcidPages.js) · a scenarios.md line beside the `author` key naming where the seeded iD shows (now added), or a patterns.md "Locator pitfalls" row for the list
2026-09-13 · U04 · test author tops (revision) · one red run lost to scenario 10's `{OJS OMP OPS}` read-only bullet, which U41's own Actors row and register OPS1 already contradict for preprint servers (the submitting author edits their unposted preprint's contributors), so the OPS test had to be reshaped after the run instead of before it · the scenario writer's brief (or the lint) checking a common scenario's "read-only"/"cannot" claims against the sibling specs' per-app register entries for the same screen before the tests are written
2026-09-13 · U06 · test author tojs (revision) · two probe reads lost to a kit timing fact no doc names: a Vue form's inline errors (the account step's "The password must be at least 6 characters.", the empty role row's "This field is required.") render after the 422 response resolves, later than `idle()` returns, so `screen()` right after the press recorded the refusal with no error text twice and a third probe with a 3 s wait was needed to see them · patterns.md "Probe kit" (or `screen()` itself) naming that a refused Vue save's inline errors land a beat after the response, and to read them through an auto-waited locator
2026-09-13 · U06 · test author tomp (revision) · one red run lost to fn-s naming scenario 2's password as `Password<tag>` while the harness's `asUser` signs in by the roster rule alone (the shipped test had used the doubled username), so the "new credentials" sign-in has to go through `LoginPage` on the fixture page; and one probe rerun lost to `window.tinymce.editors` being empty on the compose step (TinyMCE 6: `tinymce.get()` or the iframe body) · fn-s (or the brief) saying a non-roster password means a `LoginPage` sign-in, and patterns.md's TinyMCE "UI realities" entry naming `tinymce.get()` / the iframe body as the read on Vue composers
2026-09-13 · U06 · test author tops (revision) · one search lost to the brief naming a sibling `screen-locators.md` to grep for another agent's locators, which no U06 agent created (only `screen-notes.md` exists under `.reports/U06/`) · the test-author brief template naming the locators file only when the orchestrator has seen it created, or the kit writing it from the first probe
2026-09-13 · U22 · test author tomp (revision) · one probe rerun lost to `MySubmissionsPage.openView(name)` returning on the heading's name alone while the previous view's count and rows were still on screen ("Revisions requested (1)" listing the Declined row), which read as a product finding until a by-address read showed "(0)" · the POM's `openView` (or its doc line) waiting for the view's own fetch, or `expectViewHeading` requiring the count on a view switch
2026-09-13 · U22 · test author tojs (revision) · one red run and one snapshot read lost to the runner's error-context.md being the first browser context's page, not the page of the failing locator, so in a multi-actor test (S1's manager control) the "screen record" showed the author's list while the failure was on the manager's page · the test-author brief (or harness.md "Running") saying the aria snapshot is the fixture page's and that a failure on an `asUser` page is read from the log's received value
2026-09-13 · U15 · test author tojs (revision) · one probe rerun (a 30 s click timeout) on scenario 12: after "Return to Workflow" → "Confirm" the workflow panel stays on the Publication page it opened on ("Publication: Title & Abstract"), whose action area is empty, so `WorkflowPage.actionButton('Decline Submission')` never resolves until the "Submission" stage entry is selected · fn-s12 (or U24's Rule 18a) saying the panel stays on the page it was opened on and the decline needs the stage entry selected first, or `WorkflowPage.returnToWorkflow()` documenting that it leaves the selection alone
2026-09-13 · U15 · test author tomp (revision) · one red run (a 3-minute timeout) and one probe spent on scenario 12's "Decline Submission": a published book's workflow panel opens on "Publication: Title & Abstract" and stays there after "Return to Workflow", so the stage's action buttons are absent until the "Submission" stage entry is selected; fn-s12 names the buttons but not the page the panel opens on · fn-s12 (or WorkflowPage.returnToWorkflow's doc line) saying the panel stays on the publication page after the return and the stage entry must be selected before a decision button is pressed
2026-09-13 · U15 · test author tops (revision) · one red run lost to scenario 12's "its title still opens the landing page" and fn-s12's "`article/view/{id}` still answers" (live-probed 2026-09-02 on all three apps): on OPS the visitor's `preprint/view/{id}` answers "404 Not Found" after a screen-driven decline, as the OMP author had already found on a press (T-omp-1) in the same session; the assertion was dropped and the fact returned as T-ops-1 · the orchestrator forwarding a sibling app author's finding that touches a common scenario to the other authors mid-session (or the brief pointing at the sibling findings files), so the second app's author drops the claim before the run instead of after it
2026-09-13 · U24 · test author tops (revision) · three red runs of S11/S14 lost to the shared `WorkflowPage.expectErrorShell` (added mid-session by the sibling OJS author) reading the shell through `getByRole('dialog')`, which the stacked "Error" dialog aria-hides (patterns.md pitfall 6, a race that passed once), and one more to `expectAccessDeniedPage`'s `exact: true` on the non-Author sentence, which on OPS shares its text node with a second sentence; both re-done as local helpers · the brief naming which shared methods the sibling author is adding (or the sibling's additions being probed on every app before they land), so the second app's author reads the shell by `data-cy` and the sentence as a substring from the start
2026-09-13 · U24 · test author tojs (revision) · one red run on scenario 8's scheduled seed: fn-s's "in the same scheme" beside "`published: true` with `issue` Vol 2 No 1 (2015) … the seed My Submissions' scenario 3 uses" reads as a no-decision seed, whose bubble is "Submission"; the bullet's "Production striped" needs `decisions` through Production (U22 S3's shape) · fn-s naming the scheduled seed's decisions outright
2026-09-13 · U24 · test author tojs (revision) · one red run on scenario 14: `EditorialDashboardPage.listReload()` matched `_submissions?` only, while a panel opened by address sits on "Assigned to me", whose fetch is `_submissions/assigned?`; the regex now takes both · the POM doc line (or patterns.md) naming the two list endpoints
2026-09-13 · U24 · test author tomp (revision) · one probe run spent learning where the press's "Monograph" work-type control sits in the header for every state the revised bullets read exactly (`expectHeaderButtons`): after "Library", before the return buttons, in the assistant's and Series editor's views too, never in the author's · the brief (or Rule 6 / fn-s) naming the press's header order per state beside the OJS list, so the OMP author writes the exact reads on the first pass
2026-09-14 · sync · probe driver s14-13304 · one red run on the manager-side control: the workflow menu's "Metadata" link resolves but is hidden until the "Publication" group and its version node ("Unassigned version" …) are opened, a shape only the 13109 probe's `openTitleAbstract` helper knows · a Locator pitfalls line saying the Publication pages nest under a version node that must be clicked first (or `WorkflowPage` exposing that walk)
2026-09-14 · sync · probe driver s14-13304 · one rerun to read the reviewer's "View All Submission Details" rows: the window is a display-only `PkpForm` (`field-heading-element="h2"`), so its labels are `h2.text-xl-bold` with the value in the next sibling and none of the `.pkpFormField__*` classes a Vue form carries · a patterns.md line naming the display-only form's markup beside the editable form's
2026-09-14 · sync · regression reader rr6 · two extra probe runs: the brief says "one `jobs.php run`" but the verb's loop counts with a builder bound at loop start, so a chain's next jobs (created a second later) are invisible and `run` returns with them unreserved (the harness's `runJobs()` loops for that reason); and the workflow's "Publication › References" entry is hidden until the Publication group expands, reached in the end by the dashboard address `…/dashboard/editorial?workflowSubmissionId=<id>&workflowMenuKey=publication_<pubId>_citations` · the regression-read brief saying "loop `run` until `jobs.php total` is 0 (or use `runJobs()`)" and a Locator pitfalls line with the `workflowMenuKey` address for any Publication panel
2026-09-15 · sync · regression reader rr7 · four reads of the plugin grid handler and the contexts/dois controllers to put a scratch journal into the "Crossref enabled, DOIs on, Crossmark ticked, DOI assigned" state (grid `enable` action with csrfToken in the form body, `PUT contexts/{id}`, `PUT contexts/{id}/registrationAgency` with the agency's required depositor fields, `POST dois/submissions/assignDois`), none of which the scenario API carries · the `plugins: {name: {enabled, settings}}` and DOI passthrough keys scenarios.md lists under "not built yet", or a scenarios.md recipe naming those four session-API calls
2026-09-15 · U25 · test author tojs (revision) · one probe run spent learning the main column's child order (language line, status box, then the panels, the box's next sibling being "Submission Files") and that every panel heading, the right-hand column's included, is an `h3`, before the "above Submission Files" and "in that order" bullets could be read exactly; the shared `WorkflowPage` header named the box's classes but not its place · the shared POM's DOM-facts header (now extended) or patterns.md naming the primary column's child order and the h3 heading list as the one read for "which panels, in what order"
2026-09-15 · U29 · test author tojs (revision) · one probe run spent learning the aria shape of the editor's Review Details window (the "Reviewer Recommendation" section as `group` › `heading "Recommendation"` › `p`, the form items as `h2` + answer, the "Recommendation:" line only after the window settles) before S10's "-" read and S13's item reads could be located; the OJS ReviewStagePages helpers for that window named its buttons but not its sections · a DOM-facts header on the Review Details helpers in apps/ojs/playwright/pages/ReviewStagePages.js (now carried by the new helpers' docblocks) or a patterns.md line for the window's sections
2026-09-15 · U29 · test author tomp (revision) · two 5-minute timeouts: a `waitForResponse` armed on the "Create Review Form" window's "Save" with the title empty (the refusal is jQuery validate's, in the browser, nothing posted), and the item window's empty "Save" the same way; the shared `ReviewFormsList.createForm` waits on the POST and nothing in patterns.md says the legacy windows' required-box refusals never reach the server · a Locator pitfalls line: "a legacy (FBV) form's empty required box is refused client-side by jQuery validate (`label.error` after the box, no request), so a refused press is asserted on the label, never bounded by a response"
2026-09-15 · U40 · test author tops (revision) · one run spent on the discussions form's "At least two participants are required for a discussion." (422) when a discussion with only the creator's box ticked was tried as the Mailpit positive control, and a second on a scratch server listing no second manager in that form until seeded as a stage participant; the U05 `addDiscussion` helper's docblock names the creator's ticked box but not the two-participant rule or that the list is the stage's participants · a scenarios.md "Mailpit" line naming the cheapest mail a test can cause per app (a discussion with one seeded stage participant besides the creator) and the form's two-participant rule
2026-09-15 · U40 · test author tomp (revision) · two runs spent on the seeded wizard walk's own Activity Log lines: `POST scenarios/submission` leaves two "Submission metadata updated" rows attributed to the submitter, so an "exactly one line per Saved" read on a seeded item must filter by the saving user or count before and after, and neither scenarios.md nor seed-facts.md says so; one more deliberation on S1's "seeded journal" Given against footnote s1's "submitter's throwaway address" (a throwaway submitter on `publicknowledge` would be a 19th enrolment there, PRINCIPLES A1), settled by running S1 on a scratch press — the spec could say which.
2026-09-15 · U40 · test author tojs (revision) · two red runs and two probe runs spent on the mailbox controls: the Participants "Notify" window answers 500 with no template chosen (nothing in patterns.md), its template body overwrites a message set before the body lands, a closed Notify window leaves its TinyMCE editor registered so a second window on the same page takes the text into the stale one, and a `participants[]` row for a user not enrolled in the seeded journal is invisible in the panel; plus the context scenario lacking the `issues[]` footnote s9 names (the future issue created on the Issues screen instead) · a patterns.md "UI realities" line for the Notify window (template required, body lands after the fetch, editors persist across windows) and `issues[]` on `POST scenarios/context` for OJS
2026-09-16 · U41 · test author tomp (revision) · one red run and one probe run spent on scenario 11's read: the brief and footnote s11 name "the contributors request the reviewer's submission page makes", but the reviewer's submission page fetches no publication on load; the payload (GET api/v1/submissions/{id}/publications/{pubId}) is fetched by step 1's "View All Submission Details" window, so the listener must be armed before that link is pressed; one more red run on the contributor form's Contributor Roles group, a fieldset whose legend class is pkpFormField--options__legend, not the pkpFormFieldLabel a label-anchored field filter expects · a footnote s11 line naming the window that makes the request, and a patterns.md Locator pitfalls line for the options-group legend class
2026-09-16 · U41 · test author tops (revision) · four probe runs spent on the contributor form's CRediT table and affiliation name boxes before one passed: "Add Another Role" preselects "Conceptualization" with that option disabled, so a `selectOption` by that label hangs; clearing a typed institution's primary-language box re-renders its row without the institution name, so a row located by name is lost; the second language's box has no accessible name (A10) — none of it in patterns.md or the spec's footnote d · a patterns.md "UI realities" line for the CRediT row's preselected-and-disabled option and the affiliation row's re-render on an emptied name box
2026-09-16 · U41 · test author tojs (revision) · two probe runs spent finding S11's read (footnote s11 names "the contributors request the reviewer's submission page makes", but the wizard page fetches nothing of the submission and the dashboard's assignments payload holds no authors; the publication GET is the step-1 "View All Submission Details" window's) and S3's publish-window path (the scenario says "continue past Review Publishing Details (its Confirm)", but on the seeded journal Confirm refuses in place until Publication Stage, Revision Significance and an issue-free assignment are picked), plus one red run on a `has:` inner locator built from a dialog scope · footnote s11 naming the request and the control that fires it, S3's bullet naming the panel's required picks, and a patterns.md Locator pitfalls line: "a `has:` inner locator is resolved relative to the outer element — build it from `page`, never from a scoped locator"
2026-09-16 · U43 · test author tops (revision) · one red run spent on a `has:` inner locator built from the panel's dialog scope (`panel.locator('input[name="grantDoi"]')` inside `filter({has})` resolved 0 rows; built from `page` it works), the very pitfall tojs's 2026-09-16 U41 line proposed for patterns.md and which is still not there; one more red run on the panel's error summary, whose `getByText(/Please correct/)` resolves to the whole `.pkpFormErrors` block (summary plus "Go to …" list), so `toHaveText` never matches — a patterns.md Locator pitfalls line for the `has:` scope, and one for the summary block (read with `toContainText`)
2026-09-16 · U43 · test author tomp (revision) · two red runs and one traffic probe spent on scenario 6's read: the brief, footnote h and footnote s6 name a publication payload whose `funders` is emptied on the anonymous assignment and full on the open one, but at the pinned tips the publication GET the reviewer's details window makes carries no `funders` key on either (pkp-lib 747af277af moved funders to the submission schema; the reviewer's pages fetch no submission), so the scenario's control has no request to read and the suite's control had to be the manager's own submission fetch · a footnote h re-probe after any schema move the register records (A13's retirement named the same commit family), and a scenarios.md line that the reviewer's screens fetch the publication only
2026-09-16 · U43 · test author tojs (revision) · one red run on scenario 6's control (the reviewer's publication payload carries no `funders` key in either mode: pkp-lib `747af277af` moved funders to the submission schema on 2026-08-28, the day footnote h was probed, and the spec's revision re-used that read unchecked) and one 6-minute red run on scenario 1's "Cancel" (the Edit Funder panel has only the header's "Close"; the wait ran to the test timeout), plus one red run on `WorkflowPage.gotoEditorial` against a seeded `published: true` item (it waits on a "Workflow:" heading the published item's workflow never shows) · the scenario writer checking a traffic-shaped read against the pinned checkout's schema commits (or footnote h naming the payload and its commit), the bullet naming the panel's real exit button, and patterns.md naming `PublicationScreen.gotoWorkflow` as the open for published items
2026-09-16 · U49 · harness hU49 · one timed-out drive (20 s) and a rerun of the OPS Emails-tab parity script: the docs call the settings forms' save "the form's PUT contexts/{id}" (scenarios.md, the ledger), but the browser's own request is a form-encoded POST to `/api/v1/contexts/{id}` (the Vue form's method override), so a `waitForResponse` keyed on method PUT never matches · a patterns.md "Probe kit" line: wait on the settings forms' save as a POST to `/api/v1/contexts/`, never on method PUT
2026-09-16 · U49 · test author tops (revision) · one red S5 run spent on the scenario's "untick the email box" (the Profile › Notifications row's box reads "Do not send me an email for these types of notifications." and arrives unticked, so the opt-out is ticking it; the run delivered a second "Publication Published" before the form's own `execute()` settled the direction, T-ops-1), and one red S15 run on a `contains: title` marker for the posted acknowledgement, whose body names the server and not the preprint (footnote z names the recipients but not the body) · the scenario naming the box's label and direction, and a scenarios.md "Mailpit" line that the OPS acknowledgements carry the server name only
2026-09-16 · U49 · test author tomp (revision) · one red S5 run on the scenario's "untick the email box" (the Profile › Notifications row's box reads "Do not send me an email for these types of notifications." and arrives unticked on a fresh account, so the opt-out is ticking it, T-omp-1, the same read as T-ops-1), and one 5-minute red S18 run plus a probe on the Production stage's "Production Ready Files" list, which carries no data-cy hook and no page-object helper (the panel is only reachable as the div holding the table named "Production Ready Files"; its "Upload" is the legacy three-step wizard ReviewStagePages already drives for review files, undocumented for production files) · the scenario naming the box's label and direction, and a shared file-manager helper (or a patterns.md line) for the Vue file lists' "Upload" wizard and per-row "More Actions" menu
2026-09-16 · U49 · test author tojs (revision) · four probe runs and two red slices spent on scenario 6's review-round leg: the spec's "pre-filled" premise holds only on the version the round was opened for (a new PMUR version lists it greyed and is refused untouched), the picker is a reka-ui combobox no doc names, and an unscoped `getByRole('option')` also counts the panel's native selects; one red slice on the Notify control (the Production stage's template list has no "Discussion (Submission)", and the helper's template-body wait is the known ci-triage class), replaced by a second submission's own "Publication Published" · footnote h naming which version the round pre-fills for and the picker's widget, and patterns.md naming that the Notify helper is Submission-stage only (or the helper taking the stage's template label)
2026-09-16 · U14 · harness hU14 · one timed-out drive (30 s) on the kit's `screen()` taken right after the Comments page panel's "Approve Comment": the panel closes on the save, and `screen()` waits on `[role="dialog"]:visible` for the dialog it has just seen, so the read hangs to its timeout and the run's later steps (the report) needed a second script; plus one strict-mode miss on the OJS landing page, where the whole comments region is itself an `<article>` so `locator('article', {hasText})` matches the region and the comment alike (take `.last()`) · a patterns.md "Probe kit" line: read a Vue side panel's result after an action that closes it through the page's own text, not `screen()`, and a note that the comments region nests `<article>`s
2026-09-16 · U14 · claim checker ccK2 · one red run on Profile › Contact (a scratch user has no Country, the tab's required field, so the save POST never fires and `ProfilePage.save()` times out with nothing on screen), one red run on the Comments page row (a journal seeded with 30+ `userComments[]` pages the table at 25, so the target row sat on page 2), and one on the page-object path from `checks/<feature>/<chunk>/` (five `..` to reach `apps/`, no example in the kept checks) · a ProfilePage line naming Country as required on the Contact save, a scenarios.md sentence that a bulk `userComments[]` seed pages the Comments page, and a checks/README line with the `apps/` require path
2026-09-16 · U14 · claim check ccK1 · two 30 s timeouts and three re-runs of the `on` phase spent on the Comments tab's Save: the page reloads itself after the POST and lands on Appearance › Theme (no "Saved", the box off screen), so a post-save read of the form hangs; patterns.md "Locator pitfalls" 14 says a Vue settings save shows "Saved" inline, which this one form does not. One more run spent on the side menu: the PrimeVue panelmenu's collapsed groups carry no `aria-expanded` attribute at all (patterns.md pitfall 2 says the entries "are not in the DOM until the group is opened"; they are, in a display:none region), so an expansion loop keyed on `aria-expanded="false"` reads nothing; and the Roles grid's manager row has no Edit (footnote b's "the manager role › Edit" premise), so the permit-off leg needs the editor role.
2026-09-16 · U14 · claim checker ccK3 · two red runs of the page phase on the Comments page (the four tabpanels keep their tables in the DOM, so an unscoped `main table tbody tr` read counted 100 rows for 25 and the first paging click aimed at a hidden panel's nav; then `filter({hasText: /^2$/})` on the numbered paging button never matched because the label sits in whitespace, the accessible name "Go to Page 2" does), one toast-less panel run (the notice's element also holds its "×", so `getByText(text, {exact: true})` never matched it), and a page walk added mid-chunk because a `userComments[]` batch is created within one second and the table's `created_at DESC` order leaves the batch shuffled, the target rows on page 1 or 2 at random (ccK1's note on the hidden panels landed after my first run) · a patterns.md "Locator pitfalls" line for Vue tab groups that keep every panel's table mounted (scope to the visible tabpanel), a Pagination line naming the buttons' "Go to Page N" names, a scenarios.md sentence that a seeded `userComments[]` batch shares one second and so has no fixed order on the Comments page, and a "Probe kit" line that a toast's text element includes its "×" (substring match)
2026-09-16 · U14 · claim checker ccK4 · two extra probe runs on Rule 18's deletion legs: footnote o's "the dashboard row's Delete through the workflow" premise (no stage of a published or unpublished submission offers Delete; it takes unpublish › Decline Submission › Delete, U25 Rule 6), the Users tab's "Remove User" being an unenrolment (the account deletion is "Merge user", a legacy grid dialog whose row action hides behind "Settings"), and three waits burnt on `/api/` response predicates that the legacy $$$call$$$ POSTs (Delete confirm, Remove User, merge-users) never match; plus `mail.messageCount()` useless as a no-mail control with Mailpit at its 500-message cap · a scenarios.md line that a published submission is deleted through unpublish › decline › Delete and that account deletion on screen is "Merge user", a patterns.md Probe kit line that legacy grid actions answer on `$$$call$$$` not `/api/`, and a Mailpit line that the cap makes the total count a non-control
2026-09-16 · U14 · test author tomp · two red runs on reads no doc names: the Comments page's and the Reports table's column headers come back UPPER-CASE through `innerText` (the theme's `text-transform`) while the aria snapshots the claim checks recorded read "Submission" / "Reported By", so an `allInnerTexts()` comparison against the spec's labels fails; and on the press's catalog page the frontend has no `main` (patterns.md says so under "Probe kit" only) and the skip link's target `#pkp_content_main` is an empty anchor, so a `main`-anchored text read hangs and the anchor reads "" · a patterns.md "Locator pitfalls" line: compare Vue table headers by `textContent`, and read frontend page text from `body`
