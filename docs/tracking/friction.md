# Friction — what made an agent's task harder than it needed to be

What cost a screen-driving agent calls, time or retries that a better
brief, doc, kit, seed or fixture would have saved; that agent appends a row
at the end of its task. The maintenance session folds the rows and deletes
them under MAINTENANCE "The daily session".

One line per entry, appended at the end, in this shape:

`YYYY-MM-DD · U<nn> or sync · <role and agent id> · <what cost you calls, time or retries> · <what would have helped>`

Facts only, the same quarantine as everywhere else (nothing
security-shaped, no credentials).

## Entries
2026-09-25 · U51 · claim checker ccK2 · Two mail-phase runs and about four minutes lost: support/jobs.js runJobs() waited its full 120 s timeout twice on the probe fleet (its queue count asks worker 0 on the base port, which is down outside a Playwright run), and the Payments › "Subscription Policies" save that ticks the open-access email box was refused because the scratch journal had no subscription contact (Name, Email and Mailing Address are required on that tab), which nothing said before the run · patterns.md "Probe kit" could name `php lib/pkp/tools/jobs.php run` (and `scheduler.php test --name=…` for a daily task) as the probe-side runner; scenarios.md could say the subscription policy keys are saved through a form that refuses an empty contact, so a script that saves the tab on screen seeds `subscriptionName`, `subscriptionEmail` and `subscriptionMailingAddress` first
2026-09-25 · U51 · claim checker ccK3 · About three calls: each U51 chunk script (K1 landInfo, K2 readerFacts, K3 facts) re-wrote its own reader-page reader (headings, tables, forms with options, sidebar blocks, redirect chain), and the Subscription block premise from code (A13) held on every page but "My Subscriptions", which only a second read of the same block on another page showed · the probe kit could offer one readerPage(page) helper (h1, breadcrumb, tables, forms with options and help, sidebar blocks, nav chain) for the frontend, which has no main landmark; claim-check briefs could say a sidebar block is read on two pages, since page handlers assign template variables the block itself does not
2026-09-25 · U51 · claim checker ccK1 · About six calls and two reruns: the brief says to read `.reports/U51/screen-notes.md` first but it did not exist yet; the partial-expiry drive (td21) assumed an article in an issue published later by "Publish Issue" inherits the issue's date, but a seeded `published: true` article keeps today's date, so the article half could not be reached; and a `pgrep -f` wait on the script name never ended (patterns.md warns, the Bash tool's until-loop habit did not) · the submission scenario could take a `datePublished` (the publication's "Publication date" box) so partial-expiry and date-bound access checks seed their dates; fleet-prep could create an empty screen-notes.md so the "read it first" step never fails
2026-09-25 · U51 · test author tojs · About six runs (one a 6-minute hang): the legacy subscription window's date boxes need a key release for the calendar to take a typed date, and the Escape that closes the calendar closes the whole window (after a "The data on this form has changed…" confirm the recorder accepts) once the calendar is already shut, so the next step waited out the test timeout; its refusals show inside the window only while the window's top is in view (otherwise lib/pkp's NotificationHandler hands them up as a passing notice); and "Save" pressed before the "Locate a User" list has reloaded after a refusal sends no user · patterns.md pitfall 4 could say the jQuery UI calendar reads a typed date on keyup (press Shift after `fill`) and that Escape is safe only while `#ui-datepicker-div` shows; a legacy window's refusal is read in the window or as a `.pkpNotification`; the kept `SubscriptionsPages.js` `typeDate`, `refusal` and `expectUsersLoaded` are the working shapes
2026-09-25 · U17 · harness hU17 · the brief and "Field shapes not built yet" named the key `metadata.datePublished`, a nesting no submission key has (the same page's `urlPath`, `categories`, `coverImage` are root keys and the context scenario's `metadata` means the Metadata settings), so the name had to be re-decided from the builder; and a parity drive lost one run to the OJS page object's export name (`PublicationScreen` in `PublicationMetadataPages.js`, `PublishScreen` elsewhere) · record an unbuilt key in "Field shapes not built yet" at the path it will take beside its siblings; patterns.md "Probe kit" could name the per-app publish helpers (OJS `PublicationScreen.publish()`, the OMP "Publish" and OPS "Post" windows) a parity drive reuses
2026-09-25 · U17 · claim checker ccK5 · td13 asked for the empty "Archives" page on the seeded server, which the harness step's U13 re-run had filled with three posted preprints, so the empty read moved to a scratch server; the legacy Sections grid's ordering "Done" is an <a> link (a button locator silently did nothing and the order was lost on reload, one debug run); and a re-run of a mutating drive overwrote the first run's evidence under the same snapshot names · seed-facts could say which suites post into `publicknowledge` (U13 on OPS) so briefs send empty-list reads to a scratch context; patterns.md "Locator pitfalls" 10 could name the legacy grid's ordering controls ("Order" link, "Done" <a class=saveButton>, "Cancel ordering"); the probe-kit section could advise labelling snapshots by the state in force when a script toggles a setting
2026-09-25 · U17 · claim checker ccK1 · every server-side refusal of the legacy section/series windows is only a pnotify notice (nothing under the box, nothing in `screen()`), so the MutationObserver notice sweep and the recording dialog listener had to be copied again from U13 K5; a run was lost to `SubmissionWizardPage.gotoStep()` on a draft whose later steps are unreached (use Continue), and one to the manager row of Users & Roles › Roles having no "Settings" control · a kit `notices(page, since)` helper plus a recording `dialogs()` listener in `launch()`; a line in patterns.md "Locator pitfalls" that gotoStep needs reached steps and that the Journal/Press manager role row cannot be edited
2026-09-25 · U17 · claim checker ccK4 · td15 and the chunk plan asked to open the sections interface address (`api/v1/sections`, with `searchPhrase`/`typeIds`) in the browser, which the Frame forbids since no screen sends it, so Rule 17, A4 and A5 had to be declared and returned undetermined after reading the Frame against the footnote; and the section state (inactive, editor-only) that every "Submissions" page read needs had to be ticked on screen per section, since `sections[]` has no `isInactive`/`editorRestricted` key · the spec-author brief could say a to-drive question must be answerable through screens (an interface-only claim goes out as code-based ❓ with no td), and the chunk plan could list such lines as declared; a `sections[].isInactive` / `editorRestricted` pair in the context scenario
2026-09-25 · U17 · claim checker ccK2 · td9 named the OJS "Issue" page, which the workflow menu calls "Publication Settings" (key `issue`; OPS "Preprint entry"), and that page refuses a section move on an unscheduled article until the issue question is answered, one debug run; a four-phase all-app run passed the Bash tool's 600 s cap and lost the OPS phase; the notice observer copied from K1 records every pnotify notice twice · the spec-author brief could ask for workflow pages by their menu labels; patterns.md "UI realities" could carry the Publication Settings issue-assignment refusal; the kit could offer the notice sweep (deduplicated) as a helper, and "Probe kit" could say to run one phase per process when a phase drives three apps
2026-09-25 · U17 · claim checker ccK3 · the wizard's current-step locator (`.pkpSteps__step--current`) matches nothing, so an unbounded read burned 30 s per step and one run hit the 600 s cap; `abstract: ""` on the submission scenario silently became the default abstract, so the abstract-less states had to be rebuilt on screen; every OAI page logged the kit's own "Cannot read properties of null (reading 'appendChild')" (`support/motion.js` on an XML document) as an app crash, and `response.text()` handed back the XSLT view on some loads; one run's OMP "Editors" reads were lost to the Series grid's extra "Categories" column · patterns.md could name the wizard's step heading ("Make a Submission: {step}") as the step read and say the rail pills do not navigate; scenarios.md could say an empty abstract is read as absent (or the key could take `null`/`""` as "no abstract"); motion.js could skip documents without a `head`, and the kit could keep it out of `crashes`; patterns.md "Locator pitfalls" 10 could note the Series grid's column order (Title, Categories, Editors, Inactive)
2026-09-25 · U17 · probe · the OMP context scenario does not accept a `series[]` list (its `ContextScenarioBuilder` overrides only `structureKey`/`resolveStructureId`, so `parseStructure` throws "the series list is not supported by this app's context scenario yet"), unlike OJS/OPS `sections[]`, so a scratch press's series had to be built on screen (two extra "Add Series" drives) where a one-line seed would have done · give OMP's context scenario a `series[]` overlay (path/title, mirroring `BootstrapSeeder::addSeries`) so a scratch press can be seeded with series the way OJS/OPS seed sections
2026-09-25 · U17 · probe · the OJS section window has no "path" box (OPS has one), so a wait copied across the two forks hung 20 s, and About › "Submissions" lists only sections with a policy, so a section created from the grid without one never appeared there; two reruns, both now in screen-notes · a context scenario key for a section policy would have saved the second
2026-09-25 · U17 · test author tops · users.md says display names read "Site Admin" for `admin`, but the section window's "Editorial Assignments" box on a scratch server reads "Assign admin admin as Preprint Server manager" (seed-facts has "admin admin"), one red run; the shared SectionsPages.js changed its notice API (`dismissNotices` → `markNotices`, `notices(page, text, {fresh})`) between my switch and my next run, one more red run · users.md could carry the "admin admin" display name (or point to seed-facts); a shared page object still in flux could be announced to the co-author when its exports change
2026-09-25 · U17 · test author tojs · the spec asserts a top-right notice after every legacy save, and patterns.md only says to avoid toasts (parallel lesson 2), so the first run closed standing notices by their "×", which hangs under an open legacy window (five reds, one run) · a patterns.md line on reading a legacy save's notice: `.app__notifications .pkpNotification`, arm the wait before the action, and tell a fresh notice from a standing one by marking the standing ones (SectionsPages.js markNotices), never by clicking "×"
2026-09-25 · U17 · test author tomp · the top-right notices stack downward and each lives 5 s (ui-library Page.vue), so a scenario with three refusals before its save put the fourth notice below the viewport's top third and SectionsPages' `expectNoticeTopRight` failed on a notice that was on screen (one red run) · patterns.md "Waiting strategy" could name the 5 s expiry and the stacking, and point to SectionsPages `noticesGone` for a place read after several notices
2026-09-25 · U17 · claim checker ccK6 · one category-phase run lost 180 s: support/jobs.js runJobs() copied from K3's inline drain answered "X-Test-Key header missing or wrong" to its queue count and threw after the jobs had run, because the probe bag carries no testApiKey and TEST_API_KEY was not in the environment · the probe kit could export a drain(app) helper that sets TEST_API_KEY from checkouts/<app>/.env.playwright and PLAYWRIGHT_BASE_PORT to the probe port, so no chunk script re-copies it
2026-09-25 · U17 · claim checker ccK7 · About two calls: the brief names chunk lines 894–898 (Left out Budget item) and 1511 (surfaces table) that `claims.txt` does not list, and the earlier chunks K1/K4 had read or declared the same interface under the old Frame, so the owned-line count had to be rebuilt by hand from the spec against the checklist · a follow-up chunk brief could paste its exact checklist line numbers (or `lint-spec --claims` could emit Left-out and Reference-table lines), and say which earlier chunk reports it supersedes
2026-09-25 · U53 · claim checker ccK1 · About eight calls and five partial reruns: the Users & Roles row menu does not reliably close on Escape (the next row's "…" click then hangs 30 s), the page buttons are "Go to Page 2" with a capital P, the French list is named "Utilisateurs-trices actuels-elles", and a new role row on the Edit page needs its masthead select chosen before "Save And Continue"; the search's preferred-name, bio and interest ends needed a Profile drive per app because `users[]` has no such keys · patterns.md pitfall 3 could say a Users & Roles row menu is closed by Escape plus a click elsewhere until no menuitem is left (closeMenus() in checks/U53/K1/k1.js); the context scenario's `users[]` could take `preferredPublicName`, `biography` and `interests`
2026-09-25 · U53 · claim checker ccK2 · About five reruns and one 10-minute hang: on the Users list the row menu stays open after Escape (its items render inside the row and the next row's button click times out); the "Merge user" window's and the Settings wizard's filter form is hidden until the grid header's "Search" link is pressed; a phase that threw left a second browser from `launch()` open, so the process never exited; and a session-end read taken right after a link click read the old page · patterns.md pitfall 3 could say the Users list's row menu is closed by pressing its own button again (Escape does not); pitfall 10 could say the legacy user grid's filter opens from the header's "Search" link; the kit could close every browser `launch()` opened when the process ends; the probe-kit section could say a click that navigates is awaited with `waitForNavigation`
2026-09-25 · U53 · claim checker ccK3 · About six calls and three partial reruns: on the Settings wizard's "Users" grid a panel locator built with `has: tr.gridRow` resolved to nothing once a search matched no row, role queries on the grid returned nothing for ~2 s after a legacy window closed ("Add User" stayed disabled), a refused sign-in on the Site Administrator's page made the kit's next `signIn(page, 'admin')` time out on /login, and each rerun of a mutating phase needed a fresh seed because the phase's accounts had already been disabled or created · patterns.md pitfall 10 could say a legacy grid's container is found by `.pkp_controllers_grid` (never by its rows) and its links by CSS text while a closed window's shell is present; the probe kit's `signIn` could sign out and retry once when the login page did not change, or patterns.md could say refused sign-ins go in a second `launch()`
2026-09-25 · U53 · test author tojs · Two runs and one probe: a legacy form's refusal on the older grid ("You need to select at least one role…") never reached S8's page because S7, run beside it as `admin`, fetched and cleared the notification first; and the refusals of "Add User" and "Edit User" arrive in `.app__notifications`, not the `.ui-pnotify` the claim-check notes named · patterns.md parallel lesson 2 could say a legacy form's refusal on screen is a notification too, so tests that read one as the same shared account (`admin`) run in one ordered group (`test.describe.configure({mode: 'default'})`); screen-notes' ccK3 line could name `.app__notifications` as where those refusals show
2026-09-26 · U53 · test author tops · One extra run: the OJS suite's single reload after "Edit User" (its T-ojs-3 workaround) read the ended role again on OPS, because the older grid lists a role ended within the same second; the first run had passed it only because ten seconds of soft reads came before the reload · a test author's finding that rests on "only a reload shows it" could say how long after the save the reload came, so the next app's author reads it as a timing fact and reloads until the value settles
2026-09-26 · U53 · security probe sec1 · Two extra runs: a `_test` scratch context's urlPath is the tag, not `publicknowledge`, so a path built from `app.contextPath` and a `signIn` without `{contextPath: tag}` land in the wrong journal; and `pkp.currentUser.csrfToken` is absent on the page `signIn` leaves you on, so a probe that needs it first opens a page of the journal · patterns.md "Probe kit" could name both
2026-09-25 · U18 · claim checker ccK2 · feeds read with `page.request.get` never reach the kit's run record (the `context.on('response')` listener does not see APIRequestContext traffic), so the RSS 2.0 500 on an empty context had to be read from my own status field, and the legacy settings window's `[role="dialog"]` also holds the editorial header (Tasks, user menu, `##common.help##`), which polluted a whole-dialog button/text read once · the kit could record `page.request` responses (or export a `readFeed()` that records status ≥ 500 under `crashes`), and patterns.md pitfall 4 could say a legacy window's dialog text starts with the editorial header, so read it through its form id
2026-09-26 · U18 · claim checker ccK1 · About six calls and three reruns: `sidebar: ['WebFeedBlockPlugin']` alone was refused on a scratch context although the plugin arrives enabled, and that refused context build left its context behind (path taken, user groups created), so the retry needed a new path; the first Masthead save was refused for the scratch journal's empty "Journal initials" (scenarios.md names only the missing country); the RSS 1.0 address is a download in Chromium, so `page.goto` threw until the read waited on the download event · scenarios.md could say `sidebar` with the Web Feed block needs `plugins.webfeedplugin.enabled` in the same request and that `context.acronym` is needed before any Masthead save; the harness could roll back the context row on a refused build; the probe kit could offer a `readFeed()` that handles a download and records the status
2026-09-26 · U18 · claim checker ccK3 · About four calls and one extra run per app: the site-level plugin setting is stored with `context_id` NULL, while the spec's footnote h (and my first psql read) assumed 0, so the "restored" check read nothing; the refused `sidebar`-alone seed again left a context behind (ccK1's line); and every gateway and site finding had to be driven a second time because "a fact seen in one run only is undetermined" is in the digest-block brief but not in the claim-check brief's run plan · harness.md or seed-facts could say site-level `plugin_settings` rows carry context NULL; the claim-check brief could say up front that each non-holds finding needs two runs, so a script plans a repeat switch from the start
2026-09-26 · U18 · report writer (rep) · one failed OMP run and a script patch: `scenarios/submission` refuses `keywords` (and the other term lists) on OMP with a 400 ("not built for OMP yet"), which scenarios.md's "Metadata" term-list entry does not say, so the terms had to be typed on the book's "Metadata" page; and the RSS 1.0 link's download raced its own navigation wait again (ccK1's line) · scenarios.md could mark the publication term lists OJS/OPS only; the probe kit could offer the `readFeed()`/download helper ccK1 and ccK2 asked for
2026-09-26 · U18 · test author tojs · two red runs: footnote s's recipe "articles published one after another in the seed change in that order" tied on the second (`submissions.last_modified` has one-second precision, and three seeds land within one), so S5's recent list read "Beta, Gamma, Alpha"; and S7's Masthead save stayed grayed out on the scratch journal's missing Country, although footnote s names only `acronym` (seed-facts already records the Country gap) · scenarios.md "POST scenarios/submission" could say two seeds within one second tie in any last-change order (the suite waits on the server's Date header between them), and the spec author's brief could point at seed-facts' scratch-context Country line before any Masthead save; a `context.country` key would remove the on-screen pick
2026-09-26 · U18 · test author tomp · one red run: the shared `WebFeedPages.PublicPages.homeBlock()` reads `[class*="page_index_"]`, a journal's and a preprint server's home page class, while a press's home page is `.page_homepage` (OMP `pages/index.tpl`), so S1's gateway landing and S3's switched-off home page read zero; the screen notes record `.page_index_journal` for OJS only · a shared page object's per-app landmark could be checked on all three apps when it is written (or the screen notes could name each app's home page class), and patterns.md could list the public home page class per app
2026-09-26 · U54 · harness hU54 · Two reruns of the parity drive: the Roles grid's first cell reads "Settings <role name>" (the row arrow's label is inside it), so an exact-name cell match found no row; and the draft calls the role window's submit button "Save" and says a level change unticks "Permit submission metadata edit.", where the recording shows "OK" and the box left ticked, so the first seed stored the box off · screen-notes or patterns.md could name the legacy grid rows' first-cell text ("Settings" + name, match by `hasText`), and the harness brief could say to diff the recorded POST against the key's before the first compare, since the draft's window defaults are unverified
2026-09-26 · U54 · claim checker ccK1 · About eight calls of duplicate work: the Actors table rows K1 owns cite the to-drive footnotes of rules other chunks own (fn-k create window, fn-q remove, fn-o lock-out guard, fn-t site access with a published monograph), so K1 drove K3 and K4 questions in full alongside them; and the grid reads needed two premises no doc names, which cost a rerun each: the Roles filters sit hidden behind a "Search" link and fold away after each choice, and saving a role window reorders the rows (a positional read after a save breaks) · the chunk plan could hand a to-drive footnote to the chunk that owns its rule and let an Actors row cite it as settled there; patterns.md pitfall 10 could name the legacy grids' hidden filter form (`form.filter`, shown by the header "Search" link, re-hidden on every redraw) and say row order after a save is not stable on Postgres
2026-09-26 · U54 · claim checker ccK3 · Two partial reruns (OJS greyed, OMP greyed/save/remove/ops2) and about 25 minutes: a role made with "Create New Role" can land first in the unordered Roles list, where its row has no "Edit", and on a press the seeded context already holds 24 roles so every created role went to page 2 (the list keeps 25 a page after a redraw); a rerun of one phase also overwrote the earlier k3-facts-<app>.json · the harness could seed a scratch context with fewer default-looking extras or the scenarios doc could say the Roles list pages at 25 and has no stable order (create a filler role first, choose 50 in its "Items per page" select); the probe kit could let record() merge by default for a *-facts file
2026-09-26 · U54 · claim checker ccK2 · One full OJS rerun and one failed OPS run: nine `customRoles[]` on top of the journal's 18 installed roles put the last two on the Roles list's page 2, where a name lookup finds no row, and an OPS `scenarios/submission` with `files[]` is a 400 (OPS seeds galleys instead), which the script only learned from the error · scenarios.md `customRoles[]` could say the list shows 25 rows (installed roles: journal 18, press 19, preprint server 5), and the submission key list could say at `files[]` that OPS refuses it
2026-09-26 · U54 · claim checker ccK4 · One extra seed context and a rerun per app: the "Assign Participant" search lists no one already assigned to the submission, so seeding the Section editor as a participant (to read Rule 19 on existing assignments) left nobody to pick when reading what "Assign" pre-ticks for that role; and a drive's outputs had to be scrubbed and the script narrowed after the fact once one observation proved to be for the private file · scenarios.md `participants[]` could say a seeded participant is absent from "Assign"'s search, so a check that reads the window's pre-ticked boxes seeds one member of the role left unassigned; the claim-check brief could say to keep access-restriction drives (a sign-in setting against content) on a narrow state until the first read shows whether the restriction holds, so nothing quarantined reaches `.reports/`
2026-09-26 · U54 · test author tops · Three calls reading the test database: S1 read a new server's five roles out of the spec's order once ("Author" first, no arrow) and in order on the next run, which only the `user_groups` storage order explained (a new context's rows reuse slots freed by deleted ones); and the brief says the error-context snapshot is the fixture page's, while S1's held the `asUser` page and S4's held no snapshot at all · the test-author brief could say an unordered legacy list (Roles, no ORDER BY) is read as a set unless the spec's order claim is unmarked, and that the error-context may or may not carry the `asUser` page, so the assertion's received value is the evidence either way
2026-09-26 · U55 · harness hU55 · One wasted smoke phase: six parallel `createContext` calls through one probe server proved nothing, since each fleet's `php -S` is a single process and serializes them; a real race needed the probe and validation servers together (two PHP processes, one database), which nothing in harness.md says · harness.md "Running" could say that a probe server serves one request at a time and name the base+50 / base+90 pair as the way to run seeds truly in parallel
2026-09-26 · U55 · claim checker ccK2 · One OJS debug run: after a reload the Settings wizard (index/admin/wizard/{id}) opens on the top tab kept in the address (#users), so a side-tab click on "Restrict Bulk Emails" waited out its 30 s timeout; and support/jobs.js runJobs() still needs PLAYWRIGHT_BASE_PORT pointed at the probe port by hand (U51 ccK2 row) · patterns.md "Locator pitfalls" 2 could say tab sets with track-history reopen on the tab in the address, so press the top tab (#setup-button) before a side tab; the probe kit could export a drain(app) that sets the port, key and config itself
2026-09-26 · U55 · claim checker ccK1 · One extra phase per app and a rerun on OJS: the refusal notices of a Vue form ("The form was not saved because…", a 403's error text) are pop-ups that expire after 5 s, and the kit's settled `screen()` (idle first) read after them every time, so Rule 7's and Rule 10's notices looked absent until a MutationObserver caught them; also each permission level's sign-in plus access-denied read cost about 40 s, so a nine-level gate phase took 6–7 minutes per app · the probe kit could record `.pkpNotification` texts as they appear (a page-level observer in `launch()`, listed in the run record like dialogs), and patterns.md pitfall 14 could say the page notice is a 5-s pop-up
2026-09-26 · U55 · test author tojs · About two calls deciding scenario 5: the brief says never to save Administration › Site Settings › "Bulk Emails" from a test and to make the change "through the scenario API or a second browser context", but no scenario key unticks an existing context and the untick is the scenario's behavior under test, so the only route is the screen's "Save" in a second browser, which I put in a `@solo` test (alone in `ojs-solo`, nothing seeds meanwhile, the U08 S8 precedent) · the test-author brief could name `@solo` as the way a test saves a site-wide list the scenario drives, instead of forbidding the save outright
2026-09-26 · U55 · test author tops · Two extra S5 runs: `NotifyTab.goto()` returns once the tab is selected, but the address gains `#notify` a moment later, so the OJS model's `const bookmark = page.url()` right after it read the bare Users & Roles address on OPS and the bookmark opened on "Users" · `NotifyTab.open()` (shared/playwright/pages/NotifyUsersPages.js) could wait for the address to end in `#notify` itself, which also closes the latent race in the OJS suite
2026-09-26 · U20 · claim checker ccK2 · One wasted seed context and a wrong premise: `scenarios/context` with `context.contactName: ""` is accepted and silently stored as "Site Admin", so the sitemap's no-"Contact" end could not be seeded and only the drive's read showed it · scenarios.md `POST scenarios/context` could state that an empty `contactName` reads as absent (default "Site Admin"), or the builder could refuse it with a 400
2026-09-26 · U20 · claim checker ccK1 · One rerun of the not-enabled drive on the three apps and two of the Google Analytics drive: Hosted Journals › "Edit" on a scenario-made context first refuses "Save" in the window on the empty "Journal initials" (seed-facts names only the Country 400), and a plugin drive that failed mid-way left "Google Analytics Plugin" ticked, so the rerun's "unticked" reads were taken on a ticked row · seed-facts could name the initials refusal (or the context scenario could default `context.acronym`), and patterns.md "Probe kit" could say a phase that toggles a plugin reads and restores the arrival state first
2026-09-26 · U20 · claim checker ccK3 · Four prep reruns on three apps: a Vue form located as `wf().locator('form').filter({has: <wf()-rooted locator>})` never matches (the has-locator is evaluated inside the form, so its own dialog root fails), twice; Permissions & Disclosure's "Copyright Holder" and "Copyright Year" are disabled on an unpublished version, which nothing said, so the licence moved to Distribution › License; an OMP format row's text starts "Settings PDFDigital (on physical carrier)", so a `^PDF` match found no row; and OJS offers the URN "Assign" only once the article is in an issue · patterns.md "Locator pitfalls" could say `filter({has})` takes a selector relative to the filtered element (a page-level `[id=…]`, never a `wf()` chain) and list the format-row text shape; scenarios.md could say a published version's publication pages stay editable (the warning line) while an unpublished one's copyright boxes are locked, and that an OJS URN needs the issue first
2026-09-26 · U20 · claim checker ccK4 · One OJS phase lost to the Bash cap and one wasted read: a multi-value phase ran 561 s in one process, and the kit's motion init script throws "Cannot read properties of null (reading 'appendChild')" on the XSLT-rendered `{journal}/oai` page, which the run record files under `crashes` as a page script error of the app · patterns.md "Probe kit" could say to run any value-axis phase detached from the start, and `shared/playwright/support/motion.js` could guard `document.head` so XML/XSLT pages leave no false crash
2026-09-26 · U20 · test author tojs · Three single-scenario reruns: a legacy plugin row's controls stay open (`a.hide_extras`) after its settings window closes on "Cancel", so a reopen that presses the arrow again closes them and waits out its timeout; the Google Analytics window's form wraps its fields in a `fieldset.error#webFeedSettingsFormArea`, so the Web Feed window's `label.error, .error` refusal locator matched two; the "Search Indexing" help text shows in `.v-popper__popper--shown`, not `[role="tooltip"]` · patterns.md pitfall 10 could add that a row's controls stay open after its window closes (reuse the next `tr`, never press the arrow again); screen-notes K4 could name `label.error` as the GA window's refusal
2026-09-26 · U19 · harness hU19 · One parity rerun: scenarios.md calls the `plugins` key "the Plugins grid's `plugin` id, e.g. `announcementfeedplugin`", so the JATS row was located as `tr[id$="-row-oaimetadataformatplugin_jats"]`, but the grid keeps the class name's case in the row id and the request (`OAIMetadataFormatPlugin_JATS`), which only an all-lowercase class hides · scenarios.md `plugins` could say the key is the class name lowercased while the grid's row id and `plugin=` keep its case (match the row id case-insensitively), and `WebFeedPluginRow` could take the plugin name as a parameter so any plugin row is reusable
2026-09-26 · U19 · claim checker ccK1 · One full OJS re-run (face, site, access, about six minutes) and a false lead: every OJS record request answered 500 for the first half hour because harness journals seeded with `doiVersioning: true` put the install into the state that breaks OJS OAI on Postgres (ccK2 turned it off mid-run); and raw OAI reads through a separate request context never reach the kit's `crashes`, so the 500s had to be collected from the probe server log · the harness log could say which install-wide states a seed leaves behind that change another feature's answers (a journal with DOI versioning on changes every OJS journal's OAI query), and fleet-prep could reset them before the claim check; the probe kit could offer a `rawGet(app, url)` that records its status into the run record like browser traffic
2026-09-26 · U19 · claim checker ccK2 · A whole read run of OJS (about 110 OAI requests, ten minutes) answered 500 and cost a diagnosis round: the hU19 harness smoke and parity left four scratch journals with DOIs and "DOI Versioning" on, and while any journal has it on every OJS OAI list, record and Identify fails on Postgres (the per-version union), so each later chunk on the fleet would have met the same wall; the chunk plan also assumed publicknowledge has no published item, but OJS and OPS suites leave published and deleted records there · the harness brief could say to switch back, or list in screen-notes, any seeded setting that another context's reads depend on install-wide (here doiVersioning), and fleet-prep could report it; the chunk plan could name publicknowledge's OAI content as "whatever the suites left" and send list reads to scratch contexts
2026-09-26 · U19 · claim checker ccK3 · About eight calls and three reruns: a locale-map `abbrev` on the context scenario's first (renamed) section is stored as the string "Array" (so `section: 'ART'` then 400s "Unknown section abbrev" and the set reads `{path}:Array` until the section is re-saved on screen); OJS Publication Settings refuses "Pages" and "Summary of Changes" without the "Issue Assignment" radio, which cost an unpublish-and-republish of two articles; a shared request context carried the app's `currentLocale` cookie so every later raw OAI read silently came back in French; and loc.gov serves MARC21slim.xsd only behind a Cloudflare challenge · the context builder could store a locale-map abbrev per locale (or 400 on it), as it does the title; scenarios.md could list `pages` and `summaryOfChanges` among the unbuilt publication keys with that page's issue-choice rule; patterns.md "Probe kit" could say an OAI raw read uses a fresh request context per request
2026-09-26 · U19 · claim checker ccK4 · About six calls: the OPS seed failed on `galleys: [{file: 'article.pdf'}]` (the OPS fixture is `preprint.pdf`, which scenarios.md's `galleys[]` example does not say), `app.mail.count({contains})` threw "every Mailpit read must be scoped by a recipient (to:)", the site's contact email box has no `name="contactEmail"` (it is multilingual), and the JATS window drive timed out on `tr.gridRow` because the Website page's hidden Languages grid rows come first · scenarios.md `galleys[]` could name the per-app PDF fixture (`article.pdf` OJS, `preprint.pdf` OPS); patterns.md "Probe kit" could say `mail.count()` needs `to:` and that Plugins-grid waits are scoped to `#pluginGridContainer`
2026-09-26 · U19 · claim checker ccK5 · About six calls and a stalled ten-minute run: a Settings-page sweep that clicked every role="tab" (hidden inner tabs included) waited the default click timeout on each hidden one on OMP; and the A11 premise ("the app fails with a server error") came from reading the plugin callback without Hook::call, which catches a plugin exception and logs "Plugin … failed to handle the hook" while the request answers 200, so the drive had to go to the probe server log to explain a clean unpublish · patterns.md "Probe kit" could say a page-wide text sweep reads document.body.textContent instead of clicking tabs; the spec-author brief could say a code-read crash inside a plugin hook callback is a log line, not a 500, until driven (lib/pkp Hook::call, pkp-lib discussion 9083)
2026-09-26 · U19 · test author tojs · About four calls: the brief pointed to "JatsBodyTextPages.js and the plugin tab helpers" for Settings › Website › "Plugins", but that file has no plugin-grid helper; each suite has built its own row class for one plugin (WebFeedPluginRow, CitationStyleSettings, UrnPluginSettings, CustomContentPages), so a fifth, by name, went into OaiPages (PluginGrid) · one shared plugin-grid page object (row by name, box, disable question, arrow, "Settings", category) that briefs can name, or briefs that name the existing per-plugin classes as patterns rather than as helpers
2026-09-26 · sync · regression reader rr12798 · One retry: a CLI PHP script that loads an app's `tools/bootstrap.php` (to check a class declaration against the checkout's code) fails with "Cannot read configuration file …/config.inc.php" until `PKP_CONFIG_FILE=<root>/config.test.inc.php` is set, which the kit docs do not say for code-level checks outside `bin/probe.js` · harness.md could name `PKP_CONFIG_FILE=checkouts/<app>/config.test.inc.php php …` as the way to bootstrap an app in a one-off CLI read (both lines)
2026-09-26 · sync · claim checker ccK4s26 (U32 K4) · Two retries: the brief said the never-reviewed case "must now press 'Move to Submission'", but the workflow button's label comes from the ui-library's own `t('editor.submission.decision.backFromCopyediting')`, not from the submission API's label, so the screen still offers "Move to Review" and only the decision page shows the new label; and the kept script's composer typing used `tinymce.editors`, which TinyMCE 6 no longer has (20 s timeout per move) · a sync brief that names which layer renders each place the changed string reaches (PHP template, API map, ui-library) before the drive; patterns.md could name `tinymce.get()` as the only editor list
2026-09-26 · sync · flake diagnostician u13s3 · Two calls and a near miss: the brief asks for `--repeat-each 5` at eight workers but pins OJS to PLAYWRIGHT_BASE_PORT=8020, where an eighth worker's port 8025 is Mailpit's (a run would adopt it through reuseExistingServer), so the verification ran at four; and a one-file run shares `apps/ojs/playwright/test-results` (cleaned at each start) and the timed-shards recorder with the session's runs unless `--output` and `--reporter=list` are passed · flake briefs could give a base port whose whole band (base..base+7 and base+90) is free, e.g. 8030, and harness.md "Running" could say a side run passes `--output <own dir> --reporter=list`
2026-09-26 · sync · flake diagnostician u30 · About six runs and one command lost to the Bash cap: the class had been watched for a trace since 2026-09-12, but `--trace on` hides it (0 / 5 traced, 5 / 8 untraced on the same database) because a traced action outlasts the two animation frames the race lives in, and `PLAYWRIGHT_CPU_THROTTLE=6` did not reproduce it either (0 / 3); what did was an init script holding `requestAnimationFrame` (10 / 10), and a `--repeat-each` of a `test.slow()` test with a lowered `--timeout` still ran past 600 s because slow() triples it · patterns.md "Probe kit" (or support/throttle.js) could offer a frame-hold lever beside the CPU throttle, and the flake-diagnosis brief could say that a class a trace never catches is a sub-action timing window
2026-09-26 · sync · flake diagnostician u14 · About 16 minutes and one command moved to the background: two OPS lever runs hung to the 8-minute test timeout on a click at a task row the app had deleted (the config sets no `actionTimeout`, so a missing element's click waits out the test), and a cross-app probe needed a per-app branch because the OJS Comments page object names the delete `deleteFromRow` while OMP/OPS split it into `deleteCommentFromRow` + `confirmDeleteComment` · the config (or the page objects that click rows by content) could fail a click on a missing element within a named bound; the three `ReaderCommentsPages.js` could share method names for the same screen action; the flake brief could name `--output` for side runs
2026-09-26 · U14 · security probe sec26 · One wasted probe run and a couple of retries: a signed-in non-manager's CSRF token (pkp.currentUser.csrfToken) is exposed only on a frontend page that loads the pkp JS with the comments component (the article landing page), not on the journal index, so a probe that must POST as a Reader has to route through such a page; and createSubmission returns the submission id as `submissionId` (and `publicationId`), not `id`, so `sub.id` silently became `article/view/undefined` · patterns.md "Probe kit" could name `pkp.currentUser.csrfToken` as landing-page-only for non-backend roles and list the createSubmission return keys (`submissionId`, `publicationId`, `userComments[].id`, `userComments[].reports[]`)
2026-09-26 · sync · flake diagnostician fixAD · A hazard, no retry: the slot's ports put each run's validation server (base+90) on another agent's band: OJS 8060 → 8150 (the OMP probe server, adopted by `reuseExistingServer`), OMP 8140 → 8230 (fixC's OPS worker 0; a run started while mine ran would adopt my OMP server), and u14/fixC collide the same way (8030 → 8120, 8130 → 8220); also the brief asked for eight workers while pinning PLAYWRIGHT_WORKERS=4 · flake briefs could allot bases whose base..base+7 AND base+90 are free of every other slot and probe port (or config-factory could take PLAYWRIGHT_VALIDATION_PORT), and name one worker count
2026-09-26 · sync · flake diagnostician fixC · Two waits and a lost background call: the validation server sits at base+90, so the brief's bases collide across fleets and apps (my OJS 8040 → 8130 = my OMP base; my OMP 8130 → 8220 = u14's OPS base; fixAD's OMP 8140 → 8230 = my OPS base, which my runs found busy and had to wait out), and every run of an app rewrites the one shared `config.test.validation.inc.php` with its own port; and a `while pgrep -f` wait on my own run matched itself (patterns.md warns of it, for probes only) · flake briefs could give bases whose base..base+7 AND base+90 are clear of every other slot's (e.g. OJS 8300, OMP 8400, OPS 8500), and config-factory could let a side run skip the validation server (or name its port) when the selected tests do not use `variants`
2026-09-26 · sync · flake diagnostician esc · One red run and a rerun: the Bash tool's 600 s cap needs `timeout` around a long ×5 run, and a `timeout`-killed Playwright run leaves its web-server restart loops running (parented to init), so the next run on that port band adopted them and an OPS run drove OMP pages; and the U30 rAF lever existed only as generated spec copies, so exposure checks across 17 files had no lever to switch on · harness.md "Running" could say to kill `php -S` loops on the band after a killed run (or config-factory could refuse a band whose server answers for another app); the lever is now `PLAYWRIGHT_RAF_HOLD_MS` in `support/throttle.js` (documenting it in patterns.md "Probe kit" is the session's call)
2026-09-26 · sync · flake diagnostician fixB · Three lost rounds: a ×5 run of two OMP files outlived the Bash 600 s cap and went to the background (split per file after that); the first "row moved on disk" lever (a plain no-op UPDATE) left index-scan plans unchanged (a HOT update keeps the row's index entry), so two lever runs were green for the wrong reason until the lever became a non-HOT update; and the brief both asks for a harness seed change to be re-run and forbids touching `checkouts/`, where `npm run mount` writes (mounted ojs and ops to verify) · flake briefs could say a seed change is verified after `npm run mount <app>` (or name who mounts), give a per-file split rule for ×5 runs, and the probe kit could carry the "move a row past another" lever (`.reports/flake-s26/fixB/lever.js`) for order-class hunts
2026-09-26 · sync · flake diagnostician foldtests · One wasted lever run (4 reds for the wrong reason): a `page.route` lever that rewrites API answers with `route.fetch()` throws "Request context disposed" at teardown when a refetch is still in flight as the context closes, and Playwright fails the test on it; the fix (catch around `route.fetch()`/`fulfill`) was found only after the run · patterns.md "Probe kit" (or a shared lever helper beside fixB's `lever.js`) could carry a teardown-safe "reorder an unordered list in the API answer" route lever for order-class hunts
