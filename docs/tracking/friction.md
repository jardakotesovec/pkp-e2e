# Friction — what made an agent's task harder than it needed to be

Any agent may append here, at the end of its task, when something cost it
calls, time or retries that a better brief, doc, kit, seed or fixture would
have saved. Nobody reads this file mid-feature. A maintenance session on a
quiet day, or the maintainer at review, reads it and folds the real
patterns into RUNBOOK, TEMPLATE, the harness or the seeds through the
usual maintainer review. A row is deleted when it has been acted on or
judged noise; git keeps it.

One line per entry, appended at the end, in this shape:

`YYYY-MM-DD · U<nn> · <role and agent id> · <what cost you calls, time or retries> · <what would have helped>`

Facts only, the same quarantine as everywhere else (nothing
security-shaped, no credentials). A rule you disagree with is fine to name;
say what it cost.

## Entries
- 2026-09-05 · U29 spec author · Quoting labels from code: the OJS `locale/en/manager.po` overrides several `manager.setup.reviewOptions.*` keys that lib/pkp also defines, so a lib-only grep returns the wrong string and the app override has to be grepped second; a one-line note in patterns.md ("grep the app's locale first, lib/pkp second") would have saved a round-trip.
- 2026-09-05 · U29 · probe pB · Two reruns from premises: the item said press "Upload File" under "Submission Files" (the panel is the Vue file manager's button "Upload"), and the kit's `getByRole('dialog').last()` idiom returns the full-page workflow panel, not the legacy upload modal nested in it; a third rerun came from `Control+A` not selecting all in TinyMCE on macOS · a screen-notes line for the Submission-stage upload button and a kit helper `selectAll(page)` (Meta+A on darwin) or a note in patterns.md "Probe kit"
- 2026-09-05 · U29 · probe pA · Three reruns from kit/DOM premises no doc carried: the Vue form's Save is a POST (the brief's Reference table says PUT), field errors use class `pkpFieldError` (capital E, so the usual `[class*=error]` sweep misses them), and the Add Reviewer entry button is named after the reviewer ("Select Scratch Reviewer"), while U28's notes quote "Select Reviewer" · a line each in patterns.md "Probe kit" (save verb, error class) and in the U27/U28 locators would have saved them.
- 2026-09-05 · U29 · probe pC1 · P15's premise ("record whether a second language tab exists") could not be settled from the scenario API: form languages are a separate context setting (`supportedFormLocales`, Website › Setup › Languages "Forms" column) that `POST scenarios/context` does not accept, so a scratch journal with `supportedLocales: ['en','fr_CA']` has single-language forms and the answer needed three extra runs through the Languages grid · a `supportedFormLocales` key on the context scenario (scenarios.md "Configuring a scratch context") and a seed-facts line saying the seeded journal's forms are English-only.
- 2026-09-05 · U29 · probe pD · Two reruns from the roster premise: the item names `reviewer.julia` as the scratch journal's reviewer, but a seeded reviewer not enrolled on the scratch context is refused the wizard (`roleBasedAccessDenied`), so the reviewer-side steps needed a second context with a throwaway `externalReviewer` in `users[]`; pA had recorded the same gap for the Add Reviewer search only · a users.md/scenarios.md line "a scratch context's reviewer must be created in `users[]`; the seeded reviewers are not enrolled there" and a probe-list premise that says so.
- 2026-09-05 · U29 · probe pC2 · Five reruns from DOM/kit premises no doc carried: the legacy grid's row actions live in the NEXT `tr` (not inside `tr.gridRow`), fixed-position modals have `offsetParent === null` (a visibility filter reads them as hidden), the side modals open empty and fill seconds later (a `readDialog` without a content wait records a blank "Preview"), and the item's seed (`reviewer.julia`, `reviewForm:` on an inactive form) cannot be built as written · a patterns.md "Probe kit" paragraph on legacy grids (row/actions rows, `getClientRects`, wait for `.ui-tabs-nav li`) and a scenarios.md line that `reviewForm:` needs an active form and a reviewer from `users[]`.
- 2026-09-05 · U29 · checker K1 · One rerun (new scratch) because the only throwaway reviewer was already assigned to the seeded round, so the Add Reviewer search offered nobody (`Select …` never appeared); a second `externalReviewer` in `users[]` is needed whenever a script both seeds a request and opens Add Reviewer. A second lost read: `nav a` on the dashboard is empty until the Vue sidebar renders (`idle()` then wait for `nav a`) · a scenarios.md line under `reviewRounds[]` ("an assigned reviewer leaves the Add Reviewer search; seed a spare") and a patterns.md "Probe kit" sentence on waiting for the sidebar.
- 2026-09-05 · U29 · checker K3 · One rerun because a typed `?step=2` on an API-seeded accepted request lands on step 1 until "Save and continue" is pressed once — pB's note said typed steps "work", pC1/pD's said they do not, and the checker trusted the earlier one · a single line in scenarios.md under `reviewRounds[]` ("a seeded `accepted` reviewer starts at step 1; typed `?step=n` renders only after the earlier steps' buttons") would settle it for every reviewer-side agent.
- 2026-09-05 · U29 · claim check K2 · Five reruns (11 script runs for a 40-call chunk) from premises no doc carried: the workflow page is a dialog (`screen().text.main` reads the dashboard list behind it, so the round status line and the suggestions panel looked absent); the submission wizard's rail is not clickable, every uploaded file needs a type before "Submit", and an unassigned section editor gets "Error" on a new submission's workflow; Settings › Workflow › "Emails" is not the templates list (that is "Add and edit templates" → `manageEmails`) · a patterns.md "Probe kit" paragraph on the workflow dialog and the wizard walk, and the scenarios.md "Field shapes not built yet" keys `reviewerSuggestions[]` and a `files[]` with a genre, so the suggestions panel can be seeded instead of walked.
- 2026-09-05 · U29 · checker K4 · Two reruns from premises no note carried: a legacy grid row's arrow, clicked a second time to "close" the controls, hangs the click itself (pC1's note said only that the action link never shows); and a form in use offers no "Edit" (Rule 12), so a checker that seeds an active form carried by a reviewer must open its preview from the row's "Preview" — the script's `openEditWindow` waited 30 s for a link that never exists. A third lost read: the listbuilder's "Add Item" renders the new row after the click returns, so `.last()` on the visible boxes fills the previous row · a patterns.md "Probe kit" paragraph on legacy grids (arrow once; row actions depend on state; listbuilder rows arrive late) would save every agent on the Review Forms, Sections and Languages grids.
- 2026-09-05 · U29 · checker K6 · Two reruns from premises no note carried: the reviewer wizard's hidden step-1 "Save and continue" stays in the DOM, so pD's button filter with `.first()` pressed it three times and never reached step 3 (pD's own note blamed a stale locator); and a Vue form's "Save" is disabled after a refused save until the errors are fixed, so the second save hung for 30 s · one patterns.md "Probe kit" line ("filter wizard buttons with `:visible`; a refused Vue save disables Save until the flagged boxes change") would have saved both.
- 2026-09-05 · U29 · test author OJS · Six debug reruns from premises no note carried: a TinyMCE box on a Vue settings form must be `initialized` before a fill or the save silently posts nothing for it; the "Response Options" listbuilder keeps one row editable (the visible-box count never grows, only the row count); "Reviewer Recommendations" titles are `th` row headers; the Add Reviewer window has no "Cancel"; a `hasText` row filter is case-insensitive so "Inactive form X" matched `row('Form X')` · a patterns.md line on Vue rich-text fields (wait for `tinymce.get(id).initialized`, then `body.fill`) and one on listbuilders (rows, not boxes) would have saved four of the six.
- 2026-09-05 · U29 · test author OMP · One rerun from a shared page-object race no note carried: a Vue settings form's "Saved" footer briefly coexists with the leaving "Saving" span (two `.pkpFormPage__status` elements), so a strict `toHaveText` on the bare locator fails when the save answers between two polls · a patterns.md line on Vue form status ("filter the status span by its text; the transition leaves two for a beat") would have saved it for every settings-form suite.
