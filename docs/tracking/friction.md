# Friction — what made an agent's task harder than it needed to be

Any agent may append here, at the end of its task, when something cost it
calls, time or retries that a better brief, doc, kit, seed or fixture would
have saved. Nobody reads this file mid-feature. A maintenance session on a
quiet day, or the maintainer at review, reads it and folds the real
patterns into RUNBOOK, TEMPLATE, the harness or the seeds through the
usual maintainer review. A row is deleted when it has been acted on or
judged noise; git keeps it.

One line per entry, appended at the end, in this shape:

`YYYY-MM-DD · U<nn> · <role and agent id> · <what cost you calls, time or retries> · <what would have helped>`

Facts only, the same quarantine as everywhere else (nothing
security-shaped, no credentials). A rule you disagree with is fine to name;
say what it cost.

## Entries


2026-09-04 · U05 · probe agent pR · Two toast/notice reads came back empty and cost a re-probe script and three extra browser runs: the wait keyed on a `PUT` for the Emails settings save (the form POSTs `…/api/v1/contexts/{id}`) and on `.pkp_notification` for the Password tab (the placeholder exists empty, and the in-tab refusal notice removes itself after ~6.7 s) · A patterns.md line naming the save method per form family (Vue pkp-form = POST to the entity's API address; legacy AjaxFormHandler = POST `…/$$$call$$$/…/save-…`) and the in-place notice's lifetime, or a kit helper `afterSave(page, formLocator)` that waits for the form's own POST and returns toasts plus in-place notices read right after it.
2026-09-04 · U05 · probe agent pR · P28's "no role anywhere" case needed a detour: `POST scenarios/context` refuses `roles: []` and a context-level registration always enrols a Reader; only the site-level Register page with no context box ticked produces the state, found by reading `RegistrationForm.php` · A `users[]` entry (or a site-level scenario) that mints a user with no role, or a seed-facts line naming the site-level Register page as the one path.
2026-09-04 · U05 · finalizer · Two footnote mechanisms (the needs-editor listener's second check, the OPS task-link policy) had to be read from `checkouts/` because the digest's blocks carry no code anchor for a `corrects` block that changes a rule's mechanism, and the draft's footnote named neither; the finalizer's brief could allow one grep per new register entry, or the digest's block could carry the report's anchor line when the report has one · A digest field `Anchor:` (optional, the symbol the probe or the author already named) would have saved the two reads.
2026-09-04 · U05 · rewrite agent (rewrite-1) · two of the three blockers were the shipped convention itself ("accounts and passwords in the footnote", "the footnote names the mail catcher"), which every persona reading the body alone will flag as a blocker on every spec · the persona brief could say the preamble's footnote pointers for accounts, passwords and the mail catcher address are the campaign convention and not a stumble, so the readability gate does not spend a rewrite cycle on them each feature
2026-09-04 · U05 · rewrite agent (rewrite-2) · scenario 9's confirm button ("OK") and the issue email's subject were in pA P26 but not folded into footnote s9, so the rewrite had to leave a fact stumble that one footnote sentence would close · the fold brief could ask that every label and subject a scenario presses or reads be carried into its footnote when the probe report quotes it
2026-09-04 · U05 · claim checker K4 (ccK4) · four re-runs went to locators no doc carried: the Login page's password `maxlength=32` on a 15-character scratch tag (users.md names the trap for the roster only), the OJS issue row's folded "Publish Issue" (pA's report said it was visible), OMP's "Schedule For Publication" stage action landing on the Publication page instead of the window, and the reviewer step-3 form's `select#reviewerRecommendationId` / "Save and continue" buttons (no U26/U27 page object or note drives the reviewer's steps) · a `tag()` that caps scratch usernames at 14 characters (or a kit `fillPassword` helper), and a reviewer-side page object or screen-notes section from U27, would have saved the four runs
2026-09-04 · U05 · claim checker ccK1 · two extra OJS runs (~10 min) shaking out the wizard drive from its start page: the abstract is required on a scratch journal (not stated in seed-facts), `iframe[id*="bstract"]` matched the title editor of the `titleAbstract` form, and `getByRole` skips the hidden front-end user-menu links · a kit or fixture helper "submit through the wizard from the start page" (the suites have one per app in `apps/<app>/playwright/pages/SubmissionWizardPage(s).js`, which checks may not import) and a seed-facts line on what the wizard demands on a scratch context
2026-09-04 · U05 · claim checker ccK3 · three extra OJS runs: the Register page password box `maxlength=32` truncated a 34-character scratch password at registration (the kit strips the attribute only on the Login page, so the later sign-in failed), the profile tab bar renders after `idle()` so a bare `count()` on the "Notifications" tab read 0, and the site announcement form needed pA's locator recipe rather than a generic dialog wait · a `tag()` cap at 14 characters or a kit `register(page, …)` helper that uses a 32-character password, and a screen-notes pointer to reusable form recipes per screen (pA's announcement form, pR's register form) instead of each checker re-deriving them
2026-09-04 · U05 · claim checker K2 (ccK2) · four remainder runs on OPS went to the discussion display modal's second message: the "Add New Message" press is swallowed at times, the editor's wrapper intercepts a pointer click once a reply is listed, and `frameLocator('iframe').last()` can resolve to a hidden iframe (pN/pU/ccK1 notes cover only the first message); the scenario premise "Manager A's window reads No Items" also cost a re-read because the seeded `submitted: true` carrier had already given every Manager a task row · a screen-notes line on second-and-later discussion messages (now added) and a seed-facts line saying a seeded submission raises the Managers' needs-editor task (no email) would have saved them
2026-09-04 · U05 · merge agent (claimcheck-merge) · one wasted call locating the spec's footnotes: the brief said to map line numbers to footnote IDs but not that footnotes are anchored as `<a id="fn-<id>"></a>` followed by `**<id>** —`, so a grep for the Markdown `[^id]` shape came back empty · a sentence in the merge brief (or TEMPLATE's footnote section) naming the anchor shape
2026-09-04 · U05 · fold agent (fold-log-2) · one rewrite cycle on footnote e: putting "no screen" into a footnote makes `--claims` tag every line of every block that cites it (25 lines of screen-verified Rule 7a and Rule 6 preamble came up `no-screen (fn-e)`), so the declaration had to move onto Rule 7a's own line and the footnote reworded around the regex · a sentence in the fold brief or TEMPLATE's lint section saying the no-screen phrase belongs on the claim's line, never in a footnote cited by verified lines, and naming the phrases the regex accepts
2026-09-04 · U05 · rewrite agent (rewrite-3) · one extra evidence read on the scheduled-task blocker: the brief said the preamble's "When emails arrive" paragraph and its footnote pointer cover the three scheduled-task rows, but footnote s0's recipe is the queued-job runner and footnote e says the fleets run no scheduler, so the rows could not honestly point at that recipe and the rewrite had to say instead that no scenario produces those emails · a seed-facts or harness line stating whether `jobs.php run` also runs the scheduled tasks (`OpenAccessNotification`, `EditorialReminder`, `StatisticsReport`) or a separate scheduler command does, so a writer can give the reader a by-hand pointer or know there is none
2026-09-04 · U05 · test author (tojs, OJS) · two whole-project runs (~5 min each) instead of the feature's 10 tests: selecting the serial spec makes Playwright run its dependency projects (`shared`, `ojs`) in full, and neither the CLI path filter nor `--grep` narrows them; one probe (two runs) went to the Create Issue form's silent refusal of an empty title (no error line, no doc said the "Title" show-box arrives ticked) · a harness.md "Running" line: run a serial spec by itself with `--project=<app>-serial --no-deps` on a warm install, and the feature's parallel file with the plain path; and an Issues screen-note (now in U05's screen-notes) or a small `IssuePage` helper that fills the title
2026-09-04 · U05 · test author (tomp, OMP) · the feature's `screen-notes.md` had grown to 1342 lines by step 8, most of it the kit's auto-appended locator tables repeating the same row (the Tasks bell row alone appears about twenty times across pT/pN/ccK sections), so the one file every screen-driving agent must read first no longer fits one read and had to be filtered by grep; the hand-written sections (about 80 lines) held everything the suite needed · the kit's exit hook deduplicating locator rows already present in the file (same app, screen, element, locator), or writing the tables to a sibling `locators.md` and keeping `screen-notes.md` for the hand-written notes
2026-09-04 · U05 · test author (tops, OPS) · one extra 200 KB read: `screen-notes.md` is mostly the kit's appended locator tables (five agents, ~1,300 rows), and the hand-written notes every later agent actually needs sit under "— written by hand" headings scattered between them, so the file had to be grepped for prose before it could be read · have the kit append its locator tables under one "Locators" section at the end of the file (or into a sibling `screen-locators.md`) so the hand-written sections stay together at the top
2026-09-04 · U05 · rewrite agent (rewrite-4) · one extra read of the K4 probe script: persona-4 asked what address the three broken Unsubscribe links actually have (does the "?" stay; what number counts as unknown), and cc-K4 names the probes only by label ("the code deleted", "an unknown number"), so the resulting address shapes (`?id={number}`, 999999999) had to be read from `k4.js`; the previous rewrite had guessed a shape ("?&{number}") that was never probed · a claim-check brief line: when a block confirms a broken or altered address, record the exact address shape driven, not the probe's label
2026-09-04 · U05 · rewrite agent (rewrite-5) · one extra spec read: the brief asked for the pass a tester should expect on a press for the auto-assignment row, which turns on whether *Submission wizard* A8 (the defect) is recorded for OMP, and neither the brief's evidence list nor cc-K2 263 ("OMP not driven") says; U21 A8's summary row had to be opened to see it carries no application badge · when a brief asks a rewrite to state a cross-spec expectation, name the other spec's entry and its badge scope in the evidence list
2026-09-04 · U05 · rewrite agent (rewrite-7) · one contradiction to resolve before the blocker could be written: the brief's parenthetical attributed the Managers' needs-editor task and email to the "A new article, "Title," has been submitted." row's own boxes, while scenario 3, footnote s3's box names and A10 tie them to the "…needs to be assigned." row; three extra reads (scenario 3, s3, f-a10) to be sure which row the evidence drove · a rewrite brief that names a tab row should quote the row sentence from the scenario or the footnote's box name that was actually driven, not restate it from memory
2026-09-04 · U05 · persona reader (persona-8) · one wasted call: the brief names passages by rule number, row sentence and "first sentence"/"last sentence", so the first attempt read the whole body (67 KB, spilled to a file) before a heading grep gave the line ranges; the assigned passages fit in one 200-line read once located · a persona brief that names line ranges (or a `grep -n "^[0-9]\+\. \*\*|^   - [0-9][a-z]\."` recipe) for the passages to read, since the persona may not read anything else on the page
2026-09-04 · U05 · writing agent (trim-1) · a second edit pass over the scenarios: the brief's item 2 asked to keep the by-hand Create Journal and invite route in "Setting up a scratch journal", and a mid-task ruling then cut every by-hand fixture recipe from the scenarios, so that paragraph and the preconditions of scenarios 1, 5, 7 and 9 were trimmed twice · TEMPLATE's Canonical scenarios guidance (and the writing brief) stating the fixture rule up front: a precondition is stated as a given, how a fixture comes to exist is the test tooling's business and never a by-hand recipe
