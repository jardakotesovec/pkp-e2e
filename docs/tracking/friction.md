# Friction — what made an agent's task harder than it needed to be

What cost a screen-driving agent calls, time or retries that a better
brief, doc, kit, seed or fixture would have saved; that agent appends a row
at the end of its task. The maintenance session folds the rows and deletes
them under MAINTENANCE "The daily session".

One line per entry, appended at the end, in this shape:

`YYYY-MM-DD · U<nn> or sync · <role and agent id> · <what cost you calls, time or retries> · <what would have helped>`

Facts only, the same quarantine as everywhere else (nothing
security-shaped, no credentials).

## Entries
2026-09-22 · sync · regression reader rr14 (pkp/ojs#5813) · A scratch journal has no ISSN, so every default JATS it produces fails the JATS 1.2 DTD on `journal-meta` (`issn+`) whatever the change under read, and a `published: true` seed copies the journal licence at publish time, so a licence set after the seed never reaches the publication: run 1 of the check read as invalid and licence-less for those two reasons and had to be re-run on a second scratch journal with `onlineIssn`, `publishingMode` and `licenseUrl` PUT before seeding · A context scenario key for `onlineIssn`/`printIssn` and `licenseUrl` (or a seed-facts line saying a scratch journal has no ISSN and that the licence is copied at publish), and a scenarios.md line that `GET submissions?status=3` on `publicknowledge` lists no published submission for `manager.maya`, so a read-only "published seeded article" does not exist there
2026-09-22 · U35 · harness agent hU35 · The regression step matched 42 suites through `participants[]` (grep "participants:"), run one at a time under a desktop load of 25–43 on 10 cores: six reds across six suites, each read against ci-triage and re-run (two known load classes, two session-looking "not authorized" reds that went green alone, one deterministic OPS U40 S1 red that only an A/B against the pre-change builder, a `git stash push -- shared/php` plus re-mount, could clear), about fifteen calls · a harness.md recipe for the baseline-builder A/B, and a brief rule narrowing the regression to the suites that pass the changed sub-keys when the default path is byte-for-byte unchanged
2026-09-22 · U35 · claim checker ccK4 · About six calls lost to two premises: scenarios.md "Facts tests rely on" says a real wizard submit auto-assigns the section's editors, so an automatic-assignment leg on a scratch context cost a wizard drive and a code read before it proved that no scratch context ever gets one (only the install's first context does, a product bug); and the kit reads a page's toasts only if a script installs its own watcher (they vanish before `screen()`), and the Activity Log window leaves the workflow rows unreadable until the page is landed again · a scenarios.md line that automatic assignment works on `publicknowledge` only, and a kit helper that records the page notices raised since a given action
2026-09-22 · U35 · claim checker ccK1 · Two OJS reruns (about four calls, ten minutes) lost to the workflow screen itself: the stage headings are CSS-uppercased so an innerText match on "Participants" found no panel on any stage, and Escape on a row menu closed the whole workflow dialog after the first row; then a by-role, by-stage read of one app outran the 600 s Bash cap and had to go to the background · a kit reader for the Participants panel (rows, top buttons, each row's menu, closing it by its own button) next to `screen()`, and a line in patterns.md pitfall 7 that a headlessui row menu is closed by re-clicking its button, not Escape
2026-09-22 · U35 · claim checker ccK2 · About six script runs lost to three probe traps: a warning locator filtered by /anonymous review/ matched the workflow dialog itself (reviewer rows read "Anonymous Reviewer/…") and reported a warning that never shows; a role sweep in one window read carried box ticks as defaults (the window keeps "Permissions" across a role change) and Playwright check() on an already-checked radio silently skips the window's click handler; and reading a saved flag that has no box on screen (a manager-level row's metadata permission) took three runs to find in the page's own participants request, since the kit records no response bodies · patterns.md "Probe kit": a line on check() no-ops and on filtering dialogs by a sentence, not a word; a kit helper that records chosen fields of the page's own JSON responses by URL pattern
2026-09-22 · U35 · claim checker ccK3 · Two reruns (about four calls) lost to reads that stay silent on failure: the predefined-message list's fetch-template-body answers 500 with no on-screen sign (blank entry again, OPS "Assign Editor", any Settings-made template), so a "wait until the box changes" read returned the previous template's text as if it were the new one until the run record's 500s were checked; and the Tasks and Discussions tab's `main tr` filter hit a hidden row of another tab. A kit helper that reports a window's failed component requests next to each read would have caught the first at once.
2026-09-22 · U35 · claim checker ccK5 · About six calls lost to two premises: footnote td12 asks for a submission to the seeded journal (the only context where the automatic assignment happens), which the brief's read-only rule forbids, so the email had to be reached another way (editors seeded on a draft, then the wizard's Submit) after a code read; and the Manage Emails page object and notes assume every mailable opens a window with "Add Template", while "Editor Assigned (Auto)" opens its "Edit Template" window at once, so the first emails drive burned two 30 s waits · The spec author writes a td that a scratch context can run (or the brief names the exception), and DecisionSettingsPages.openMailable handles the one-template mailables' direct "Edit Template"
2026-09-22 · U35 · test author tojs · Two briefs disagree on S6's and S8's givens: brief deliverable 5 says a state with no scenario key is returned as a harness need and never driven through a settings screen, while footnote s (and the shipped U12 suite) sets the person's own Profile › Notifications boxes on screen before the scenario; the suite follows footnote s and returns the key family as a harness need, which cost a precedent search · a `users[].notifications` key (the Profile › Notifications pairs, `{settingName: {enabled, email}}`) or a brief line saying a person's own Profile preference counts as a given the test may set on screen
2026-09-22 · U35 · test author tops · One red run and a code read lost to the notices premise: the screen notes and the shared page object say every participant notice is a top-right toast, but on a preprint server's Production entry the stage's own notification box refetches on every Participants action without request options and can take the notice first ("Notification sent to users." under a "Notification" heading in the main column, T-ops-1); and a `--repeat-each` check of S3 went red because its three copies, all signed in as `manager.maya`, drained each other's notices (patterns.md parallel lesson 2), a risk every S3 that reads a toast as a roster account carries in a full run · A screen note per app for where each notice lands, and footnote s letting the toast-reading scenarios sign in as a throwaway manager
2026-09-23 · U35 · security verification probe sec · A judgment call with no rule to settle it: the private-file brief says a matching observation updates the existing entry, and the task brief says the older maintainer files may be read for the duplicate check only, so a match found in one of those files could be neither updated there nor dropped without losing the new context · The brief (docs/process/briefs/security-verify.md "The private file") says what to do when the match lives in a read-only file (for example: carry the update in the Open file under the old id, marked for merge on triage)
