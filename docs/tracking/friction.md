# Friction — what made an agent's task harder than it needed to be

What cost a screen-driving agent calls, time or retries that a better
brief, doc, kit, seed or fixture would have saved; that agent appends a row
at the end of its task. The maintenance session folds the rows and deletes
them under MAINTENANCE "The daily session".

One line per entry, appended at the end, in this shape:

`YYYY-MM-DD · U<nn> or sync · <role and agent id> · <what cost you calls, time or retries> · <what would have helped>`

Facts only, the same quarantine as everywhere else (nothing
security-shaped, no credentials).

## Entries
2026-09-24 · sync · claim checker fr0924 · About five calls lost: the U11 kept checks (K1–K3) re-read the French strings only inside long phase chains (seed, one, several, then lang), so re-running "the ones that cover your lines" meant a new focused script instead; and a whole-page raw-key grep of a Website settings page listed dozens of keys from hidden tab panels and inline script JSON, needing a second run with visibility and location · kept check scripts carry a small read-only "strings" phase per language that reuses a seeded state, and the probe kit offers one rawKeys(page) helper that reports visible "##key##" codes with their landmark (the one in shared/playwright/checks/U08/K1/k1-fr0924.js)
2026-09-24 · U42 · harness agent hU42 · One smoke rerun: a `citationsRaw` list seeded with an empty line ("" for a blank line) came to the builder as null, because the `_test` POST body goes through the same ConvertEmptyStringsToNull middleware as the screens, inside lists too; scenarios.md says so only under `copySubmissionAckAddress` · one line in scenarios.md "How the endpoints work": every empty string in a request body, nested ones included, reaches the builder as null
2026-09-24 · U42 · claim checker ccK1 · About four runs lost: the q9 set-up ("end the Site Administrator's own manager role… and sign in again") cannot be done on screen, since "Remove Role" on a user's last role answers "You cannot remove the role. At least one role must be assigned to the user." and sends nothing; the working route (enrol `admin` with a second role through the context `users[]`, then end Journal manager) was only in U38 K3's script; and I lost one run to the References search, which filters only on Enter even though patterns.md says so, because the spec line said "as you type" · scenarios.md "POST scenarios/context" could say that `users[]` may name `admin` to give the administrator a second role, and that a user's last role in a context cannot be ended on screen; a spec author's set-up line in a to-drive footnote could be checked against users.md "Users & Roles" first
2026-09-24 · U42 · claim checker ccK3 · About six runs lost: the A9 drive first came back "refused" only because a seeded draft has no main file ("You must upload at least one Article Text file."), then OPS has no `article.pdf` fixture (`preprint.pdf`, as a galley); and table reads straight after a side-panel save or delete showed the old rows (the table refreshes about a second after the panel closes), which read as "not updated in place" until a bounded wait on the row was added · scenarios.md `submitted: false` says a draft has no file and a draft that must submit needs `files[]` (OJS/OMP) or `galleys[]` with `preprint.pdf` (OPS); patterns.md pitfall 4 adds that a Vue list behind a side panel refreshes after the close, so wait on the row
2026-09-24 · U42 · claim checker ccK2 · Two OJS reruns lost: closing a row's "More Actions" menu with Escape closed the whole workflow dialog (every later step then timed out on the dashboard list), and an Author Information row count included the table's text-only empty row, so the first "Add" was read as the second row; patterns.md pitfall 7 names Escape only for inner windows · pitfall 3 or 7 adds that Escape on an open headlessui menu inside the workflow closes the workflow too (close a menu by its own button), and pitfall 4 notes that a PkpTable's empty state is a tbody row (count rows :has(input))
2026-09-24 · U42 · claim checker ccK4 · About four reruns lost to two copied helpers: the wizard `wizardTo` (from K3) presses "Continue" when the target step is not yet reached and the rail otherwise, and "Continue" saves the step at once while a rail move saves nothing until the autosave a minute later, so the same drive meant two different things (it also turned K3's "rail" leave drive into a Continue); and the References row-menu reader (from K1/K2) closed the menu with Escape, which now and then closed the whole workflow dialog and hung the next box fill for 30 s · a SubmissionWizardPage note (or kit helper) that names both moves and says which one saves, and a patterns.md pitfall 3 line that a headlessui menu is closed by pressing its own button again, never Escape inside the workflow dialog
2026-09-24 · U42 · test author tojs · One 7-minute hung run and two extra runs: scenario 4's "Require…" leg said "no warning" on Review and "Submit › Submit refused", both read by the claim check from a snapshot taken while the Review step still showed "Checking your submission"; once the check answers the warning is there and "Submit" is disabled, so the test's Submit press waited out the test timeout · the probe kit's screen() (or claim-check brief) waits for the wizard Review step's `…/submit` check to answer before recording, as SubmissionWizardPage.continueToReview does; and a test author runs a new wizard test with a short `--timeout` first
2026-09-24 · U09 · harness agent hU09 · Two parity reruns lost on the legacy Plugins grid: `check()` on a row's "Enabled" box aborted the drive and the pending waitForResponse then crashed the process (click() inside Promise.all with the response worked), and an enabled plugin's row changes its accessible name to "Settings <name> <description>" (a "Settings" link is prepended to the name cell), so a `/^<name> /` row locator found nothing after the tick · patterns.md could say: tick a Plugins grid box with click() inside Promise.all with the `settings-plugin-grid/enable` response, and match its row by name and description, never by a leading name
2026-09-24 · U09 · claim checker ccK1 · About eight runs lost in the legacy "Custom Page" item and block windows: Escape to close a TinyMCE menu ("Insert Tag", the Paragraph choice) closed the whole side window through the browser's "form has changed" confirm and every later step timed out; with one form language the Content textarea id carries no locale (`content-<uniq>`, `content-en-<uniq>` only with two), so an id-prefix lookup found nothing; the second language's popover (open while a box has focus) covered "Save"; a click into the editor after a tag selected the tag and the next key replaced it; and the sidebar Language block's links land on the site home on the fleets (source without the port) · patterns.md pitfall 4 or "UI realities": close a TinyMCE menu by clicking into the editor, never Escape; find a legacy rich box by `textarea[name="content[en]"]`; blur (click another box) before a window's buttons when two form languages are on; after "Insert Tag" keep typing (End) without clicking; seed-facts names the Language block limit
2026-09-24 · U09 · claim checker ccK3 · About four OJS reruns (4–6 min each) lost in the legacy block window and the public reads: a multilingual box's popover of the other languages stays open over the window's "Save" and swallows the click until the window heading is clicked, and a click on the page behind the window closes it through the "form has changed" confirm; a browser that once opened a `/fr_CA/` address keeps French for every later address without a segment (the visitor's sidebar reads and the manager's Plugins row text), so reads meant for English came back French. Also: "Delete" on a custom block 500s on the PostgreSQL fleets, so the site-level block Rule 27 needs cannot be removed afterwards (left unplaced, plugin unticked) · patterns.md "Locator pitfalls" (a legacy multilingual popover over the window foot; name the locale segment in every address after a French read) and a brief line for site-level mutations that the screens cannot undo
2026-09-24 · U09 · claim checker ccK2 · About two reruns lost: the legacy window's close button is named "Fermer" in the French interface, so a French-interface preview drive died on a `/^Close/` locator, and a toast watcher on a clean save kept catching earlier refusal messages that the app re-shows at the top right (K2-7), which first read as that save's own message; also the K1 helpers I needed (readPublic, mceType, preview, addCustom) live unexported inside k1.js and had to be copied · the U09 lib.js exports the public-page reader, the TinyMCE typing and the preview helpers, and patterns.md pitfall 4 says a legacy window's close button is named by the interface language ("Close" / "Fermer")
2026-09-24 · U09 · claim checker ccK5 · Rule 29 (pictures), 29a, 29b, A5 and two Side-effects lines could not be driven at all: every picture upload on the three fleets answers 500 "The public files directory was not found…" because `make-test-config.js` writes an absolute `public_files_dir` and `PKPUploadPublicFileController` prefixes the app root (the same cause as the seed-facts profile-image 404); found only after the first upload run. Also one OMP rerun lost to a psql `journals` lookup (the table is per app; `createContext` already answers `contextId`) · the harness writes `public_files_dir = public` (relative, the config template's default) or a spec author's to-drive picture footnote is checked against one upload first; seed-facts names the upload failure until then
2026-09-24 · U09 · harness hU09b · One 7-minute run lost and four orphaned `php -S` servers to clean up: the brief's regression command (`npx playwright test -c configs/<app>.config.js <suite> …`) with suite `U12` also matched `tests/serial/U12-announcements.spec.js`, whose `<app>-serial` project depends on the whole app project, so 331 tests started and `timeout` killed the runner but not its servers · RUNBOOK step 4's regression command names `--project=<app>` (and a serial suite separately with `--project=<app>-serial --no-deps`), or harness.md "Running" says a bare suite id also pulls the serial file
2026-09-24 · U09 · claim checker ccK4 · One 10-minute Bash timeout lost waiting on parallel per-app probe runs with a `while pgrep -f k4.js` loop that matched its own shell (the pitfall patterns.md names for detached runs), and a Plugins-grid category read by DOM siblings that returned the grid's empty-row text ("No Items") instead of "Generic Plugins"; also ccK5's upload blocker (500 on every picture) had been lifted in the working tree by the time this chunk ran, which no brief or screen note said · patterns.md "Probe kit": run parallel per-app probes as `node … & … & wait` in one foreground command; a kit helper `pluginRow(page, id)` returning the row with its category from `screen().text.main`; the orchestrator adds a screen-notes line when a harness blocker another checker reported is fixed mid-session
2026-09-24 · U09 · claim checker ccK5b · One 4-minute OJS upload run redone: K5's kept script chained twelve uploads into one box, and TinyMCE's "Insert/edit image" with the last inserted picture still selected replaces that picture instead of adding one, so the saved block showed only the last picture and the public-side read of each accepted type was lost; the fix (caret to the end before each upload) is now in `checks/U09/K5/k5.js` and in screen-notes. Also the brief asked both to "extend K5's kept script" and to write "one entry script" under `K5b/`: resolved as a thin `K5b/k5b.js` that sets the tag prefix and phases and requires `../K5/k5.js`; a brief for a re-drive chunk could name that shape.
2026-09-24 · U09 · security verification sec1 · Two extra OJS runs: the private entry's steps, written from reading before the claim checks, named a drive that the K3 claim check had already shown cannot complete, so the first run placed nothing and the second only printed the 400 body; a verification brief that says "re-read the steps against the claim-check findings (cc-K*.md) of the same screen before driving" would have saved both.
2026-09-24 · U09 · security verification sec2 · One OJS rerun: the Manage Custom Blocks grid's "Block Name" column shows each block's derived lowercased kebab name, not the typed title, so a case-sensitive read of the title came back empty and read as a failure until the second run; sec1's screen note said the derived name is the Sidebar value but not that the grid column shows it · a screen note (now added) that the block grid lists the derived lowercased name, so a block is confirmed case-insensitively against it
2026-09-24 · U09 · test author tojs · Two reruns: a legacy grid's "No … have been created." line is `tbody.empty` (hidden, not removed, once a row exists), which I first read as `tr.empty` (patterns.md pitfall 10 names the rows and controls but not the empty line), and the Custom Block Manager window's `label[for="showName"]` is the section title "Show Name" while the box's own words sit in the label wrapping the input · pitfall 10 gains "the empty line is `tbody.empty`", and the ccK3 screen note on the block window names both labels
2026-09-24 · U09 · test author tops · One probe run to find the Plugins grid's category heading for S8's "under Generic Plugins" control: the ccK4 screen note said the heading is not reachable from the row's DOM and pointed to `screen().text.main`, which a test cannot use; it is the first row of the row's own `tbody.category_grid_body` · the claim checker writes the locator that does reach a fact a scenario asserts, not only the one that failed (now in screen-notes as tops, and `PluginsTab.categoryHeading()`)
2026-09-24 · U44 · claim checker ccK2 · One OMP rerun lost: the legacy URN settings window keeps its stored "pattern" suffix choice between drives, so the OMP1 "Chapters only" save was refused in the browser for the empty chapter pattern instead of reaching the server check; and each run record counted the known plugin-gallery-grid 500 as a new crash on every Plugins landing (23–30 per run), burying the real script error · patterns.md "Probe kit" notes that a legacy plugin settings window starts from its stored state (reset every choice the drive depends on), and the kit tags crashes that match a known incidentals.md row (plugin-gallery-grid) as known in its end-of-run count
2026-09-24 · U44 · claim checker ccK1 · About six calls lost: the Rule 19 context seeded through `plugins.urnpubidplugin.settings` without `urnCheckNo` rendered an empty article "Identifiers" page (the form GET answers `fields: []`), which read as an app bug until a re-save of the settings window and a with/without seed pair (urn-seed-checkno.js) showed the seed was the cause; and OMP "Select Files" offers only production-ready files, so a format file needed the upload wizard ("Change File") · scenarios.md `plugins` could say that `urnpubidplugin` needs every key its settings window writes (at least `urnCheckNo`), or the builder could default the window's keys; a `publicationFormats[]` submission key {OMP} (format name plus a proof file) would spare every press check the wizard
2026-09-24 · U44 · claim checker ccK3 · Four OJS reruns lost: no seed key puts an unpublished article in an issue (the submission `issue` key applies only with `published: true`), so every default-pattern URN drive re-does Publication Settings on screen, and ccK2's note named "Assign To Current/Back Issue", which a journal with only future issues does not offer; the tab's "Clear" confirms with "OK" where the spec said "Delete"; and a version saved at stage PMUR can never be scheduled again (the publish button opens the refused window at once), so the scheduling drive needed a fresh article · the submission scenario's `issue` key also taking effect for an unpublished submission (the Publication Settings save it mirrors), and one patterns.md line that legacy confirmation windows ("Delete" title) answer with OK / Cancel
2026-09-24 · U44 · claim checker ccK4 · About eight calls and one OMP rerun lost: every reader-page, "Nothing else" and Clear line needed a stored URN, and no scenario key stores a pub-id (and a `published: true` seed's Identifiers page is read-only), so the drive seeds unpublished items, assigns the issue, the URN and the Publisher IDs on screen and publishes on screen per app; the OMP book page then showed no format block until the format was also made "Available", which neither the spec nor seed-facts says; and a copied Dublin Core toggle hung on OPS, which ships no such plugin · a `pubIds` map on the submission scenario (publication, galleys, issue: `{urn, publisherId}`) and on `issues[]`, stored as the tabs store them, plus an OMP `publicationFormats[]` key with `approved`/`available`; seed-facts lines for the OPS plugin set and the OMP "Available" gate
2026-09-24 · U44 · test author tojs · Two runs and three by-hand probes lost: U49's `openPublishPanelExpectingIssueFields` arms its wait on the `issueAssignmentStatus` GET after landing, but the workflow already fetches it at landing, so the wait timed out (60 s) on an item that sat on the page a while; the second "Schedule For Publication" press opens the confirmation window straight away (the version stage is saved by the first "Confirm"), which `openPublishPanel` does not expect and then hangs its retry click until the test timeout; and every scenario page is an `asUser` page, so the run's error-context.md is the unused fixture page's and each failure needed a probe · PublishSchedulePages gains one opener that accepts either the panel or the confirmation window (the one in the U44 OJS suite, `openConfirmation`), bounded by what is on screen rather than a request; and the fixtures offer the first `asUser` page as the error-context page (or attach its aria snapshot on failure)
2026-09-24 · U44 · test author tomp · One OMP rerun: the mailbox control copied from U42 OMP (a discussion the Press Manager opens with a spare) also mails the Press Manager, so the suite's "no email to the manager" read found the control's own copy; U42 reads the author only, and nothing says the opener is mailed · scenarios.md "Mailpit" (or the U42/U43 control helper's comment) says a discussion mails every ticked participant and its opener, so a silence read on the opener counts that one copy
2026-09-24 · sync · regression reader rr13263 (PR review pkp-lib#13346 + ui-library#987) · Two French reruns: `user/setLocale/fr_CA?source=…` lands on `/index.php/index/fr_CA` and ignores the source, and every reviewer-row helper keys on English names (`panelRow('Reviewers')`, the "More Actions" button, menu items), so a translated-UI probe rebuilt its own row/menu locators; also the brief's "reset first" and "hold on a fresh reset" had to be overridden by the session because a suite shared the database · a probe-kit `switchLocale(page, contextPath, locale)` plus locale-free row/menu locators in ReviewStagePages (`button[aria-haspopup]`), and an rr brief variant that names a fresh scratch context as the reset-equivalent when the fleet is in use
