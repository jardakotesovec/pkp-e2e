# Friction — what made an agent's task harder than it needed to be

What cost a screen-driving agent calls, time or retries that a better
brief, doc, kit, seed or fixture would have saved; that agent appends a row
at the end of its task. The maintenance session folds the rows and deletes
them under MAINTENANCE "The daily session".

One line per entry, appended at the end, in this shape:

`YYYY-MM-DD · U<nn> or sync · <role and agent id> · <what cost you calls, time or retries> · <what would have helped>`

Facts only, the same quarantine as everywhere else (nothing
security-shaped, no credentials).

## Entries
2026-09-23 · U36 · harness agent hU36 · The probe kit records no request bodies, so what each screen posts (the wizard's genre PUT, `save-metadata` posting `name[en]` alone, the review-files window's `selectedFiles[]` on "OK", the component window's fields) needed a hand-written `page.on('request')` listener in both parity scripts; the "OK" step it revealed was the one difference the first DB compare showed, a second drive round · a kit option on `launch()` that records the form-encoded bodies of non-GET requests matching a pattern (multipart parts minus file contents) into the run record
2026-09-23 · U36 · claim checker ccK1 · About eight calls and two extra seeded submissions lost to three premises nothing recorded: the legacy "Upload/Select Files" window's grid fills after idle(), so the first tick found no row and the Rule 4 leg ran with no copy on the list; deleting that original then silently took its copies along (a database cascade), destroying the state the next leg needed; and the upload wizard's "Summary of Changes" is a TinyMCE box whose hidden textarea times out on fill() · a patterns.md pitfall line for the select window's late grid and the TinyMCE summary box (both now in U36 screen-notes), and a seed-facts line that a copy dies with its original
2026-09-23 · U36 · claim checker ccK2 · About six calls and two reruns lost to three reads that lie quietly: the upload wizard hides its step-1 upload box with the class pkp_screen_reader, so isVisible() and getClientRects() report it visible and only the screenshot settled Rule 5a; the History tab puts each upload's "Download" behind the row's "Settings" toggle and a download collapses it again; and the publish/post panel needed an OJS and an OPS recipe copied from two suites' page objects · a patterns.md line on pkp_screen_reader-hidden controls (read the class, not visibility) and on the legacy grid "Settings" toggle re-collapsing, and a kit helper publish(page, app) that publishes the open workflow on a scratch context
2026-09-23 · U36 · claim checker ccK4 · Two things cost about five calls: the spec put the anonymizing-link box on Review › "Setup" (it is on "Reviewer Guidance"), so the first settings read found nothing; and a hand-written response-body capture added to read one 500 pulled an error text into .reports that had to be scrubbed and routed instead · a claim-check brief line that a probe script never records response bodies (the kit's run record keeps URL and status only, on purpose), and that a 500 whose content matters is described from the screen and, if security-shaped, routed without saving the body
2026-09-23 · U36 · claim checker ccK3 · About six reruns lost to legacy-grid traps nothing recorded: a file-number row regex with \b never matched "180article.pdf" (the number and name share one text run), the History tab's "Show events from prior versions" box and every History "Download" sit behind collapsed controls (the grid's "Search" link, each row's arrow), a nested "Upload a Dependent File" wizard is not the `.last()` dialog holding a fileUploadWizard div (the image went into the outer wizard and polluted the list), and the legacy grid search needs its own toggle and submit whose effect only the fetch-grid POST proves · a kit helper for legacy pkp grids (rows by index, open a row's extras, run the collapsed search and return the fetch-grid answer) and a patterns.md pitfall 10 line that grid filter forms are collapsed behind "Search"
2026-09-23 · U36 · claim checker ccK5 · About four calls lost to two quiet reads: the kit's `crashes` list stayed at 0 while the OJS review stage logged a Vue TypeError twice (Vue's error handler writes a console error, not an uncaught pageerror), found only by grepping every run record's `console`; and "which file does this galley serve now" has no screen but the publication's "Preview" › galley › "Download" name, which took three script passes to find (the Preview navigates the same tab, the galley link's name carries an icon) · a kit rule that counts console errors whose text starts with "TypeError"/"ReferenceError" (Vue-handled script failures) under `crashes`, and a patterns.md line on reading a galley's current file through the Preview's download name
2026-09-23 · U36 · security verification probe sec · One extra call and a judgment: the brief asks for the probe scripts under .reports/U36/sec/ with contents that do not describe the problem, but a script that verifies a concern necessarily shows what it checks, so both scripts were moved to the session scratchpad after the run (the run records, kept empty by record:false, stayed) · the verify brief says where a verification script lives (the scratchpad, outside every repo) instead of .reports/<feature>/sec/
2026-09-23 · U36 · test author tojs · One rerun and a findings block for a seed tie: `files[].note` is written in the same second as its file's upload, so the file's History orders the seeded upload above "Posted new note.", against scenario 6's bullet (a by-hand note comes later and heads the table); nothing in scenarios.md says the seeded rows tie · scenarios.md's `files[]` bullet states that the note's History row shares the upload's second (or the builder stamps the note a second later), so a test reads order only on rows it makes itself
2026-09-23 · U07 · harness agent hU07 · the Profile › Contact tab's "Affiliation" box has no accessible name (its label is not tied to the input) and the tab will not save without "Country*", so a drive typing into `getByRole('textbox', {name: 'Affiliation'})` timed out on all three apps and needed a second script; the phase-1 recording had covered only the Identity tab · patterns.md or users.md: a line on the Contact tab (the unlabeled multilingual boxes located by `input[name^="affiliation"]`, the required country), and record every tab a drive will type into, not just the landing one
2026-09-23 · U07 · claim checker ccK3 · a scratch context has no Country (and no initials unless seeded), so Settings › Journal › Masthead refused Save and Administration › Hosted Journals › Edit answered 400 on Country, and the Contact tab refused Save without a technical support contact: two drives (td10, bullet 8) needed a rerun after reading the refusals, and a closed-phase rerun on all three apps · scenarios.md: a `context.country` passthrough (or the builder setting a default country), and a line in seed-facts that a scratch context cannot save the Masthead, Contact or Hosted Journals forms as seeded
2026-09-23 · U07 · claim checker ccK1 · the Settings pages' tabs write the chosen tab into the address about a second after the click, so a `page.url()` read straight after the click showed no hash on every tab and looked like a failed Rule 1 until a separate diagnostic script and a rerun of the chunk's menu phase on three apps · a line in patterns.md "Locator pitfalls" (tab hash lands late: wait for `location.hash`), and the kit's `screen()` recording `location.hash` after its idle wait
2026-09-23 · U07 · claim checker ccK2 · three reruns on premises no doc carried: the per-language French boxes of Settings › Journal are hidden behind a "French" button (a fill timed out), the Hosted Journals "Create Journal" form is a Vue form in a legacy modal whose "Languages" and "Primary locale" are required (two failed create drives), and Appearance opens on its "Theme" side tab so the header-logo input was unreachable (two failed logo drives); also a `until ! pgrep -f <script>` wait matched its own shell and hung two waits · patterns.md "UI realities": a line on multilingual Vue fields (the language button, "n/2 languages completed") and on the Create Journal form's required language boxes; the probe kit could export a `waitForChildren()` or the docs could say "wait on the node PIDs, never pgrep the script path"
2026-09-23 · U07 · claim checker ccK4 · two premises no doc carried cost a full rerun on three apps: "Remove Role" refuses a user's only role (the seed gave the td14 member one role), and disabling a listed member 500s the masthead and history until a rebuild, which blanked every later read of the A4 drive; also the kit's `record('k4', R)` from a partial-phase rerun overwrites the full run's aggregate record (saved by a copy in time), and the three apps' UserInvitationPages expose different property names so a shared drive could not use them · scenarios.md `users[]`: say that an entry a screen will end needs a second role; seed-facts or patterns.md: the disabled-member 500 on the About pages; the probe kit could suffix a record name with PHASES, or the brief could say so
2026-09-23 · U07 · test author tojs · a repeat run went red once in S8: after the Roles grid's role window saves with "OK", the grid redraws that role's row without its "Settings" link, and a second `page.goto` of `…/access#roles` from the same page was a same-document hash navigation that kept the stale grid, so the next role's window could not be reached; one extra repeat run to find it · patterns.md "Legacy pkp jQuery grids" (pitfall 10): a line that a legacy grid's row redrawn after its edit window loses the row controls until the page really reloads (load the address without the hash, then press the tab)
2026-09-23 · U37 · harness agent hU37 · One timed-out parity drive: the draft and the controller route say "Begin Task Upon Saving" sends `PUT …/tasks/{id}/start`, but the browser sends it as a POST with `X-Http-Method-Override: PUT` (as every Vue useFetch PUT does; scenarios.md says so only under the announcements key), so a `waitForResponse` on method PUT never matched; and, as for hU36, the request bodies needed a hand-written `page.on('request')` listener in each of three parity scripts · a patterns.md pitfall line that Vue-side PUT/DELETE calls travel as POST with the override header (match the URL, not the method), and the kit body-recording option hU36 asked for
2026-09-23 · U37 · claim checker ccK1 · About three calls and one rerun per app: after an item window's "Close", the panel's "Add" resolved by a CSS locator (so role-query blindness was not the cause) but its click timed out for 30 s, because the closed side modal's shell still covers the workflow dialog; patterns.md pitfall 4 says only that role queries miss the page behind it · a pitfall 4 line that a click on anything behind a just-closed Vue side modal times out too, so land the page afresh (or wait out the close) before the next click there
2026-09-23 · U37 · claim checker ccK3 · One rerun on OPS: the author at `mySubmissions?workflowMenuKey=workflow_5` lands on the publication's "Title & Abstract" with no panel; the author's panel is behind the publication menu's "Production Tasks & Discussions", a fact ccK1 found but left only in cc-K1.md, not in screen-notes.md · the kit's note() for every recipe a sibling will need (a claim-check brief line: a navigation recipe goes to screen-notes, not only to the report), or a users.md line on where a preprint author reaches a stage panel
2026-09-23 · U37 · claim checker ccK2 · About five reruns lost to three unrecorded screen facts: a page.goto after the "Add" window aborted with net::ERR_ABORTED because the page raises a beforeunload box even after a save (the kit dismisses browser dialogs by default, which cancels the navigation); "Cancel" on a changed window opens a Vue "Warning" that a script must answer, against the spec draft's premise; and a preprint server Author's panel is behind the publication menu link, not workflowMenuKey=workflow_5, which cost a whole OPS phase on the full run · the kit's launch() accepts beforeunload by default and logs it in the run record, and patterns.md lists the OPS author route to "Production Tasks & Discussions"
2026-09-23 · U37 · claim checker ccK4 · Two things cost about four calls: the kit's `screen().text.dialog` for a Settings side window starts with the app header ("##common.help##", "Tasks", the user), so a "window title = first line" read printed "##common.help##" until I read the aria level-1 heading instead; and a chunk run on OMP (~10 min) outruns the 600 s Bash cap, so the full runs went detached with a wait loop (patterns.md "Probe kit" could say: read a side window's title from `aria.dialogs` heading level 1, and run a chunk script over ~8 minutes with nohup).
2026-09-23 · U37 · claim checker ccK5 · About six calls lost to four reads that race or mislead. The header Tasks panel fills by AJAX after its window opens, so a read taken when "Loading" is gone found no rows. Reading a message file link by first link picked the header help link. The stage notice selector matched nothing (it is plain text under "Notification"). And a ccK2 screen-note said the discussion window's boxes change nothing (an instant read of "Save"), which cost a re-drive of every box on three apps · a kit helper tasksPanel(page) that waits for a tbody row or "No Items" and returns the rows, and a patterns.md line that a Vue form's Save state is read after a short settle, never at once after a box press
2026-09-23 · U37 · claim checker ccK6 · Two things cost about four calls: the one-hour edit limit (Rule 15c, A12, A7's second text) has no seed that ages an item's first message, so the "past the hour" state needed a psql update of `notes.date_created` on the scratch items (the draft had declared it undrivable); and an Author's Edit refused on the hidden `submissionFileIds` key keeps "Save" greyed whatever changes, so the follow-up step hung 30 s until the script reopened the window · a `tasks[].messageAgeMinutes` (or `messageDate`) key on POST scenarios/submission that stamps the head note's date_created, so scenarios and checks reach the expired state without SQL; and a patterns.md pitfall 4 line that a refusal on a key with no form field leaves "Save" greyed for the rest of the window
2026-09-23 · U37 · claim checker ccK7 · Two things cost about three calls: the checklist tagged Rule 19 (399–404) "no-screen (fn-z)" because footnote z says the deleted files' database records have no screen, though every body line of the rule is a screen claim, so the header's declared list had to be re-derived by reading the rule; and the row helper copied from the K6 script matched item names with includes(), so "K7 Hist" read the "K7 Hist2" row and one History leg ran twice · the claims lint marks a line no-screen only when the line itself (not a footnote sentence about something else) says so, and the U37 check scripts share one helpers module with an anchored row matcher instead of each chunk copying it
2026-09-23 · U37 · test author tojs · About six runs lost to three reads nothing recorded: a greyed "Save" after a page-side refusal made one scenario-2 press wait the default three minutes (ccK2 had seen it and drove each refusal in a fresh window, but the scenario chains them in one), a `target=_blank` file link's download arrives on the opener page, not the popup, and the Vue form's "Please correct N errors." is a bare text node no `getByText` matches · the scenario writer's brief says a scenario that chains refusals in one window checks each press against screen-notes' "Save stays greyed" line, and patterns.md gets one pitfall line each for the opener-page download and the `.pkpFormErrors` text node
