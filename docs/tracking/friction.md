# Friction — what made an agent's task harder than it needed to be

What cost a screen-driving agent calls, time or retries that a better
brief, doc, kit, seed or fixture would have saved; that agent appends a row
at the end of its task. The maintenance session folds the rows and deletes
them under MAINTENANCE "The daily session".

One line per entry, appended at the end, in this shape:

`YYYY-MM-DD · U<nn> or sync · <role and agent id> · <what cost you calls, time or retries> · <what would have helped>`

Facts only, the same quarantine as everywhere else (nothing
security-shaped, no credentials).

## Entries
2026-09-18 · U32 · claim checker ccK4 · Two calls and a re-upload phase lost to the legacy "Upload/Select Files" window: pressing its "OK" right after the wizard closes saves nothing because the grid redraws and ticks the new row afterwards (first upload of a run lost on OJS and OMP); a further call went to the Move to Review round status, which the chunk premise sent me to U26 for · A kit helper (or patterns.md pitfall) for the select-files window: wait for the new row ticked before "OK"; and seeded submissions carrying files (`files[]` on the submission seed) so decision chunks need not drive K3's upload windows
2026-09-18 · U32 · claim checker ccK3 · One probe process and a re-authoring of the row-menu drive lost to two premises: (1) the K4 screen note said the legacy select window is titled "Upload Review File" for both lists, while K4's own JSON and this drive show "Upload/Select Files" from "Draft Files" (a note written from memory, not from the record); (2) a legacy side window closed with its own Cancel leaves the Vue table behind it invisible to getByRole until the workflow is re-landed (patterns.md pitfall 4 says it for Vue side modals, not for the legacy ones), so the second row-menu press of a sequence found no "More Actions" · A screen note is written from the recorded JSON, and pitfall 4 names the legacy windows too.
2026-09-18 · U32 · claim checker ccK2 · One probe process on OJS lost to the "Assign Participant" form's template list: a /Copyedit/i match picked "Discussion (Copyediting)" before "Request Copyedit", so the first "Request Copyedit" drive went out under the wrong template and needed a second submission; the K3/K4 `notices` selector also misses the Rule 3 box (a plain heading "Notification" + paragraph), which two earlier notes describe by its text only · A kit `screen()` field or patterns.md line naming the workflow notice box's markup, and the seed-facts line "a scratch context has no discussion templates" retired (the registry task templates now fill the Assign form's list)
2026-09-19 · U32 · claim checker ccK1 · Three probe processes and two patches lost to premises the docs carry wrong or not at all: (1) the spec's Settings bullet names "Request Copyedit" as one of three templates on Settings › Workflow › Emails, but it is a task template on the "Tasks and Discussions" tab (table rows with a "More Actions" menu), so the Manage Emails drive searched for nothing twice; (2) the Assign Participant form's user grid does not refresh when the group changes until the search form is submitted (K2's note says so in passing, the spec's Rule 7 does not), so two a5b runs assigned nobody; (3) the K4 `wfInfo` idiom's `offsetParent` visibility test drops the fixed-position workflow dialog, so its headings and header read the dashboard behind it until replaced by `getClientRects()`. A patterns.md line for (2) and (3) and a spec fix for (1) would have saved them.
2026-09-19 · U32 · test author tomp · Three suite runs lost to premises the docs and notes carry wrong for the press: (1) footnote s and users.md name `sectioneditor.omar` as the recommend-only Series editor, but on OMP he is a Series editor of `monographs` (seed-facts) and so auto-assigned on submit, invisible to the "Assign" form; (2) the K4 screen note said the select window's new row "arrives ticked" and the wizard's steps are `role=tab`, while on the press the row arrives clear and the rail is a named list of buttons, so two page-object waits burned their timeouts across seven tests; (3) footnote s seeds the accepted-from-review state with `sendExternalReview` where the press route the check scripts used is `skipInternalReview` (grep calls, no run) · users.md naming the OMP recommend-only account (ravi), screen notes written from the recorded JSON rather than memory and marked per app, and footnote s carrying the press's decision names.
2026-09-19 · U32 · test author tops · One grep round lost to the brief's page-object list: it named EditorialDashboardPage, DecisionPage and WorkflowPage, but scenario 10's Roles screen lives in `UsersRolesPage` (apps/ops/playwright/pages/UserInvitationPages.js), which U27 and U28 reach with an inline `#roleGridContainer tr.gridRow` instead of a method; an absence brief that names the Roles screen could name that page object, and the Roles tab helpers now live there (openRolesTab, roleRows, roleNames). Nothing else cost a retry: the suite was green on the first run.
2026-09-19 · U32 · test author tojs · Several suite runs lost to a harness gotcha no doc names: on OJS, `test.use({user})` at describe level combined with `asUser` in the same test strands the `asUser` session — both the default page and the asUser context land on /login — even when the test never uses the default `page` fixture (reproduced isolated: a describe with a default user fails, the same drive with no default user passes). U26/U25 avoid it by convention (no default user, every actor via `asUser`) but nothing says why · a patterns.md line under "Fixture selection": on these fleets, do not combine `test.use({user})` with `asUser`; open every actor through `asUser` and set no default user.
2026-09-19 · U33 · claim checker ccK4 · Two reads lost to a legacy side window whose form arrives by AJAX after the window opens ("Edit Assignment"): a wait keyed on "a button in the window" matched its "Close" at once and read an empty form (recommendOnly "absent"), and a first read of the panel opened from the Declined view caught the shell before its side menu rendered · patterns.md pitfall 4 says settled() on a field for legacy windows, but the probe-kit section and the U32 K1 setRecommendOnly helper (which I copied) still wait on any button; a kit helper `waitPanel(page)` that waits for the workflow dialog's side menu or an error dialog before screen() would have saved both.
2026-09-19 · U33 · claim checker ccK1 · Two re-runs (about four calls) lost to re-authoring the workflow-dialog helpers every chunk copies from the previous feature's script (wfInfo, openWorkflow, assignForm, setRecommendOnly, runDecision, the select-files upload): each copy re-learns the same gotchas (the Edit Assignment form arriving by AJAX after the window opens, Escape closing the workflow dialog behind a row menu, the select window found by its `allStages` box) that U32's screen-notes already record. A shared helper module under `shared/playwright/checks/` that the chunk scripts import, or the brief pointing at the previous feature's helpers by function name, would have saved the re-runs.
2026-09-19 · U33 · claim checker ccK2 · One script re-run (about three calls) lost to reading the Activity Log as the assignee: an assistant's workflow header carries Preview and Library only, no "Activity Log", so the "logged" half of a side effect must be read as the editor; neither users.md nor patterns.md says which header buttons each level gets · a line in patterns.md "UI realities" (or the per-level header roster in screen-notes) would have saved it.
2026-09-19 · U33 · claim checker ccK3 · One 10-minute OMP run and one OPS run lost to my own script (the context path read before the seed), and one assign drive lost to a seed premise no doc states: a user seeded as a participant is not offered by the Participants "Assign" form, so a drive that assigns through the form needs an unassigned user of the role (k3.js seeds a second context for it); scenarios.md "participants[]" could say so in one line.
2026-09-19 · U33 · harness hU33 · One background-orphaned Bash call (12 OJS suites piped through `tail`, the 10-minute ceiling overshot with nothing printed) and one lost remote-galley drive (the upload wizard's modal closes by a button named "Close", not "Cancel"): the brief's regression clause names 70 suites for a submission-builder change with no runner, so every harness agent writes its own one-suite-at-a-time loop · a shared runner (`bin/run-suites.js <app> <suite>…`, one result line per suite, unbuffered, stopping before the ceiling) named in briefs/harness.md, and a patterns.md line that legacy modals close by "Close".
2026-09-20 · U33 · test author tojs · One suite rerun (two calls) lost to the spec's placeholder "Reply to this comment at #{id} {authors}": the screen renders the authors' family names ("#13 Author u33…"), which K2's chunk report quotes but the Side effects line does not say; a spec that writes the placeholder as "{authors' family names}", or the scenario bullet quoting the rendered line, would have saved it.
2026-09-20 · U34 · claim checker ccK4 · One run (about three calls) lost to the seed premise footnote r and scenarios.md share: `reviewers[].status: 'completed'` reads "Review Submitted" on the round, which the minimum-reviews count does NOT treat as confirmed, so "seed two completed" cannot reach the met end; each review had to be marked "Complete" on screen (Read Review → Mark as Complete) · a `status: 'complete'` (editor-confirmed) value on the reviewer key, and one sentence in scenarios.md saying `completed` is the reviewer's submit, not the editor's confirmation.
2026-09-20 · U34 · claim checker ccK4 · Two runs (about five calls) lost to the payments screens no scenario key covers: Settings › Distribution › Payments needs the plugin's "Manual Payment Instructions" box filled before "Accept Submission" gains its payment page (an empty box leaves the plugin unconfigured with no message), and the fee lives on a second page (/payments › "Payment Types"); scenarios.md lists no payments passthrough and seed-facts.md says nothing about the instructions gate · a context passthrough `payments: {enabled, currency, paymentPluginName, manualInstructions, publicationFee}` (OJS), or at least the gate stated in seed-facts.md.
2026-09-20 · U34 · claim checker ccK1 · One run (about four calls) lost to "Cancel Review Round" never appearing on the seeded round 1 (s2, s3, s3b: absent with an invited, accepted or completed reviewer), so the Rule 2 row needed a second seed with two `reviewRounds[]` entries (round 2 with an invited reviewer) before it could be driven · a scenarios.md "Decision behaviour" line saying the button's presence depends on the round's reviewer state and that a second `reviewRounds[]` entry builds round 2
2026-09-20 · U34 · claim checker ccK1 · Three reader fixes (about three calls) to the wizard page: TinyMCE 6 has no `tinymce.editors` array (use `tinymce.get()`), the Subject fills after `idle()` returns (the initial template load), and the rail list must be located by its accessible name because `main ol li button` hits the breadcrumb first · a patterns.md "Decision wizard" entry with those three idioms (ccK4's screen-note now carries the first)
2026-09-20 · U34 · claim checker ccK1 · One OPS run (about two calls) lost to a declined preprint opening on its publication tab (workflowMenuKey=publication_<id>_titleAbstract) with no decision button, so "Revert Decline" was "absent" until the workflow was opened with workflowMenuKey=workflow_5 · a seed-facts/scenarios.md line for OPS: after a decline the workflow lands on Title & Abstract; the Production entry holds "Revert Decline"
2026-09-20 · U34 · claim checker ccK3 · Two reruns (about four calls) lost to seeding a review round whose decisions chain the checker needs twice: "Request Revisions" and "Resubmit for Review" have a "Notify Reviewers" page (the chunk's first pass read one page and pressed "Record Decision" on it), and once a submission is accepted the round chain cannot be re-driven, so a second `sendExternalReview` seed with two completed reviews was needed · a scenarios.md "Decision behaviour" line listing which decisions carry the "Notify Reviewers" page (Request Revisions, Resubmit, Accept and Decline on a round with completed reviews; Cancel Review Round with an invited one) would have saved it
2026-09-20 · U34 · claim checker ccK3 · One rerun (about three calls) lost to the reviewer wizard's step 3 on OJS: `select[name="recommendation"]` does not exist (it is `reviewerRecommendationId`) and "Submit Review" stays on step 3 with no visible error when the list is unset, so the review read "Request Accepted" for the whole chunk and the author's "Read Review" was absent until a fix pass · a `reviewRounds[].reviewers[].files[]` seed key (scenarios.md "Field shapes not built yet" already lists it) or a `ReviewerPages`-style helper under shared/ for "upload a reviewer file and submit" would have saved it
2026-09-20 · U34 · claim checker ccK2 · Four reruns (about eight calls) lost to composer reads that picked the hidden sibling composer's editor and subject box (the wizard keeps every visited page's composer mounted), one more to the Contributors page key carrying the publication id rather than the submission's, and one to the Section editor's "Find Template" 401 Error dialog covering the page · a patterns.md "UI realities" line on the wizard's hidden sibling composers (read the visible editor and subject box) and a scenarios.md line on the publication-id menu key would have saved them.
2026-09-20 · U34 · test author tops · About three calls spent reading lib/ui-library sources (Composer.vue, FileAttacher*.vue, record.tpl, SideModalBody.vue) for the composer's class anchors, because every screen-locators row for the wizard's controls is an OJS/OMP row and the screen-notes describe the kit's evaluate readers rather than locators · a claim checker's `loc()` rows on the OPS composer (the templates list, the attach sources, the chips), or one screen-notes line naming the anchors, would have saved them (now added as the tops line).
2026-09-20 · U34 · test author tojs · About eight suite reruns (some twelve calls) lost to facts the scenarios' recipe or the kept records state wrongly or not at all: "Insert Content" › "Search" commits on Enter (K2's chunk report read its own record of 29 unfiltered rows as a filter, and the spec carries "as you type"); scenario 6 presses "Record Decision" on a page whose footer reads "Continue"; footnote s1's two-form-language journal cannot add its contributor on screen (the save answers 400 with no message until French is a submission language too); Mailpit's `subject:` query breaks on a subject carrying quotes ("Your review for \"{title}\" has been cancelled"), which scenarios.md "Mailpit" does not say; and a `filter({has})` locator built from a dialog-scoped locator silently matches nothing (patterns.md has no line on it). A scenario recipe that combines two givens for the first time (form languages plus an on-screen contributor) deserves one driven pass by the scenario writer or a claim-check line · patterns.md / scenarios.md
2026-09-20 · U35 · claim checker ccK3 · Three reruns of the manager phase (about six calls) lost to Participants-row reads scoped to the first visible dialog right after an inner legacy window or the Activity Log closed: the workflow dialog stays aria-hidden for the 450 ms modal slot, so a dialog-scoped getByRole finds no row and the script logged "no row menu"; one more rerun to the legacy Cancel confirm ("The data on this form has changed…") that Playwright dismisses, leaving the window open · a patterns.md pitfall-4/6 line saying "after an inner window closes, read the workflow dialog through page-level role queries or wait on the row locator itself", and a "UI realities" line naming the legacy form Cancel confirm, would have saved them.
2026-09-20 · U35 · claim checker ccK1 · Two extra OMP runs (about four calls) finding that a press's Internal Review panel lives on the round entry (workflow_2_<roundId>) and not on the stage entry, which no doc or note stated (ccK3's U35 note names the round entry for the decision buttons only); one more call lost to a premise of my own (a manager who does not hold the Production editor group is not offered under it). Fix: a scenarios.md or patterns.md line naming the round entry as the press's Internal Review panel address.
2026-09-20 · U35 · claim checker ccK2 · Two reruns of the window phase (about four calls) lost to premises no doc carried: the user grid's next twenty rows come from a "Load more" link, not from scrolling (footnote d's InfiniteScrollingFeature reads as scroll-loading, and the modal's scroll container is not the grid's), and a refused "OK" on "Assign Participant" re-renders the window with no message, so a Cancel-by-link close after it needs the "Close" arrow as a fallback and a fresh landing; one more call to find the probe servers' PHP error log (apps/<app>/playwright/.server-logs/server-<port>-probe.log, named in bin/probe-servers.js only) for the two 500s the browser traffic showed.
2026-09-20 · U35 · claim checker ccK4 · Three reruns (about seven calls) lost to premises no doc stated: a seeded submitted submission leaves the managers' "needs an editor" row whatever participants[] holds, so the "removing the last editor raises no task" read needed an Assign-through-the-window first; an OMP wizard submit assigns nobody unless the series radio on "For the Editors" is picked (scenarios.md names the key but not the step, and U05 K1's driver skipped OMP); and the Notify toast on OPS vanishes before a settled read, so the claim needed a MutationObserver. A seed-facts line for the first and a wizard note for the second would have saved them.
2026-09-20 · U35 · claim checker ccK5 · One misdirected flip and an undetermined block (about two calls) from footnote t16 naming a box that does not exist ("Assign Diana Editor as Section editor"): users.md reads "also a section editor of both sections" while seed-facts' Users table shows one group per person, and no doc states that nobody on publicknowledge holds two groups. A seed-facts line saying so, or a t16 written from the section form's box list (K4 had recorded it), would have saved it.
2026-09-20 · U35 · test author tops · Two reruns (about four calls) lost to the spec's "Submission" entry on a preprint server, which no OPS screen has (the OPS U25 suite already asserts the stage entries are exactly ["Production"]; footnote s and seed-facts name a workflow_1 "Submission" entry that lands on "Workflow: Production"); one rerun each to the "Please ensure…" refusal being a top-right toast where Fields places it in the window, and to the workflow dialog being an active-modal itself with the discussion view's wrapper visibility:hidden. A claim-check line reading the OPS side menu once, and a Fields wording "at the top right", would have saved them.
2026-09-20 · U35 · test author tojs · Four reruns of the OJS suite (about eight calls) lost to premises no doc or note carried: the "Notify" empty-message refusal is a top-right toast, not text in the window (the spec's Fields line and scenario 4 read as in-window text, and ccK4's record held the toast without a note saying so); the Assign and Edit windows keep the inapplicable "Assignment privileges"/"Permissions" headings and boxes in the DOM hidden, so a count-0 absence read fails; the "Edit Assignment" "No changes" sentence is a bare text node of the fieldset; a seeded submission on publicknowledge raises no needs-editor task (footnote s says the seed auto-assigns there, but no seed-facts line says the task is therefore absent, where the scratch-context line says it is present); and the "Editor Assigned (Auto)" template's stored subject holds "{$contextName}", not the journal name the spec quotes. One screen-note line each in seed-facts/scenarios.md would have saved them.
2026-09-20 · U35 · test author tomp · One rerun (about two calls) lost to the "Edit Assignment" › "Cancel" leave question the spec, scenario 2 and a seed-facts line state for all three apps: the OMP run asked nothing, as the OJS run had (T-ojs-1 was in the findings file but not yet folded into the spec or seed-facts when this suite was written), and the claim check's OMP record rested on a kit locator whose `.first()` pressed the "Close" arrow rather than the "Cancel" link. A claim-check driver that names the control it pressed in the record, and a fold of the OJS author's findings before the next app's author starts, would have saved it.
2026-09-20 · U35 · test author tojs · One final red and a rerun cycle (about five calls) on the S2 self-removal step: a participant removing their own row fires six workflow refetches, each refused (401) and each reopening the modal store's single "Error" dialog, so an "OK" pressed before the last answer lands is followed by a fresh dialog under four workers (never alone). No doc names the refetch fan-out or that `WorkflowPage.dismissErrorDialog()` assumes one refusal; a patterns.md line ("a refused landing raises one Error dialog per failed fetch, in turn: settle the page's API fetches before pressing OK") would have saved the cycle.
