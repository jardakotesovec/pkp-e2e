# Friction — what made an agent's task harder than it needed to be

What cost a screen-driving agent calls, time or retries that a better
brief, doc, kit, seed or fixture would have saved; that agent appends a row
at the end of its task. The maintenance session folds the rows and deletes
them under MAINTENANCE "The daily session".

One line per entry, appended at the end, in this shape:

`YYYY-MM-DD · U<nn> or sync · <role and agent id> · <what cost you calls, time or retries> · <what would have helped>`

Facts only, the same quarantine as everywhere else (nothing
security-shaped, no credentials).

## Entries
2026-09-23 · U36 · harness agent hU36 · The probe kit records no request bodies, so what each screen posts (the wizard's genre PUT, `save-metadata` posting `name[en]` alone, the review-files window's `selectedFiles[]` on "OK", the component window's fields) needed a hand-written `page.on('request')` listener in both parity scripts; the "OK" step it revealed was the one difference the first DB compare showed, a second drive round · a kit option on `launch()` that records the form-encoded bodies of non-GET requests matching a pattern (multipart parts minus file contents) into the run record
2026-09-23 · U36 · claim checker ccK1 · About eight calls and two extra seeded submissions lost to three premises nothing recorded: the legacy "Upload/Select Files" window's grid fills after idle(), so the first tick found no row and the Rule 4 leg ran with no copy on the list; deleting that original then silently took its copies along (a database cascade), destroying the state the next leg needed; and the upload wizard's "Summary of Changes" is a TinyMCE box whose hidden textarea times out on fill() · a patterns.md pitfall line for the select window's late grid and the TinyMCE summary box (both now in U36 screen-notes), and a seed-facts line that a copy dies with its original
2026-09-23 · U36 · claim checker ccK2 · About six calls and two reruns lost to three reads that lie quietly: the upload wizard hides its step-1 upload box with the class pkp_screen_reader, so isVisible() and getClientRects() report it visible and only the screenshot settled Rule 5a; the History tab puts each upload's "Download" behind the row's "Settings" toggle and a download collapses it again; and the publish/post panel needed an OJS and an OPS recipe copied from two suites' page objects · a patterns.md line on pkp_screen_reader-hidden controls (read the class, not visibility) and on the legacy grid "Settings" toggle re-collapsing, and a kit helper publish(page, app) that publishes the open workflow on a scratch context
2026-09-23 · U36 · claim checker ccK4 · Two things cost about five calls: the spec put the anonymizing-link box on Review › "Setup" (it is on "Reviewer Guidance"), so the first settings read found nothing; and a hand-written response-body capture added to read one 500 pulled an error text into .reports that had to be scrubbed and routed instead · a claim-check brief line that a probe script never records response bodies (the kit's run record keeps URL and status only, on purpose), and that a 500 whose content matters is described from the screen and, if security-shaped, routed without saving the body
2026-09-23 · U36 · claim checker ccK3 · About six reruns lost to legacy-grid traps nothing recorded: a file-number row regex with \b never matched "180article.pdf" (the number and name share one text run), the History tab's "Show events from prior versions" box and every History "Download" sit behind collapsed controls (the grid's "Search" link, each row's arrow), a nested "Upload a Dependent File" wizard is not the `.last()` dialog holding a fileUploadWizard div (the image went into the outer wizard and polluted the list), and the legacy grid search needs its own toggle and submit whose effect only the fetch-grid POST proves · a kit helper for legacy pkp grids (rows by index, open a row's extras, run the collapsed search and return the fetch-grid answer) and a patterns.md pitfall 10 line that grid filter forms are collapsed behind "Search"
2026-09-23 · U36 · claim checker ccK5 · About four calls lost to two quiet reads: the kit's `crashes` list stayed at 0 while the OJS review stage logged a Vue TypeError twice (Vue's error handler writes a console error, not an uncaught pageerror), found only by grepping every run record's `console`; and "which file does this galley serve now" has no screen but the publication's "Preview" › galley › "Download" name, which took three script passes to find (the Preview navigates the same tab, the galley link's name carries an icon) · a kit rule that counts console errors whose text starts with "TypeError"/"ReferenceError" (Vue-handled script failures) under `crashes`, and a patterns.md line on reading a galley's current file through the Preview's download name
2026-09-23 · U36 · security verification probe sec · One extra call and a judgment: the brief asks for the probe scripts under .reports/U36/sec/ with contents that do not describe the problem, but a script that verifies a concern necessarily shows what it checks, so both scripts were moved to the session scratchpad after the run (the run records, kept empty by record:false, stayed) · the verify brief says where a verification script lives (the scratchpad, outside every repo) instead of .reports/<feature>/sec/
2026-09-23 · U36 · test author tojs · One rerun and a findings block for a seed tie: `files[].note` is written in the same second as its file's upload, so the file's History orders the seeded upload above "Posted new note.", against scenario 6's bullet (a by-hand note comes later and heads the table); nothing in scenarios.md says the seeded rows tie · scenarios.md's `files[]` bullet states that the note's History row shares the upload's second (or the builder stamps the note a second later), so a test reads order only on rows it makes itself
