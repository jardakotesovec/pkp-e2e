# Scenario-endpoint parity ledger

One row per audited builder path per app: a dated verdict that the
scenario builder produces the state a real user would (PRINCIPLES A2),
corrected by dated notes in the Notes cell, never rewritten. The evidence
detail lives in `.reports/`, and a disputed row is settled by a fresh probe
on the current build.

| Date | App | Builder path | Parity check | Verdict | Notes |
|---|---|---|---|---|---|
| 2026-07-31 | all | context create (`ContextFactory` → `PKPContextService::add`) | code path identity: same service + validate call as the admin hosted-contexts REST flow; acting user is the seeding request's admin | PASS (by construction) | Parity fact: every scratch context auto-enrols `admin` as Manager, exactly like a UI-created context |
| 2026-07-31 | all | submission create/submit (`PKPSubmissionScenarioBuilder`) | mirrors `PKPSubmissionController::add()` step-for-step (submit-as group resolution incl. author fallback enrolment, stage assignment flags, author record, primary contact), then real `Repo::submission()->submit()` (fires `SubmissionSubmitted` → discussions/AssignEditors/tasks) + the submit endpoint's `NotificationManager::updateNotification([APPROVE_SUBMISSION])` tail | PASS after fixes (below) | Step-2 core seeds NO submission file — the wizard requires one; tests needing files upload via UI until a `files` key lands with its own parity entry |
| 2026-07-31 | ojs | submission (submitted) — full UI-wizard vs scenario DB diff | live spot-check: real Chromium wizard run vs scenario twin, field-by-field psql diff (`lib/pkp/docs/e2e/tracking/.reports/step1-harness/parity-ojs-submission.md`) | 3 DEFECTS found → FIXED same session, re-verified by SQL against the same UI baseline | (1) `publications.primary_contact_id` NULL — a stale-object publication edit erased it; builder now re-fetches before the title edit. (2) APPROVE_SUBMISSION + FORMAT_NEEDS_APPROVED_SUBMISSION notifications missing — builder now runs the submit endpoint's NotificationManager tail. (3) abstract absent (wizard-required) — the core now defaults an abstract; pass `abstract` to override |
| 2026-07-31 | ojs | submission (submitted) — event log + files | same spot-check | ACCEPTABLE DEVIATIONS | UI carries wizard-autosave `metadataUpdated`/`fileRevised` rows the scenario lacks (log noise, no state depends on it); `submission_files` 1 (UI, required upload) vs 0 (core seeds none — documented limitation) |
| 2026-07-31 | ojs, ops | bootstrap first section vs the Context::add default section | fresh-bootstrap section listing (was: TWO active "Articles"/ART sections — the hook default + the seeded one; ambiguous wizard radios and abbrev lookups) | DEFECT → FIXED | the first declared bootstrap section now RENAMES the hook-created default (what a journal manager would do) instead of adding a duplicate; verified: OJS seeds exactly ART+REV, OPS exactly PRE |
| 2026-07-31 | all | decisions (`Repo::decision()->add` with app-resolved `DecisionType`) | real repository add: event log, `runAdditionalActions` (stage moves, review-round creation), `DecisionAdded` event; empty actions array = the UI's skip-email path | PASS | Decision editor is the installer's `admin` (site admin), not a context editor — extend with an `editor` key when a feature needs attribution |
| 2026-07-31 | all | reviewers (`EditorAction::addReviewer` + `ReviewerAction::confirmReview`) | real editor/reviewer actions; seeding uses the Add Reviewer form's `skipEmail` path | DELIBERATE DEVIATION | Seeded assignments carry no `REVIEW_REQUEST` email-log row (the skip-email path writes none); accept/decline logs match the UI path. Scenario-side mail is dropped by `Mail::fake()` regardless |
| 2026-07-31 | all | builder harness (`PKPTestController::runBuilder`) | every build runs inside one DB transaction; failure rolls back (no half-created state); router context is forced to the target context so notification managers/mailables see the same context the UI path would | PASS | Files/dirs created before a rollback (context dirs) may orphan on disk — DB state stays clean Correction 2026-08-23: the router-context forcing lives in `PKPSubmissionScenarioBuilder::build` alone (`forceRequestContext`); context and bootstrap builds run with the site-wide router context. Verdict unchanged. |
| 2026-07-31 | ojs | issues overlay (bootstrap `issues[]`, publish via `Repo::issue()->updateCurrent`) | `IssueGridHandler::publishIssue` parity: published flag + datePublished + current issue | PASS | Scheduled-publication publishing on issue publish not exercised (no publications assigned at bootstrap) |
| 2026-07-31 | ops | `reviewRounds` key | rejected with 400 (`specKey: reviewRounds`) — never silently dropped | PASS (verified live) | |
| 2026-07-31 | all | review-rounds remainder loop (`reviewRounds[]` + round-creating `decisions[]`) | U26 OJS suite build: declaring `newExternalReviewRound` in `decisions[]` alongside two `reviewRounds[]` plans seeded THREE rounds — the promoting-decision branch consumes no round plan (the decision reports no new stage id), so the remainder loop builds every declared plan on top | DOCUMENTED QUIRK | Workaround: declare extra rounds without the round-creating decision. Fix option: make same-stage round-creating decisions consume one plan. Evidence: `lib/pkp/docs/e2e/tracking/.reports/u26-review-stage-and-rounds/forwarded-observations.md` |
| 2026-08-02 | all | reviewer due dates (`seedRoundReviewers` → `EditorAction::setDueDates`) | U27 claim check: builder seeded review due = today + (response + review) weeks; the app's Add Reviewer form (`HasReviewDueDate::getDueDates`) sets each date from its OWN interval (review due = today + review weeks). Coincided on the 4/4 baseline (both 28 days), which masked it — a 2/6 journal exposes it | DEFECT → FIXED | Builder now `use`s the app's own `HasReviewDueDate` trait (inheriting its 4-week review / 3-week response fallbacks — the old code fell back to 4/4) and formats `Y-m-d` as the datepicker's altField submits. Verified live post-fix: seeded `date_due` = `date_response_due` = today + 4 weeks on the 4/4 baseline (ojs_test review 228). OJS suite S8's row assertion updated from the old +8-week value to the app-true +4 weeks |
| 2026-08-02 | all | reviewer "Review pending." task (`EditorAction::addReviewer` notification) | U27 claim check reported seeded assignments lack the reviewer task notification (`claimcheck-B.md`, B2a). Re-verified live: fresh seeds DO carry `NOTIFICATION_TYPE_REVIEW_ASSIGNMENT` level-TASK rows (ojs_test 228 / omp_test 149, invited AND accepted) and julia's live Tasks grid shows "Review pending." for them | CLAIM DISPROVEN — PASS, no change | The B2a probe read the Tasks dialog one second AFTER driving the seeded review to submission; `PKPReviewerReviewStep3Form::execute` (line 254) deletes the task on the reviewer's own submit, so the absence was app-true behavior, not a builder gap. `EditorAction::addReviewer` (which the builder calls) is itself the app path that creates the task |
| 2026-08-07 | all | context scenario `orcid` passthrough (`PKPContextScenarioBuilder::parseOrcidSettings` → `PKPContextService::edit`) | U04: the builder writes the "ORCID" settings-tab state through the SAME service call the tab's form save runs (PUT `contexts/{id}` → `PKPContextService::edit`, schema-level `orcidClientSecret` encryption included). Verified live against `ojs_test` (context 168): `orcidEnabled`/`orcidApiType`/`orcidClientId` plus the encrypted secret and the schema-materialized `orcidCity`/`orcidLogLevel` defaults — the same row shape a UI save leaves (probe-groupA persistence check) | PASS | Defaults: enabled, Public Sandbox, dummy credentials (`APP-TESTCLIENTID`); `city`/`sendMailToAuthorsOnPublication` optional. OAuth can never complete in tests (outbound HTTP fails fast at the dead-port `[proxy]`, and no real ORCID account backs the dummy sandbox credentials), so dummy credentials are exactly as good as real ones for every screen this state gates Correction 2026-08-20 (maintainer): there is no OS or network firewall and no DTD mirror; the blocking mechanism is the dead-port `[proxy]` in `config.test.inc.php` (missing from the rebuild until 2026-08-20) plus the dummy sandbox credentials. The wording was corrected in place; the verdict is unchanged. |
| 2026-08-07 | all | ORCID iD fixture state: context-users `orcid`/`orcidIsVerified` (`UserSeeder`) and submission `author {orcid, orcidIsVerified}` (`PKPSubmissionScenarioBuilder`) | U04: seeds the u27-proven recipe (`user_settings`/`author_settings` rows `orcid` + `orcidIsVerified`) through `Repo::user()->edit` / `Repo::author()->edit` — the same rows the OAuth verify landing stores. Verified live (`ojs_test` user 482 / author 370) | DELIBERATE DEVIATION | The token fields the real OAuth flow also stores (`orcidAccessToken`/scope/refresh/expiry) are NOT seeded — ORCID-issued, meaningless offline, and no screen state reads them (`Identity::hasVerifiedOrcid()` gates on `orcidIsVerified` alone). The verified state is unreachable through ANY UI in the test env (dead-port `[proxy]` + dummy sandbox credentials with no real ORCID account to verify against), so a fixture write is the only route; deposits/token-revocation jobs seeded this way fail fast at the dead-port proxy (reinstated 2026-08-20; absent at the time of this entry) if a queue drain pops them — harmless, asserted by no test Correction 2026-08-20 (maintainer): there is no OS or network firewall and no DTD mirror; the blocking mechanism is the dead-port `[proxy]` in `config.test.inc.php` (missing from the rebuild until 2026-08-20) plus the dummy sandbox credentials. The wording was corrected in place; the verdict is unchanged. |
| 2026-08-23 | all | reviewer method (`seedRoundReviewers` → `EditorAction::addReviewer`) | builder passed `$reviewMethod = null`, letting the `review_assignments` column default (1 = anonymous) win; the Add Reviewer form ALWAYS submits the context's `defaultReviewMode` with a double-anonymous fallback (ReviewerForm::initData ~206-210; context schema default 2) | DEFECT → FIXED | Builder now resolves the method exactly as the form does and passes it. Verified live post-fix (`ojs_test` reviews 76/77: `review_method` = 2 on the baseline journal). No per-reviewer `method` spec key existed, so none was added (A3) |
| 2026-08-23 | all | reviewer post-add stamp (`seedRoundReviewers`) | the Add Reviewer form's save also stamps `considered = REVIEW_ASSIGNMENT_NEW` and the section/series default `reviewFormId` (ReviewerForm::execute ~371-375); builder stamped only `dateNotified` | DEFECT → FIXED | Now mirrors the form's edit call: `dateNotified` + section-resolved `reviewFormId` (ReviewerForm::initData ~212-221 resolution plus the execute-side ReviewFormDAO validation) + `considered` = NEW (0). Verified live: `considered` = 0 explicit; `review_form_id` NULL where the section sets no default form (form-identical) |
| 2026-08-23 | all | `published: true` (`PKPSubmissionScenarioBuilder::execute`) | builder called bare `Repo::publication()->publish()`; the real endpoint (PKPSubmissionController::publishPublication ~1413-1445) additionally (a) runs `validatePublish` and refuses on errors, (b) sweeps author stage assignments to `canChangeMetadata = 0` (controller-only code, exists nowhere else), (c) publishes with `$submissionStatus = false` then re-derives status/current publication off a fresh fetch | DEFECT → FIXED | Full controller tail mirrored (cited in-code). `validatePublish` errors now surface as a 400 SpecException and roll back — verified live: a `decline` + `published: true` spec 400s with the endpoint's own "declined" error. Post-fix seed verified: author stage assignment `can_change_metadata` = 0, publication status 3, `date_published` stamped (`ojs_test` submission 113) Note 2026-08-25: OJS `643b0521de` (pkp/pkp-lib#12800) adds `pmurNeedsReviewRound()` to `validatePublish`; the builder mirrors that gate, so a seed publishing a PUBLISHED_MANUSCRIPT_UNDER_REVIEW version with no linked review round 400s with `publication.required.pmurReview` — app-true, not a builder defect. The endpoint's new `reviewRoundIds` write path has no builder counterpart; add one only when a spec needs it. No current seed is affected, and the rebase left every other audited path untouched. Note 2026-09-09: pkp/pkp-lib#13109 (lib/pkp `18f402e585`) removed the controller's author-assignment sweep (b); the builder dropped its mirror the same sync, so a seeded publish leaves an Author's `can_change_metadata` exactly as a screen publish does (the U40 suites' A4 legs are the live check). |
| 2026-08-23 | all | review-rounds remainder loop (`reviewRounds[]` beyond round-creating decisions) | bare `ReviewRoundDAO::build` left `status` NULL and created no notification; the real path (DecisionType::createReviewRound ~511-541) builds with `REVIEW_ROUND_STATUS_PENDING_REVIEWERS`, the latest UNPUBLISHED publication id (~556-562), and creates the `NOTIFICATION_TYPE_REVIEW_ROUND_STATUS` row | DEFECT → FIXED | All three mirrored (cited in-code; same `NotificationManager::createNotification` call). Verified live: remainder round carries the notification row and, after reviewer assignment, the same recalculated status (7) as its decision-created sibling round (`ojs_test` rounds 94/95) |
| 2026-08-23 | all | enrolment masthead (`UserSeeder::seed` → `assignUserToGroup`) | seeder passed `masthead` NULL — a state only self-registration leaves. Live probe (scratch journal `mhprobe23`): the site-admin Users grid's edit form pre-checks every masthead checkbox and `UserForm::saveUserGroupAssignments` (~188-194) writes an EXPLICIT boolean for every assigned group on save — the grid enrolment wrote `masthead` = 1 for the added role AND flipped the pre-existing NULL row to 1; the context Users & Roles "Edit" action routes into the invitation wizard, whose acceptance stores the inviter's explicit choice (UserRoleAssignmentReceiveController ~136-142; reviewers forced true); both reviewer enrol forms pass true | DEFECT → FIXED | Seeder now passes `masthead: true` — the dominant admin-enrolment default. Variants: invitation flow has NO preselected default (required select; inviter chooses; reviewers auto-true); self-registration and the wizard's auto-enrol-as-author leave NULL (the submission builder's submit-as fallback mirrors PKPSubmissionController ~684 and correctly stays NULL). Public masthead display remains gated by the GROUP's masthead flag, exactly as in the app. Cold-bootstrap verified: roster rows `masthead` = 1 |
| 2026-08-23 | all | bootstrap categories (`PKPBootstrapSeeder::addCategory`) | UI-created categories always store `sortOption` — the category form's select defaults to `Repo::submission()->getDefaultSortOption()` (CategoryForm ~124-131) and submits a value; seeder stored none (NULL) | DEFECT → FIXED | Seeder writes the same default. Cold-bootstrap verified: all 7 seeded categories carry `datePublished-DESC` |
| 2026-08-23 | ojs | bootstrap issue create (`BootstrapSeeder::executeOverlay`) | the Create Issue form derives `accessStatus` from the journal's publishing mode (IssueForm::execute ~246-258); seeder relied on the column default | DEFECT (benign) → FIXED | Now derived the form's way: open/unset → `ISSUE_ACCESS_OPEN`, subscription/none → `ISSUE_ACCESS_SUBSCRIPTION`. Effect on the open-access base seed unchanged (`access_status` = 1, also the column default); the derivation matters the moment a subscription-mode seed appears |
| 2026-08-23 | ojs | bootstrap issue publish (`BootstrapSeeder::executeOverlay`) | `IssueGridHandler::publishIssue` (~556-597) also runs `Repo::issue()->createDoi`, fires `Hook::call('IssueGridHandler::publishIssue', [&$issue])` and marks DOIs stale via `Repo::doi()->issueUpdated`; seeder only set published/datePublished/updateCurrent | DEFECT → FIXED | All three mirrored in the handler's order. The seed enables no issue DOI type (bootstrap.js sets no DOI settings), so `createDoi`/`issueUpdated` are internally-guarded no-ops today — mirrored anyway so the shape stays true if the seed ever enables issue DOIs. The handler's delayed-open-access branch (subscription-only) and its scheduled-publication sweep (no publications exist at bootstrap) don't apply and are noted in-code |
| 2026-08-23 | all | wizard submit `confirmCopyright` event log | the real submit endpoint writes a `SUBMISSION_LOG_COPYRIGHT_AGREED` event-log row when the wizard's copyright checkbox was ticked (PKPSubmissionController ~893-901); seeded submissions carry no such row | ACCEPTED GAP (documented, no code) | The checkbox is optional per-submission UI input, not derived state, and no screen state reads the row (activity-log listing only). Revisit if a spec asserts the submission event log's copyright entry |
| 2026-08-23 | all | UserSeeder + bootstrap mechanisms not named above (user create via `Repo::user()->add`, role-key resolution by `nameLocaleKey`, sub-editor assignment via `SubEditorsDAO::insertEditor`, section/series creation incl. the first-section rename, category tree creation, issues overlay plumbing) | retroactive audit sweep (2026-08-23): each mechanism reviewed against its UI counterpart (registration/user forms, section/series settings forms, category form, issue form/grid) | PASS (with this sweep's dated DEFECT rows as the exceptions) | Blanket verdict drafted in the audit; everything not named in a 2026-08-23 DEFECT row matched the UI path at audit time |
| 2026-08-25 | omp | submission `workType` (`SubmissionScenarioBuilder::parseSubmissionOverlay`, new shared hook `parseSubmissionOverlay`) | the OMP start form always posts a work type (the Monograph radio arrives preselected and the field is required), so every wizard-created monograph stores `workType` = 2; the builder's direct `Repo::submission()->add()` left it NULL — a state no UI path produces, and the wizard header then read "Submitting an Edited Volume." on seeded drafts (`getSubmittingTo()` strict-compares against WORK_TYPE_AUTHORED_WORK) | DEFECT → FIXED | New optional spec key `workType: 'monograph' \| 'editedVolume'` (default monograph — the form's preselected value); unknown values 400. Verified live: a seeded OMP draft's wizard header reads "Submitting a Monograph." (U21 S5 asserts it) |
| 2026-08-25 | all | submission `participants[]` (`PKPSubmissionScenarioBuilder`) | new optional key `participants: [{username*, role*}]` — writes the same `Repo::stageAssignment()->build()` row the workflow's Assign Participant form writes, with the resolved user group's own `recommendOnly`/`permitMetadataEdit` defaults. Verified live (U21 S4): a seeded `sectionEditor` participant opens another author's draft wizard with Save for Later/Continue but no Cancel — the fn-f-probed assignment state | DELIBERATE DEVIATION (documented) | The UI flow additionally sends the assignment notification/email (`StageParticipantGridHandler`); the seed writes neither (seed-side, and `Mail::fake()` would drop the mail anyway). Add a mail-bearing variant only when a spec asserts the assignment email |
| 2026-08-25 | ojs (shared hook) | context scenario `sections[]` (`PKPContextScenarioBuilder` structure hook + OJS `ContextScenarioBuilder`) | scratch contexts can now declare a section list with the SAME shape and machinery as the bootstrap seed (`BootstrapSeeder::addSection`): the first declared section renames the hook-created default, later ones are added; seeded before `users[]` so sub-editor assignments resolve. Verified live (U21 S5/S12): declared sections appear on the start form and wizard exactly as UI-created ones | PASS | Apps without an override 400 on the key (`parseStructure` default throws) — never silently dropped. Section flags (`editorRestricted`, `isInactive`) deliberately NOT seedable: the closure tests drive the section form UI (the flag change IS the behavior under test) |
| 2026-08-25 | all | context `supportedSubmissionLocales` (`ContextFactory::parseParams`) | new optional context key mirroring the Languages settings grid's submission toggles: sets `supportedSubmissionLocales` and keeps `supportedSubmissionMetadataLocales` in step (the grid handler unions an enabled submission locale into the metadata locales — `LanguageGridHandler::saveLanguageSetting` ~105-110), plus `supportedAddedSubmissionLocales`. Verified live (U21 S5): a `['en','fr_CA']` scratch journal shows the start form's Submission Language radio and the wizard's "Submitting to the {section} section in {language}." line with the Change control, and reconfigure to fr_CA behaves as the UI-configured bilingual journal probed for the spec | PASS | Passes through `PKPContextService` validation like every other context param; bootstrap specs are unaffected (key absent) |
| 2026-08-26 | ops | context scenario `sections[]` (OPS `ContextScenarioBuilder::parseStructure`/`addStructure`) | mirrors the OJS override on the same shared structure hook, reusing OPS `BootstrapSeeder::addSection` (field roster kept in step with the OPS bootstrap seeder: abbrev*, path, title, policy — OPS sections carry a path). Verified live (U21 OPS S5/S12): declared sections render on the start form, the reconfigure panel and the sections grid exactly as UI-created ones; the first declared entry renames the hook-created default "Preprints" | PASS | Same deliberate limit as the OJS row: `editorRestricted`/`isInactive` flags are NOT seedable — the closure tests flip them through the section form/grid UI (the flag change is scenario 12's own step) |
| 2026-09-05 | ojs, omp | context scenario `review` passthrough (`PKPContextScenarioBuilder::parseReviewSettings` → `PKPContextService::validate` + `::edit`) | the Review "Setup" and "Reviewer Guidance" forms save by PUT `contexts/{id}` (`PKPContextController::edit`: schema validation with the context's form locales, then `PKPContextService::edit`); the builder runs the same validate + edit with the same value shapes (integers, booleans, locale maps). Verified live on `ojs_test` context 11: `journal_settings` rows `defaultReviewMode=3`, `restrictReviewerFileAccess=1`, `reviewerAccessKeysEnabled=1`, `numWeeksPerResponse=2`, `numWeeksPerReview=6`, the four `numDays*ReminderDue` values, `reviewGuidelines`/`competingInterests` under `en`; on screen (hK `screens.js`) the Setup radios/boxes/fields and the guidance editors show exactly those values, the reviewer's step 2 shows the guidelines and step 1 asks the competing-interests question. OMP context 12 built with `internalReviewGuidelines` too | PASS | Keys are accepted only when the app's context schema carries them (`reviewerSuggestionEnabled`, OMP `internalReviewGuidelines`), so a foreign key is a 400 rather than a silent row; a non-integer count is a 400 (`ctype_digit`), where the form's `convertStringsToSchema` would also refuse it. Evidence: `.reports/U28/hK/` |
| 2026-09-05 | ops | context scenario `review` and `reviewForms` keys (OPS `ContextScenarioBuilder::assertReviewSupported`) | rejected with 400 (`specKey: review` / `reviewForms`) — OPS renders no Settings › Workflow › Review tab (`workflow.tpl` `{if $hasReviewStage}`), so no UI path writes these settings | PASS (verified live) | Same rule as the OPS `reviewRounds` row. Evidence: `.reports/U28/hK/` |
| 2026-09-05 | ojs, omp | context scenario `reviewForms[]` (`PKPContextScenarioBuilder::addReviewForm`) | mirrors the Review Forms grid step for step: `ReviewFormForm::execute` (inactive, `REALLY_BIG_NUMBER` sequence, `insertObject` + `resequenceReviewForms`), `ReviewFormElementForm::execute` per item (`included=1`, `possibleResponses` only on checkboxes/radio/dropdown as a per-locale list, `insertObject` + `resequenceReviewFormElements`), then the grid's "Active" checkbox (`activateReviewForm`: `setActive(1)` + `updateObject`). Verified live on `ojs_test` context 11: `review_forms` rows seq 1/2 with `is_active` 1/0, `review_form_settings` title/description rows, `review_form_elements` seq 1/2 with `element_type` 5/3 and `required` 1/0, `review_form_element_settings` `possibleResponses=["Yes","No"]`; on screen the grid lists "hK form" with the Active box checked and "hK inactive form" unchecked | DELIBERATE DEVIATION (minor) | The activate handler's `createTrivialNotification` (the manager's "saved" toast) is not created — a UI feedback row no screen reads back. An untouched description stores an empty-string row, as the legacy form's empty editor posts. Evidence: `.reports/U28/hK/` |
| 2026-09-05 | ojs, omp | reviewer `reviewForm: "<title>"` (`PKPSubmissionScenarioBuilder::resolveActiveReviewFormId` → `Repo::reviewAssignment()->edit(['reviewFormId'])`) | the reviewer row's "Edit" window lists the context's ACTIVE forms only and saves the chosen id through `Repo::reviewAssignment()->edit` (`EditReviewForm::execute` ~207-215); the builder resolves the title against `ReviewFormDAO::getActiveByAssocId` and runs the same edit after the Add Reviewer stamp. Verified live: `ojs_test` review 129 (`review_form_id=3`), the seeded (accepted) reviewer's step 3 shows "hK form", its description, the required radio question with "Yes"/"No" and the free-text item; an inactive title is a 400 naming the active titles | PASS | The window's "assignment changed" notification and `EditReviewNotify` mail fire only when dates or method change, which a form-only edit never does, so nothing is skipped. Evidence: `.reports/U28/hK/` |
| 2026-09-06 | ojs, omp | context scenario `reviewForms[].elements[].included` (`PKPContextScenarioBuilder::parseReviewForms` → `addReviewForm`) | the item window's "Included in message to author" box posts `included` and `ReviewFormElementForm::execute` stores it as `setIncluded($this->getData('included') ? 1 : 0)` (line ~149), on by default for a new item (`initData` `included => 1`); the builder now reads the optional boolean the way `required` is read (`(bool) get('included', true)`) and writes the same `setIncluded(... ? 1 : 0)` before `insertObject`. Verified live on screen (hI `included.js`, OJS context 22 / OMP context 21): a form seeded with one `included: false` item and one default item opens Edit › Form Items › item "Edit" with the box unticked for the first and ticked for the second (`item1-excluded-*`, `item2-included-*`) | PASS | Before this row the builder wrote `included=1` unconditionally, so the key was not seedable; the earlier `reviewForms[]` row's other deviations (no toast, empty description row) are unchanged. Evidence: `.reports/U29/hI/` |
| 2026-09-06 | ojs, omp | reviewer `status: 'completed'` (+ `recommendation`, `comments`) (`PKPSubmissionScenarioBuilder::runReviewerStep` / `completeReview`) | the wizard's "Accept Review, Continue to Step #2", "Continue to Step #3" and "Submit Review" + "OK" are `PKPReviewerHandler::saveStep` → `PKPReviewerReviewStep{1,2,3}Form::execute`; the builder instantiates those same forms on the assignment under the reviewer's identity (`Registry` user swap, as `confirmReview` already ran) with step 3's posted fields set (`comments`, `commentsPrivate` empty, `reviewerRecommendationId`, no form responses), so `confirmReview`, `saveReviewComment`, the `dateCompleted`/recommendation edit, the editors' `REVIEWER_COMMENT` notifications + `ReviewCompleteNotifyEditors` email-log rows, the reviewer-task deletion, the `reviewReady` event-log row and the `Mailable::build` / `*form::execute` hooks are the app's own. psql diff of a seeded twin against one completed by hand through the wizard (`db-diff-{ojs,omp}.md`): `review_assignments` (all 33 columns), `submission_comments`, `notifications`, `event_log` (incl. `user_id` = the reviewer), `email_log` (subject and body) and `review_form_responses` identical after masking ids, timestamps and the tag. On screen (`editor-review-stage-A-*`, `reviewer-completed-list-A-*`): the editor's row reads "Review Submitted" ({OJS} with the recommendation) with "Read Review", and "Request Response"/decisions are offered; the reviewer's "Completed" view lists it | PASS after fixes | Two defects found by the diff and fixed the same session: (1) a bare `comments` string was stored raw where TinyMCE posts `<p>…</p>` — the builder now wraps a markup-less string; (2) see the harness row below. `recommendation` is the tail of the default translation key (`accept` default, `pendingRevisions`, …), resolved against the context's ACTIVE recommendations (a 400 names them); OMP has no step-3 list (`hasCustomizableReviewerRecommendation()` false) and 400s on the key; both keys 400 on any other status; a review form with required questions is refused as the wizard refuses it (`getRequiredReviewFormElementIds`). Not seeded: reviewer files, private comments, form responses (add with their own rows when a spec needs them). Evidence: `.reports/U30/harness/` |
| 2026-09-06 | all | builder harness mail fake (`PKPTestController::fakeMail`, replaces `Mail::fake()`) | the app's `PKP\mail\Mailer::send` runs `$mailable->setData($locale)` and (via Laravel's `Mailable::send`) `build()` before the transport, and `Repo::emailLogEntry()->logMailable` — which the app calls right after `Mail::send` — compiles the logged subject and body from that `viewData`. Under Laravel's bare `MailFake` neither step ran, so EVERY seeded `email_log` row (submission acknowledgement, editor assignment, review accepted, review complete, …) carried the raw template (`Review complete: {$reviewerName} recommends {$reviewRecommendation} …`) where the UI path logs the rendered text — the U30 diff's first run exposed it on the review-complete rows | DEFECT (all builders, since 2026-07-31) → FIXED | The fake is a `MailFake` subclass whose `send` runs the same `setData` + `build` first; mail is still recorded in memory and never delivered. Verified: the seeded and the by-hand twins' whole `email_log` (8 rows OJS, 6 OMP) identical in subject and body, URLs included. Suites unaffected (U28 OJS green after the change); the `logMailable` rows of earlier seeds in a warm DB stay raw until the next reset. Evidence: `.reports/U30/harness/` |
| 2026-09-06 | ojs, omp | reviewer `status: 'accepted'` (unchanged: bare `ReviewerAction::confirmReview`) | the on-screen accept is the step-1 form: besides `confirmReview` it moves `step` to 2 and, when the context carries competing-interests guidelines, stamps `competingInterestsDeclared` (`PKPReviewerReviewStep1Form::execute`). A seeded acceptance leaves `step` = 1 and no declaration, so the wizard reopens on step 1 with "Save and continue" | DELIBERATE DEVIATION (documented) | Tried the step-1 form for `accepted` in this session: 9 of 16 U28 OJS scenarios walk the wizard from step 1 on a seeded acceptance (`walkToStep3`) and failed, so the old path stays and `scenarios.md` now states the fact. Lift it together with the U28 suites' `walkToStep3` in a maintenance slice if a spec ever asserts the post-accept step. Evidence: `.reports/U30/harness/` |
| 2026-09-06 | ojs, omp | submission scenario `reviewerSuggestions[]` (`PKPSubmissionScenarioBuilder::parseReviewerSuggestions` → `ReviewerSuggestion::create`) | the wizard's "Add Reviewer Suggestion" window posts `POST submissions/{id}/reviewers/suggestions`, and `ReviewerSuggestionController::add` is `validated()` + `ReviewerSuggestion::create()`; the builder runs the same `create` with the same payload (validated by that request's own rules, `AddReviewerSuggestion::rules` rebuilt through `ValidatorFactory` with the `MultilingualInput` rule over the context's form locales, the per-submission unique email checked within the list; `suggestingUserId` = the submitter and `submissionId` merged in as `prepareForValidation` does), before `submit()`, because the API's `SubmissionIncompletePolicy` allows adds only on an incomplete submission; a bare reason is wrapped in `<p>` as the rich-text box posts it. psql diff of a seeded twin against one typed by hand on the wizard step (`db-diff-{ojs,omp}.md`): `reviewer_suggestions` (all 10 columns) and `reviewer_suggestion_settings` (givenName, familyName, affiliation, suggestionReason under `en`) identical after masking ids and timestamps. On screen (`wizard-step-listed-B-*`, `editor-submission-stage-A-*`, `editor-review-stage-C-*`): the manager's Submission stage shows "Reviewers Suggested by Author" with the seeded row and no action, a review round shows each row with a "<name> More Actions" button, on both apps | PASS | Not seeded: `orcidId` (the box exists only with ORCID on; its own row when a spec needs it) and an approved suggestion (`approvedAt`/`reviewerId`: reached through the panel's "Add Reviewer" on screen). A context with `reviewerSuggestionEnabled` off is a 400 (the step does not exist there); OPS 400s on the key; seven bad shapes 400 (`bad-shapes-*.json`). Evidence: `.reports/U31/harness/` |
| 2026-09-07 | all | context scenario `copyrightNotice` (`PKPContextScenarioBuilder::parseIntakeSettings` → `saveFormSettings`: `PKPContextService::validate` + `::edit`) | the Settings › Workflow › Submission › "Author Guidance" form (`SubmissionGuidanceSettings`, a lib/pkp form with no app subclass) saves by PUT `contexts/{id}` (`PKPContextController::edit`: `convertStringsToSchema`, schema validation with the form locales, `PKPContextService::edit`); the builder runs the same validate + edit with the same locale-map shape, in ONE call together with the other settings passthroughs (`review` included), as a form save is one PUT. Driven by hand on OJS, OMP and OPS (`settings.js`): the seeded context's `copyrightNotice` row (`en`, `<p>…</p>`) is byte-identical to the one the editor typed and saved (`db-diff-{ojs,omp,ops}.md`), and the seeded context's Author Guidance screen shows the notice in the editor (`seeded-readback-*`) | PASS | A bare string is wrapped under the primary locale without `<p>` (the editor posts `<p>…</p>`): pass markup when the on-screen shape matters. A non-string/non-map value is a 400. Evidence: `.reports/U21/harness/`, `.reports/U21/hU21/` |
| 2026-09-07 | all | context scenario `metadata {item: off/enable/request/require}` (`PKPContextScenarioBuilder::parseIntakeSettings`) | the Settings › Workflow › Submission › "Metadata" form (`PKPMetadataSettingsForm`; the OJS/OMP/OPS subclasses only ADD `enablePublisherId`/`enableArticleNumber` fields) posts each `FieldMetadataSetting` form-encoded, so its "off" reaches the API as the string `"0"` (`convertStringsToSchema` has no string case and the schema's `string` rule refuses the bare integer `Context::METADATA_DISABLE`); the builder maps the words to `Context::METADATA_*` with `off` as `(string) METADATA_DISABLE`. Driven by hand on OJS, OMP and OPS: keywords → require, citations → off (box unticked), dataAvailability → require, and keywords → off: every seeded row equals the by-hand row (`keywords=require`, `citations=0`, `dataAvailability=require`, `keywords=0`, `citations=enable`; `db-diff-*.md`, `db-diff-off-*.md`); the seeded context's Metadata screen shows the require radio / the unticked box (`seeded-readback-*`, `off-seeded-readback-ojs`) | PASS | Only the items `PKPMetadataSettingsForm` lists are accepted, each gated on the app's context schema (`metadata.publisherId` is a 400); a number or another word is a 400. The by-hand form posts every item, so a by-hand context also carries rows the seed leaves at the install default (`citations=request`): same values, one row more. Evidence: `.reports/U21/harness/`, `.reports/U21/hU21/` |
| 2026-09-07 | all | context scenario `submissionAcknowledgement` (off/submittingAuthor/allAuthors), `copySubmissionAckPrimaryContact`, `copySubmissionAckAddress` (`PKPContextScenarioBuilder::parseIntakeSettings`) | the Settings › Workflow › Emails form (`PKPEmailSetupForm`; OJS/OMP use it bare, OPS's `EmailSetupForm` subclass adds the posted-acknowledgement field) saves by the same PUT; the builder maps the words to `Context::SUBMISSION_ACKNOWLEDGEMENT_*` (`off` = null) and passes the address string through the service's own `after` validator (each comma-separated part `email_or_localhost`). An empty address box arrives as null on the form's PUT and on this POST alike (`ConvertEmptyStringsToNull`), so null is accepted as the cleared state. Driven by hand on OJS, OMP and OPS: submitting author only + "Yes, send a copy to …" + Notify Anyone, and "Do not send an email.": every seeded row equals the by-hand row (`submissionAcknowledgement=submittingAuthor`, `copySubmissionAckPrimaryContact=1`, the address verbatim; at off: NO `submissionAcknowledgement` row and NO address row on either context; `db-diff-*.md`, `db-diff-off-*.md`), and the seeded context's Emails screen shows the radio, the copy radio and the address (`seeded-readback-*`, `emails-seeded-*.png`) | PASS | OMP's context schema overrides `copySubmissionAckAddress` with a single `email_or_localhost` rule, so two comma-separated addresses are a 400 on OMP (the screen refuses them the same way, although its help text says commas separate addresses: a product finding for U21's register, not a builder deviation); OMP's schema also drops the `false` default of `copySubmissionAckPrimaryContact`, so a bare OMP context has no row where a seeded `false` writes `0` (both read false; the form's "No" writes `0` through the same boolean coercion). At off, the reopened Emails screen shows NO Submission Confirmation option selected, seeded or by hand alike (`emails-off-byhand-reloaded-ojs`). Evidence: `.reports/U21/harness/`, `.reports/U21/hU21/` |
| 2026-09-07 | ops | context scenario `sections[].wordCount` and `sections[].abstractsNotRequired` (OPS `ContextScenarioBuilder::parseStructure` / `BootstrapSeeder::addSection`) | the OPS sections grid's Edit form (`controllers/grid/settings/sections/form/SectionForm`) reads both (`readUserVars` incl. `abstractsNotRequired`, `wordCount`) and writes `setAbstractsNotRequired(… ? 1 : 0)` / `setAbstractWordCount((int) …)`; the overlay now mirrors the OJS overlay field for field (rename-the-default branch through `Repo::section()->edit`, added sections through `setData` + `Repo::section()->add`), keeping the OPS bootstrap seeder's roster in step. Driven by hand on OPS (`settings.js`): the seeded default section and the one edited on the grid (Word count 10, "Abstracts not required" ticked) hold identical `sections` rows (`abstracts_not_required=1`, `abstract_word_count=10`; `db-diff-ops.md`), and the seeded section's Edit form shows 10 and the ticked box (`sections-seeded-ops`) | PASS | `identifyType` stays unaccepted on OPS (`sections.0.identifyType` is a 400): no spec needs it. Suites re-run green after the change: U21 OPS (15 passed), U21 OJS (14), U21 OMP (15), U29 OJS (11; S9 failed once under 8 workers on the Reviewer Recommendations order, unrelated to this change, green alone and on the full re-run). Evidence: `.reports/U21/harness/` |
| 2026-09-11 | all | context create (`ContextFactory::create`) — default editorial task templates | sync session: the admin hosted-contexts endpoint (`PKPContextController::add`) has called `Repo::editorialTask()->installTaskTemplates($context)` after `PKPContextService::add` since pkp/pkp-lib#12593 (`30693f9e21`, 2026-08-03); the factory stopped at `add`, so no fleet context (bootstrap `publicknowledge` or scratch) carried a single task template while a UI-created context carries the registry's defaults (`registry/taskTemplates.xml`: 10 on OJS/OMP, 2 on OPS, all `include` = false). Found while verifying pkp/pkp-lib#13214 (the template FK): `edit_task_templates` was empty on all three fleets | DEFECT → FIXED | The factory now runs the endpoint's tail. Verified live post-fix (`.reports/sync/s11/tasktemplates-13213.log`): a scratch context carries the registry rows; with `include` flipped on, a seeded submit creates one task per stage template through the real `Repo::submission()->submit()`, and a decision chain that re-enters a stage adds none twice. The 2026-07-31 "code path identity" row above was true when written and is superseded by this one for the templates tail. No `include`/task-template spec key exists; a feature needing one lists it under scenarios.md "Field shapes not built yet" |
| 2026-09-13 | all | submission `author {orcid, orcidIsVerified: true}` (`PKPSubmissionScenarioBuilder` → `UserSeeder::orcidOAuthData` → `Repo::author()->edit`) | U04 coverage revision, scenario 8 "A contributor already verified": the verified state now carries the fields the emailed link's landing stores (`VerifyIdentityWithOrcid::setIdentityData()`: `orcid`, `orcidIsVerified`, `orcidAccessDenied` null, `orcidAccessToken`, `orcidAccessScope`, `orcidRefreshToken`, `orcidAccessExpiresOn` = now + `expires_in`), the array shape `AuthorizeUserData::getOrcidOAuthAccessData()` builds. Read back live on all three apps (`.reports/U04/hU04/author_settings-<app>.json`, `contributor_form_orcid_field-<app>.json`): `author_settings` holds the six rows (`orcidAccessDenied` null leaves no row, as the app's own null write does), scope `/authenticate` on a public-sandbox context, expiry 2046-09-13; the publication read the workflow's Contributors panel makes agrees on the iD and the verified mark; the contributor form shows the solid-icon iD link with Delete and no "Request verification" (the Contributors list row shows no iD). Two submissions seeded through the real Accept on a toggle-on context (`accept-mail-<app>.json`, OJS and OMP): one `SendAuthorMail` job, carrying the unauthenticated contributor only | PASS | Supersedes the 2026-08-07 DELIBERATE DEVIATION for the verified state: the app gates on the token, not the mark (`SendAuthorOrcidEmail`, `Publication\Repository::validatePublish`, `PKPSendSubmissionToOrcid`), so the tokenless verified state was one the app itself re-requests (U04 T-ojs-1, 2026-09-13). Token strings are fixtures (`test-orcid-access-token-<tag>`, `test-orcid-refresh-token-<tag>`), the lifetime ORCID's 20-year `expires_in` (631138518 s, `UserSeeder::ORCID_TOKEN_EXPIRES_IN`), the scope the one `OrcidManager::buildOAuthUrl()` requests for the context's API type. `orcidIsVerified: false` is unchanged (iD alone). The verified-but-lapsed state (a token a deposit dropped as expired) has no key. Jobs a seeded token can reach (revocation on Delete, deposits on publish) still fail fast at the dead-port proxy when a drain pops them, asserted by no test. |
| 2026-09-13 | all | context-users `orcid`/`orcidIsVerified: true` (`UserSeeder::seed` → `User::setVerifiedOrcidOAuthData` → `Repo::user()->edit`) | The same fields through the app's own setter, the one the profile popup's completion runs (`AuthorizeUserData::execute()` case `profile`); `user_settings` read back on all three apps (`.reports/U04/hU04/user_settings-<app>.json`): the six rows, scope `/authenticate`, expiry 2046-09-13, token `test-orcid-access-token-<username>`. User-level readers of the token exist (the editorial masthead's reviewer iD, `RevokeOrcidToken` on Delete, review deposits), so a user seeded verified now matches the completion too | PASS | Regression on the change: U03 green on OJS (13), OMP (13), OPS (13); U04 main green on OJS (9); OPS 9 of 10 twice with S10 red on a seed carrying no ORCID key (`.reports/U04/harness/report.md`), so no shipped test read the tokenless user state. The wizard's copy of a profile into a contributor carries neither the mark nor the token (`Repo::author()->newAuthorFromUser`), so U03 S12's "Request verification" reading is unchanged. |
| 2026-09-16 | ops | context scenario `postedAcknowledgement` (boolean; `PKPContextScenarioBuilder::parseIntakeSettings` → `saveFormSettings`: `PKPContextService::validate` + `::edit`) | the Settings › Workflow › Emails form's "Preprint Posted" radio (OPS `EmailSetupForm::FIELD_POSTED_ACK`, a `FieldOptions` with the values `true` / `false`) saves by the same form-encoded POST-as-PUT `contexts/{id}` (`postedAcknowledgement=false` in the posted body, `convertStringsToSchema` turning it back into the schema's boolean, `PKPContextService::edit` storing `0`); the builder accepts a real boolean only, gated on the app's context schema carrying the key, and writes it in the one validate + edit call the other settings passthroughs share. Driven by hand on OPS (`posted-ack.js`): the manager of a fresh scratch server picks "Do not send an email." under "Preprint Posted" and saves; the seeded-false server's `server_settings` row equals the by-hand row (`postedAcknowledgement=0`; seeded true and every fresh context `1`, the schema default; `db-diff-ops.md`), and the seeded-false server's Emails screen reopens with "Do not send an email." checked under "Preprint Posted" exactly as the by-hand server's does after a reload (`radios-seeded-off-ops.json`, `radios-byhand-reloaded-ops.json`, `emails-seeded-off-ops.png`), the seeded-true control with "Send an email to all authors." | PASS | The key is refused on OJS and OMP with a 400 naming it (their context schema lacks it; the "Preprint Posted" group is the OPS `EmailSetupForm` subclass's own), and a string or number is a 400 on OPS (`api-refusals-{ojs,omp}.json`, `api-results-ops.json`). One sibling row differs, not the key's: the Emails form posts every field of the tab, so its empty "Notify Anyone" box arrives as null and the by-hand save drops the install-default empty `copySubmissionAckAddress` row that every fresh context carries, while the seed leaves that default row in place; both read as no address (the same form-posts-everything nuance as the Metadata row of 2026-09-07). Not verified here: the posted mail itself at `false` (the listener's own gate, `SendPostedAcknowledgement::handle`, read by U49 scenario 15 on screen). Suites re-run green after the change: U21 OPS (17 passed, 1.4 min), and the two suites that seed through the sibling `submissionAcknowledgement` of the same parser, U21 OJS (16 passed, 1.3 min) and U21 OMP (17 passed, 1.3 min). Evidence: `.reports/U49/harness/`, `.reports/U49/hU49/` |
| 2026-09-16 | all | context scenario `enablePublicComments` (boolean; `PKPContextScenarioBuilder::parseIntakeSettings` → `saveFormSettings`: `PKPContextService::validate` + `::edit`) | the Settings › Website › Content › "Comments" tab's one box, "Enable Public Comments" (`ContentCommentsForm`, a lib/pkp form with no app subclass, a checkbox `FieldOptions` over the schema's boolean), saves by the same form-encoded POST-as-PUT `contexts/{id}` whose whole body is `enablePublicComments=true` (`convertStringsToSchema` → the boolean, `PKPContextService::edit` → `1`); the builder accepts a real boolean only, gated on the app's context schema, and writes it in the one validate + edit call the settings passthroughs share. Driven by hand on OJS, OMP and OPS (`parity-record.js`, `parity-drive.js`): the manager of a fresh scratch context ticks the box and saves; the by-hand context's `<app>_settings` row equals the seeded-true context's (`enablePublicComments=1`; seeded false and every fresh context `0`, the schema default written at creation; `db-diff-ojs.md`), the seeded context's tab reopens with the box ticked as the by-hand one does after a reload (`checkbox-seeded-<app>.json`, `checkbox-byhand-after-<app>.json`), and the seeded journal's article landing page carries the two comments blocks while the seeded press's and server's pages carry none (`landing-seeded-rd1-<app>.json`) | PASS | A string `"true"` or the integer 1 is a 400 naming the key on every app (`smoke-<app>.json`). Regression: U21 green on OJS (16), OMP (17), OPS (17). Evidence: `.reports/U14/harness/`, `.reports/U14/hU14/` |
| 2026-09-16 | all | submission `userComments[] {user, text, approved?, reports[] {user, note}}` (`PKPSubmissionScenarioBuilder::parseUserComments` / `seedUserComments`, after the publish) | the landing page's "Submit" posts `{publicationId, commentText}` to `POST comments` (`UserCommentController::submit`: `UserComment::query()->create()` with `isApproved` false, then the private `notifyModerators()`), the Comments page panel's "Approve Comment" posts `{approved: true}` to `comments/{id}/setApproval` (`isApproved`, `approvedAt` now, `approvedByUserId` the acting user, written on the model), the landing page's "…" › "Report" dialog's "Submit" posts `{note}` to `comments/{id}/reports` (`Repo::userComment()->addReport()`, then `notifyModerators()`); the builder runs the same `create()`, the same model writes, the same `addReport()`, and the controller's own `notifyModerators()` by reflection (the roster query and `createNotification` are the app's, not a copy), text and note through the form requests' `stripUnsafeHtml`. Driven by hand on OJS (`parity-drive.js`, `parity-drive-report.js`): a reader's two comments, the manager's approval of the second, a second reader's report on it; the seeded twin (pending, approved, approved + reported) holds identical `user_comments`, `user_comment_settings`, `user_comment_reports` and `notifications` rows (level TASK, `USER_COMMENT_POSTED` on the comment and `USER_COMMENT_REPORTED` on the report, one per manager and site admin of the context; `db-diff-ojs.md`) but for `approvedByUserId` (the acting moderator: the by-hand manager there, the seeding admin here) and the approval's later timestamp. The seeded landing page reads as the by-hand one (the writer's pending comment under its notice, the approved ones for everyone, "All Comments (2)"; `landing-seeded-rd1-ojs.json`, `landing-seeded-anon-ojs.json`), and the Comments page on OJS, OMP and OPS lists the three rows with "Hidden/Needs Approval", "Approved", "Approved, Reported" (`comments-page-seeded-<app>.json`) | PASS | Refusals follow the screens, not the API: no comment without `published: true`, no report on a pending comment (a moderator could through the API; no screen offers it), no self-report; an unknown user, an empty text or note, a non-boolean `approved` and an unknown key are 400s (`smoke-<app>.json`). Seeded comments of one build share a `created_at` second, so their newest-first order is not fixed. The key does not read `enablePublicComments`, as the API does not. Evidence: `.reports/U14/harness/`, `.reports/U14/hU14/` |
| 2026-09-16 | all | context scenario `context.supportedFormLocales` (a list of locale codes; `ContextFactory::parseParams` validates it with the other context params through `PKPContextService::validate`, `::create` then runs `::enableFormLocales` after `PKPContextService::add`) | the Languages grid's "Forms" box (`LanguageGridHandler::saveLanguageSetting`, `setting=supportedFormLocales&value=1`) runs, per tick, `PKPContextService::restoreLocaleDefaults` for the locale, `Repo::reviewerRecommendation()->setLocalizedDataOnNewLocaleAdd`, then one `PKPContextService::edit` with the pushed-and-sorted list; the builder runs the same three calls per listed locale not yet ticked, in that order, on the same stale context object the grid uses. Driven by hand on the three apps (`form-locales.js`): the manager of a fresh scratch context seeded `supportedLocales: ['en', 'fr_CA']` ticks the French "Forms" box on Settings › Website › Setup › Languages (`save-language-setting` 200, the toast "Locale settings saved."); a second context seeded `supportedFormLocales: ['en', 'fr_CA']` through the key. The `<app>_settings` rows of the two contexts are identical once the name, acronym and path are normalised (`db-diff-normalized-<app>.txt` empty; `supportedFormLocales` = `["en","fr_CA"]` on both; 16 / 14 / 13 fr_CA rows on OJS / OMP / OPS, both sides), the six default reviewer recommendations carry an fr_CA `title` on both OJS contexts (OMP and OPS keep none: `hasCustomizableReviewerRecommendation()` is false there), the Languages grid reads UI ✓ Forms ✓ on both rows for both contexts, and "Add Highlight" on both opens with the "French" and "English" toggles and "Title in French", "Description in French", "Button Label in French" beside the primary fields (`compare-<app>.json`: `addHighlightPanelSame: true`) | PASS | Refused with a 400 naming `context.supportedFormLocales`: a string, an empty list, a list without the primary locale, a locale not under `supportedLocales` ("tick it under UI first"; the grid itself would allow a Forms tick with no UI tick, a state no scenario needs); a locale the site lacks fails the whole context spec as `supportedLocales` does (`api-refusals-<app>.json`). `['en']` alone is accepted as the install default. Not mirrored: the grid's trivial "Locale settings saved." toast for the acting user, consumed by that user's next page load (the `notifications` table carried no level-1 row after the by-hand drive). Parity fact: a context with French under "UI" alone already stores the French default texts (`PKPContextService::add` sets defaults over `supportedLocales`), so the tick's `restoreLocaleDefaults` rewrites the same values; the "Forms" tick is what makes the settings forms and the Highlights panel two-language. Suites re-run after the change: see `.reports/U11/harness/report.md` |
| 2026-09-17 | all | context scenario `enableAnnouncements` (boolean), `announcementsIntroduction` (localized), `numAnnouncementsHomepage` (integer ≥ 0 or null) (`PKPContextScenarioBuilder::parseIntakeSettings` → `saveFormSettings`: `PKPContextService::validate` + `::edit`) | the Settings › Website › Setup › "Announcements" tab (`PKPAnnouncementSettingsForm`, lib/pkp, no app subclass; the text and the number fields show once "Enable announcements" is ticked) saves by one form-encoded POST-as-PUT `contexts/{id}` whose body is `enableAnnouncements=true&announcementsIntroduction[en]=<p>…</p>&numAnnouncementsHomepage=2` (recorded on the three apps, `requests-<app>.json`); the builder accepts a real boolean, a string or locale map, and an int or null, gated on the app's context schema, and writes them in the one validate + edit call the settings passthroughs share. Driven by hand on OJS, OMP ("Additional Information" is the text's label there) and OPS (`parity-drive.js`): the manager of a fresh scratch context ticks the box, types the introduction and 2, saves; the by-hand context's `<app>_settings` rows (`enableAnnouncements` 1, `announcementsIntroduction` en `<p>Seeded introduction.</p>`, `numAnnouncementsHomepage` 2) equal the seeded context's row for row (`compare.txt`, ids in `ids-<app>.json` / `reseed-ids-<app>.json`). A fresh context has none of the three rows | PASS | Regression: OJS U14, OMP U21, OPS U49 green once each (`.reports/U12/harness/report.md`) |
| 2026-09-17 | all | context scenario `sidebar` (a list of block plugin names; `parseIntakeSettings` → `saveFormSettings`, after `loadBlockPlugins()`) | the Settings › Website › Appearance › "Setup" form's orderable "Sidebar" boxes (`PKPAppearanceSetupForm`, lib/pkp) save by the same PUT with `sidebar[]=<name>` among the form's other fields (recorded: `sidebar[]=AnnouncementFeedBlockPlugin` on OJS, `informationblockplugin` on OMP, `languagetoggleblockplugin` on OPS: the value is the plugin's registry name, the lowercased class name for a stand-alone block and the mixed-case override of the feed plugin's block); `PKPContextService::validate` refuses a name no loaded, enabled block carries. The builder runs at site level, where no block is loaded, so before the same validate it loads the context's enabled stand-alone blocks (`PluginRegistry::loadCategory('blocks', true, contextId)`) and re-registers for the context each generic plugin the `plugins` key enabled (`Plugin::register` with the context id, which is where the feed plugin registers its block), the loading a request inside the context does at bootstrap; no write differs. Driven by hand on the three apps (`parity-drive.js`, `sidebar-options-<app>.json` for the offered list): the by-hand `sidebar` row (`["AnnouncementFeedBlockPlugin"]` etc.) equals the seeded one; a name the form would not offer is the form's own 400 ("The X block can not be found…", `seed-refusals-<app>.json`) | PASS | On OJS the feed block needs `plugins.announcementfeedplugin.enabled: true` in the same seed, as the form needs the plugin ticked first |
| 2026-09-17 | all | context scenario `plugins {<lowercased class name>: {enabled, settings?}}` (`PKPContextScenarioBuilder::parsePlugins` / `applyPlugin`) | the Settings › Website › Plugins grid's "Enabled" box (`PluginGridHandler::enable`: `LazyLoadPlugin::setEnabled(true)` = `Plugin::updateSetting(<request context>, 'enabled', true, 'bool')`, then `AuditLog::log(PLUGIN_ENABLE)` and a trivial toast) — the builder writes the same `updateSetting` naming the scratch context (its request has none), the same audit-log line, and no toast (session-bound, as the other builders' toasts); `settings` values go through `Plugin::updateSetting(contextId, name, value)`, the call the plugin's settings form's `execute` makes (`AnnouncementFeedSettingsForm`: `displayPage`, `recentItems`). Driven by hand on OJS (`parity-drive.js`, step `pluginEnable`: the manager ticks the row's box; the grid posts `settings-plugin-grid/enable?plugin=announcementfeedplugin&category=generic`); the by-hand `plugin_settings` row (`announcementfeedplugin`, `enabled`, `1`, `bool`) equals the seeded one. The settings window itself was not driven (its expand-row action defeated three tries, `plugin-settings.js`), so `settings` rests on the code equality above. Establishing fact: the Announcement Feed plugin is OFF on every fresh journal, `publicknowledge` included (no `enabled` row: the plugin never declares its `settings.xml` as a context settings file, so nothing installs it), so the not-built list's assumption "enabled by default" was wrong and the key was built | PASS | Site-wide plugins (`isSitePlugin()`) are refused; a plugin the grid cannot enable or disable is refused |
| 2026-09-17 | all | context scenario `announcementTypes[] {name}` (`PKPContextScenarioBuilder::addAnnouncementType`) | the "Announcement Types" tab's "Add Announcement Type" window posts `name[en]=…` to `announcement-type-grid/update-announcement-type` (`AnnouncementTypeForm::execute`: a new `AnnouncementType` with the context id and the localized name, `AnnouncementTypeDAO::insertObject`); the builder runs the same DAO insert on the same object. Driven by hand on the three apps (`parity-drive2.js`; the window's "Name" box carries no accessible name, `locators.md`): `announcement_types` + `announcement_type_settings` rows equal (`compare.txt`) | PASS | The "Add Announcement" panel lists a type only after the page reloads (the type options are part of the page's form config; `screen-notes.md`) |
| 2026-09-17 | all | context scenario `announcements[] {title, descriptionShort?, description?, dateExpire?, type?}` (`PKPContextScenarioBuilder::addAnnouncement`, after `users[]`) | the panel's "Save" posts `title[en]=…&descriptionShort[en]=…&description[en]=…&image=&dateExpire=…&typeId=…&sendEmail=false` to `POST announcements` (`PKPAnnouncementController::add`: `convertStringsToSchema` (empty strings to null except the multilingual ones), `Repo::announcement()->validate` against the context's form locales, `Announcement::create`, then `notifyUsers()` — one `NewAnnouncementNotifyUsers` job per announcement in a `Bus::batch`, recipients every user subscribed in the context, no sender with the box unticked). The builder posts the same props (every multilingual box per form locale, empty as `''`; `image`, `dateExpire`, `typeId` null when absent — the first seed wrote no row for an empty box and the by-hand rows showed `description` `''` and `image` `''`, fixed the same day), runs the same validate and create, and invokes the controller's own `notifyUsers()` by reflection with `sendEmail` false. Driven by hand on the three apps (`parity-drive2.js`, `requests3-<app>.json`, `steps3-<app>.json`): `announcements` (assoc type, type id, expiry, posted today) and `announcement_settings` rows equal row for row, both contexts hold no notification yet, and the queued job's arguments equal (`recipientIds` the context's manager, reader and admin; contextId; announcementId; locale en; sender null; `compare.txt`) | PASS | The image is not seedable (a state the panel builds); `datePosted` is now, as the panel's |
| 2026-09-17 | all | `ContextFactory::create` (task templates) and `PKPSubmissionScenarioBuilder` (the submitter's contributor type and roles) behind `method_exists(Repo::class, 'editorialTask')` / `class_exists(ContributorType::class)` | both hold on `main`, so the seeded state there is unchanged; on the `stable-3_5_0` line (harness.md "The stable line") the two features do not exist and the steps are skipped, as the 3.5 screens would | PASS (by construction) | Driven on the line: install, bootstrap, a scratch context and a submitted submission on the three apps (`.reports/sync-3_5/`, `.reports/line-3_5/`) |
| 2026-09-19 | ojs, ops | submission scenario `galleys[] {label, locale?, file | urlRemote}` (`PKPSubmissionScenarioBuilder::parseGalleys` / `seedGalleys`, after the decisions and before a publish) | the "Galleys" page's "Add galley" opens the legacy window "Create New Galley" (label, the language list preselecting the submission's locale, "Remotely hosted content" + URL) whose Save is `ArticleGalleyGridHandler::updateGalley` / OPS `PreprintGalleyGridHandler::updateGalley` (`ArticleGalleyForm::execute`: `Repo::galley()->add` with `publicationId`, `label`, `locale`, `urlPath` null, `urlRemote`; then, on a submission at Copyediting or Production, `NotificationManager::updateNotification` of the grid's notice types: OJS the assign-production-user + awaiting-representations pair, OPS awaiting-representations alone), and at once opens "Upload a File Ready for Publication" (`FileUploadWizardHandler`, `fileStage` PROOF, `assocType` REPRESENTATION on the new galley): step 1 `SubmissionFilesUploadForm::execute` (file service `add` into the submission directory as `<uniqid>.<ext>`, a new SubmissionFile at PROOF with the genre picked from a list that preselects nothing, so the first entry "Article Text" / "Preprint Text"; the app's `Repo::submissionFile()->add` links `galley.submissionFileId`), step 2 "Review Details" › Continue `PKPManageFileApiHandler::saveMetadata` (`SubmissionFilesMetadataForm::execute` = `Repo::submissionFile()->edit` with the name and null artwork/supplementary props, the authors' pending-external-revisions notification update, `MetadataChanged`), step 3 Complete (a template fetch, no write). A remote galley: the window with the box ticked, the wizard opened and closed by its "Close" button (no file, the galley kept). The builder runs those same calls in that order, acting as `admin`; `file` is read from `classes/testing/fixtures/`, which `bin/mount.js` now fills from `apps/<app>/playwright/fixtures/files/`. Driven 2026-09-19 as a throwaway editor (OPS moderator) on a scratch context per app, one screen-made and one seeded submission per shape (`.reports/U33/harness/parity-galleys.js`, `compare.sh`, `<app>-{A,B,C,D}-*.txt`): `publication_galleys`, `submission_files` + settings (name only; the nulls store nothing), `files` (path shape, mimetype `application/pdf`), the four event-log rows (`fileUploaded`, `fileRevised`, `fileEdited` ×2 with the same settings), the submission's notification rows and the on-screen reads (the Galleys row "PDF English" / "HTML English"; on OJS the assigned editor's "Assign a user to create galleys…" gone on the next landing, on both the screen-made and the seeded one; OPS shows no notice box) identical, the only difference the acting user (`admin` vs the editor) in `uploader_user_id` and the event-log `user_id`/`username`, the harness's standing convention for every seeded editor action | PASS (verified live) | OMP: the key is a 400 (`specKey: galleys`, `SubmissionScenarioBuilder::assertGalleysSupported`): a press has publication formats and no "Galleys" page, so no UI path writes a galley there. A galley on a `published: true` seed is created before the publish, as an editor builds galleys and then publishes (OPS driven by seed only, `.reports/U33/hU33/seed-published-ops.json`). The wizard's genre list preselects nothing, so a screen user picks; the seed takes the list's first entry and offers no `genre` key. |
| 2026-09-22 | all | submission scenario `participants[].recommendOnly` and `participants[].canChangeMetadata` (`PKPSubmissionScenarioBuilder`, parse of `participants[]`) | the workflow's "Assign" › "Assign Participant" window posts its two boxes, "Assignment privileges" (`recommendOnly`) and "Permissions" (`canChangeMetadata`), which its script pre-ticks from the role's own settings; `AddParticipantForm::execute` passes them to `Repo::stageAssignment()->build()` (forcing the permission on a manager-level role; the recommend-only box exists for manager- and sub-editor-level roles only, `AddParticipantForm::initialize`). The builder passes the two keys to the same `build()` call, each defaulting to the role's `recommendOnly` / `permitMetadataEdit` as the pre-tick does, and refuses what the window cannot post. Driven on the three apps (`.reports/U35/harness/parity.js`, `.reports/U35/hU35/parity-<app>.json`): a manager assigned a Section Editor (OMP Series editor, OPS Moderator) with "Assignment privileges" ticked and "Permissions" unticked (both started at the install defaults, unticked and ticked); the `stage_assignments` row (`recommend_only` 1, `can_change_metadata` 0, the role) equals the seeded one. On-screen read of a seed (`reads.js`): the row shows "Only allowed to recommend an editorial decision" under the role, and on a `roles`-key context "Assign" pre-ticks the boxes from the role | PASS (the assignment row); DEFECT found in the family, not fixed (below) | Screen-only side effects of `StageParticipantGridHandler::saveParticipant` the seed has never written (the same for the key's defaults, so older than U35): the Activity Log line `submission.event.participantAdded`, and, for an editor-level role, the deletion of the "needs an editor" task rows (`NOTIFICATION_TYPE_EDITOR_ASSIGNMENT_REQUIRED`, one per manager; the seed leaves both) and, for a manager-level role, the decision-stage notices' update. Not changed in this harness run: 42 shipped suites seed through `participants[]`, so a fix changes a state they read and gets its own entry and regression; recorded as a parity fact in `scenarios.md` ("Facts tests rely on") |
| 2026-09-22 | all | context scenario `roles {<role key>: {recommendOnly?, permitMetadataEdit?}}` (`PKPContextScenarioBuilder::parseRoleOptions` / `applyRoleOptions`, before `users[]`) | Settings › Users & Roles › Roles, the role row's "Settings" › "Edit" window (heading "Edit"): the "This role is only allowed to recommend a review decision…" and "Permit submission metadata edit." boxes, saved by `UserGroupGridHandler::updateUserGroup` → `UserGroupForm::execute`. The builder runs that form itself: `initData()` from the stored role, the posted stages narrowed to the boxes the window renders (the app's workflow stages, `initData`'s `stages`), the two boxes set, `execute()` (the role's flags, the assignment rewrite on a metadata change, the stage re-save, the audit-log line); not mirrored: the grid's trivial toast and the form's POST/CSRF validation. Driven on the three apps (`parity.js`): a manager ticked the recommend-only box and unticked the metadata box on the Section Editor role (OMP Series editor, OPS Moderator) of one scratch context; a second context seeded with the key; `user_groups` row, `user_group_settings` (`recommendOnly` 1) and every role's `user_group_stage` rows equal. Establishing fact: the window has no box for the Done stage (6), so the screen's save, and the key's, drops that stage from the saved role (OJS `1,3,4,5,6` → `1,3,4,5`, OMP `1,2,3,4,5,6` → `1,2,3,4,5`, OPS `5,6` → `5`). On-screen read (`reads.js`): the seeded role's "Edit" window opens with the two boxes as seeded | PASS (verified live) | Role keys are checked at parse against `registry/userGroups.xml` (the roles every new context gets), so an unknown key is a 400 before the context exists. A manager-level role saved through the form gets every workflow stage (the form's always-active stages), on screen and through the key alike; for the stage-less Journal Manager (`manager`) that adds stages it never has at install. The OJS recording of the Roles tab showed no "Settings" link on the "Journal manager" row, so whether any screen saves that role is open; no scenario should seed `roles.manager` until it is driven |
| 2026-09-23 | ojs, omp | submission scenario `files[] {file, genre?, uploader?, note?}` (`PKPSubmissionScenarioBuilder::parseFiles` / `uploadThroughSubmissionWizard` before the submit / `uploadThroughWizard` after it / `addFileNote`) | three screen paths, recorded with their POST bodies (`.reports/U36/hU36/parity-posts-<app>.json`). The author's wizard "Upload Files" › "Add File" is `POST submissions/{id}/files` (multipart, `fileStage` 2, no genre: `PKPSubmissionFileController::add`) and the component button is `PUT submissions/{id}/files/{fileId}?stageId=1` `genreId=…` (`::edit`); the builder runs the controller's steps (file service add, the lone-genre default, `Repo::submissionFile()->validate` and `->add`, then validate and `->edit` with the genre) as the submitter before `submit()`. The editor's "Submission Files" › "Upload" is the legacy wizard (`upload-file` with `fileStage=2`, then `save-metadata` posting `name[en]` alone, step 3 no write): `SubmissionFilesUploadForm::execute` + `PKPManageFileApiHandler::saveMetadata` (the edit with the empty artwork and supplementary boxes, the authors' notice recompute, `MetadataChanged`), run by the new `uploadThroughWizard` helper as the uploader, the same helper `galleys[]` now uses (behavior unchanged for galleys: same calls, same order, OMP's `ManageFileApiHandler` notice list as an overlay). "More Information" › "Notes" › "Add Note" posts `newNote=…` to `save-note`: `NewFileNoteForm::execute` + the handler's own `_logEvent` (notePosted). Driven on OJS and OMP (`.reports/U36/harness/parity-files.js`, `compare-files.sh`, `<app>-<id>-{screen,seed}.txt`): a draft with the author's upload vs one seeded; a submitted one with the editor's upload and a note vs one seeded with `uploader` and `note`. `submission_files` (stage, genre, uploader, assoc, viewable), `submission_file_settings`, `files` (path shape, mimetype), `notes`, the event-log rows (fileUploaded, fileRevised, fileEdited ×2 per edit, notePosted) and the notification rows equal; on screen the seeded note reads as the typed one | PASS (verified live) | Two differences, both conventions: the note's writer and its notePosted row name `admin` (the Notes tab shows "admin admin"), where the screen names the editor; and the author's file has `created_at = updated_at` when seeded (the screen's genre PUT lands a second or more after the upload). OPS refuses the key (400, `assertFilesSupported`): no workflow file list on a preprint server. The dependent-files fixture `article.html` (OJS, OMP) / `preprint.html` (OPS) was added next to the PDFs |
| 2026-09-23 | ojs, omp | submission scenario `reviewRounds[].files[] {file, genre?}` (`PKPSubmissionScenarioBuilder::seedRoundFiles`, before the round's reviewers) and the reviewer grant in `seedRoundReviewers` | "Files for Review" › "Upload/Select Files" › "Upload Review File": the legacy wizard with `fileStage=4` and `reviewRoundId` (the submission-file DAO writes `review_round_files`); the new row lands unticked, so the user ticks it and presses "OK" (`update-review-files` `selectedFiles[]=…`: `ManageReviewFilesForm::execute`, which edits the file `viewable` true, two more fileEdited rows); then "Add Reviewer", whose file list ticks every round file by default (`LimitReviewFilesGridHandler::isDataElementSelected` with no assignment) and `ReviewerForm::execute` grants each (`ReviewFilesDAO::grant`). The builder uploads through `uploadThroughWizard` as `admin`, runs `ManageReviewFilesForm::execute` itself over the round's files, all selected, and in `seedRoundReviewers` grants every review file of the round to each seeded reviewer the way `ReviewerForm::execute` does. Driven on OJS and OMP (`parity-files.js` E/F): `submission_files` (stage 4, assoc the round, viewable 1), `review_round_files`, `review_files` (one grant for the reviewer), `review_assignments`, the event-log rows and the notification rows equal; the reviewer's step 1 lists `article.pdf` under "Review Files" on both | PASS (verified live) | The file's uploader and its log rows name `admin` where the screen names the editor (the standing convention). The grant loop is a no-op for a round seeded without files, so every older seed is unchanged (32 suites re-run, harness report). `saveMetadata`'s clean-up of the authors' "revisions requested" notices needs a review round the handler never authorizes (its URL carries `reviewRoundId=null`), so the screen never runs it and the seed does not either |
| 2026-09-23 | all | context scenario `components {<name>: false \| {metadata?, dependent?, supplementary?, required?}}` (`PKPContextScenarioBuilder::parseComponents` / `applyComponent`, after the settings forms) | Settings › Workflow › Submission › "Components": "Add a Component" and a row's "Settings" › "Edit" post the window to `update-genre` (`name[en]`, the ticked boxes as `on`, `category`, `required`, `key`, `genreId` on an edit): `GenreGridHandler::updateGenre` → `GenreForm::execute`; "Settings" › "Delete" › "OK" is `delete-genre` (`GenreGridHandler::deleteGenre`: the in-use refusal, then `GenreDAO::deleteObject`, a soft delete to `enabled` 0). The builder runs `GenreForm` itself (initData from the stored component, or empty for a new one with the window's own start values: Document, boxes unticked, "No", empty key; the spec's fields set; execute) and the delete handler's two steps; names are resolved at parse against `registry/genres.xml` in the primary locale, so a bad entry is a 400 before the context exists. Driven on the three apps (`parity-components.js`, `compare-components.sh`, `<app>-components-{X-screen,Y-seed}.txt`): one context by hand (add "Appendix Table" Supplementary Content with the supplementary box; "Other" to Artwork; delete "Research Instrument", a press "Glossary"), one seeded; every `genres` row (key, seq, category, the three flags, file variants, enabled) and `genre_settings` row equal on all three | PASS (verified live) | Parity fact, the app's own: the added component is saved at seq 0, the first default's position, so it and "Article Text" ("Appendix", "Preprint Text") sort in no fixed order: on one seeded OJS context the grid listed "Article Text" first and the upload wizard "Appendix Table" first. Recorded in `scenarios.md`; a test must not assert the added component's place |
| 2026-09-23 | all | context scenario `users[].masthead {<role key>: bool}` (`UserSeeder::parseMasthead`, `seed`: `assignUserToGroup(…, masthead)`) | the choice an accepted invitation stores (`UserRoleAssignmentReceiveController::finalize` → `assignUserToGroup` with the invite's masthead) and the users list's "Edit" page changes (roles table, "Journal Masthead" select → "Does not appear on the masthead" → "Confirm masthead visibility change" › Confirm → `PUT users/{id}/masthead/{userUserGroupId}` → `setUserUserGroupMasthead`, cache cleared). Driven on the three apps (`.reports/U07/harness/parity.js`, `parity2.js`, `compare.sh`, `compare-<app>-{A,B}.txt`): a manager set Hana's editor role (OPS Moderator) to "Does not appear" on context A; B seeded `masthead: {editor: false}`; `user_user_groups` rows equal (masthead 0 on that role, 1 on the other), and "Editorial Masthead" drops her from that role on both | PASS (verified live) | The screen's change also emails the member (`UserRoleMastheadUpdateNotify`); the seed sends none, as the invitation path's final write sends none. On OMP and OPS the screen's PUT answers 500 after saving the row (the template `USER_ROLE_MASTHEAD_UPDATE` is missing from their registries: U06's register), the seeded row equal to the saved one. `false` on a reviewer role is a 400 (no choice on screen; the API refuses it) |
| 2026-09-23 | all | context scenario `users[].affiliation` (`UserSeeder::seed`, `setAffiliation` per locale before `Repo::user()->add`) | Profile › Contact "Affiliation" (`ContactForm::execute` → `setAffiliation(…, null)`, saved with the account). Driven on the three apps: Alma typed "Parity University" on A and saved; B seeded `affiliation: 'Parity University'`; `user_settings` `affiliation`/`en` equal, and both mastheads print it under her name | PASS (verified live), two by-construction differences | The Contact tab refuses to save without "Country*" (required), so the by-hand account also carries `country` (CA) and an empty `signature`/`en` row the tab co-saves; the seeded account has neither. Nothing on the About pages reads them. The tab's "Affiliation" box has no accessible name (its label is not tied to the input; located by `input[name^="affiliation"]`). Refused on an existing account (roster, admin: A1) |
| 2026-09-23 | all | context scenario `users[].pastRoles[] {role, dateStart?, dateEnd?}` (`UserSeeder::seedPastRole`: `assignUserToGroup(dateStart)` then `endAssignments`, then `date_end` written for an earlier `dateEnd`) | the users list's "Edit" page › the role row's "Remove Role" › dialog "Remove Role" → `PUT users/{id}/endRole/{userGroupId}` → `Repository::endAssignments` (date_end now, masthead and history caches cleared). Driven on the three apps: Paul held editor (OPS Moderator) + Reader on A, the manager removed the editor role; B seeded `roles: ['reader'], pastRoles: [{role: editor}]`; `user_user_groups` rows equal (start and end today, masthead 1), "Editorial History" lists "2026 – 2026 Paul Past" under the role and "Editorial Masthead" no longer does, on both | PASS (verified live) for the defaults; deliberate D9 deviation for earlier dates | The screen emails the member (`UserRoleEndNotify`); the seed does not. No screen gives a start before today (the invitation moves a past start to today) or an end before today, so `dateStart` goes through `assignUserToGroup`'s own parameter and an earlier `dateEnd` is written onto the ended row (caches cleared again), verified by rows only (smoke: `2019-03-01`–`2024-06-30`). The screen refuses to remove a user's last role, so `roles` stays required non-empty; refused on an existing account |
| 2026-09-23 | ojs, omp | submission scenario `reviewRounds[].reviewers[].dateCompleted` (`PKPSubmissionScenarioBuilder::backdateCompletedReview`, after the wizard's step forms) | no screen completes a review on another day (D9). The builder completes it through the reviewer wizard's step 1–3 forms exactly as `completed` does (parity ledger 2026-09-06), then shifts every set date of the assignment by one delta through `Repo::reviewAssignment()->edit`. Compared on both apps (`compare-reviews-<app>.txt`): the backdated assignment equals a same-seed `completed` one without the key and the by-hand context's, column for column, except the dates, each moved by the same number of days (e.g. assigned/notified/confirmed/completed 2025-06-15, both due dates 2025-07-13, where today's read 2026-09-23 and 2026-10-21); the round's status equal (8). "Editorial Masthead" of B shows "Peer Reviewers in Previous Year", "…the reviewers for 2025 listed below." and the reviewer; A's today-completed review shows no block | PASS (by construction) + deliberate D9 deviation | The submission, round, notifications and event log keep today's dates. OPS answers 400 on `reviewRounds` as before |
| 2026-09-23 | ojs, omp | context scenario `roles.<key>.permitSettings` (`PKPContextScenarioBuilder::parseRoleOptions` / `applyRoleOptions`, `UserGroupForm::execute`) | the Roles tab's "Settings" › "Edit" window's "Permit changes to Settings" box, saved by `UserGroupGridHandler::updateUserGroup` → `UserGroupForm::execute` (`permitSettings` stored only for a manager-level role). Driven on OJS and OMP: the manager unticked it on "Journal editor" / "Press editor" of A; B seeded `roles: {editor: {permitSettings: false}}`; `user_groups` rows equal (permit_settings 0, the stage rewrite to `1,3,4,5` / `1,2,3,4,5` the same as U35's), and the editor signed in on either context sees no "Settings" in the side menu and, at `management/settings/context`, a redirect to a page reading "The current role does not have access to this operation." | PASS (verified live) | Refusals follow the window: `true` below manager level (box disabled), `false` on `manager` (its row has no "Settings" link; the lock-out guard disables the box on the acting admin's only settings role). OPS has no other manager-level role, so the key there can only restate `manager: {permitSettings: true}`. The draft spec's "Access denied." wording differs from the page read (U07 claim check) |
| 2026-09-23 | all | context scenario `roles.<key>.masthead` (same path) | the same window's "Consider role in masthead list" box. Driven on the three apps: on A the manager ticked it on "Copyeditor" (OPS "Author") and unticked it on "Editorial Board Member"; B seeded `roles: {copyeditor: {masthead: true}, editorialBoardMember: {masthead: false}}` (OPS `author`); `user_groups` rows equal (masthead flags; the Copyeditor stage row `4`, the Done stage dropped as U35 recorded), and both mastheads list the Copyeditor (Author) heading with its members and no Editorial Board Member heading. A masthead read before the by-hand change (cache filled) showed the change on the next load | PASS (verified live) | `UserGroupForm::execute` clears no masthead cache, yet the next load was right on all three apps; seeded roles are saved before `users[]`, whose enrolment clears the cache anyway |
| 2026-09-23 | all | submission scenario `tasks[] {title, creator, participants, type?, stage?, owner?, dateDue?, started?, message?}` (`PKPSubmissionScenarioBuilder::parseTasks` / `seedTasks`, last in the build; the new `ApiCall` helper) | a stage panel's "Add" › "Save" posts `POST submissions/{id}/tasks` with the JSON body `{type, title, stageId, dateDue?, participants: [{userId, isResponsible?}], description, submissionFileIds: []}` (`EditorialTaskController::addTask` behind the `AddTask` request's rules), and "Begin Task Upon Saving" then `POST …/tasks/{id}/start` (`X-Http-Method-Override: PUT`, `::startTask`). The builder builds that same body, runs `AddTask`'s own `prepareForValidation` and `rules()` through the container's validator, and hands the request to `addTask` itself (then `startTask`), acting as the creator, with the submission set in the controller's authorized context; not run: the route middleware and policies. Driven on the three apps (`.reports/U37/harness/parity-tasks.js`, `compare-tasks.sh`, `<app>-tasks-{screen,seed}.txt`, bodies in `.reports/U37/hU37/tasks-calls-<app>.json`): a manager added a discussion (manager, section editor, author) and a started task (manager, section editor; owner the section editor; due a week out) on the first stage (OPS Production) of one submission; the same two seeded on a second. `edit_tasks`, `edit_task_participants`, the head notes, the task event-log rows and settings, the "Discussion added." notification rows, the submission's notices (OPS's Production update included) and the `email_log` rows equal; the posted bodies equal the seed's field for field | PASS (verified live) | One lifted rule, by design: the window refuses a due date before today (`after_or_equal:today`, the spec's A10), so a past `dateDue` stands for a task whose due date has passed since the save; everything else of the item is stamped at the seed's time, so its History's "created" line is later than its due date. Read on screen (S3): such a task sits in its group with "This task is overdue. Remind the task owner to complete it as soon as possible" and the window badge "Overdue", on all three apps. The validator admits a participant whose account is disabled when they are assigned to the stage, which the window's list never offers (`getParticipants` skips disabled accounts): the item then reads as one saved while they were enabled and disabled later, minus that person's first "Discussion added." row and email (`notifyParticipants` loads active users only) |
| 2026-09-23 | all | context scenario `taskTemplates[] {stage, title, type?, dueInterval?, roles?, include?, message?}` (`PKPContextScenarioBuilder::parseTaskTemplates` / `applyTaskTemplate`, after components, before users[]) | Settings › Workflow › "Tasks and Discussions": "Add template" › "Save" posts `POST editTaskTemplates` `{title, stageId, restrictToUserGroups, userGroupIds (null when unrestricted), include, dueInterval (null for a discussion), description, type}` (`PKPEditTaskTemplateController::add` behind `AddTaskTemplate`); a row's "More Actions" › "Edit" › "Save" posts the same body, started from the stored template, to `editTaskTemplates/{id}` (`X-Http-Method-Override: PUT`, `::update` behind `UpdateTaskTemplate`). The builder builds that body (an edit from the stored template's localized title, text, flags and roles, as the window opens with them), runs the request's own rules and the controller action itself (`ApiCall`), with the request's context pointed at the scratch context. Driven on the three apps (`parity-templates.js`, `compare-templates.sh`, `<app>-templates-{X-screen,Y-seed}.txt`, bodies in `hU37/templates-calls-<app>.json`): on context X a manager added "Parity auto task" on the first stage (limited to the Section editor / Series editor / Moderator role, a task "2 weeks from the creation date", auto-add on) and ticked "Automatically add…" in the installed "Discussion (Submission)" ("Discussion (Production)") window; context Y seeded with both. Every `edit_task_templates` row of the two contexts, its settings (both locales) and its role links equal; one seeded submission on each then carries the same two auto-added items (`<app>-autoadd-{X,Y}.txt`: type, due date, no participants, "Created by: system" head note, the template link) | PASS (verified live) | The window's client-side refusals (a required "Due Date" for a task, at least one role when limited, the roles the window offers for the stage) are the builder's, as 400s; the API alone accepts a task without an interval. An edit posts the stored text back as the window does, so an installed template's other-locale title and text stay as installed. On screen (Y's panel after a seeded submit): "Parity auto task" under "Yet to begin" with "Task Owner:" empty and its due date, "Discussion (Submission)" under "In progress" with "Created by: system", on all three apps |
| 2026-09-23 | all | context scenario `users[].disabled` (`UserSeeder::parse` / `disable`, after the account's roles) | Settings › Users & Roles › "Current Users", a row's menu › "Disable User": the legacy window (`UserGridHandler::editDisableUser`) whose "OK" posts `disableReason` (empty when the box is left unfilled) to `disable-user`, `UserDisableForm::execute` (the disabled flag and reason, the audit-log line, the account's other sessions ended). The builder runs `UserDisableForm` itself with the reason posted empty, as the seeding admin (full administration over a throwaway account). Driven on the three apps (`parity-disabled.js`, `harness/<app>-disabled.txt`, the POST in `hU37/disabled-calls-<app>.json`): one section editor disabled on screen, one seeded `disabled: true` in the same context; `users` (`disabled` 1, `disabled_reason` the empty string, the other columns), their role rows and their sessions equal, and the two rows read the same on the Users list | PASS (verified live) | Refused on an account that already exists (a roster account is shared by every worker, PRINCIPLES A7). Not run: the form's POST and CSRF checks and the grid's refresh event. The account is disabled at context creation, before any submission seed (see the `tasks[]` row for what that means for a seeded item's notifications) |
| 2026-09-24 | ojs | context scenario `issues[] {volume, number, year, published?}` (OJS overlay: `ContextScenarioBuilder::parseOverlay` / `executeOverlay` → `BootstrapSeeder::parseIssues` / `addIssues(…, asTheForm: true)`, the bootstrap's own issue path, after users[]; OMP and OPS read no overlay and answer 400 on the key) | Issues › Future Issues › "Create Issue": the form arrives with "Title" ticked (an empty title is refused), so the by-hand issue without a title unticks it; "Save" (`update-issue`, `IssueForm::execute`: access status from the publishing mode, `add`, the title and description per form locale as posted, the closing `Repo::issue()->edit`); the row's "Publish Issue" › "OK" with "Send an email about this to all registered users." unticked (`publish-issue`, `IssueGridHandler::publishIssue`: `createDoi`, published + datePublished, the hook, `updateCurrent`, `Repo::doi()->issueUpdated`). Driven on OJS (`.reports/U08/harness/parity-issues.js`, recordings in `.reports/U08/hU08/`, rows in `harness/ojs-issues.txt`): a scratch journal's manager created Vol 1 No 1 (2024) and Vol 1 No 2 (2025) and published the first; a second journal seeded the same two. First compare: the seed lacked the form's empty `title` and `description` rows and, on the unpublished issue, the `last_modified` the form's closing edit stamps; both now written on the context path (`asTheForm`); re-seeded, `issues` (every column but the timestamps), `issue_settings` and `journals.current_issue_id` equal, no notification, job or DOI row on either side, the two Archives pages read the same, no response of 400 or more and no console error | PASS after fix (verified live) | The bootstrap keeps its old rows (no empty title/description rows, no `last_modified` on the unpublished Vol 2 No 1): a bootstrap change needs the deliberate re-check of every shipped spec (PRINCIPLES "Bootstrap data policy"), and neither difference shows on a screen. Not mirrored: the publish dialog's email box ticked (its default), which queues `IssuePublishedNotifyUsers` jobs for the journal's users; the pub-id form's `assignPubId` (no pub-id plugin on a fresh journal); a second form locale's empty rows are written by construction, not driven. A published submission seeded with `issue {volume, number, year}` lands in a seeded issue (checked 2026-09-24 on a three-issue journal) |
| 2026-09-24 | all | context scenario `libraryFiles[] {name, type, description?, publicAccess?, file?}` (`PKPContextScenarioBuilder` → `LibraryFileSeeder::parse` / `add`, after the task templates, before users[]) | Settings › Workflow › "Publisher Library" ("Press Library", "Preprint Server Library") › "Add a file": the upload area posts the file to `library-file-admin-grid/upload-file` (`LibraryFileGridHandler::uploadFile` → `TemporaryFileManager::handleUpload`), "OK" posts `save-file` `temporaryFileId`, `libraryFileName[<locale>]`, `fileType` (the list's code), `description[<locale>]` (empty ones posted as ''), `publicAccess=1` when ticked (the settings `NewLibraryFileForm::execute`: the type-coded stored name, the `library_files` row and settings, the temporary file deleted). The builder runs `handleUpload` itself with the fixture standing in for the upload (copied, not `move_uploaded_file`), then the form's own `execute` with that data, acting as `admin`. Driven on the three apps (`.reports/U39/harness/parity-library.js`, `compare-library.sh`, `<app>-library-{X-screen,Y-seed}.txt`, the POST bodies in `.reports/U39/hU39/library-posts-<app>.json`): context X's manager added "Parity form" (Permissions, a description, the PDF fixture, "Public Access" ticked); context Y seeded the same. `library_files` rows (type, public_access 1, `article-PER.pdf` / OPS `preprint-PER.pdf`, original name, MIME type, size, equal upload and modified dates), `library_file_settings`, the stored file's bytes, no `temporary_files` left, no event-log line and the same notification count; the tab's list reads the same on both | PASS (verified live) | Not run: the form's POST and CSRF checks and the grid's refresh event. `type` is the "Type" list's English label, resolved through the app's own `LibraryFileManager::getTypeTitleKeyMap()`; on OMP that map is renumbered by `array_merge` (Contracts 0 … Other 4, not lib/pkp's `LibraryFile` constants 1–5), and the screen posts and stores the same codes |
| 2026-09-24 | all | submission scenario `libraryFiles[] {name, type, description?, file?}` (`PKPSubmissionScenarioBuilder` → `LibraryFileSeeder`, after everything but tasks[]) | the workflow header's "Library" › "Submission Library" › "Add a file": `submission-documents-files-grid/upload-file?submissionId=…` then `save-file` with `submissionId` and the same fields, no `publicAccess` (the submissionDocuments `NewLibraryFileForm::execute`, the row's `submission_id` set). The builder runs the same two steps as the context key, acting as `admin`. Driven on the three apps (same script and files): on submission SX of context X the manager added "Parity sub" (Marketing, no description, the PDF fixture); submission SY of context Y seeded the same. Rows, settings (the empty description stored as ''), stored file and the absent temporary file equal; the "Submission Library" window lists both the same ("Marketing" › "Parity sub", the other groups "No Items") | PASS (verified live) | `publicAccess` is a 400 (the window has no box); a draft refuses the key. The stored name is chosen across the whole context's library, so on `publicknowledge` it depends on what other workers seeded (scenarios.md) |
| 2026-09-24 | all | context scenario `roles.<key>.stages {<stage word>: bool}` (`PKPContextScenarioBuilder::parseRoleStages` / `applyRoleOptions` → `UserGroupForm::execute`, now before the components and task templates) | Settings › Users & Roles › "Roles" tab, the role row's "Settings" › "Edit" window, its "Stage Assignment" boxes (labels read: OJS Submission, Review, Copyediting, Production; OMP Submission, Internal Review, External Review, Copyediting, Production; OPS Production), "OK" posting `user-group-grid/update-user-group` with `name[en]`, `abbrev[en]`, the ticked `assignedStages[]` and the ticked option boxes → `UserGroupForm::execute` (`_assignStagesToUserGroup`: the role's `user_group_stage` rows deleted and re-created from the posted boxes). The builder runs the same form: `initData()` from the stored role, `assignedStages` the installed boxes the window renders with the named ones ticked or unticked, `execute()`; not run: the POST/CSRF checks and the grid refresh. Driven on the three apps (`.reports/U39/harness/parity-stages.js`, `parity-stages.log`, `.reports/U39/hU39b/parity-stages-<app>.json`): context A's manager ticked "Submission" on Copyeditor (OPS "Production" on Editorial Board Member); context B seeded `roles: {copyeditor: {stages: {submission: true}}}` (OPS `editorialBoardMember: {stages: {production: true}}`). Every role's `user_group_stage` rows equal (Copyeditor `4` → `1,4`; OPS Editorial Board Member none → `5`; no other role touched), the role's `user_groups` row and settings equal (the form's `recommendOnly` 0 setting written on both), the same notification count; the Roles list row's stage boxes and the reopened "Edit" window's boxes read the same on A and B; 0 crashes, 0 console errors | PASS (verified live) | Refusals follow the window (`smoke-stages.js`, all apps): another app's stage word, a box the window disables (every box of a manager-level role, whose group the window hides; a reviewer role's non-review boxes; a reader's), a non-boolean, an empty map or a list, and a result with no box ticked (by code: `UserGroupForm::execute` re-saves stages only when some are posted, so a save with none ticked keeps the stored rows) are 400s, each before the context exists |
| 2026-09-24 | all | context scenario `citationsMetadataLookup` (bool) (`PKPContextScenarioBuilder::parseIntakeSettings` → `saveFormSettings`: `PKPContextService::validate` + `edit`, the save the context API's PUT runs) | Settings › Workflow › Submission › "Metadata", the "References Metadata Lookup" group's box "Enable references structuring and metadata lookup" ticked, "Save": one form-encoded POST (`X-Http-Method-Override: PUT`) to `contexts/{id}` carrying the whole form, `citationsMetadataLookup=true` among it. Driven on the three apps (`.reports/U42/harness/parity.js`, the bodies in `.reports/U42/hU42/parity-posts-<app>.json`, rows in `harness/<app>-{X-screen,Y-seed}.txt` via `compare.sh`): context X's manager ticked and saved; context Y seeded `citationsMetadataLookup: true`. The row equal (`1`), and both contexts queued one lookup chain per reference added afterwards (4 each) | PASS (verified live) | The screen's save also writes every other Metadata item (an unset one as `0`, which reads as off like a missing row); the key writes the lookup row alone, as `metadata` writes only the items it names. The form posts the box even with "Enable references metadata" unticked, so the key does not read `metadata.citations` |
| 2026-09-24 | all | submission scenario `citationsRaw` (string or list of lines) (`PKPSubmissionScenarioBuilder::parseCitationsRaw`, then the title/abstract `Repo::publication()->edit()` before the submit, as the submitter: `publication\DAO::update` → `Repo::citation()->importCitations`) | the wizard's "Details" step "References" box, then "Continue": the step's POST (`X-Http-Method-Override: PUT`) to `submissions/{id}/publications/{id}` posting `citationsRaw` as typed (`PKPSubmissionController::editPublication` → `Repo::publication()->edit`). Driven on the three apps (same script and files): draft SX's author typed five lines (a blank line, a line with doubled and edge spaces, a repeated line) into X (lookup on); draft SY of Y seeded the same text. `citations` rows (seq, raw text: 4 rows, trimmed, spaces shrunk, the repeat kept) and `citation_settings` (`isStructured=0`, `processingStatus=0`) equal, one queued chain per reference on both; both Details steps read back the same box text and table. Lookup off (`lookup-off-seed.txt`, seeded only): a DOI in the text kept on no row and no job queued, the wizard path's own A7 | PASS (verified live) | The key does not read the context's References setting (seeded references on a references-off context are the "switched off, references kept" state). The seed's one edit also carries the title and abstract, where the screen's step save posts the box alone; the rows are the same |
| 2026-09-24 | all | submission scenario `dataCitations[] {title, relationshipType, identifierType?, identifier?, repository?, year?, authors?, url?}` (`PKPSubmissionScenarioBuilder::parseDataCitations` / `seedDataCitations` → `PKPDataCitationController::add` itself through `ApiCall`, the publication set as the policy would authorize it; before the submit, in list order) | the Data Citations table's "Add Data Citation" side panel › "Save" in the wizard's "Details" › "Data" section: a form-encoded POST to `submissions/{id}/publications/{id}/dataCitations` with every box (an empty one as '', null after the middleware; the year as typed; `authors[0][givenName]`, `[familyName]`, `[orcid]`) → `convertStringsToSchema`, `Repo::dataCitation()->validate`, `DataCitation::create` (its saving hook strips an identifier to its bare form). Driven on the three apps (same script and files): SX's author added "Parity data B" (DOI `https://doi.org/10.1234/xyz`, generated, Zenodo, 2024, creator Ada Lovelace, a URL) and "Parity data A" (supporting only); SY seeded the same two. `data_citations` (seq 0 each) and `data_citation_settings` equal (identifier stored `10.1234/xyz`, the creator stored without the empty ORCID, unset boxes null, `authors=[]`), and both Data Citations tables read "10.1234/xyz Parity data B", "Parity data A" | PASS (verified live) | Not run: the route middleware and `PublicationWritePolicy` (the seed is the harness). The key does not read the context's Data Citations setting. The panel's refusals are the seed's 400s (`smoke-seed.js`: missing title, unknown relationship or identifier type, invalid DOI, a two-digit year, an identifier without its type) |
| 2026-09-24 | all | context scenario `plugins.customblockmanagerplugin {enabled}` (`PKPContextScenarioBuilder::parsePlugins`, its site-wide refusal now asked through `isSitePluginInContext()` with a stand-in context on the router, then the unchanged `applyPlugin`: `Plugin::updateSetting(<context>, 'enabled')` and the audit-log line) | Settings › Website › "Plugins" › "Installed Plugins", the "Custom Block Manager" row's checkbox ticked: one POST to `settings-plugin-grid/enable?plugin=customblockmanagerplugin&category=generic` (`PluginGridHandler::enable` → `LazyLoadPlugin::setEnabled`, which asks `isSitePlugin()` inside the context, where this plugin answers false, so the row is written for the context). Before, the seed refused the key with a 400 (the seeding request runs outside any journal, where the plugin calls itself site-wide). Driven on the three apps (`.reports/U09/harness/parity.js`, `compare.sh`, `parity-<app>.json`): context X's manager ticked the box; context Y seeded `plugins: {customblockmanagerplugin: {enabled: true}}`. Every `plugin_settings` row of the two contexts equal (`customblockmanagerplugin enabled 1 bool` the only CBM row), no notification row on either, no site-level (context 0) row written; on screen both rows ticked with a "Settings" link offering "Manage Custom Blocks", whose window reads the same ("Custom Blocks", "Add Block", "No custom blocks have been created."), and the Appearance "Sidebar" lists read the same | PASS (verified live) | Not mirrored, as for every `plugins` entry: the grid's trivial "plugin enabled" toast. The key seeds no block (`settings` on it is a 400). `enabled: false` is accepted too (an `enabled 0` row, what the grid's untick writes); a non-boolean and the Usage Event plugin (still site-wide inside a context) are 400s as before (`smoke-seed.js`, three apps). The only crash in the drives is the known Plugin Gallery `fetch-grid` 500 (incidentals, U62) |
